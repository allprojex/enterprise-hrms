openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Enterprise HRMS API
servers:
  - url: /api
    description: Base API path
tags:
  - name: forms
    description: WS-26 Tenant Form, Workflow & Signature Engine
  - name: health
    description: Health operations
  - name: auth
    description: Authentication operations
  - name: users
    description: User operations
  - name: organizations
    description: Organization operations
  - name: notifications
    description: Notification operations
  - name: employees
    description: Employee directory operations
  - name: branches
    description: Branch operations
  - name: departments
    description: Department operations
  - name: positions
    description: Position operations
  - name: roles
    description: Role operations
  - name: permissions
    description: Permission operations
  - name: members
    description: Organization membership management
  - name: primary-hr
    description: Primary HR assignment operations
  - name: vehicles
    description: VR-01 organizational vehicle register
  - name: audit-events
    description: Audit log operations
  - name: invitations
    description: Administrative user invitation operations
  - name: reports
    description: Reporting Foundation operations
  - name: self-service
    description: Employee Self-Service operations
  - name: employment-lifecycle
    description: Employment terms, probation extension/outcome, acting appointments, and secondments
  - name: onboarding
    description: Onboarding, induction, and handbook/policy acknowledgement operations
  - name: recruitment
    description: Recruitment Configuration operations (settings, workflows, stages)
  - name: vacancies
    description: Vacancy management operations (the postable unit produced from an approved job requisition)
  - name: applications
    description: Internal recruitment application pipeline operations (review, stage movement, reject/withdraw/reopen) — the start of the ATS
  - name: public-careers
    description: Unauthenticated public careers portal operations (organization-slug-scoped listing, detail, and anonymous application submission)
  - name: candidates
    description: Internal candidate directory, notes, and tags operations (W53 — candidate records themselves are W49's, this workstream adds internal read access plus collaboration features)
  - name: talent-pools
    description: Reusable, org-scoped candidate grouping operations (W53) — never tied to a single vacancy or requisition
  - name: interviews
    description: Interview scheduling and panel composition operations (W54) — scheduling only, never evaluation (scores/recommendations/hiring decisions are W55's interview_scorecards)
  - name: interview-scorecards
    description: Independent, lockable interview evaluation operations (W55) — one scorecard per panel member per interview, never redesigning W54 scheduling or mixing with W52's screening scores
  - name: reference-checks
    description: Referee contact and outcome tracking operations (W56) — status-tracked, no vendor integration
  - name: background-checks
    description: Background check tracking operations (W56) — status-tracked, no vendor/government integration; its own dedicated, organization-wide-only permission pair since result content is highly sensitive
  - name: offers
    description: >-
      Versioned offer envelope, single-step approval, and issue/withdraw
      operations (W57) — the final Recruitment stage. offer.manage is the
      first Recruitment write permission this phase seeded to every role
      (assigned recruiter/hiring manager can manage their own draft, per
      §7); offer.approve/.issue/.withdraw stay organization-wide only
  - name: pre-employment-requirements
    description: >-
      Checklist tracking before employee conversion (W58) — no dedicated
      permission exists, reuses application.read/.manage; status is freely
      settable (pending/satisfied/waived, no one-way lifecycle) and never
      itself creates an employee or activates any HR module
  - name: employee-conversion
    description: >-
      The controlled hand-off from Recruitment to HR (W59) — reuses the
      existing employee-creation service unchanged (one authoritative
      pathway); requires both candidate.convert_to_employee and
      employee.write, neither alone sufficient
  - name: recruitment-reporting
    description: >-
      Recruitment Dashboard & Reporting operations (W61) — read-only,
      server-computed metrics over existing Recruitment tables, no stored
      aggregate. Reuses the Reporting Foundation's registry/export
      conventions (W17/ADR-016) via dedicated, visibility-scoped routes
      (GET /reports still lists these entries for catalog purposes, but
      they only execute here — see report-definitions.ts). Single
      permission, recruitment.reports.read: assigned recruiter/hiring
      manager sees their own workload/pipeline scope, organization-wide
      HR sees everything.
  - name: organization-domains
    description: >-
      Multi-Organization Tenant Infrastructure — platform-admin management
      of the hostnames (platform testing subdomains and custom domains)
      that resolve a request to an organization. Reserved to the platform
      super_admin role; a resolved hostname only ever identifies tenant
      context, it never grants authorization on its own — every
      org-scoped route still independently re-verifies a live membership
      the same way it always has. See docs/TENANT_DOMAINS_AND_ACCESS.md.
  - name: tenant-context
    description: >-
      Unauthenticated, safe tenant branding lookup for the login
      experience — resolved only from the caller's own request hostname,
      never from a client-supplied hostname parameter (not a tenant
      directory).
  - name: installations
    description: >-
      WS-4, Owner Decision #29 — durable registry identifying each deployed
      HRMS installation/environment and its exact software/deployment state.
      Platform super_admin only. Not the fleet-management Control Plane
      (deferred) — this is only the identity foundation later workstreams
      (health, backup, update status) attach to. One installation may host
      one or many organizations; see installation_organizations. See
      docs/INSTALLATION_AND_BREAK_GLASS.md.
  - name: break-glass
    description: >-
      WS-4, Owner Decision #31 — the explicit, reason-bound,
      organization-scoped, permission-scoped, time-limited, revocable, fully
      audited mechanism that lets a platform super_admin exercise a narrow,
      explicit, read-only set of permissions against one organization's
      data. Platform-owner status alone grants zero standing access to
      customer HR data; only an active grant created here does, and only
      within its own scope and expiry. See
      docs/INSTALLATION_AND_BREAK_GLASS.md.
  - name: performance-cycles
    description: Phase 3C, W75 — Performance cycle configuration and bulk review assignment. HR/admin only (performance.manage).
  - name: performance-reviews
    description: Phase 3C, W75 — read access to assigned Performance reviews. HR/admin only (performance.manage); self/manager visibility is a later workstream's concern.
  - name: performance-reporting
    description: Phase 3C, W81 — Performance dashboard and reports. Read-only, server-computed aggregation over existing Performance data, no stored analytics table. Scoped own/reviewer-of-record/organization-wide (performance.reports.read, org-wide reach signaled by performance.manage).
  - name: performance-evidence
    description: Phase 3C, W82 — Evidence/attachments on Performance reviews. A lightweight join over the existing employee_documents storage layer (§8.9) — no new file-storage code. No new permission key; upload is stage-gated by the caller's own relationship to the review, view/download are not.
  - name: organization-documents
    description: >-
      WS-5, Owner Decision #4 — the organization-level digital document
      repository (handbook, HR policy, forms, procedures): documents owned by
      the organization itself rather than by one employee or candidate. NOT
      Personnel Files — physical file custody remains its own domain
      (personnel_files et al, zero column overlap). Immutable version history:
      a new upload supersedes the previous version but never overwrites its
      row or its stored object, and a partial unique index guarantees exactly
      one current version per document. See docs/DOCUMENTS_AND_RECORDS.md.
  - name: document-templates
    description: >-
      WS-5, Owner Decision #4 — the reusable, organization-scoped document
      template and generation engine (offer/appointment/confirmation/
      promotion/transfer/warning/service/separation letters and any other
      organization-defined HR letter). A platform service only: it renders
      and stores artifacts, and never decides when a letter should be issued
      — that belongs to the owning downstream workstream. Template content is
      plain text with an allow-listed {{merge.field}} syntax that is never
      evaluated, so there is no template-injection surface; output is PDF.
  - name: document-requirements
    description: >-
      WS-5 — the shared required/provided/verified/expiry checklist
      primitive later HR workflows (Recruitment, Onboarding, employee
      lifecycle/compliance) compose their own checklists from. Deliberately
      not a workflow engine: recording a document only ever marks it
      "provided"; only document.verify can move a requirement to "verified"
      or "rejected". The expiry endpoint is the read-only contract a future
      reminder workstream consumes — no notification is ever sent from here.
  - name: document-retention
    description: >-
      WS-5, Owner Decision #4 — retention, archive, legal hold, and
      controlled disposal, shared across every digital document table via a
      polymorphic (documentTable, documentId) key. Archive is never
      deletion; disposal eligibility is a reviewable determination, never
      automatic; legal hold blocks disposal outright regardless of elapsed
      retention. Every state change requires document.retention.manage,
      seeded to no role by default — a deliberate per-organization
      delegation, not implied by ordinary HR/records authority.
  - name: scheduled-jobs
    description: >-
      WS-6, Owner Decision #13 — the platform operational surface over the
      database-authoritative job scheduler. Platform super_admin only, and
      deliberately narrow: read/cancel/reschedule/retry only. There is no
      route here to create an arbitrary job or execute one on demand —
      domain modules create jobs through their own server-side calls to the
      scheduler service; a jobType a client could invent has no handler and
      fails safely. See docs/SCHEDULED_JOBS_AND_NOTIFICATIONS.md.

  - name: skills
    description: >-
      WS-14 skills catalogue, proficiency scale, employee capability records,
      assessment/verification, position skill requirements, gap analysis and
      development actions (§30)
  - name: succession
    description: >-
      WS-14 succession plans, candidate pools and readiness bands (§30).
      Confidential and never employee-facing: no ESS route exists here

  - name: action-centre
    description: >-
      WS-15 HR Action Centre (§31) — runtime cross-module federation of current
      actionable work. Stores nothing, mints no permission, and returns only
      generic redacted rows plus deep links

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check (liveness)
      description: >-
        Liveness only — confirms the process itself is up. Never touches the
        database, so it can't be dragged down by a database outage; see
        /readyz for that. Includes the deploy-time release version when set
        (RELEASE_VERSION / GIT_COMMIT_SHA), "unknown" otherwise.
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /readyz:
    get:
      operationId: readinessCheck
      tags: [health]
      summary: Readiness check
      description: >-
        Confirms this instance can actually reach the database. A load
        balancer/orchestrator should stop routing traffic here (not restart
        the process) on a failing readiness check — that distinction is why
        this is a separate endpoint from /healthz.
      responses:
        "200":
          description: Ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessStatus"
        "503":
          description: Not ready — the database is unreachable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessStatus"

  /tenant-context:
    get:
      operationId: getTenantContext
      tags: [tenant-context]
      summary: Get safe tenant context for the current hostname
      description: >-
        Public, unauthenticated. Resolved only from the request's own
        hostname (Host header, or X-Tenant-Hostname in this project's
        split-port dev setup) — accepts no hostname parameter, so it can
        never be used to enumerate other tenants. Returns `resolved: false`
        for every unresolvable case alike (unmapped hostname, disabled
        domain, suspended organization) — the response never distinguishes
        which.
      responses:
        "200":
          description: Safe tenant context, or an unresolved marker
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TenantContext"

  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Login
      description: Authenticate with email and password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginInput"
      responses:
        "200":
          description: Authenticated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthSession"
        "401":
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      summary: Logout
      description: Invalidate the current session
      responses:
        "200":
          description: Logged out successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /auth/forgot-password:
    post:
      operationId: forgotPassword
      tags: [auth]
      summary: Request password reset
      description: >-
        Sends a password reset email via Resend if the address is registered.
        Always returns the same confirmation regardless of whether the email
        is registered or delivery succeeds, to avoid revealing account
        existence.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForgotPasswordInput"
      responses:
        "200":
          description: Request accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /auth/reset-password/{token}:
    get:
      operationId: getPasswordResetStatus
      tags: [auth]
      summary: Preview a password reset token
      description: Public -- no authentication required. Used by the reset-password page.
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Reset token status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordResetStatus"
    post:
      operationId: resetPassword
      tags: [auth]
      summary: Reset password
      description: >-
        Public -- no authentication required. Sets a new password and
        consumes the token so the link can't be replayed.
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResetPasswordInput"
      responses:
        "200":
          description: Password reset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "404":
          description: Invalid or already-used reset link
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "410":
          description: Reset link expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /auth/switch-organization:
    post:
      operationId: switchOrganization
      tags: [auth]
      summary: Switch active organization
      description: >-
        Switches the caller's active organization context for the current
        session. Only succeeds if the caller has an active membership in the
        target organization; never trusts the client beyond that check.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SwitchOrganizationInput"
      responses:
        "200":
          description: Active organization switched
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No active membership in the requested organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /auth/me:
    get:
      operationId: getMe
      tags: [auth]
      summary: Get current user
      description: Returns the authenticated user's profile
      responses:
        "200":
          description: Current user profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /users/me:
    patch:
      operationId: updateMyProfile
      tags: [users]
      summary: Update own profile
      description: Update the current user's profile information
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserProfileUpdate"
      responses:
        "200":
          description: Profile updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations:
    get:
      operationId: listOrganizations
      tags: [organizations]
      summary: List organizations
      description: Returns all organizations the user has access to
      responses:
        "200":
          description: List of organizations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Organization"
    post:
      operationId: createOrganization
      tags: [organizations]
      summary: Create (onboard) an organization
      description: >-
        Platform control plane — Platform Super Admin only. Creates a new
        organization and, atomically, makes the caller its first active member
        with the org_admin role and Primary HR. A tenant user cannot provision
        a tenant.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOrganizationInput"
      responses:
        "201":
          description: Organization created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Organization"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Slug already in use
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{id}:
    get:
      operationId: getOrganization
      tags: [organizations]
      summary: Get organization
      description: Returns an organization by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Organization details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Organization"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateOrganization
      tags: [organizations]
      summary: Update organization details
      description: >-
        Partial update of name/slug/type/logoUrl/industry/employeeCount.
        Does not change lifecycle status — use suspend/reactivate for that.
        Requires org_admin (within the organization) or super_admin.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOrganizationInput"
      responses:
        "200":
          description: Organization updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Organization"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Slug already in use
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{id}/logo:
    patch:
      operationId: uploadOrganizationLogo
      tags: [organizations]
      summary: Upload or replace an organization's logo
      description: >-
        multipart/form-data upload. JPEG/PNG/WebP only, validated by file
        signature (not just Content-Type), 5MB max. Resized only if larger
        than 1024px on either side (aspect ratio preserved, never cropped);
        original format is kept so a transparent PNG/WebP logo stays
        transparent. Sets organizations.logoUrl to the resulting public
        (unauthenticated) asset URL. Requires org_admin (within the
        organization) or super_admin.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Logo updated
          content:
            application/json:
              schema:
                type: object
                required: [logoUrl]
                properties:
                  logoUrl:
                    type: string
        "400":
          description: Invalid file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: deleteOrganizationLogo
      tags: [organizations]
      summary: Remove an organization's logo
      description: >-
        Governed removal of the organization's logo, restoring the neutral
        fallback identity. Sets organizations.logoUrl to null and best-effort
        deletes the stored object through the same org-scoped storage
        abstraction the upload uses (an already-missing file is non-fatal).
        Same authority as the upload: authenticated, active membership in the
        path organization, organization.update. Audited as
        organization.logo_updated (after: logoUrl null). Idempotent when there
        is no current logo.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Logo removed (logoUrl is now null)
          content:
            application/json:
              schema:
                type: object
                required: [logoUrl]
                properties:
                  logoUrl:
                    type: ["string", "null"]
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Organization not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{id}/suspend:
    post:
      operationId: suspendOrganization
      tags: [organizations]
      summary: Suspend an organization
      description: Sets status to suspended. Platform tenant lifecycle — Platform Super Admin only. The body must name the target tenant's slug (confirmSlug) and it must match the organization in the path — a typed confirmation, because this is a dangerous platform action.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrganizationStatusChangeInput"
      responses:
        "200":
          description: Organization suspended
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Organization"
        "400":
          description: Missing or mismatched confirmSlug
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{id}/reactivate:
    post:
      operationId: reactivateOrganization
      tags: [organizations]
      summary: Reactivate a suspended organization
      description: Sets status to active. Platform tenant lifecycle — Platform Super Admin only. The body must name the target tenant's slug (confirmSlug) and it must match the organization in the path.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrganizationStatusChangeInput"
      responses:
        "200":
          description: Organization reactivated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Organization"
        "400":
          description: Missing or mismatched confirmSlug
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/domains:
    get:
      operationId: listOrganizationDomains
      tags: [organization-domains]
      summary: List an organization's domains
      description: Platform super_admin only.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of domains
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OrganizationDomain"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createOrganizationDomain
      tags: [organization-domains]
      summary: Assign a domain to an organization
      description: >-
        Platform super_admin only. platform_subdomain hostnames become
        active immediately (the platform itself controls those);
        custom_domain hostnames start pending until an operator activates
        them once DNS ownership has been confirmed out-of-band.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOrganizationDomainInput"
      responses:
        "201":
          description: Domain created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationDomain"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Hostname already assigned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/domains/{id}/activate:
    post:
      operationId: activateOrganizationDomain
      tags: [organization-domains]
      summary: Activate a domain
      description: >-
        Platform super_admin only. Moves a pending custom domain (DNS
        ownership confirmed out-of-band) or a disabled domain to active.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Domain activated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationDomain"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/domains/{id}/disable:
    post:
      operationId: disableOrganizationDomain
      tags: [organization-domains]
      summary: Disable a domain
      description: >-
        Platform super_admin only. The domain immediately stops resolving
        to any tenant context; the row and its history are kept, not
        deleted. Clears the domain's primary flag if it held one.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Domain disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationDomain"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/domains/{id}/set-primary:
    post:
      operationId: setPrimaryOrganizationDomain
      tags: [organization-domains]
      summary: Mark a domain primary
      description: >-
        Platform super_admin only. Only an active domain may become
        primary; atomically clears any previous primary for the same
        organization.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Domain marked primary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationDomain"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Domain is not active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /platform/users/{userId}/disable:
    post:
      operationId: disablePlatformUser
      tags: [platform-users]
      summary: Disable a user's platform account
      description: >-
        Platform super_admin only (WS-2, Owner Decision #20). Blocks the
        user across every organization, immediately revokes every existing
        session, and is re-checked live on every subsequent authenticated
        request — never merely on next login. Distinct from, and
        independent of, organization membership status: a disabled user's
        organization memberships are left untouched. A super_admin cannot
        disable their own account.
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DisablePlatformUserInput"
      responses:
        "200":
          description: User disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformUserSummary"
        "400":
          description: Cannot disable your own account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /platform/users/{userId}/enable:
    post:
      operationId: enablePlatformUser
      tags: [platform-users]
      summary: Re-enable a user's platform account
      description: >-
        Platform super_admin only (WS-2, Owner Decision #20). Restores
        platform account eligibility only — does NOT restore any
        organization membership, role, or permission grant that changed
        while the account was disabled; those remain separately
        authoritative and are managed through their own existing
        membership-management operations.
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: User re-enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformUserSummary"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /platform/installations/{installationId}/restore-requests:
    get:
      operationId: listRestoreRequests
      tags: [platform-restore]
      summary: Restore requests for an installation
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Restore requests, newest first
          content:
            application/json:
              schema:
                type: object
                required: [requests]
                properties:
                  requests:
                    type: array
                    items:
                      $ref: "#/components/schemas/RestoreRequest"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: submitRestoreRequest
      tags: [platform-restore]
      summary: Submit a governed physical restore request
      description: >-
        Requires platform.restore.request. A partial recovery point is blocked
        unless the incomplete component is explicitly acknowledged, and the
        target-bound confirmation phrase must match exactly.
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitRestoreRequestInput"
      responses:
        "201":
          description: Submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreRequest"
        "400":
          description: Governance rule refused the submission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/restore-requests/{requestId}:
    get:
      operationId: getRestoreRequestDetail
      tags: [platform-restore]
      summary: Everything an approver must see before deciding
      description: >-
        Returns the request, prior approvals, execution attempts, validation
        evidence, the live dispatch precheck including blast-radius drift, and
        observed metrics — so a blind one-click approval is not possible.
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Restore request detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreRequestDetail"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/restore-requests/{requestId}/decision:
    post:
      operationId: decideRestoreRequest
      tags: [platform-restore]
      summary: Approve or reject a restore request
      description: >-
        Requires platform.restore.approve. In production the requester can
        never approve their own restore, compared on immutable user id.
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestoreDecisionInput"
      responses:
        "201":
          description: Decision recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreApproval"
        "400":
          description: Governance rule refused the decision
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/restore-requests/{requestId}/cancel:
    post:
      operationId: cancelRestoreRequest
      tags: [platform-restore]
      summary: Cancel a restore request before dispatch
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreRequest"
        "400":
          description: Governance rule refused the transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestoreCancelInput"
  /platform/restore-requests/{requestId}/acknowledge-drift:
    post:
      operationId: acknowledgeRestoreDrift
      tags: [platform-restore]
      summary: Acknowledge non-invalidating operational drift
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreRequest"
        "400":
          description: Governance rule refused the transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
  /platform/restore-requests/{requestId}/invalidate-approval:
    post:
      operationId: invalidateRestoreApproval
      tags: [platform-restore]
      summary: Invalidate a standing approval
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreRequest"
        "400":
          description: Governance rule refused the transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestoreCancelInput"
  /platform/restore-requests/{requestId}/quiescence:
    post:
      operationId: recordRestoreQuiescence
      tags: [platform-restore]
      summary: Record write-quiescence evidence
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreRequest"
        "400":
          description: Governance rule refused the transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestoreQuiescenceInput"
  /platform/restore-requests/{requestId}/dispatch:
    post:
      operationId: dispatchRestore
      tags: [platform-restore]
      summary: Produce governed execution intent
      description: >-
        Dispatch executes nothing and does not mean the restore succeeded. It
        is idempotent by key, so a repeated call never starts a second restore.
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestoreDispatchInput"
      responses:
        "201":
          description: Execution intent recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreExecution"
        "400":
          description: Dispatch blocked by a precondition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/restore-executions/{executionId}/evidence:
    post:
      operationId: recordRestoreExecutionEvidence
      tags: [platform-restore]
      summary: Record what an external executor reported
      description: A completed attempt can never be rewritten into a different outcome.
      parameters:
        - name: executionId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestoreExecutionEvidenceInput"
      responses:
        "200":
          description: Evidence recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreExecution"
        "400":
          description: Refused
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/restore-requests/{requestId}/validations:
    post:
      operationId: recordRestoreValidation
      tags: [platform-restore]
      summary: Record one post-restore validation check
      description: >-
        Execution success is not validation success. An operator attestation
        may never override an automated failure.
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestoreValidationInput"
      responses:
        "201":
          description: Validation recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreValidation"
        "400":
          description: Refused
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/restore-requests/{requestId}/conclude-validation:
    post:
      operationId: concludeRestoreValidation
      tags: [platform-restore]
      summary: Conclude validation from recorded evidence
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreRequest"
        "400":
          description: Governance rule refused the transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
  /platform/fleet:
    get:
      operationId: getFleetHealth
      tags: [platform-operations]
      summary: Fleet health across all installations
      description: >-
        Requires platform super_admin AND the platform.fleet.read grant.
        Super-admin alone is deliberately insufficient.
      responses:
        "200":
          description: Health for every installation
          content:
            application/json:
              schema:
                type: object
                required: [installations]
                properties:
                  installations:
                    type: array
                    items:
                      $ref: "#/components/schemas/InstallationHealth"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/installations/{installationId}/health:
    get:
      operationId: getInstallationHealth
      tags: [platform-operations]
      summary: Health signals for one installation
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Installation health
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallationHealth"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/installations/{installationId}/deployments:
    get:
      operationId: listInstallationDeployments
      tags: [platform-operations]
      summary: Deployment history for an installation
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Deployment history, newest first
          content:
            application/json:
              schema:
                type: object
                required: [deployments]
                properties:
                  deployments:
                    type: array
                    items:
                      $ref: "#/components/schemas/InstallationDeployment"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: recordInstallationDeployment
      tags: [platform-operations]
      summary: Record deployment evidence reported by an external executor
      description: >-
        The HRMS records; infrastructure executes. Only a succeeded result
        advances the installation's current version.
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordDeploymentInput"
      responses:
        "201":
          description: Recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallationDeployment"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/installations/{installationId}/backup-policy:
    get:
      operationId: getInstallationBackupPolicy
      tags: [platform-operations]
      summary: Backup policy targets for an installation
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Backup policy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallationBackupPolicy"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No policy configured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    put:
      operationId: upsertInstallationBackupPolicy
      tags: [platform-operations]
      summary: Set backup policy targets
      description: >-
        Targets only. Populating an RPO records an intention, never a claim
        that it is met. Unconfigured stays null; there is no default SLA.
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertBackupPolicyInput"
      responses:
        "200":
          description: Updated policy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallationBackupPolicy"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/installations/{installationId}/backup-requests:
    get:
      operationId: listInstallationBackupRequests
      tags: [platform-operations]
      summary: Backup requests for an installation
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Backup requests, newest first
          content:
            application/json:
              schema:
                type: object
                required: [requests]
                properties:
                  requests:
                    type: array
                    items:
                      $ref: "#/components/schemas/InstallationBackupRequest"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: requestInstallationBackup
      tags: [platform-operations]
      summary: Ask an executor to take a backup
      description: >-
        A request is not a backup and never becomes evidence. Only a backup run
        record is evidence.
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RequestBackupInput"
      responses:
        "201":
          description: Request recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallationBackupRequest"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/backup-requests/{requestId}/status:
    post:
      operationId: updateInstallationBackupRequestStatus
      tags: [platform-operations]
      summary: Report a backup request's status transition
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateBackupRequestStatusInput"
      responses:
        "200":
          description: Updated request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallationBackupRequest"
        "400":
          description: Illegal transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/installations/{installationId}/backup-runs:
    get:
      operationId: listInstallationBackupRuns
      tags: [platform-operations]
      summary: Backup evidence for an installation
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Backup runs, newest evidence first
          content:
            application/json:
              schema:
                type: object
                required: [runs]
                properties:
                  runs:
                    type: array
                    items:
                      $ref: "#/components/schemas/InstallationBackupRun"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: recordInstallationBackupRun
      tags: [platform-operations]
      summary: Record backup evidence
      description: >-
        The overall result is DERIVED from the component results and is never
        accepted from the caller, so a database-only backup can never be
        reported as complete.
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordBackupRunInput"
      responses:
        "201":
          description: Evidence recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallationBackupRun"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /platform/installations/{installationId}/telemetry:
    post:
      operationId: recordInstallationTelemetry
      tags: [platform-operations]
      summary: Record current telemetry for an installation
      description: >-
        Current state only. An out-of-order report is ignored rather than
        allowed to overwrite fresher truth.
      parameters:
        - name: installationId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordTelemetryInput"
      responses:
        "201":
          description: Telemetry recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallationTelemetry"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /installations:
    get:
      operationId: listInstallations
      tags: [installations]
      summary: List installations
      description: Platform super_admin only.
      responses:
        "200":
          description: List of installations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Installation"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createInstallation
      tags: [installations]
      summary: Register a new installation
      description: Platform super_admin only.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInstallationInput"
      responses:
        "201":
          description: Installation registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Installation"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: installationKey already registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /installations/{id}:
    get:
      operationId: getInstallation
      tags: [installations]
      summary: Get an installation
      description: Platform super_admin only.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Installation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Installation"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateInstallation
      tags: [installations]
      summary: Update installation metadata
      description: Platform super_admin only. installationKey is immutable.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateInstallationInput"
      responses:
        "200":
          description: Installation updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Installation"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /installations/{id}/organizations:
    get:
      operationId: listInstallationOrganizations
      tags: [installations]
      summary: List organizations linked to an installation
      description: Platform super_admin only. Active links only (unlinked history is not returned here).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of active links
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InstallationOrganizationLink"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: linkInstallationOrganization
      tags: [installations]
      summary: Link an organization to an installation
      description: >-
        Platform super_admin only. Supports one installation hosting many
        organizations (a shared deployment) — this is additive, not a
        single-organization replacement.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkInstallationOrganizationInput"
      responses:
        "201":
          description: Organization linked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallationOrganizationLink"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Installation or organization not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Organization already linked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /installations/{id}/organizations/{organizationId}:
    delete:
      operationId: unlinkInstallationOrganization
      tags: [installations]
      summary: Unlink an organization from an installation
      description: >-
        Platform super_admin only. Soft unlink (unlinkedAt set) — the
        association's history is kept, not deleted.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Organization unlinked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallationOrganizationLink"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No active link found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /platform/organizations/{organizationId}/identity:
    get:
      operationId: getTenantIdentity
      tags: [platform-tenants]
      summary: Tenant identity card for one organization (platform super_admin)
      description: >-
        Everything a platform operator needs to positively identify the tenant they are about to act on —
        immutable tenant UUID, tenant code (slug), display name, status, hostnames, the installation(s) it is
        linked to with their environment and deployed version, this process's own release identity, module
        state and feature flags. Contains no HR data. Platform-scoped (super_admin), explicit target in the path.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Tenant identity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TenantIdentity"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /platform/organizations/{organizationId}/feature-flags:
    get:
      operationId: listTenantFeatureFlags
      tags: [platform-tenants]
      summary: Feature flags / controlled extensions for one organization (platform super_admin)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Every registered flag with its state for this organization (default off)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FeatureFlagState"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /platform/organizations/{organizationId}/feature-flags/{flagKey}:
    put:
      operationId: setTenantFeatureFlag
      tags: [platform-tenants]
      summary: Enable or disable one feature flag for exactly one organization (platform super_admin)
      description: >-
        Tenant-scoped, explicitly targeted, audited (feature_flag.enabled / feature_flag.disabled with the
        blast radius and reason). Enabling a flag for this organization never affects any other organization.
        Unregistered keys are refused.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: flagKey
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetFeatureFlagInput"
      responses:
        "200":
          description: Updated flag state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeatureFlagState"
        "400":
          description: Invalid key or body
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Organization or flag not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /break-glass/grants:
    get:
      operationId: listBreakGlassGrants
      tags: [break-glass]
      summary: List break-glass grants
      description: Platform super_admin only.
      parameters:
        - name: targetOrganizationId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: List of grants
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BreakGlassGrant"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createBreakGlassGrant
      tags: [break-glass]
      summary: Create and activate a break-glass grant
      description: >-
        Platform super_admin only (Owner Decision #31). Activated
        immediately by the same actor who creates it — there is no separate
        approval workflow in this foundation. scope must be a non-empty list
        of real, read-only permission keys; expiresAt must be in the future
        and within the platform's configured maximum duration.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBreakGlassGrantInput"
      responses:
        "201":
          description: Grant activated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BreakGlassGrant"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /break-glass/grants/{id}:
    get:
      operationId: getBreakGlassGrant
      tags: [break-glass]
      summary: Get a break-glass grant
      description: Platform super_admin only.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Grant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BreakGlassGrant"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /break-glass/grants/{id}/revoke:
    post:
      operationId: revokeBreakGlassGrant
      tags: [break-glass]
      summary: Revoke a break-glass grant
      description: >-
        Platform super_admin only. Takes effect immediately on the next
        privileged request under this grant — no session invalidation or
        frontend flag involved.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Grant revoked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BreakGlassGrant"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Grant already revoked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /me/organizations:
    get:
      operationId: listMyOrganizations
      tags: [organizations]
      summary: List my organization memberships
      description: >-
        Returns every organization the caller has an active membership in,
        with their role(s) and Primary HR status in each.
      responses:
        "200":
          description: List of memberships
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MembershipSummary"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /me/employee:
    get:
      operationId: getMyEmployee
      tags: [self-service]
      summary: Resolve the caller's own linked employee (Employee Self-Service, W39)
      description: >-
        Resolves "which employee is me" via the employee-user link (W14) for
        the caller's currently active organization only — never a
        client-supplied employee ID, never another organization's data.
        `linked: false` (with `employee: null`) is the intentional response
        when the caller's login isn't yet linked to an employee record, or
        no longer has an active organization membership resolvable — not an
        error. Gated by the `employee_self_service` module.
      responses:
        "200":
          description: The caller's own employee, or an intentional not-linked response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyEmployeeResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No active organization membership, or the employee_self_service module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /me/employee/profile-picture:
    post:
      operationId: uploadMyEmployeeProfilePicture
      tags: [self-service]
      summary: Set/replace the caller's own profile picture (Employee Self-Service)
      description: >-
        multipart/form-data upload, sharing the exact same validation and
        processing as the HR-administrator upload route (JPEG/PNG/WebP only,
        signature-validated, 5MB max, re-encoded to a resized, EXIF-stripped
        JPEG). Identity is always server-resolved from the caller's own
        employee-user link, the same way GET /me/employee resolves it — never
        a client-supplied employee ID. Gated by the employee_self_service
        module.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: The caller's own employee, with the new picture applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyEmployeeResponse"
        "400":
          description: Invalid file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No linked employee record, no active organization membership, or the employee_self_service module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: removeMyEmployeeProfilePicture
      tags: [self-service]
      summary: Remove the caller's own profile picture (Employee Self-Service)
      description: >-
        Identity is always server-resolved from the caller's own
        employee-user link — never a client-supplied employee ID.
      responses:
        "200":
          description: The caller's own employee, with the picture removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyEmployeeResponse"
        "403":
          description: No linked employee record, no active organization membership, or the employee_self_service module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /me/employment-history:
    get:
      operationId: getMyEmploymentHistory
      tags: [self-service]
      summary: The caller's own internal employment history (Employee Self-Service Career Profile foundation, Phase 3F, W105)
      description: >-
        Resolves "which employee is me" the same way GET /me/employee does —
        never a client-supplied employee ID. Reuses the existing
        employment_periods table (Phase 2A, W22) verbatim — transfer/
        promotion/confirmation events, most recent first. `linked: false`
        (with `items: []`) is the intentional not-linked response, mirroring
        GET /me/employee. Gated by the employee_self_service module.
      responses:
        "200":
          description: The caller's own employment history, or the controlled not-linked state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyEmploymentHistoryResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No active organization membership, or the employee_self_service module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /me/skills:
    get:
      operationId: getMySkills
      tags: [self-service]
      summary: The caller's own skills (Employee Self-Service Career Profile, Phase 3F, W106)
      description: >-
        Resolves "which employee is me" the same way GET /me/employee does —
        never a client-supplied employee ID. Reuses the existing
        employee_skills table (Phase 2A, W24) verbatim. `linked: false` (with
        `items: []`) mirrors GET /me/employee's own intentional not-linked
        state. Gated by the employee_self_service module.
      responses:
        "200":
          description: The caller's own skills, or the controlled not-linked state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MySkillsResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No active organization membership, or the employee_self_service module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /me/qualifications:
    get:
      operationId: getMyQualifications
      tags: [self-service]
      summary: The caller's own qualifications (Employee Self-Service Career Profile, Phase 3F, W106)
      description: >-
        Resolves "which employee is me" the same way GET /me/employee does.
        Reuses the existing employee_qualifications table (Phase 2A, W24)
        verbatim. `linked: false` (with `items: []`) mirrors GET /me/employee's
        own intentional not-linked state. Gated by the employee_self_service
        module.
      responses:
        "200":
          description: The caller's own qualifications, or the controlled not-linked state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyQualificationsResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No active organization membership, or the employee_self_service module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /me/certifications:
    get:
      operationId: getMyCertifications
      tags: [self-service]
      summary: The caller's own external/HR-maintained certifications (Employee Self-Service Career Profile, Phase 3F, W106)
      description: >-
        Resolves "which employee is me" the same way GET /me/employee does.
        Reuses the existing employee_certifications table (Phase 2A, W24)
        verbatim — deliberately never learning_certificates (Phase 3D), which
        remains My Learning's own exclusive surface. Expired records are
        always included, never filtered. `linked: false` (with `items: []`)
        mirrors GET /me/employee's own intentional not-linked state. Gated by
        the employee_self_service module.
      responses:
        "200":
          description: The caller's own certifications, or the controlled not-linked state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyCertificationsResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No active organization membership, or the employee_self_service module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /me/internal-vacancies:
    get:
      operationId: listMyInternalVacancies
      tags: [self-service]
      summary: List internal vacancies eligible for the caller to apply to (Employee Self-Service Internal Applications, W60)
      description: >-
        Resolves "which employee is me" the same way GET /me/employee does —
        never a client-supplied employee ID. `linked: false` (unlinked
        login) or `active: false` (linked but not an active employee) both
        return `items: []` as an intentional 200-OK controlled state, not
        an error, mirroring GET /me/employee's own "linked: false"
        precedent. Only vacancies that are published, internally visible
        (visibility internal or both), and within their open/close window
        are included — never recruiter, hiring-manager, requisition,
        approval, or audit detail. Gated by both employee_self_service and
        recruitment (independently — a disabled recruitment module
        degrades only this section, per §8).
      responses:
        "200":
          description: Eligible internal vacancies, or the controlled not-linked/not-active state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InternalVacanciesResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No active organization membership, or a required module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /me/internal-vacancies/{publicId}/apply:
    post:
      operationId: applyToInternalVacancy
      tags: [self-service]
      summary: Submit an internal application to a vacancy (W60)
      description: >-
        Employee identity, candidate resolution/linkage, and vacancy
        eligibility are all re-derived and re-validated server-side —
        nothing supplied by the caller beyond the vacancy's publicId and
        optional screening answers is trusted. Idempotent: a repeat
        submission to the same vacancy returns the existing application
        (isNew: false), never a second row (applications.candidateId +
        vacancyId is unique). Enters the existing W51 pipeline unchanged,
        source marked "internal_ess". Never invokes W59's employee
        conversion and never changes the employee record. 403 if the
        caller has no active employee link (unlike the two GET endpoints,
        a blocked write action is a conventional error here, not a
        200-with-flag response).
      parameters:
        - name: publicId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitInternalApplicationInput"
      responses:
        "201":
          description: Application submitted (or the existing one returned idempotently)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyInternalApplicationSubmitResult"
        "403":
          description: No active employee link for this user in this organization, or the employee is not active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Vacancy not found (or not internally eligible)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /me/applications:
    get:
      operationId: listMyInternalApplications
      tags: [self-service]
      summary: List the caller's own internal applications (W60)
      description: >-
        Only the caller's own applications, resolved via the same
        employee-candidate linkage established at submission time — never
        another employee's. No recruiter notes, screening scores, interview
        scorecards, reference/background checks, or offer/approval detail
        is ever included. Same controlled `linked`/`active` 200-OK state as
        GET /me/internal-vacancies.
      responses:
        "200":
          description: The caller's own internal applications, or the controlled not-linked/not-active state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyInternalApplicationsResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No active organization membership, or a required module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees:
    get:
      operationId: listEmployees
      tags: [employees]
      summary: List employees
      description: >-
        Search, filter, and paginate the organization's employee directory.
        Gated employee.read (the directory grant every role holds). Personal
        identity fields on each row are nulled with
        `sensitiveFieldsRedacted: true` unless the caller holds
        employee.sensitive.read or the row is their own record — see the
        Employee schema.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: search
          in: query
          schema:
            type: string
        - name: departmentId
          in: query
          schema:
            type: integer
        - name: branchId
          in: query
          schema:
            type: integer
        - name: positionId
          in: query
          schema:
            type: integer
        - name: employmentStatus
          in: query
          schema:
            type: string
            enum: [active, probation, on_leave, suspended, terminated]
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated employee list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeListResponse"
        "403":
          description: No active membership in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createEmployee
      tags: [employees]
      summary: Create an employee
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEmployeeInput"
      responses:
        "201":
          description: Employee created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "400":
          description: Invalid input, or a referenced department/branch/position/manager is invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}:
    get:
      operationId: getEmployee
      tags: [employees]
      summary: Get an employee
      description: >-
        Gated employee.read. Personal identity fields are nulled with
        `sensitiveFieldsRedacted: true` unless the caller holds
        employee.sensitive.read or this is their own record (resolved
        server-side via employee_user_links) — see the Employee schema.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Employee details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateEmployee
      tags: [employees]
      summary: Update an employee
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEmployeeInput"
      responses:
        "200":
          description: Employee updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "400":
          description: Invalid input, or a referenced department/branch/position/manager is invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/profile-picture:
    post:
      operationId: uploadEmployeeProfilePicture
      tags: [employees]
      summary: Upload or replace an employee's profile picture
      description: >-
        multipart/form-data upload. JPEG/PNG/WebP only, validated by file
        signature (not just Content-Type), 5MB max. Re-encoded to a resized,
        EXIF-stripped JPEG before storage.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Profile picture updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "400":
          description: Invalid file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: removeEmployeeProfilePicture
      tags: [employees]
      summary: Remove an employee's profile picture
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Profile picture removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"

  /organizations/{organizationId}/employees/{employeeId}/link-user:
    post:
      operationId: linkEmployeeToUser
      tags: [employees]
      summary: Link an employee record to a login account
      description: >-
        Optional. The target application user must already have an active
        membership in this organization — linking never grants access on its
        own.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkEmployeeUserInput"
      responses:
        "200":
          description: Linked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "400":
          description: Target user has no active membership in this organization, or is already linked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: unlinkEmployeeFromUser
      tags: [employees]
      summary: Remove an employee record's link to a login account
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Unlinked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "404":
          description: Employee not found, or not currently linked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/separate:
    post:
      operationId: separateEmployee
      tags: [employees]
      summary: Separate (terminate) an employee
      description: >-
        The employee record is never deleted, only marked terminated with a
        date/reason (ADR-013). Rejected if already separated.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SeparateEmployeeInput"
      responses:
        "200":
          description: Employee separated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "400":
          description: Employee is already separated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/rehire:
    post:
      operationId: rehireEmployee
      tags: [employees]
      summary: Rehire a separated employee
      description: >-
        Starts a new employment period on the same record (ADR-013) — clears
        the separation date/reason; their prior values remain in the audit
        trail. Rejected if not currently separated.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Employee rehired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "400":
          description: Employee is not currently separated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/transfer:
    post:
      operationId: transferEmployee
      tags: [employees]
      summary: Transfer an employee to a new department, branch, and/or position
      description: >-
        Records a dated employment_periods event (W22) alongside updating the
        employee's current placement; cross-org reference validation reuses
        the same check create/update already runs. Rejected if none of
        departmentId/branchId/positionId would actually change.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TransferEmployeeInput"
      responses:
        "200":
          description: Employee transferred
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "400":
          description: Invalid reference, or no actual change
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/promote:
    post:
      operationId: promoteEmployee
      tags: [employees]
      summary: Promote an employee to a new position
      description: >-
        Position/title change with an effective date, recorded as a dated
        employment_periods event (W22). Does not include compensation or
        salary — no such concept exists in this schema. Rejected if the
        target position is the employee's current one.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PromoteEmployeeInput"
      responses:
        "200":
          description: Employee promoted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "400":
          description: Invalid reference, or no actual change
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/confirm:
    post:
      operationId: confirmEmployee
      tags: [employees]
      summary: Confirm an employee off probation
      description: >-
        Formalizes the existing employmentStatus "probation" -> "active"
        transition into an audited, dated employment_periods event (W22).
        Rejected if the employee is not currently on probation.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfirmEmployeeInput"
      responses:
        "200":
          description: Employee confirmed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "400":
          description: Employee is not currently on probation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/disciplinary-records:
    get:
      operationId: listEmployeeDisciplinaryRecords
      tags: [employees]
      summary: List an employee's disciplinary records
      description: >-
        Gated by employee.disciplinary.read, a narrower permission than
        employee.read (Architecture Decision 5) — the same precedent as
        employee.notes.read.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Disciplinary records for the employee, most recent action first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeDisciplinaryRecord"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: addEmployeeDisciplinaryRecord
      tags: [employees]
      summary: Record a disciplinary action for an employee
      description: >-
        Append-only — there is no update or remove endpoint, so a new action
        never replaces a prior record.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddEmployeeDisciplinaryRecordInput"
      responses:
        "201":
          description: Disciplinary record added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeDisciplinaryRecord"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/exit-process:
    get:
      operationId: listEmployeeExitProcesses
      tags: [employees]
      summary: List an employee's exit (off-boarding) processes
      description: >-
        One row per separation cycle — an employee separated, rehired, and
        separated again gets a new exit process each time, most recent first.
        Gated employee.write (the same key as creating/updating an exit
        process): exit records are HR data, so the directory grant
        employee.read is not sufficient (2026-09-15).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Exit processes for the employee
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeExitProcess"
        "403":
          description: Forbidden — requires employee.write
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createEmployeeExitProcess
      tags: [employees]
      summary: Start the off-boarding process for an employee's current separation
      description: >-
        Attaches to the existing separation event (W15) — does not modify or
        duplicate separateEmployee. Rejected if the employee is not currently
        separated, or if an exit process already exists for this separation.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "201":
          description: Exit process created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeExitProcess"
        "400":
          description: Employee is not currently separated, or an exit process already exists for this separation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/exit-process/{exitProcessId}:
    patch:
      operationId: updateEmployeeExitProcess
      tags: [employees]
      summary: Update checklist/clearance/exit-interview progress on an exit process
      description: Never creates a competing record — updates the same row in place.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: exitProcessId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEmployeeExitProcessInput"
      responses:
        "200":
          description: Exit process updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeExitProcess"
        "404":
          description: Employee or exit process not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/documents:
    get:
      operationId: listEmployeeDocuments
      tags: [employees]
      summary: List an employee's documents
      description: >-
        Gated employee.read, then (WWM Employee Access Remediation,
        2026-09-07) the caller must either be this employee themselves
        (resolved server-side via employee_user_links — ESS "My Documents")
        or hold employee.documents.read; any other colleague receives 403.
        Upload/delete remain employee.write.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Documents for the employee
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeDocument"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: uploadEmployeeDocument
      tags: [employees]
      summary: Upload a document for an employee
      description: >-
        multipart/form-data upload. PDF, JPEG, PNG, DOCX, and XLSX only,
        validated by file signature (not just Content-Type), 10MB max. Each
        upload creates a new document row — re-uploading does not replace or
        version a prior one; remove it separately if it should be superseded.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, categoryCode]
              properties:
                file:
                  type: string
                  format: binary
                categoryCode:
                  type: string
                  description: Code from the "document_category" Master Data domain.
      responses:
        "201":
          description: Document uploaded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeDocument"
        "400":
          description: Invalid file or missing category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/documents/{documentId}:
    delete:
      operationId: removeEmployeeDocument
      tags: [employees]
      summary: Remove an employee document
      description: Deletes both the stored file and its record.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Document removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "404":
          description: Employee or document not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/skills:
    get:
      operationId: listEmployeeSkills
      tags: [employees]
      summary: List an employee's skills
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Skills for the employee
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeSkill"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: addEmployeeSkill
      tags: [employees]
      summary: Add a skill to an employee
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddEmployeeSkillInput"
      responses:
        "201":
          description: Skill added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeSkill"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/skills/{skillId}:
    patch:
      operationId: updateEmployeeSkill
      tags: [employees]
      summary: Update an employee's skill
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: skillId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEmployeeSkillInput"
      responses:
        "200":
          description: Skill updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeSkill"
        "404":
          description: Employee or skill not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: removeEmployeeSkill
      tags: [employees]
      summary: Remove an employee's skill
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: skillId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Skill removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "404":
          description: Employee or skill not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/qualifications:
    get:
      operationId: listEmployeeQualifications
      tags: [employees]
      summary: List an employee's qualifications
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Qualifications for the employee
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeQualification"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: addEmployeeQualification
      tags: [employees]
      summary: Add a qualification to an employee
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddEmployeeQualificationInput"
      responses:
        "201":
          description: Qualification added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeQualification"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/qualifications/{qualificationId}:
    patch:
      operationId: updateEmployeeQualification
      tags: [employees]
      summary: Update an employee's qualification
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: qualificationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEmployeeQualificationInput"
      responses:
        "200":
          description: Qualification updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeQualification"
        "404":
          description: Employee or qualification not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: removeEmployeeQualification
      tags: [employees]
      summary: Remove an employee's qualification
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: qualificationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Qualification removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "404":
          description: Employee or qualification not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/certifications:
    get:
      operationId: listEmployeeCertifications
      tags: [employees]
      summary: List an employee's certifications
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Certifications for the employee
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeCertification"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: addEmployeeCertification
      tags: [employees]
      summary: Add a certification to an employee
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddEmployeeCertificationInput"
      responses:
        "201":
          description: Certification added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeCertification"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/certifications/{certificationId}:
    patch:
      operationId: updateEmployeeCertification
      tags: [employees]
      summary: Update an employee's certification
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: certificationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEmployeeCertificationInput"
      responses:
        "200":
          description: Certification updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeCertification"
        "404":
          description: Employee or certification not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: removeEmployeeCertification
      tags: [employees]
      summary: Remove an employee's certification
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: certificationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Certification removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "404":
          description: Employee or certification not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/employment-history:
    get:
      operationId: listEmployeeEmploymentHistory
      tags: [employees]
      summary: An employee's internal employment history (Phase 3F, W105)
      description: >-
        HR-administration read of employment_periods (Phase 2A, W22) —
        closes the pre-existing gap where Transfer/Promote/Confirm wrote
        this data but nothing ever rendered it back. Requires employee.write,
        matching the same HR-authoritative floor every other HR-
        administration route on this resource requires — not an own-scope
        self-service read (see GET /me/employment-history for that).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The employee's employment history, most recent first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmploymentPeriodSummary"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/number/allocate:
    post:
      operationId: allocateEmployeeNumber
      tags: [employees]
      summary: Allocate a staff number to an employee (Phase 3H, W114)
      description: >-
        Generates a number via the organization's numbering-format engine,
        or accepts a manual override — either path is validated against
        employee_number_allocations' history and this organization's reuse
        policy. Requires employee_number.allocate. Fails with 400 if this
        employee already has an active allocation (release it first).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AllocateEmployeeNumberInput"
      responses:
        "201":
          description: The newly allocated staff number
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeNumberAllocationResult"
        "400":
          description: Invalid request, already has an active number, or reuse disabled for this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This staff number is already actively allocated to another employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/number/release:
    post:
      operationId: releaseEmployeeNumber
      tags: [employees]
      summary: Release an employee's active staff number (Phase 3H, W114)
      description: >-
        Explicit HR action, never automatic on separation. Requires
        employee_number.allocate. Blocked (400) while the employee is still
        actively employed (anything other than employmentStatus terminated).
        Closes the allocation historically — never deletes it — and clears
        the employee's current employeeNumber.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The released allocation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeNumberAllocationResult"
        "400":
          description: Employee still actively employed, or no active allocation to release
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/number/history:
    get:
      operationId: listEmployeeNumberHistory
      tags: [employees]
      summary: An employee's full staff-number allocation history (Phase 3H, W114)
      description: >-
        The authoritative historical source (frozen plan §6) — never
        employees.employeeNumber alone. Requires employee.write, matching
        the same HR-authoritative floor listEmployeeEmploymentHistory uses.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: This employee's allocation history, oldest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeNumberAllocation"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employee-numbers/{employeeNumber}/history:
    get:
      operationId: listEmployeeNumberOwnershipHistory
      tags: [employees]
      summary: Every employee who has ever held this exact staff number (Phase 3H, W114)
      description: >-
        The reuse-ambiguity-safe view (frozen plan §10) — a reused number
        must always show every past and present holder, clearly labeled,
        never silently collapsed to the current one. Requires employee.write.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeNumber
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Every allocation of this exact number, oldest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeNumberAllocation"

  /organizations/{organizationId}/employees/{employeeId}/personnel-file:
    post:
      operationId: createPersonnelFile
      tags: [personnel-files]
      summary: Create the employee's permanent personnel file / PIF (Phase 3H, W115)
      description: >-
        Requires personnel_file.manage. Fails with 400 if this employee
        already has a personnel file — exactly one exists per employee,
        permanently (frozen plan §7a); the database's own unique index on
        employeeId is the final authority, not merely this pre-check.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePersonnelFileInput"
      responses:
        "201":
          description: The newly created personnel file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelFile"
        "400":
          description: Invalid request or this employee already has a personnel file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This PIF number is already in use in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    get:
      operationId: getPersonnelFileByEmployee
      tags: [personnel-files]
      summary: Get an employee's personnel file (Phase 3H, W115)
      description: Requires personnel_file.read. 404s if this employee has no personnel file yet.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The employee's personnel file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelFile"
        "404":
          description: Employee not found, or this employee has no personnel file yet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-files/{personnelFileId}:
    get:
      operationId: getPersonnelFileById
      tags: [personnel-files]
      summary: Get a personnel file by id (Phase 3H, W115)
      description: Requires personnel_file.read.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: personnelFileId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The personnel file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelFile"
        "404":
          description: Personnel file not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-records/search:
    get:
      operationId: searchPersonnelRecords
      tags: [personnel-files]
      summary: HR/records search by name, current or historical staff number, or PIF number (Phase 3H, W115)
      description: >-
        Requires personnel_file.read — deliberately separate from
        GET .../employees (employee.read), which is never extended to
        expose PIF or historical-number data (frozen plan §10/§13). A
        reused staff number never collapses to the current holder: matches
        are returned one per allocation, each labeled current or historical.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: search
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Matching personnel records, one row per match reason
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PersonnelSearchResult"

  /organizations/{organizationId}/records-locations:
    get:
      operationId: listRecordsLocations
      tags: [records-locations]
      summary: List an organization's physical records locations (Phase 3H, W116)
      description: Requires personnel_file.read. Flat list — the caller reconstructs the hierarchy from parentId.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: All records locations in this organization
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RecordsLocation"
    post:
      operationId: createRecordsLocation
      tags: [records-locations]
      summary: Create a records location (Phase 3H, W116)
      description: Requires personnel_file.manage. parentId (if given) must belong to this organization and must not create a cycle.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRecordsLocationInput"
      responses:
        "201":
          description: The newly created records location
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordsLocation"
        "400":
          description: Invalid parent (cross-organization or would create a cycle)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/records-locations/{id}:
    patch:
      operationId: updateRecordsLocation
      tags: [records-locations]
      summary: Update a records location's name, description, or parent (Phase 3H, W116)
      description: Requires personnel_file.manage.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRecordsLocationInput"
      responses:
        "200":
          description: The updated records location
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordsLocation"
        "400":
          description: Invalid parent (cross-organization, self-parent, or would create a cycle)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Records location not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/vehicles:
    get:
      operationId: listVehicles
      tags: [vehicles]
      summary: List the organization's vehicles (VR-01)
      description: >-
        Requires the vehicle_management module and vehicle.read. Optional status
        and search filters; search matches registration number, make or model.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [available, in_use, maintenance, inactive]
        - name: search
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: The organization's vehicles, ordered by registration number
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Vehicle"
    post:
      operationId: createVehicle
      tags: [vehicles]
      summary: Register a vehicle (VR-01)
      description: >-
        Requires the vehicle_management module and vehicle.manage. The
        registration number is normalized (trimmed, single-spaced, upper-cased)
        and must be unique within the organization. A new vehicle always starts
        available.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateVehicleInput"
      responses:
        "201":
          description: The newly registered vehicle
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vehicle"
        "400":
          description: Invalid field, or a branch or employee from another organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: That registration number is already registered in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/vehicles/{vehicleId}:
    get:
      operationId: getVehicle
      tags: [vehicles]
      summary: Get one vehicle (VR-01)
      description: Requires the vehicle_management module and vehicle.read. Another organization's vehicle is not found.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: vehicleId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The vehicle
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vehicle"
        "404":
          description: Vehicle not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateVehicle
      tags: [vehicles]
      summary: Update a vehicle's register details or status (VR-01)
      description: >-
        Requires the vehicle_management module and vehicle.manage. Status may be
        set to available, maintenance or inactive only — in_use belongs to the
        VR-02 release/return flow, and a vehicle that is currently out must have
        its return recorded before its status changes here.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: vehicleId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateVehicleInput"
      responses:
        "200":
          description: The updated vehicle
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vehicle"
        "400":
          description: Invalid field, disallowed status, or a branch or employee from another organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Vehicle not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: That registration number is already registered in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/records-locations/{id}/retire:
    post:
      operationId: retireRecordsLocation
      tags: [records-locations]
      summary: Retire a records location (Phase 3H, W116)
      description: >-
        Requires personnel_file.manage. Never cascades — existing occupants
        are untouched; retirement only blocks this location from being
        chosen for a NEW custody assignment going forward.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The retired records location
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordsLocation"
        "404":
          description: Records location not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/records-locations/{id}/reactivate:
    post:
      operationId: reactivateRecordsLocation
      tags: [records-locations]
      summary: Reactivate a retired records location (Phase 3H, W116)
      description: Requires personnel_file.manage.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The reactivated records location
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordsLocation"
        "404":
          description: Records location not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-files/{personnelFileId}/custody:
    get:
      operationId: getPersonnelFileCustody
      tags: [personnel-file-custody]
      summary: Live-derived current custody state for a personnel file (Phase 3H, W116)
      description: >-
        Requires personnel_file.read. currentCustodyState/currentLocationId
        are the file-level cache (used only when this organization does not
        use volumes for this file); overdue is always computed live, never
        stored.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: personnelFileId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The current custody snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelFileCustodyDetail"
        "404":
          description: Personnel file not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-files/{personnelFileId}/volumes:
    get:
      operationId: listPersonnelFileVolumes
      tags: [personnel-file-custody]
      summary: List a personnel file's physical volumes (Phase 3H, W116)
      description: Requires personnel_file.read. Empty if this organization does not use volumes for this file.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: personnelFileId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: This file's volumes, ordered by volume number
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PersonnelFileVolume"
        "404":
          description: Personnel file not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createPersonnelFileVolume
      tags: [personnel-file-custody]
      summary: Create the next sequential volume for a personnel file (Phase 3H, W116)
      description: Requires personnel_file.manage. volumeNumber is assigned automatically, concurrency-safe.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: personnelFileId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "201":
          description: The newly created volume
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelFileVolume"
        "404":
          description: Personnel file not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-files/{personnelFileId}/movements:
    get:
      operationId: listPersonnelFileMovements
      tags: [personnel-file-custody]
      summary: A personnel file's full custody/movement history (Phase 3H, W116)
      description: >-
        Requires personnel_file.read. Append-only, immutable, most recent
        first. Pass volumeId to scope to one volume's own history.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: personnelFileId
          in: path
          required: true
          schema:
            type: integer
        - name: volumeId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: This file's (or volume's) movement history, most recent first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PersonnelFileMovement"
        "404":
          description: Personnel file not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-files/{personnelFileId}/checkout:
    post:
      operationId: checkoutPersonnelFile
      tags: [personnel-file-custody]
      summary: Check out a personnel file or volume (Phase 3H, W116)
      description: >-
        Requires personnel_file.movement.write (separate from
        personnel_file.read/.manage — viewing or managing the registry does
        not by itself grant the ability to move files). Valid only from
        in_registry.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: personnelFileId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CheckoutPersonnelFileInput"
      responses:
        "201":
          description: The checked_out movement event
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelFileMovement"
        "404":
          description: Personnel file, volume, or the given location was not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Illegal transition — the file/volume is not currently in_registry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-files/{personnelFileId}/return:
    post:
      operationId: returnPersonnelFile
      tags: [personnel-file-custody]
      summary: Return a checked-out (or missing) personnel file or volume (Phase 3H, W116)
      description: >-
        Requires personnel_file.movement.write. Valid from checked_out (the
        ordinary case) or missing (found and brought back by the person who
        had it).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: personnelFileId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnPersonnelFileInput"
      responses:
        "201":
          description: The returned movement event
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelFileMovement"
        "404":
          description: Personnel file, volume, or the given location was not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Illegal transition — the file/volume is not currently checked_out or missing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-files/{personnelFileId}/mark-missing:
    post:
      operationId: markPersonnelFileMissing
      tags: [personnel-file-custody]
      summary: Mark a checked-out personnel file or volume missing (Phase 3H, W116)
      description: Requires personnel_file.movement.write. Valid only from checked_out. A reason is mandatory.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: personnelFileId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkPersonnelFileMissingInput"
      responses:
        "201":
          description: The marked_missing movement event
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelFileMovement"
        "400":
          description: A reason is required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Personnel file or volume not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Illegal transition — the file/volume is not currently checked_out
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-files/{personnelFileId}/recover:
    post:
      operationId: recoverPersonnelFile
      tags: [personnel-file-custody]
      summary: Recover a missing personnel file or volume (Phase 3H, W116)
      description: >-
        Requires personnel_file.movement.write. Valid only from missing —
        found independently of the holder (e.g. in the wrong location),
        distinct from a direct return by the original holder.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: personnelFileId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecoverPersonnelFileInput"
      responses:
        "201":
          description: The recovered movement event
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelFileMovement"
        "404":
          description: Personnel file, volume, or the given location was not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Illegal transition — the file/volume is not currently missing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/leave-requests:
    get:
      operationId: listLeaveRequests
      tags: [leave-requests]
      summary: List an employee's leave requests
      description: >-
        Own resource, manager-scoped, or leave_request.manage (organization-
        wide) — gated by leave_request.read.own at the route level, refined
        by an authorization check against the specific employee. Gated by
        the "leave" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Leave requests for the employee, most recent first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeaveRequest"
        "403":
          description: Not authorized to view this employee's leave requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createLeaveRequest
      tags: [leave-requests]
      summary: Submit a leave request
      description: >-
        Own resource only. The applicable leave policy is resolved
        server-side from the employee's eligibility — never client-supplied.
        Day count is calculated server-side from the policy's rules; the
        client's arithmetic is never trusted.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLeaveRequestInput"
      responses:
        "201":
          description: Leave request submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveRequest"
        "400":
          description: Invalid request, ineligible employee, or a conflicting/overlapping request already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Can only submit a leave request for yourself
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/leave-requests/{id}/cancel:
    post:
      operationId: cancelLeaveRequest
      tags: [leave-requests]
      summary: Withdraw a pending leave request
      description: Own resource only. Rejected if the request is not currently pending.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Leave request withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveRequest"
        "400":
          description: Request is not currently pending
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Can only withdraw your own leave request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Leave request not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/leave-requests/pending-approvals:
    get:
      operationId: listPendingApprovals
      tags: [leave-approvals]
      summary: List pending leave requests the caller is authorized to act on
      description: >-
        Org-wide for leave_request.manage holders; otherwise scoped to
        direct reports only (employees.reportingManagerId). Gated by the
        "leave" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Pending requests the caller may approve or reject
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeaveRequest"

  /organizations/{organizationId}/leave-calendar:
    get:
      operationId: listLeaveCalendar
      tags: [leave-calendar]
      summary: Organization-scoped calendar of approved leave within a date range
      description: >-
        A read model over leave_requests — never a second source of truth.
        Only status "approved" is ever included. Org-wide for
        leave_request.manage holders; otherwise scoped to the caller's own
        approved leave plus their direct reports' (employees.
        reportingManagerId), same as leave-requests/pending-approvals.
        Confidential fields (reason, attachments, approval comments) are
        never returned. Holidays (W37) are overlaid as a separate, read-only
        list — never merged into leaveEntries and never represented as a
        leave request. The date range is bounded server-side. Gated by the
        "leave" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: from
          in: query
          required: true
          description: Inclusive range start, YYYY-MM-DD.
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          description: Inclusive range end, YYYY-MM-DD. Range cannot exceed 100 days.
          schema:
            type: string
            format: date
        - name: departmentId
          in: query
          required: false
          schema:
            type: integer
        - name: branchId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Approved leave and overlapping holidays within the requested range, visible to the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveCalendarResponse"
        "400":
          description: Missing/invalid from or to, or the range exceeds 100 days
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/attendance-events:
    post:
      operationId: recordAttendanceEvent
      tags: [attendance-events]
      summary: Self-service clock-in or clock-out
      description: >-
        Own resource only. employeeId is always resolved server-side via
        employee_user_links — never accepted from the client. occurredAt is
        always the server's own clock — never client-supplied. No sequencing
        validation is applied (Phase 3B, Open Decision 1): a multi-segment
        day, or a raw duplicate clock-in with no intervening clock-out, is
        recorded as-is; the daily summary (a later workstream) decides how
        to interpret it. Gated by the "attendance" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordAttendanceEventInput"
      responses:
        "201":
          description: Attendance event recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceEvent"
        "400":
          description: Invalid eventType
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No employee record linked to this account, or the employee is not eligible to record attendance (on_leave, suspended, terminated, or before hireDate)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    get:
      operationId: listAttendanceEvents
      tags: [attendance-events]
      summary: List attendance events for an employee
      description: >-
        employeeId omitted defaults to the caller's own history. Own
        resource, team (employees.reportingManagerId), or organization-wide
        (attendance.manage) — gated by attendance.read.own at the route
        level, refined by an authorization check against the specific
        employee. Gated by the "attendance" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Attendance events for the employee, most recent first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AttendanceEvent"
        "403":
          description: Not authorized to view this employee's attendance events
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/attendance-adjustments:
    post:
      operationId: recordAttendanceAdjustment
      tags: [attendance-adjustments]
      summary: HR direct-entry correction (auto-approved), or an employee's own correction request (pending)
      description: >-
        One shared endpoint, two branches, decided by whether the caller
        holds attendance.manage — not by any request field. Requires
        attendance.read.own at minimum (every role has it).


        HR direct entry (attendance.manage holder): employeeId is
        caller-supplied (the target employee). Auto-decided in the same
        operation it's created (status "approved", decidedByMembershipId =
        requestedByMembershipId).


        Employee-initiated request (no attendance.manage): employeeId is
        always server-derived from the authenticated caller — a
        client-supplied employeeId in the body is ignored. Always created
        "pending"; never auto-approves itself. Decided later via
        POST .../attendance-adjustments/{id}/approve or .../reject.


        reason is always required for both branches. correctedClockIn is
        required when adjustmentType is manual_clock_in; correctedClockOut
        is required when adjustmentType is manual_clock_out. Gated by the
        "attendance" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordAttendanceAdjustmentInput"
      responses:
        "201":
          description: Attendance adjustment recorded — status is "approved" for HR direct entry, "pending" for an employee-initiated request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceAdjustment"
        "400":
          description: Invalid adjustmentType, missing date/reason, or a required corrected timestamp is missing for the given adjustmentType
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No employee record linked to this account (employee-initiated branch only)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization (HR direct-entry branch only)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/attendance-adjustments/{id}/approve:
    post:
      operationId: approveAttendanceAdjustment
      tags: [attendance-adjustments]
      summary: Approve a pending Attendance correction request
      description: >-
        Requires attendance.adjustment.approve — an organization-wide-only
        tier for this workstream (no delegated/manager approval). An
        atomic conditional update takes the row; a concurrent second
        decision on the same request receives 409, never a silent
        double-decision. Gated by the "attendance" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Adjustment approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceAdjustment"
        "404":
          description: Adjustment not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Adjustment is not pending — already decided
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/attendance-adjustments/{id}/reject:
    post:
      operationId: rejectAttendanceAdjustment
      tags: [attendance-adjustments]
      summary: Reject a pending Attendance correction request
      description: >-
        Requires attendance.adjustment.approve. Same atomic terminal-state
        protection as approve — a rejected or already-approved request
        cannot be redecided. Gated by the "attendance" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Adjustment rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceAdjustment"
        "404":
          description: Adjustment not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Adjustment is not pending — already decided
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/attendance/summary:
    get:
      operationId: getAttendanceDailySummary
      tags: [attendance-summary]
      summary: Daily Attendance summary — single date or a date range
      description: >-
        A computed read-model over attendance_events, approved
        attendance_adjustments, approved Leave, public holidays, and
        organization Attendance/timezone configuration — never a second
        source of truth, nothing is persisted or mutated by this read.
        Provide either `date` (single-day mode, returns one object) or both
        `from`/`to` (range mode, returns a dense array, one entry per date,
        max 100 days). Own resource, direct-report (team), or
        organization-wide (attendance.manage) — gated by attendance.read.own
        at the route level, refined by an authorization check against the
        specific employee. Gated by the "attendance" module. status is null
        when no summary is generated (before hireDate, on/after
        separationDate, or an otherwise-absent day for an employee whose
        current employmentStatus is on_leave/suspended).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: date
          in: query
          required: false
          description: Single-day mode, YYYY-MM-DD (organization-local civil date). Mutually exclusive with from/to.
          schema:
            type: string
            format: date
        - name: from
          in: query
          required: false
          description: Range mode start (inclusive), YYYY-MM-DD.
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: false
          description: Range mode end (inclusive), YYYY-MM-DD. Range cannot exceed 100 days.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: A single summary (date mode) or a dense array of summaries (from/to mode)
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/DailyAttendanceSummary"
                  - type: array
                    items:
                      $ref: "#/components/schemas/DailyAttendanceSummary"
        "400":
          description: Neither date nor from/to provided, an invalid date format, an inverted range, or a range exceeding 100 days
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not authorized to view this employee's attendance summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The organization's timezone (general.timezone) is not configured — required before an Attendance summary can be computed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/attendance:
    get:
      operationId: listAttendanceRegister
      tags: [attendance-register]
      summary: Attendance Register — internal HR/manager list view
      description: >-
        A view over the W66 daily-summary read-model, paginated by
        employee (never a raw table scan, never a second summary engine).
        attendance.manage holders get organization-wide visibility;
        everyone else (attendance.read.own) sees only themselves and their
        direct reports (employees.reportingManagerId) — the same
        service-layer tier leaveCalendar.ts already uses, with no
        dedicated attendance.read.team permission. employeeId/
        departmentId/branchId filters can only narrow this authorized
        scope, never broaden it — an employeeId outside the caller's
        scope yields zero rows, not an error. status filters the computed
        page: a row is kept if at least one of its days matches; `total`
        reflects the employee-level scope/filter count before status
        filtering. A manager with zero direct reports receives a valid
        empty result, never an organization-wide fallback. Gated by the
        "attendance" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: from
          in: query
          required: true
          description: Inclusive range start, YYYY-MM-DD (organization-local civil date).
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          description: Inclusive range end, YYYY-MM-DD. Range cannot exceed 100 days. from=to gives a single-day ("daily") register; a wider range gives a "monthly" view.
          schema:
            type: string
            format: date
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: departmentId
          in: query
          required: false
          schema:
            type: integer
        - name: branchId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          description: One of the 7 DailyAttendanceSummary statuses (present, late, partial, absent, on_leave, holiday, non_working_day).
          schema:
            type: string
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          required: false
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: A page of the register
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceRegisterResponse"
        "400":
          description: Missing/invalid from or to, an inverted or too-wide range, or an invalid status value
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The organization's timezone (general.timezone) is not configured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/attendance/dashboard:
    get:
      operationId: getAttendanceDashboard
      tags: [attendance-reporting]
      summary: Attendance dashboard — status tile breakdown for a single date
      description: >-
        A zero-filled count of every W66 status (present, late, partial,
        absent, on_leave, holiday, non_working_day, and null/"not
        applicable") for a single organization-local civil date — "today"
        (organization-timezone-derived, never browser-local) by default, or
        an explicit ?date=. Computed live over the W66 read-model, never
        cached or persisted. Visibility mirrors the Attendance Register
        (W69) exactly: attendance.manage holders see the whole
        organization; everyone else sees only themselves and their direct
        reports. Gated by the "attendance" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: date
          in: query
          required: false
          description: YYYY-MM-DD, organization-local civil date. Defaults to organization-local "today".
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Dashboard status breakdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceDashboard"
        "400":
          description: Invalid date
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The organization's timezone (general.timezone) is not configured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/attendance/reports/{reportKey}:
    get:
      operationId: runAttendanceReport
      tags: [attendance-reporting]
      summary: Run an attendance report
      description: >-
        Computes a registered attendance report (see GET /reports, category
        "attendance": attendance_daily_register, attendance_monthly_summary,
        attendance_late_arrivals, attendance_absenteeism) over a date range,
        scoped to the caller's own/team/organization-wide Attendance
        visibility tier (identical to the Register and Dashboard). from/to
        default to organization-local "today" when omitted, and reuse the
        same range validation as the Register (max 100 days, no inverted
        range). Pass ?format=csv for a CSV download instead of JSON.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: reportKey
          in: path
          required: true
          schema:
            type: string
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [json, csv]
      responses:
        "200":
          description: Attendance report result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportRunResult"
            text/csv:
              schema:
                type: string
        "400":
          description: Invalid/inverted/too-wide date range
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Unknown attendance report key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The organization's timezone (general.timezone) is not configured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/public-holidays:
    get:
      operationId: listPublicHolidays
      tags: [public-holidays]
      summary: List an organization's public holidays
      description: >-
        Active holidays by default; a recurring holiday always matches
        every year filter, since it applies every year regardless of its
        stored template year. Gated by the "leave" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: year
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Public holidays for the organization
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicHoliday"
    post:
      operationId: createPublicHoliday
      tags: [public-holidays]
      summary: Create a public holiday
      description: >-
        public_holiday.manage required — distinct from leave_request.approve
        and leave_type.manage, neither of which grants holiday management.
        scope is always "organization" in this phase regardless of what's
        sent. effectiveYear is required for a one-off holiday and rejected
        for a recurring one; observedDate is rejected for a recurring one.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePublicHolidayInput"
      responses:
        "201":
          description: Public holiday created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicHoliday"
        "400":
          description: Invalid date, scope, or recurring configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A holiday with this name already exists on this date
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/public-holidays/{id}:
    get:
      operationId: getPublicHoliday
      tags: [public-holidays]
      summary: Retrieve one public holiday
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The public holiday
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicHoliday"
        "404":
          description: Public holiday not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updatePublicHoliday
      tags: [public-holidays]
      summary: Update a public holiday
      description: public_holiday.manage required.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePublicHolidayInput"
      responses:
        "200":
          description: Public holiday updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicHoliday"
        "400":
          description: Invalid date, scope, or recurring configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Public holiday not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A holiday with this name already exists on this date
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: deletePublicHoliday
      tags: [public-holidays]
      summary: Permanently delete a public holiday
      description: >-
        Safe hard delete — no FK anywhere references a public_holidays row,
        and a past leave request's calculated days were already stored at
        submission time (W33), never recomputed later. Prefer
        deactivatePublicHoliday to keep the record for reference.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Public holiday deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "404":
          description: Public holiday not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/public-holidays/{id}/deactivate:
    post:
      operationId: deactivatePublicHoliday
      tags: [public-holidays]
      summary: Deactivate a public holiday (preferred over delete)
      description: Excluded from new leave calculations and the calendar overlay from this point on; historical requests are unaffected.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Public holiday deactivated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicHoliday"
        "404":
          description: Public holiday not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/public-holidays/{id}/reactivate:
    post:
      operationId: reactivatePublicHoliday
      tags: [public-holidays]
      summary: Reactivate a deactivated public holiday
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Public holiday reactivated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicHoliday"
        "404":
          description: Public holiday not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment-settings:
    get:
      operationId: getRecruitmentSettings
      tags: [recruitment]
      summary: Get an organization's Recruitment settings
      description: >-
        Returns the organization's saved Recruitment settings, or safe
        defaults if nothing has been saved yet — no row is created on read.
        Gated by the recruitment module and recruitment_settings.read.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Recruitment settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentSettings"
    patch:
      operationId: updateRecruitmentSettings
      tags: [recruitment]
      summary: Update an organization's Recruitment settings
      description: >-
        Merges the given fields into the organization's existing settings
        (or defaults) and validates the merged result before saving. Gated
        by the recruitment module and recruitment_settings.manage.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRecruitmentSettingsInput"
      responses:
        "200":
          description: Updated Recruitment settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentSettings"
        "400":
          description: Invalid settings, or defaultWorkflowId does not belong to this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment-workflows:
    get:
      operationId: listRecruitmentWorkflows
      tags: [recruitment]
      summary: List an organization's Recruitment workflows
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of recruitment workflows
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RecruitmentWorkflow"
    post:
      operationId: createRecruitmentWorkflow
      tags: [recruitment]
      summary: Create a Recruitment workflow
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRecruitmentWorkflowInput"
      responses:
        "201":
          description: Recruitment workflow created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentWorkflow"
        "409":
          description: A workflow with this name already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment-workflows/{workflowId}:
    patch:
      operationId: updateRecruitmentWorkflow
      tags: [recruitment]
      summary: Update a Recruitment workflow
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: workflowId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRecruitmentWorkflowInput"
      responses:
        "200":
          description: Recruitment workflow updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentWorkflow"
        "404":
          description: Recruitment workflow not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A workflow with this name already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment-workflows/{workflowId}/archive:
    post:
      operationId: archiveRecruitmentWorkflow
      tags: [recruitment]
      summary: Archive a Recruitment workflow
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: workflowId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Recruitment workflow archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentWorkflow"
        "404":
          description: Recruitment workflow not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment-workflows/{workflowId}/reactivate:
    post:
      operationId: reactivateRecruitmentWorkflow
      tags: [recruitment]
      summary: Reactivate an archived Recruitment workflow
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: workflowId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Recruitment workflow reactivated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentWorkflow"
        "404":
          description: Recruitment workflow not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment-workflows/{workflowId}/set-default:
    post:
      operationId: setDefaultRecruitmentWorkflow
      tags: [recruitment]
      summary: Set a Recruitment workflow as the organization's default
      description: >-
        Atomically unsets the current default (if any) and sets the target
        workflow, so the database's at-most-one-default-per-organization
        invariant is never briefly violated.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: workflowId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Recruitment workflow set as default
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentWorkflow"
        "404":
          description: Recruitment workflow not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment-workflows/{workflowId}/stages:
    get:
      operationId: listRecruitmentStages
      tags: [recruitment]
      summary: List a Recruitment workflow's stages
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: workflowId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of recruitment stages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RecruitmentStage"
        "404":
          description: Recruitment workflow not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createRecruitmentStage
      tags: [recruitment]
      summary: Create a stage within a Recruitment workflow
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: workflowId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRecruitmentStageInput"
      responses:
        "201":
          description: Recruitment stage created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentStage"
        "404":
          description: Recruitment workflow not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A stage with this name or display order already exists in this workflow
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment-workflows/{workflowId}/stages/{stageId}:
    patch:
      operationId: updateRecruitmentStage
      tags: [recruitment]
      summary: Update a Recruitment stage
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: workflowId
          in: path
          required: true
          schema:
            type: integer
        - name: stageId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRecruitmentStageInput"
      responses:
        "200":
          description: Recruitment stage updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentStage"
        "404":
          description: Recruitment stage not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A stage with this name or display order already exists in this workflow
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment-workflows/{workflowId}/stages/{stageId}/archive:
    post:
      operationId: archiveRecruitmentStage
      tags: [recruitment]
      summary: Archive a Recruitment stage
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: workflowId
          in: path
          required: true
          schema:
            type: integer
        - name: stageId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Recruitment stage archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentStage"
        "404":
          description: Recruitment stage not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment-workflows/{workflowId}/stages/{stageId}/reactivate:
    post:
      operationId: reactivateRecruitmentStage
      tags: [recruitment]
      summary: Reactivate an archived Recruitment stage
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: workflowId
          in: path
          required: true
          schema:
            type: integer
        - name: stageId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Recruitment stage reactivated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentStage"
        "404":
          description: Recruitment stage not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/job-requisitions:
    get:
      operationId: listJobRequisitions
      tags: [recruitment]
      summary: List job requisitions
      description: >-
        Visibility-filtered per caller: organization-wide for requisition.update
        holders, otherwise scoped to requisitions the caller requested, is the
        assigned recruiter or hiring manager on, or shares a department/branch
        with (docs/PHASE_3A_RECRUITMENT_IMPLEMENTATION_PLAN.md §7).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: status
          in: query
          schema:
            type: string
            enum: [draft, pending_approval, approved, rejected, partially_filled, filled, cancelled, closed]
        - name: requisitionType
          in: query
          schema:
            type: string
            enum: [new_role, replacement, temporary, internship, volunteer, contract, ministry]
        - name: departmentId
          in: query
          schema:
            type: integer
        - name: branchId
          in: query
          schema:
            type: integer
        - name: hiringManagerEmployeeId
          in: query
          schema:
            type: integer
        - name: recruiterEmployeeId
          in: query
          schema:
            type: integer
        - name: search
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated, visibility-filtered job requisition list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobRequisitionListResponse"
    post:
      operationId: createJobRequisition
      tags: [recruitment]
      summary: Create a job requisition
      description: Always created as draft. filledCount is never client-settable.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateJobRequisitionInput"
      responses:
        "201":
          description: Job requisition created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobRequisition"
        "400":
          description: Invalid fields, or a referenced ID does not belong to this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/job-requisitions/{id}:
    get:
      operationId: getJobRequisition
      tags: [recruitment]
      summary: Get a job requisition
      description: Returns 404 both when the requisition doesn't exist and when it exists but isn't visible to this caller — never distinguishing the two.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Job requisition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobRequisition"
        "404":
          description: Job requisition not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateJobRequisition
      tags: [recruitment]
      summary: Update a draft job requisition
      description: Only permitted while status is draft — protected fields (status, filledCount, audit metadata) are never accepted here.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateJobRequisitionInput"
      responses:
        "200":
          description: Job requisition updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobRequisition"
        "400":
          description: Only a draft requisition can be edited, or a validation/cross-org error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Job requisition not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/job-requisitions/{id}/submit:
    post:
      operationId: submitJobRequisition
      tags: [recruitment]
      summary: Submit a draft job requisition
      description: draft -> pending_approval only. Approval execution belongs to a later workstream — this only marks the requisition as awaiting a decision.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Job requisition submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobRequisition"
        "400":
          description: Only a draft requisition can be submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Job requisition not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/job-requisitions/{id}/cancel:
    post:
      operationId: cancelJobRequisition
      tags: [recruitment]
      summary: Cancel a draft or pending job requisition
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelJobRequisitionInput"
      responses:
        "200":
          description: Job requisition cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobRequisition"
        "400":
          description: Only a draft or pending requisition can be cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Job requisition not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/job-requisitions/{id}/archive:
    post:
      operationId: archiveJobRequisition
      tags: [recruitment]
      summary: Archive (close) a draft or cancelled job requisition
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Job requisition archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobRequisition"
        "400":
          description: Only a draft or cancelled requisition can be archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Job requisition not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/job-requisitions/pending-approvals:
    get:
      operationId: listPendingRequisitionApprovals
      tags: [requisition-approvals]
      summary: List job requisitions awaiting the caller's approval decision
      description: >-
        Organization-wide only (requisition.approve holders) — this
        workstream implements no delegated-approver tier yet. Gated by the
        "recruitment" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Pending-approval requisitions in the organization
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/JobRequisition"

  /organizations/{organizationId}/job-requisitions/{id}/approvals:
    get:
      operationId: listRequisitionApprovals
      tags: [requisition-approvals]
      summary: Immutable approval decision history for a job requisition
      description: >-
        Same visibility as GET .../job-requisitions/{id} — returns 404 both
        when the requisition doesn't exist and when it exists but isn't
        visible to this caller.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approval decision history, oldest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RequisitionApproval"
        "404":
          description: Job requisition not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/job-requisitions/{id}/approve:
    post:
      operationId: approveJobRequisition
      tags: [requisition-approvals]
      summary: Approve a pending job requisition
      description: >-
        Requires requisition.approve. Atomically finalizes the single
        approval step and transitions the requisition pending_approval ->
        approved in one transaction; rejected with 409 if the requisition is
        no longer awaiting a decision (already decided, concurrently or
        otherwise).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveJobRequisitionInput"
      responses:
        "200":
          description: Job requisition approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobRequisition"
        "404":
          description: Job requisition not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Requisition is not currently awaiting a decision
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/job-requisitions/{id}/reject:
    post:
      operationId: rejectJobRequisition
      tags: [requisition-approvals]
      summary: Reject a pending job requisition
      description: >-
        Same authorization as approve. Terminal — no reopening/resubmission
        exists in this workstream's frozen scope.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectJobRequisitionInput"
      responses:
        "200":
          description: Job requisition rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobRequisition"
        "404":
          description: Job requisition not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Requisition is not currently awaiting a decision
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/vacancies:
    get:
      operationId: listVacancies
      tags: [vacancies]
      summary: List vacancies
      description: >-
        Visibility-filtered per caller: organization-wide for vacancy.manage
        holders, otherwise scoped to vacancies whose linked requisition
        assigns the caller as recruiter or hiring manager (a vacancy carries
        no recruiter/hiring-manager column of its own — docs/PHASE_3A_RECRUITMENT_IMPLEMENTATION_PLAN.md §7/§9).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: status
          in: query
          schema:
            type: string
            enum: [draft, scheduled, published, paused, closed, archived]
        - name: requisitionId
          in: query
          schema:
            type: integer
        - name: search
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated, visibility-filtered vacancy list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VacancyListResponse"
    post:
      operationId: createVacancy
      tags: [vacancies]
      summary: Create a vacancy
      description: >-
        Always created as draft. Only permitted from an approved requisition
        belonging to this organization — requisitions in any other status
        are rejected with 400. filledCount is never client-settable.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateVacancyInput"
      responses:
        "201":
          description: Vacancy created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vacancy"
        "400":
          description: Invalid fields, requisition not approved, or a referenced ID does not belong to this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/vacancies/{id}:
    get:
      operationId: getVacancy
      tags: [vacancies]
      summary: Get a vacancy
      description: Returns 404 both when the vacancy doesn't exist and when it exists but isn't visible to this caller — never distinguishing the two.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Vacancy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vacancy"
        "404":
          description: Vacancy not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateVacancy
      tags: [vacancies]
      summary: Update a draft vacancy
      description: Only permitted while status is draft — protected fields (status, filledCount, publicId, audit metadata) are never accepted here.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateVacancyInput"
      responses:
        "200":
          description: Vacancy updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vacancy"
        "400":
          description: Only a draft vacancy can be edited, or a validation/cross-org error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Vacancy not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/vacancies/{id}/publish:
    post:
      operationId: publishVacancy
      tags: [vacancies]
      summary: Publish (or resume) a vacancy
      description: >-
        draft, scheduled, or paused -> scheduled or published. From draft,
        goes straight to published when openDate is unset or not in the
        future; otherwise becomes scheduled. Calling publish again on an
        already-scheduled vacancy always flips it live immediately (the
        manual operator flip — no background scheduler exists in this
        phase). Also used for the paused -> published "resume" direction,
        which likewise always goes straight to published.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Vacancy published or scheduled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vacancy"
        "400":
          description: Only a draft, scheduled, or paused vacancy can be published
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Vacancy not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/vacancies/{id}/pause:
    post:
      operationId: pauseVacancy
      tags: [vacancies]
      summary: Pause a published vacancy
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Vacancy paused
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vacancy"
        "400":
          description: Only a published vacancy can be paused
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Vacancy not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/vacancies/{id}/close:
    post:
      operationId: closeVacancy
      tags: [vacancies]
      summary: Close a vacancy
      description: >-
        draft, scheduled, published, or paused -> closed. The single
        terminal-exit action this workstream implements (the frozen plan's
        separate `cancelled` state is folded into `closed` here — see
        lib/db/src/schema/vacancies.ts).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Vacancy closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vacancy"
        "400":
          description: A closed or archived vacancy cannot be closed again
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Vacancy not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/vacancies/{id}/archive:
    post:
      operationId: archiveVacancy
      tags: [vacancies]
      summary: Archive a closed vacancy
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Vacancy archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vacancy"
        "400":
          description: Only a closed vacancy can be archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Vacancy not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications:
    get:
      operationId: listApplications
      tags: [applications]
      summary: List applications (internal ATS review)
      description: >-
        Visibility-filtered per caller: organization-wide for
        application.pipeline.move holders, otherwise scoped to applications
        whose linked requisition assigns the caller as recruiter or hiring
        manager (an application carries no recruiter/hiring-manager column
        of its own — resolved two hops out via its vacancy's requisition,
        docs/PHASE_3A_RECRUITMENT_IMPLEMENTATION_PLAN.md §7). No candidate-
        session ("own") tier exists — anonymous applicants never reach this
        route (W50 deferred, no candidate account/session mechanism exists).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: vacancyId
          in: query
          schema:
            type: integer
        - name: stageCategory
          in: query
          schema:
            type: string
            enum: [applied, screening, interview, assessment, offer, hired, rejected, withdrawn]
        - name: search
          in: query
          description: Matches candidate name or email.
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated, visibility-filtered application list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationListResponse"

  /organizations/{organizationId}/applications/{id}:
    get:
      operationId: getApplication
      tags: [applications]
      summary: Get an application (candidate info, current stage, documents, immutable history)
      description: Returns 404 both when the application doesn't exist and when it exists but isn't visible to this caller — never distinguishing the two.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Application detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDetail"
        "404":
          description: Application not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{id}/move-stage:
    post:
      operationId: moveApplicationStage
      tags: [applications]
      summary: Move an application to another active, non-terminal stage in its vacancy's own workflow
      description: >-
        Validated server-side against the vacancy's own workflow stage
        list — never a client-supplied "next stage" trusted as-is.
        Rejected with 400 if the application is currently in a terminal
        stage (use reopen first), the vacancy has no configured workflow,
        or toStageId doesn't belong to that workflow. Terminal categories
        (hired/rejected/withdrawn) are reachable only through their own
        dedicated actions, never through this one.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MoveApplicationStageInput"
      responses:
        "200":
          description: Application moved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDetail"
        "400":
          description: Invalid transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Application not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{id}/reject:
    post:
      operationId: rejectApplication
      tags: [applications]
      summary: Reject an application
      description: Moves the application to the vacancy's workflow's rejected-category stage. 400 if no such stage is configured, or the application is already in a terminal stage.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectApplicationInput"
      responses:
        "200":
          description: Application rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDetail"
        "400":
          description: Invalid transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Application not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{id}/withdraw:
    post:
      operationId: withdrawApplication
      tags: [applications]
      summary: Withdraw an application
      description: Moves the application to the vacancy's workflow's withdrawn-category stage. 400 if no such stage is configured, or the application is already in a terminal stage.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WithdrawApplicationInput"
      responses:
        "200":
          description: Application withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDetail"
        "400":
          description: Invalid transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Application not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{id}/reopen:
    post:
      operationId: reopenApplication
      tags: [applications]
      summary: Reopen a rejected or withdrawn application
      description: Moves the application from its terminal stage back to the vacancy's workflow's applied-category stage, clearing any rejection/withdrawal reason. 400 if the application isn't currently in a terminal stage, or no applied-category stage is configured.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReopenApplicationInput"
      responses:
        "200":
          description: Application reopened
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDetail"
        "400":
          description: Invalid transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Application not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{id}/scores:
    post:
      operationId: submitApplicationScore
      tags: [applications]
      summary: Submit a scoring entry for an application (W52)
      description: >-
        Append-only — a new row every time, never an edit. Distinct from
        stage movement: scoring evaluates an application without changing
        its stage, so this is gated by application.manage, not
        application.pipeline.move. applications.score is never written —
        the rollup is always recomputed on read (see ApplicationDetail.scoreRollup, §12).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitApplicationScoreInput"
      responses:
        "201":
          description: Score submitted — returns the refreshed application detail (including the recomputed scoreRollup)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDetail"
        "400":
          description: Invalid score
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Application not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/candidates:
    get:
      operationId: listCandidates
      tags: [candidates]
      summary: List candidates (internal directory)
      description: >-
        Visibility-filtered per caller: organization-wide for
        candidate.manage holders, otherwise scoped to candidates reachable
        via any application whose linked requisition assigns the caller as
        recruiter or hiring manager (a candidate carries no
        recruiter/hiring-manager column of its own — resolved three hops
        out: candidate -> applications -> vacancies -> requisitions).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: search
          in: query
          description: Matches candidate name or email.
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated, visibility-filtered candidate list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CandidateListResponse"

  /organizations/{organizationId}/candidates/{id}:
    get:
      operationId: getCandidate
      tags: [candidates]
      summary: Get a candidate
      description: Returns 404 both when the candidate doesn't exist and when it exists but isn't visible to this caller — never distinguishing the two.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Candidate detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Candidate"
        "404":
          description: Candidate not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/candidates/{id}/notes:
    get:
      operationId: listCandidateNotes
      tags: [candidates]
      summary: List a candidate's recruiter notes
      description: Never exposed to an applicant — no candidate-session read path exists anywhere in this codebase (W50 deferred). Newest first.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Candidate notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CandidateNote"
        "403":
          description: >-
            Forbidden — requires candidate.notes.read, and notes are restricted to
            organization-wide recruitment authority (candidate.manage); a hiring
            manager or assigned recruiter who can see the candidate cannot read them
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Candidate not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createCandidateNote
      tags: [candidates]
      summary: Add a recruiter note to a candidate
      description: >-
        Create-only — no update/delete path exists. `applicationId` (if
        supplied) distinguishes an application-level note from a
        candidate-level one, and must belong to this candidate.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCandidateNoteInput"
      responses:
        "201":
          description: Note created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CandidateNote"
        "400":
          description: Invalid note (empty text, or applicationId doesn't belong to this candidate)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Candidate not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/candidates/{id}/tags:
    get:
      operationId: listCandidateTags
      tags: [candidates]
      summary: List a candidate's tags
      description: Alphabetical by tag.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Candidate tags
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CandidateTag"
        "404":
          description: Candidate not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: addCandidateTag
      tags: [candidates]
      summary: Add a tag to a candidate
      description: Free-text — tag values are never a fixed enum or Master Data domain.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddCandidateTagInput"
      responses:
        "201":
          description: Tag added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CandidateTag"
        "400":
          description: Invalid tag (empty text)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Candidate not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This candidate already has that tag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/candidates/{id}/tags/{tagId}:
    delete:
      operationId: removeCandidateTag
      tags: [candidates]
      summary: Remove a tag from a candidate
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: tagId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Tag removed
        "404":
          description: Candidate not found (or not visible to this caller), or tag not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/talent-pools:
    get:
      operationId: listTalentPools
      tags: [talent-pools]
      summary: List an organization's talent pools
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of talent pools
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TalentPool"
    post:
      operationId: createTalentPool
      tags: [talent-pools]
      summary: Create a talent pool
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTalentPoolInput"
      responses:
        "201":
          description: Talent pool created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TalentPool"
        "409":
          description: A talent pool with this name already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/talent-pools/{poolId}:
    get:
      operationId: getTalentPool
      tags: [talent-pools]
      summary: Get a talent pool
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: poolId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Talent pool detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TalentPool"
        "404":
          description: Talent pool not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateTalentPool
      tags: [talent-pools]
      summary: Update a talent pool
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: poolId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTalentPoolInput"
      responses:
        "200":
          description: Talent pool updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TalentPool"
        "404":
          description: Talent pool not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A talent pool with this name already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/talent-pools/{poolId}/archive:
    post:
      operationId: archiveTalentPool
      tags: [talent-pools]
      summary: Archive a talent pool
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: poolId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Talent pool archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TalentPool"
        "404":
          description: Talent pool not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/talent-pools/{poolId}/reactivate:
    post:
      operationId: reactivateTalentPool
      tags: [talent-pools]
      summary: Reactivate an archived talent pool
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: poolId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Talent pool reactivated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TalentPool"
        "404":
          description: Talent pool not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/talent-pools/{poolId}/members:
    get:
      operationId: listTalentPoolMembers
      tags: [talent-pools]
      summary: List a talent pool's members
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: poolId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Talent pool members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TalentPoolMember"
        "404":
          description: Talent pool not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: addTalentPoolMember
      tags: [talent-pools]
      summary: Add a candidate to a talent pool
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: poolId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddTalentPoolMemberInput"
      responses:
        "201":
          description: Candidate added to the talent pool
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TalentPoolMember"
        "404":
          description: Talent pool or candidate not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Candidate is already a member of this talent pool
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/talent-pools/{poolId}/members/{candidateId}:
    delete:
      operationId: removeTalentPoolMember
      tags: [talent-pools]
      summary: Remove a candidate from a talent pool
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: poolId
          in: path
          required: true
          schema:
            type: integer
        - name: candidateId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Candidate removed from the talent pool
        "404":
          description: Talent pool member not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/interviews:
    get:
      operationId: listInterviews
      tags: [interviews]
      summary: List interviews across applications (calendar/list view)
      description: >-
        Top-level, not nested under one application — backs the frozen §11
        frontend route `/interviews` ("calendar/list of scheduled interviews
        for the caller"). Visibility-filtered per caller: organization-wide
        for interview.manage holders, otherwise scoped to interviews where
        the caller's own membership appears on the panel (§7 — "own
        scheduled interviews as interviewer").
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: query
          schema:
            type: integer
        - name: status
          in: query
          schema:
            type: string
            enum: [scheduled, completed, cancelled, no_show]
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated, visibility-filtered interview list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InterviewListResponse"

  /organizations/{organizationId}/interviews/{id}:
    get:
      operationId: getInterview
      tags: [interviews]
      summary: Get an interview (including its panel)
      description: Returns 404 both when the interview doesn't exist and when it exists but isn't visible to this caller — never distinguishing the two.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Interview detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Interview"
        "404":
          description: Interview not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/interviews/{id}/cancel:
    post:
      operationId: cancelInterview
      tags: [interviews]
      summary: Cancel a scheduled interview
      description: Only a currently-scheduled interview can be cancelled.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Interview cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Interview"
        "400":
          description: Only a scheduled interview can be cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Interview not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/interviews:
    get:
      operationId: listApplicationInterviews
      tags: [interviews]
      summary: List interviews scheduled for one application
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Interviews for this application, visibility-filtered
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Interview"
        "404":
          description: Application not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: scheduleInterview
      tags: [interviews]
      summary: Schedule an interview for an application
      description: Optionally sets the initial panel composition in the same call.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScheduleInterviewInput"
      responses:
        "201":
          description: Interview scheduled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Interview"
        "400":
          description: Invalid interview fields or panel composition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Application not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/interviews/{id}:
    patch:
      operationId: updateInterview
      tags: [interviews]
      summary: Update, reschedule, or replace the panel of an interview
      description: >-
        Supplying scheduledAt reschedules (cancels the current row, creates
        a new one — §4.5) rather than mutating the date in place; otherwise
        performs an ordinary in-place update. Only a currently-scheduled
        interview can be updated.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateInterviewInput"
      responses:
        "200":
          description: Interview updated (or the newly-created rescheduled interview)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Interview"
        "400":
          description: Invalid fields, or the interview is not currently scheduled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Interview not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/interviews/{id}/scorecards:
    get:
      operationId: listInterviewScorecards
      tags: [interview-scorecards]
      summary: List scorecards for an interview
      description: >-
        Visibility-filtered: a scorecard.read_all holder sees every panel
        member's scorecard plus a computed panelSummary (submitted/pending
        counts, recommendation tally — never stored, always recomputed on
        read, the same discipline as W52's scoreRollup); anyone else sees
        only their own scorecard (possibly none), and panelSummary is null.
        scorecard.submit never implies scorecard.read_all — an interviewer
        never sees a colleague's scorecard through this route, submitted or
        not.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Scorecards visible to this caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InterviewScorecardListResponse"
        "404":
          description: Interview not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: saveInterviewScorecard
      tags: [interview-scorecards]
      summary: Save or submit the caller's own scorecard for an interview
      description: >-
        Upserts the caller's own draft — panel membership is re-verified
        fresh on every call, never cached. Supplying `submit: true`
        transitions draft -> submitted in the same call (§10 lists no
        separate submit route); once submitted, further calls are rejected
        (400) — a submitted scorecard cannot be silently replaced. Rejected
        for a cancelled interview.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveInterviewScorecardInput"
      responses:
        "200":
          description: Scorecard saved (or submitted)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InterviewScorecard"
        "400":
          description: Invalid fields, the interview is cancelled, the caller is not a panel member, or the scorecard is already submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Interview not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/scorecards/{id}/finalize:
    post:
      operationId: finalizeInterviewScorecard
      tags: [interview-scorecards]
      summary: Finalize (lock) a submitted scorecard
      description: >-
        HR/recruiter-only — there is no "own" tier for finalize; the
        submitting interviewer can never finalize their own scorecard. Only
        a submitted, not-yet-finalized scorecard can be finalized.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Scorecard finalized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InterviewScorecard"
        "400":
          description: Only a submitted, not-yet-finalized scorecard can be finalized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Scorecard not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/reference-checks:
    get:
      operationId: listReferenceChecks
      tags: [reference-checks]
      summary: List reference checks for an application
      description: >-
        No dedicated reference-check permission exists — visibility reuses
        the application's own (assigned recruiter/hiring manager +
        organization-wide via application.read). Returns 404 both when the
        application doesn't exist and when it exists but isn't visible to
        this caller.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Reference checks for this application
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReferenceCheck"
        "404":
          description: Application not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createReferenceCheck
      tags: [reference-checks]
      summary: Request a reference check for an application
      description: >-
        Created at status "requested". Rejected (400) if an active
        (requested/in_progress) reference check already exists for the same
        refereeContact on this application.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReferenceCheckInput"
      responses:
        "201":
          description: Reference check requested
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReferenceCheck"
        "400":
          description: Invalid fields, or a duplicate active reference check for this referee already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Application not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/reference-checks/{id}:
    patch:
      operationId: updateReferenceCheckStatus
      tags: [reference-checks]
      summary: Record a reference check's status/outcome
      description: >-
        Valid target statuses are in_progress, completed, flagged, or
        unable_to_complete (never back to requested). Sets completedAt when
        moving to a terminal status. A completed/flagged/unable_to_complete
        reference check is immutable — no further update is accepted.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateReferenceCheckStatusInput"
      responses:
        "200":
          description: Reference check updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReferenceCheck"
        "400":
          description: Invalid status, or the reference check is already terminal (immutable)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Reference check not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/background-checks:
    get:
      operationId: listBackgroundChecks
      tags: [background-checks]
      summary: List background checks for an application
      description: >-
        Gated by the dedicated background_check.read permission —
        organization-wide only, no assigned tier at all (§7's narrowest
        permission row). Holding application.read/.manage never implies
        access here. Never exposes the raw evidence storage key — each item
        carries a hasEvidence boolean instead.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Background checks for this application
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BackgroundCheck"
        "404":
          description: Application not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createBackgroundCheck
      tags: [background-checks]
      summary: Request a background check for an application
      description: >-
        Created at status "requested". checkType is free text (no
        Master Data domain exists in this frozen scope). Rejected (400) if
        an active (requested/in_progress) background check of the same
        checkType already exists for this application.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBackgroundCheckInput"
      responses:
        "201":
          description: Background check requested
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackgroundCheck"
        "400":
          description: Invalid fields, or a duplicate active check of this type already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Application not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/background-checks/{id}:
    patch:
      operationId: updateBackgroundCheckStatus
      tags: [background-checks]
      summary: Record a background check's status/result
      description: >-
        Valid target statuses are in_progress, completed, flagged, or
        unable_to_complete (never back to requested). A completed/flagged/
        unable_to_complete background check is immutable — no further
        update is accepted, and no automatic rejection, stage move, offer,
        or hiring action is ever triggered by a result.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateBackgroundCheckStatusInput"
      responses:
        "200":
          description: Background check updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackgroundCheck"
        "400":
          description: Invalid status, or the background check is already terminal (immutable)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Background check not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/background-checks/{id}/evidence:
    post:
      operationId: attachBackgroundCheckEvidence
      tags: [background-checks]
      summary: Attach (or replace) evidence for a background check
      description: >-
        multipart/form-data upload, validated by file signature (not just
        Content-Type), 10MB max, same allowed types as every other document
        upload in this codebase. Replaces any prior evidence file for this
        check (the old file is deleted). Rejected once the check is
        terminal (completed/flagged/unable_to_complete) — no silent
        overwrite of finalized evidence.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Evidence attached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackgroundCheck"
        "400":
          description: Invalid file, or the background check is already terminal (immutable)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Background check not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    get:
      operationId: getBackgroundCheckEvidence
      tags: [background-checks]
      summary: Retrieve a background check's evidence file
      description: >-
        Signed, authenticated access only — never a public URL. Gated by
        background_check.read, same as the rest of this resource.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The evidence file
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "404":
          description: Background check not found, or no evidence has been attached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offers:
    get:
      operationId: listOffers
      tags: [offers]
      summary: List offers
      description: >-
        Visibility-filtered per caller: organization-wide for offer.approve/
        .issue/.withdraw holders, otherwise scoped to offers whose linked
        requisition assigns the caller as recruiter or hiring manager
        (docs/PHASE_3A_RECRUITMENT_IMPLEMENTATION_PLAN.md §7).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated, visibility-filtered offer list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferListResponse"

  /organizations/{organizationId}/offers/{id}:
    get:
      operationId: getOffer
      tags: [offers]
      summary: Get an offer by ID
      description: Returns 404 both when the offer doesn't exist and when it exists but isn't visible to this caller — never distinguishing the two.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Offer, including every version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferDetail"
        "404":
          description: Offer not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateDraftOfferVersion
      tags: [offers]
      summary: Edit the offer's current version in place
      description: >-
        Only permitted while the current version is "draft" (§14). An
        approved/issued version must go through `POST
        .../offers/{id}/versions` instead — this endpoint returns 400 if
        called on anything else (pending_approval or a terminal state too).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOfferVersionInput"
      responses:
        "200":
          description: Offer's draft version updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferDetail"
        "400":
          description: The current version is not editable in place (not draft)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Offer not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offers/{id}/versions:
    post:
      operationId: createNewOfferVersion
      tags: [offers]
      summary: Create a new offer version
      description: >-
        Only permitted while the current version is "approved" or "issued"
        (§14's second editing branch) — the prior version is marked
        superseded in the same transaction as the new draft version is
        created. Returns 400 if the current version is still draft (edit it
        in place via PATCH .../offers/{id} instead), awaiting a decision, or
        terminal.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOfferVersionInput"
      responses:
        "201":
          description: New draft offer version created; the prior version is now superseded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferDetail"
        "400":
          description: The current version does not accept a new version in its current status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Offer not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/offers:
    get:
      operationId: getOfferForApplication
      tags: [offers]
      summary: Get the offer for an application
      description: >-
        At most one offer ever exists per application
        (offers_application_unique) — returns a single object, not a list.
        404 both when no offer exists yet and when one exists but isn't
        visible to this caller.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The application's offer, including every version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferDetail"
        "404":
          description: No offer exists for this application (or it exists but isn't visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createOffer
      tags: [offers]
      summary: Create an offer for an application
      description: >-
        Creates the offer envelope and its first (draft) version in one
        transaction. Requires offer.manage — org-wide holders may create an
        offer for any application; an assigned recruiter or hiring manager
        (per the application's linked requisition) may create one for their
        own application, §7's first "assigned tier can really write" case
        this phase. 409 if the application already has an offer.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfferInput"
      responses:
        "201":
          description: Offer created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferDetail"
        "404":
          description: Application not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This application already has an offer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offers/versions/{id}/submit-for-approval:
    post:
      operationId: submitOfferVersionForApproval
      tags: [offers]
      summary: Submit a draft offer version for approval
      description: >-
        draft -> pending_approval only, creating the single required
        approval step in the same transaction. Requires offer.manage
        (org-wide or assigned, same as create/edit).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Offer version submitted for approval
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferVersion"
        "400":
          description: Only a draft offer version can be submitted for approval
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Offer version not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offers/versions/{id}/approvals:
    get:
      operationId: listOfferApprovals
      tags: [offers]
      summary: Immutable approval decision history for an offer version
      description: >-
        Same visibility as GET .../offers/{id} (resolved via the version's
        parent offer) — 404 both when the version doesn't exist and when it
        exists but isn't visible to this caller.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approval decision history, oldest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfferApproval"
        "404":
          description: Offer version not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offers/versions/{id}/approve:
    post:
      operationId: approveOfferVersion
      tags: [offers]
      summary: Approve a pending offer version
      description: >-
        Requires offer.approve (organization-wide only — no assigned tier
        for this action per §7). Atomically finalizes the single approval
        step and transitions the offer version pending_approval -> approved
        in one transaction; 409 if the version is no longer awaiting a
        decision.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveOfferVersionInput"
      responses:
        "200":
          description: Offer version approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferVersion"
        "404":
          description: Offer version not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Offer version is not currently awaiting a decision
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offers/versions/{id}/issue:
    post:
      operationId: issueOfferVersion
      tags: [offers]
      summary: Issue an approved offer version
      description: approved -> issued only. Requires offer.issue (organization-wide only — no assigned tier for this action per §7).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Offer version issued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferVersion"
        "400":
          description: Only an approved offer version can be issued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Offer version not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offers/versions/{id}/withdraw:
    post:
      operationId: withdrawOfferVersion
      tags: [offers]
      summary: Withdraw a pending, approved, or issued offer version
      description: >-
        pending_approval, approved, or issued -> withdrawn. The only abort
        action this workstream builds — no separate reject route exists (§10
        names only submit-for-approval/approve/issue/withdraw), so this also
        doubles as "reject a pending version". Requires offer.withdraw
        (organization-wide only — no assigned tier for this action per §7).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WithdrawOfferVersionInput"
      responses:
        "200":
          description: Offer version withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferVersion"
        "400":
          description: Only a pending, approved, or issued offer version can be withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Offer version not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/pre-employment-requirements:
    get:
      operationId: listPreEmploymentRequirements
      tags: [pre-employment-requirements]
      summary: List pre-employment requirements for an application
      description: >-
        No dedicated permission exists — visibility reuses the
        application's own (assigned recruiter/hiring manager +
        organization-wide via application.read). Returns 404 both when the
        application doesn't exist and when it exists but isn't visible to
        this caller. `summary.readyForConversion` mirrors §13's own
        convert-to-employee readiness rule exactly ("every non-waived row
        is satisfied") — computed on every read, never stored, and does
        not itself gate or trigger conversion.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Pre-employment requirements for this application
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreEmploymentRequirementListResponse"
        "404":
          description: Application not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createPreEmploymentRequirement
      tags: [pre-employment-requirements]
      summary: Track a new pre-employment requirement for an application
      description: >-
        Created at status "pending". Rejected (400) if this requirementCode
        is already tracked for this application (unique per
        applicationId/requirementCode).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePreEmploymentRequirementInput"
      responses:
        "201":
          description: Pre-employment requirement created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreEmploymentRequirement"
        "400":
          description: Invalid fields, or this requirement is already tracked for this application
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Application not found (or not visible to this caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/pre-employment-requirements/{id}:
    patch:
      operationId: updatePreEmploymentRequirementStatus
      tags: [pre-employment-requirements]
      summary: Update a pre-employment requirement's status
      description: >-
        Status is freely settable between pending/satisfied/waived — no
        one-way lifecycle is enforced (a checklist correction, e.g.
        reverting a mistaken "satisfied" back to "pending", is allowed).
        Sets satisfiedAt when moving to "satisfied"; clears it otherwise.
        Never creates an employee or activates any HR module by itself.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePreEmploymentRequirementStatusInput"
      responses:
        "200":
          description: Pre-employment requirement updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreEmploymentRequirement"
        "400":
          description: Invalid status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Pre-employment requirement not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/convert-to-employee:
    post:
      operationId: convertApplicationToEmployee
      tags: [employee-conversion]
      summary: Convert a hired application into an employee
      description: >-
        Requires both candidate.convert_to_employee and employee.write —
        neither alone is sufficient. Eligible only when the application is
        in a hired-category stage AND every non-waived pre-employment
        requirement is satisfied (no offer-status check — this workstream
        builds no candidate-facing offer acceptance flow). Reuses the
        existing employee-creation service unchanged; an internal
        candidate (candidates.linkedInternalEmployeeId already set) reuses
        that existing employee rather than creating a second one. 409 if
        this application (or, for an internal candidate, this employee)
        has already been converted — genuinely idempotent under retry.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "201":
          description: Application converted to an employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversionResult"
        "400":
          description: The application is not eligible for conversion yet (not hired-category, or a non-waived pre-employment requirement is not satisfied)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Application not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This application has already been converted to an employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /careers/{orgSlug}:
    get:
      operationId: getPublicCareersOrganization
      tags: [public-careers]
      summary: Public organization profile for a careers page
      description: >-
        No authentication. Resolves the organization by its own slug only —
        never a numeric ID. Returns the same 404 whether the slug doesn't
        exist, the organization is suspended, or its careers portal isn't
        enabled (recruitment_settings.enabled / externalRecruitmentEnabled)
        — these are never distinguished, so a probing request can never
        learn which condition applied.
      parameters:
        - name: orgSlug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Public organization profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicOrganization"
        "404":
          description: Careers page not available
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /careers/{orgSlug}/vacancies:
    get:
      operationId: listPublicVacancies
      tags: [public-careers]
      summary: Public, paginated, filterable vacancy list
      description: >-
        Only vacancies with status=published, visibility != internal, and
        (if set) a closeDate that has not yet passed. Never includes
        salary, internal IDs, requisition linkage, or any recruiter/hiring-
        manager identity — vacancies carry no salary field at all, and
        job_requisitions' own salary range is explicitly org-internal-only,
        never surfaced here (docs/PHASE_3A_RECRUITMENT_IMPLEMENTATION_PLAN.md
        §9). Reads never mutate a vacancy's lifecycle, even when its
        closeDate has passed.
      parameters:
        - name: orgSlug
          in: path
          required: true
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
        - name: department
          in: query
          schema:
            type: string
        - name: location
          in: query
          schema:
            type: string
        - name: employmentType
          in: query
          schema:
            type: string
        - name: workplaceType
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
            maximum: 50
      responses:
        "200":
          description: Paginated public vacancy list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicVacancyListResponse"
        "404":
          description: Careers page not available (same collapsed 404 as GET /careers/{orgSlug})
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /careers/{orgSlug}/jobs/{vacancyPublicId}:
    get:
      operationId: getPublicVacancy
      tags: [public-careers]
      summary: Public vacancy detail
      description: >-
        Looked up by publicId only — the internal serial ID is never
        accepted or exposed. Returns 404 whether the vacancy doesn't exist,
        belongs to a different organization than orgSlug resolves to, or
        simply isn't publicly eligible right now (draft/scheduled/paused/
        closed/archived/internal-only/past its own closeDate) — never
        distinguished.
      parameters:
        - name: orgSlug
          in: path
          required: true
          schema:
            type: string
        - name: vacancyPublicId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Public vacancy detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicVacancyDetail"
        "404":
          description: Job not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /careers/{orgSlug}/jobs/{vacancyPublicId}/apply:
    post:
      operationId: applyToPublicVacancy
      tags: [public-careers]
      summary: Submit an anonymous application (submission only, no candidate account)
      description: >-
        The highest-risk public route in this codebase. Rate-limited per IP
        (5/15min, stricter than /auth/login's 10/15min given the larger
        file-upload payload). A hidden honeypot field ("website") that a
        real applicant never sees — if filled, the exact same success
        response is returned with nothing persisted, never revealing
        detection to an automated submitter. The resume file passes through
        the same documentValidation.ts signature/size/type checks as every
        other upload in this codebase (PDF/JPEG/PNG/DOCX/XLSX, 10MB max). A
        second submission from the same email against the same vacancy is
        idempotent — returns the existing application (200, not 201), never
        creates a second row. On a genuinely new submission, an
        application-status link is emailed via the existing Resend
        provider (reused unchanged from the forgot-password flow, W19) —
        delivery failure is logged, never surfaced to the caller. Never
        reveals whether a given email has applied before.
      parameters:
        - name: orgSlug
          in: path
          required: true
          schema:
            type: string
        - name: vacancyPublicId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [firstName, lastName, email, resume]
              properties:
                firstName:
                  type: string
                lastName:
                  type: string
                email:
                  type: string
                phone:
                  type: string
                resume:
                  type: string
                  format: binary
                answers:
                  type: string
                  description: >-
                    JSON-encoded array of {vacancyQuestionId, answerText}
                    objects (W52) — optional, answers to any subset of the
                    vacancy's screening questions. A vacancyQuestionId that
                    doesn't belong to this vacancy is silently ignored, not
                    an error. Knockout evaluation happens server-side at
                    submission and never auto-rejects (§12).
                website:
                  type: string
                  description: Honeypot — must always be left empty. Not a real field.
      responses:
        "201":
          description: Application submitted (new)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationSubmittedResponse"
        "200":
          description: Application already existed for this email and vacancy (idempotent repeat submission)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationSubmittedResponse"
        "400":
          description: Missing required fields, invalid resume file, or the organization's applicationLimitPerCandidate was reached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Job not found (or not publicly eligible for application)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many applications submitted from this IP
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /careers/{orgSlug}/application-status/{token}:
    get:
      operationId: getPublicApplicationStatus
      tags: [public-careers]
      summary: Anonymous application status check by emailed token
      description: >-
        Never a bare email lookup — only the signed, time-limited token
        emailed at submission (90-day TTL). Scoped to the organization
        resolved from orgSlug; a token belonging to a different
        organization is indistinguishable from an unknown one. Rate-limited
        per IP to prevent token brute-forcing.
      parameters:
        - name: orgSlug
          in: path
          required: true
          schema:
            type: string
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Application status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationStatusResponse"
        "404":
          description: Not found (unknown, expired, or cross-organization token)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many status checks from this IP
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/leave-requests/{id}/approve:
    post:
      operationId: approveLeaveRequest
      tags: [leave-approvals]
      summary: Approve a pending leave request
      description: >-
        Manager-scoped or organization-wide (leave_request.manage) approval
        authority required, on top of holding leave_request.approve —
        neither alone is sufficient. Self-approval is rejected. Atomically
        transitions the request and posts the immutable usage ledger entry
        (W34) in one transaction; rejected with a 409 if the request is no
        longer pending (already decided, concurrently or otherwise).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Leave request approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveRequest"
        "400":
          description: Approving would take the balance negative and the policy disallows it
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not authorized over this employee, or self-approval
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Leave request not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Request is not currently pending
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/leave-requests/{id}/reject:
    post:
      operationId: rejectLeaveRequest
      tags: [leave-approvals]
      summary: Reject a pending leave request
      description: >-
        Same authorization rule as approve. Never posts a ledger entry —
        rejection deducts nothing.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectLeaveRequestInput"
      responses:
        "200":
          description: Leave request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveRequest"
        "403":
          description: Not authorized over this employee, or self-rejection
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Leave request not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Request is not currently pending
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/leave-balances:
    get:
      operationId: listLeaveBalances
      tags: [leave-balances]
      summary: Get an employee's leave balances, computed from the ledger
      description: >-
        Own resource, manager-scoped, or leave_request.manage (organization-
        wide) — same authorization tiers as leave requests. The balance for
        each leave type is always a live SUM of the immutable ledger, never
        a stored, directly editable value. Gated by the "leave" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: One summary row per leave type with any ledger history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeaveBalanceSummary"
        "403":
          description: Not authorized to view this employee's leave balances
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/leave-balances/ledger:
    get:
      operationId: listLeaveBalanceLedger
      tags: [leave-balances]
      summary: Get an employee's raw leave balance ledger entries
      description: >-
        Same authorization tiers as listLeaveBalances. Every entry is
        immutable and append-only — this is the auditable history a
        computed balance is reconstructed from. Gated by the "leave" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: leaveTypeId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Ledger entries, most recent effective date first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeaveBalanceEntry"
        "403":
          description: Not authorized to view this employee's leave balance ledger
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/leave-balances/adjust:
    post:
      operationId: adjustLeaveBalance
      tags: [leave-balances]
      summary: Post a manual leave balance adjustment
      description: >-
        HR organization-wide only (leave_request.manage) — never own or
        manager-scoped. A reason is required and every adjustment is
        audit-logged. Posted as a new, signed ledger entry; never a direct
        edit of a computed balance. Gated by the "leave" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdjustLeaveBalanceInput"
      responses:
        "201":
          description: Manual adjustment posted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveBalanceEntry"
        "400":
          description: Invalid adjustment (missing reason, wrong sign, ineligible employee, or invalid leave type)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Requires leave_request.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: An equivalent ledger entry has already been posted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/leave-types:
    get:
      operationId: listLeaveTypes
      tags: [leave-types]
      summary: List leave types
      description: Gated by the "leave" module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of leave types
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeaveType"
    post:
      operationId: createLeaveType
      tags: [leave-types]
      summary: Create a leave type
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLeaveTypeInput"
      responses:
        "201":
          description: Leave type created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveType"
        "409":
          description: A leave type with this code already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/leave-types/{id}:
    patch:
      operationId: updateLeaveType
      tags: [leave-types]
      summary: Update a leave type's name/code
      description: Does not change status — use archive/reactivate for that.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLeaveTypeInput"
      responses:
        "200":
          description: Leave type updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveType"
        "404":
          description: Leave type not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A leave type with this code already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/leave-types/{id}/archive:
    post:
      operationId: archiveLeaveType
      tags: [leave-types]
      summary: Archive a leave type
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Leave type archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveType"
        "404":
          description: Leave type not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/leave-types/{id}/reactivate:
    post:
      operationId: reactivateLeaveType
      tags: [leave-types]
      summary: Reactivate an archived leave type
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Leave type reactivated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveType"
        "404":
          description: Leave type not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/leave-types/{leaveTypeId}/policies:
    get:
      operationId: listLeavePolicies
      tags: [leave-types]
      summary: List a leave type's policies
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: leaveTypeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of leave policies for the type
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeavePolicy"
    post:
      operationId: createLeavePolicy
      tags: [leave-types]
      summary: Add a policy to a leave type
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: leaveTypeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLeavePolicyInput"
      responses:
        "201":
          description: Leave policy created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeavePolicy"
        "400":
          description: Invalid policy fields or cross-organization reference
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/leave-types/{leaveTypeId}/policies/{policyId}:
    patch:
      operationId: updateLeavePolicy
      tags: [leave-types]
      summary: Update a leave policy
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: leaveTypeId
          in: path
          required: true
          schema:
            type: integer
        - name: policyId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLeavePolicyInput"
      responses:
        "200":
          description: Leave policy updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeavePolicy"
        "400":
          description: Invalid policy fields or cross-organization reference
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Leave policy not found in this organization/leave type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/leave-types/{leaveTypeId}/policies/{policyId}/archive:
    post:
      operationId: archiveLeavePolicy
      tags: [leave-types]
      summary: Archive a leave policy
      description: Independent of the parent leave type's own archive lifecycle.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: leaveTypeId
          in: path
          required: true
          schema:
            type: integer
        - name: policyId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Leave policy archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeavePolicy"
        "404":
          description: Leave policy not found in this organization/leave type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/leave-types/{leaveTypeId}/policies/{policyId}/reactivate:
    post:
      operationId: reactivateLeavePolicy
      tags: [leave-types]
      summary: Reactivate an archived leave policy
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: leaveTypeId
          in: path
          required: true
          schema:
            type: integer
        - name: policyId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Leave policy reactivated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeavePolicy"
        "404":
          description: Leave policy not found in this organization/leave type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/branches:
    get:
      operationId: listBranches
      tags: [branches]
      summary: List branches
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of branches
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Branch"
    post:
      operationId: createBranch
      tags: [branches]
      summary: Create a branch
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBranchInput"
      responses:
        "201":
          description: Branch created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Branch"
        "409":
          description: A branch with this code already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/branches/{id}:
    patch:
      operationId: updateBranch
      tags: [branches]
      summary: Update a branch's name/code
      description: Does not change status — use archive/reactivate for that.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateBranchInput"
      responses:
        "200":
          description: Branch updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Branch"
        "404":
          description: Branch not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A branch with this code already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/branches/{id}/archive:
    post:
      operationId: archiveBranch
      tags: [branches]
      summary: Archive a branch
      description: Rejected if any department still references this branch (Organization Structure Service, ADR-012).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Branch archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Branch"
        "400":
          description: Branch still has dependent departments
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Branch not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/branches/{id}/reactivate:
    post:
      operationId: reactivateBranch
      tags: [branches]
      summary: Reactivate an archived branch
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Branch reactivated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Branch"
        "404":
          description: Branch not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/departments:
    get:
      operationId: listDepartments
      tags: [departments]
      summary: List departments
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of departments
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Department"
    post:
      operationId: createDepartment
      tags: [departments]
      summary: Create a department
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDepartmentInput"
      responses:
        "201":
          description: Department created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Department"
        "409":
          description: A department with this code already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/departments/{id}/restructure:
    patch:
      operationId: restructureDepartment
      tags: [departments]
      summary: Move a department to a different branch and/or parent department
      description: >-
        Structural placement only (branchId/parentDepartmentId) — not a
        general department update. Rejects a move that would make the
        department its own ancestor (Organization Structure Service, ADR-012).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestructureDepartmentInput"
      responses:
        "200":
          description: Department restructured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Department"
        "400":
          description: Invalid placement (cross-organization reference or hierarchy cycle)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Department not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/departments/{id}:
    patch:
      operationId: updateDepartment
      tags: [departments]
      summary: Update a department's name/code
      description: Does not change branch/parent or status — use restructure/archive/reactivate for that.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateDepartmentInput"
      responses:
        "200":
          description: Department updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Department"
        "404":
          description: Department not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A department with this code already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/departments/{id}/archive:
    post:
      operationId: archiveDepartment
      tags: [departments]
      summary: Archive a department
      description: Rejected if child departments or positions still reference it (Organization Structure Service, ADR-012).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Department archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Department"
        "400":
          description: Department still has dependents
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Department not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/departments/{id}/reactivate:
    post:
      operationId: reactivateDepartment
      tags: [departments]
      summary: Reactivate an archived department
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Department reactivated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Department"
        "404":
          description: Department not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/positions:
    get:
      operationId: listPositions
      tags: [positions]
      summary: List positions
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of positions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Position"
    post:
      operationId: createPosition
      tags: [positions]
      summary: Create a position
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePositionInput"
      responses:
        "201":
          description: Position created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "409":
          description: A position with this title already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/positions/{id}/restructure:
    patch:
      operationId: restructurePosition
      tags: [positions]
      summary: Move a position to a different department
      description: >-
        Structural placement only (departmentId) — not a general position
        update (Organization Structure Service, ADR-012).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestructurePositionInput"
      responses:
        "200":
          description: Position restructured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          description: Invalid placement (cross-organization reference)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Position not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/positions/{id}:
    patch:
      operationId: updatePosition
      tags: [positions]
      summary: Update a position's title
      description: Does not change department or status — use restructure/archive/reactivate for that.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePositionInput"
      responses:
        "200":
          description: Position updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "404":
          description: Position not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A position with this title already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/positions/{id}/archive:
    post:
      operationId: archivePosition
      tags: [positions]
      summary: Archive a position
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Position archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "404":
          description: Position not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/positions/{id}/reactivate:
    post:
      operationId: reactivatePosition
      tags: [positions]
      summary: Reactivate an archived position
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Position reactivated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "404":
          description: Position not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /notifications:
    get:
      operationId: listNotifications
      tags: [notifications]
      summary: List notifications
      description: >-
        Returns notifications for the current user. `organizationId` is an
        optional narrowing filter added in WS-6 — omitted, this returns
        every notification for the caller exactly as before.
      parameters:
        - name: organizationId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: List of notifications
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Notification"

  /notifications/{id}/read:
    patch:
      operationId: markNotificationRead
      tags: [notifications]
      summary: Mark notification as read
      description: Mark a specific notification as read
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Notification marked as read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Notification"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /notifications/read-all:
    post:
      operationId: markAllNotificationsRead
      tags: [notifications]
      summary: Mark all notifications as read
      description: Mark all notifications for the current user as read
      responses:
        "200":
          description: All notifications marked as read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /notifications/{id}/dismiss:
    patch:
      operationId: dismissNotification
      tags: [notifications]
      summary: Dismiss a notification
      description: >-
        WS-6. IDOR-safe by construction — the lookup always includes the
        caller's own user id, so no id resolves to another user's
        notification.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Notification dismissed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Notification"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /platform/scheduled-jobs:
    get:
      operationId: listScheduledJobs
      tags: [scheduled-jobs]
      summary: List scheduled jobs (platform super_admin only)
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [scheduled, running, completed, failed, cancelled]
        - name: organizationId
          in: query
          schema:
            type: integer
        - name: jobType
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
        - name: offset
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Scheduled jobs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ScheduledJob"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /platform/scheduled-jobs/{id}:
    get:
      operationId: getScheduledJob
      tags: [scheduled-jobs]
      summary: Get one scheduled job (platform super_admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The job
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduledJob"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /platform/scheduled-jobs/{id}/cancel:
    post:
      operationId: cancelScheduledJob
      tags: [scheduled-jobs]
      summary: Cancel a pending job (platform super_admin only)
      description: Only a job still in 'scheduled' status can be cancelled — a claimed/running job cannot be cancelled out from under its worker.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Cancelled job
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduledJob"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Job is not in 'scheduled' status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /platform/scheduled-jobs/{id}/reschedule:
    post:
      operationId: rescheduleScheduledJob
      tags: [scheduled-jobs]
      summary: Reschedule a pending job (platform super_admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [scheduledFor]
              properties:
                scheduledFor:
                  type: string
                  format: date-time
      responses:
        "200":
          description: Rescheduled job
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduledJob"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Job is not in 'scheduled' status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /platform/scheduled-jobs/{id}/retry:
    post:
      operationId: retryScheduledJob
      tags: [scheduled-jobs]
      summary: Manually retry a terminally failed job (platform super_admin only)
      description: Resets attemptCount and reschedules immediately. Only a job in 'failed' status can be retried this way.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Retried job, now scheduled again
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduledJob"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Job is not in 'failed' status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /dashboard/summary:
    get:
      operationId: getDashboardSummary
      tags: [users]
      summary: Dashboard summary
      description: >-
        Returns summary counts and status for the authenticated user's
        organization. `leaveMetrics` (W40) is null when the "leave" module is
        disabled for the organization — never a zero-filled placeholder.
      responses:
        "200":
          description: Dashboard summary data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSummary"

  /organizations/{organizationId}/dashboard/command-centre:
    get:
      operationId: getHrCommandCentre
      tags: [users]
      summary: HR dashboard command centre — tasks, attention, holidays, recent activity
      description: >-
        One aggregated, read-only call composing the owning modules' existing
        services (Action Centre providers, form engine stage resolver,
        Performance, Employment Lifecycle, personnel custody, Assets,
        Attendance, public holidays, HR-category audit events). The
        organization comes from the caller's verified membership, never from
        client input. Each section is authorized by its own module enablement
        and permission: a section the caller may not see is omitted entirely
        (no card, no count, no zero), indistinguishably from the module being
        disabled. `tasks` holds only work the caller can perform now —
        monitor-only items (e.g. a leave request still with its Department
        Head, a form at another stage) are never tasks. No permission is
        minted; a break-glass grant without a membership receives 403.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Command centre sections the caller is authorized for
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HrCommandCentre"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not an active member of the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /roles:
    get:
      operationId: listRoles
      tags: [roles]
      summary: List roles
      description: System-wide role catalog (not org-scoped)
      responses:
        "200":
          description: List of roles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Role"

  /modules:
    get:
      operationId: listModules
      tags: [modules]
      summary: List the platform module registry
      description: >-
        System-wide catalog of togglable feature modules (not org-scoped).
        Core Platform / HR Foundation capabilities are not modules and don't
        appear here — every organization always has them. A module appearing
        here does not mean any organization has it enabled; that's a
        separate, not-yet-built concern.
      responses:
        "200":
          description: List of modules
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Module"

  /permissions:
    get:
      operationId: listPermissions
      tags: [permissions]
      summary: List permissions
      description: System-wide permission catalog (not org-scoped)
      responses:
        "200":
          description: List of permissions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Permission"

  /organizations/{organizationId}/members:
    get:
      operationId: listMembers
      tags: [members]
      summary: List organization members
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OrganizationMember"
    post:
      operationId: addMember
      tags: [members]
      summary: Add an existing user as a member
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddMemberInput"
      responses:
        "201":
          description: Member added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationMember"
        "404":
          description: No user with that email
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: User is already an active member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/members/{membershipId}:
    delete:
      operationId: revokeMember
      tags: [members]
      summary: Revoke a membership
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: membershipId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Membership revoked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationMember"
        "404":
          description: Membership not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/members/{membershipId}/roles:
    post:
      operationId: assignMemberRole
      tags: [members]
      summary: Assign a role to a member
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: membershipId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignRoleInput"
      responses:
        "200":
          description: Role assigned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationMember"
        "404":
          description: Membership or role not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/members/{membershipId}/roles/{roleId}:
    delete:
      operationId: revokeMemberRole
      tags: [members]
      summary: Revoke a role from a member
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: membershipId
          in: path
          required: true
          schema:
            type: integer
        - name: roleId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Role revoked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationMember"
        "404":
          description: Membership not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/invitations:
    post:
      operationId: createInvitation
      tags: [invitations]
      summary: Invite a new user to the organization
      description: >-
        Invites someone who may not yet have an account. Returns the accept
        link's token directly -- no email is sent; the inviting admin shares
        the link out-of-band.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInvitationInput"
      responses:
        "201":
          description: Invitation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvitationCreated"
        "409":
          description: User already has a membership in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /invitations/{token}:
    get:
      operationId: getInvitation
      tags: [invitations]
      summary: Preview an invitation
      description: Public -- no authentication required. Used by the accept-invitation page.
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Invitation preview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvitationPreview"
        "404":
          description: Invalid invitation link
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /invitations/{token}/accept:
    post:
      operationId: acceptInvitation
      tags: [invitations]
      summary: Accept an invitation
      description: >-
        Public -- no authentication required. Sets the invitee's name and
        password and activates their membership. Does not log them in;
        first login is a separate /auth/login call.
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcceptInvitationInput"
      responses:
        "200":
          description: Invitation accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "404":
          description: Invalid invitation link
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Invitation already used or revoked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "410":
          description: Invitation expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/primary-hr:
    get:
      operationId: getPrimaryHr
      tags: [primary-hr]
      summary: Get the current Primary HR assignment
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Current Primary HR assignment, or null if none is set
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrimaryHrAssignmentOrNull"
    post:
      operationId: setPrimaryHr
      tags: [primary-hr]
      summary: Appoint or transfer Primary HR
      description: Atomically revokes the current Primary HR (if any) and appoints the given membership.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetPrimaryHrInput"
      responses:
        "200":
          description: Primary HR appointed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrimaryHrAssignment"
        "400":
          description: Target membership does not belong to this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/config/{namespace}:
    get:
      operationId: getOrganizationConfig
      tags: [organizations]
      summary: Get an organization configuration namespace
      description: >-
        Returns the namespace's saved configuration, or its safe defaults if
        nothing has been saved yet. 404s for a namespace the Configuration
        Engine doesn't recognize.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: namespace
          in: path
          required: true
          schema:
            type: string
          description: >-
            One of the namespaces registered with the Configuration Engine
            (currently general, terminology, attendance — see
            artifacts/api-server/src/services/organizationConfig.ts for the
            authoritative list). Left as an open string, not an enum, so new
            namespaces can be registered in code without an API contract
            change; an unrecognized namespace 404s at request time. A
            namespace may additionally require its owning module to be
            enabled for the organization (e.g. attendance requires the
            "attendance" module) — see requireModuleEnabled (W5); such a
            namespace 403s instead of 404ing when its module is disabled.
      responses:
        "200":
          description: Organization configuration namespace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationConfig"
    patch:
      operationId: updateOrganizationConfig
      tags: [organizations]
      summary: Update an organization configuration namespace
      description: >-
        Merges the given keys into the namespace's existing configuration and
        validates the merged result against that namespace's schema before
        saving.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: namespace
          in: path
          required: true
          schema:
            type: string
          description: >-
            One of the namespaces registered with the Configuration Engine
            (currently general, terminology, attendance — see
            artifacts/api-server/src/services/organizationConfig.ts for the
            authoritative list). Left as an open string, not an enum, so new
            namespaces can be registered in code without an API contract
            change; an unrecognized namespace 404s at request time. A
            namespace may additionally require its owning module to be
            enabled for the organization (e.g. attendance requires the
            "attendance" module) — see requireModuleEnabled (W5); such a
            namespace 403s instead of 404ing when its module is disabled.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOrganizationConfigInput"
      responses:
        "200":
          description: Updated organization configuration namespace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationConfig"
        "400":
          description: The merged configuration failed namespace validation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/modules:
    get:
      operationId: listOrganizationModules
      tags: [modules]
      summary: List modules with this organization's enablement state
      description: >-
        Merges the platform module registry (see GET /modules) with this
        organization's per-module enablement overrides. A module with no
        override row falls back to its registry defaultEnabled value. Does
        not gate access to anything yet — enforcing enabled/disabled on
        routes/navigation is a later workstream.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Modules with this organization's enablement state
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OrganizationModule"

  /organizations/{organizationId}/modules/{moduleKey}:
    patch:
      operationId: updateOrganizationModule
      tags: [modules]
      summary: Enable or disable a module for this organization
      description: >-
        Enabling requires the module's registry status to be active or beta
        (hidden and deprecated modules have no owning workstream yet and
        cannot be turned on) and requires every module in its
        requiredModuleKeys to already be enabled for this organization.
        Disabling is rejected if another currently-enabled module in this
        organization requires it.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: moduleKey
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOrganizationModuleInput"
      responses:
        "200":
          description: Updated module enablement
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationModule"
        "400":
          description: Module cannot be enabled or disabled (not enableable, missing dependencies, or required by another enabled module)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Unknown module key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /master-data/domains:
    get:
      operationId: listMasterDataDomains
      tags: [master-data]
      summary: List the Master Data domain registry
      description: >-
        System-wide catalog of Master Data domains (gender, marital_status,
        employment_type, ...) and their classification (system-defined,
        organization-overridable, organization-defined), not org-scoped.
      responses:
        "200":
          description: List of Master Data domains
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MasterDataDomain"

  /organizations/{organizationId}/master-data/{domain}:
    get:
      operationId: listMasterDataItems
      tags: [master-data]
      summary: List Master Data items for a domain
      description: >-
        System-defined items (shared by every organization) plus this
        organization's own items for the domain, sorted by sortOrder.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: domain
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Master Data items for the domain
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MasterDataItem"
        "404":
          description: Unknown domain
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createMasterDataItem
      tags: [master-data]
      summary: Add an organization-specific Master Data item
      description: >-
        Rejected for system-defined domains, which never accept
        organization-added items.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: domain
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMasterDataItemInput"
      responses:
        "201":
          description: Master Data item created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MasterDataItem"
        "400":
          description: Domain is system-defined and does not accept organization-added items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Unknown domain
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: An item with this code already exists for the domain in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/roles:
    get:
      operationId: listOrganizationRoles
      tags: [roles]
      summary: List roles available to this organization
      description: >-
        System role templates (see GET /roles) plus this organization's own
        customized copies. Gated membership.read (WWM Employee Access
        Remediation, 2026-09-07): the catalogue carries every role's full
        permission key list and is membership-administration metadata, not
        something the ordinary employee's organization.read entitles them
        to.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Roles available to this organization
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OrganizationRole"
    post:
      operationId: copyRoleTemplate
      tags: [roles]
      summary: Copy a system role template into this organization
      description: >-
        Creates the organization's own customizable copy of a system role
        template, including the template's current permissions. The
        template itself is never modified (ADR-015).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CopyRoleTemplateInput"
      responses:
        "201":
          description: Organization role created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationRole"
        "404":
          description: Unknown system role template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A role with this key already exists in the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/roles/{roleId}/permissions:
    post:
      operationId: grantRolePermission
      tags: [roles]
      summary: Grant a permission to an organization's own role
      description: Rejected for system role templates, which are never modified directly.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: roleId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GrantRolePermissionInput"
      responses:
        "204":
          description: Permission granted
        "400":
          description: Target role is a system role template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Role or permission not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/roles/{roleId}/permissions/{permissionId}:
    delete:
      operationId: revokeRolePermission
      tags: [roles]
      summary: Revoke a permission from an organization's own role
      description: Rejected for system role templates, which are never modified directly.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: roleId
          in: path
          required: true
          schema:
            type: integer
        - name: permissionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Permission revoked
        "400":
          description: Target role is a system role template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Role not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/audit-events:
    get:
      operationId: listAuditEvents
      tags: [audit-events]
      summary: List audit events
      description: >-
        Paginated, most recent first. Category-scoped (WS-3, Owner Decision
        #17): a caller sees only the audit categories their permissions
        grant (audit.read = all categories; audit.read.<category> = one
        category each). An explicit ?category= must be one the caller is
        allowed to see, or the request is 403. Omitting it returns the union
        of every allowed category. A caller with none of the audit.read*
        permissions gets 403.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: eventType
          in: query
          schema:
            type: string
        - name: targetType
          in: query
          schema:
            type: string
        - name: targetId
          in: query
          schema:
            type: string
        - name: actorApplicationUserId
          in: query
          schema:
            type: integer
        - name: category
          in: query
          description: One of the six audit categories. Must be within the caller's allowed set.
          schema:
            type: string
            enum: [hr, payroll, security, documents, assets_inventory, platform_configuration]
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated audit event list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditEventListResponse"

  /reports:
    get:
      operationId: listReports
      tags: [reports]
      summary: List the report registry
      description: >-
        System-wide catalog of registered reports (not org-scoped), per the
        Reporting Foundation (ADR-016). A report appearing here does not mean
        the caller can run it — that's gated per-report by
        requiredPermissionKey, checked at run time.
      responses:
        "200":
          description: List of reports
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Report"

  /organizations/{organizationId}/reports/{reportKey}/run:
    get:
      operationId: runReport
      tags: [reports]
      summary: Run a report
      description: >-
        Computes a registered report scoped to this organization. Permission
        required varies by report (see GET /reports). Pass ?format=csv for a
        CSV download instead of JSON. The required permission is resolved from
        the server code registry, not the stored report row, and a report the
        registry does not define is refused (403). The organization-wide
        workforce aggregates (headcount, workforce_status) require
        employee.write (2026-09-15).


        WS-15 P3 (§31.30) — consolidated execution. Every registered report is
        now executable here: the three organization-level aggregates run
        directly, and every module report DELEGATES to that module's own
        reporting service, resolving that module's own scope resolver first, so
        the generic path is scope-aware and enforces the same permission as the
        module route. Module routes are unchanged and remain authoritative for
        their own contracts.


        Parameters below are read individually and only where the selected
        report supports them — nothing is spread into a query builder. A report
        that genuinely requires one (Payroll needs `runId`) answers 400, which is
        distinct from an unknown report (404) and from a valid empty result
        (200 with zero rows).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: reportKey
          in: path
          required: true
          schema:
            type: string
        - name: format
          in: query
          schema:
            type: string
            enum: [json, csv]
        - name: from
          in: query
          description: Attendance range start (YYYY-MM-DD); also Assets/Office Inventory `dateFrom`. Defaults to the organization's own civil today.
          schema:
            type: string
        - name: to
          in: query
          description: Attendance range end (YYYY-MM-DD); also Assets/Office Inventory `dateTo`.
          schema:
            type: string
        - name: runId
          in: query
          description: Required for a Payroll report — reports are per locked payroll run, and there is no meaningful default.
          schema:
            type: integer
        - name: employeeId
          in: query
          schema:
            type: integer
        - name: departmentId
          in: query
          schema:
            type: integer
        - name: branchId
          in: query
          schema:
            type: integer
        - name: positionId
          in: query
          schema:
            type: integer
        - name: cycleId
          in: query
          schema:
            type: integer
        - name: reviewerId
          in: query
          schema:
            type: integer
        - name: courseId
          in: query
          schema:
            type: integer
        - name: managerId
          in: query
          schema:
            type: integer
        - name: approvalStatus
          in: query
          schema:
            type: string
        - name: itemId
          in: query
          schema:
            type: integer
        - name: storeId
          in: query
          schema:
            type: integer
        - name: movementType
          in: query
          schema:
            type: string
        - name: assetId
          in: query
          schema:
            type: integer
        - name: categoryCode
          in: query
          schema:
            type: string
        - name: maintenanceStatus
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Report result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportRunResult"
            text/csv:
              schema:
                type: string

  /organizations/{organizationId}/recruitment/dashboard:
    get:
      operationId: getRecruitmentDashboard
      tags: [recruitment-reporting]
      summary: Recruitment dashboard summary
      description: >-
        Real-time counts (open requisitions, open vacancies, total
        applicants), a current pipeline-stage breakdown, and recruiter/
        hiring-manager workload — computed live, never cached. Organization-
        wide staff see every figure; an assigned recruiter or hiring manager
        sees only their own workload/pipeline scope.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Dashboard summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentDashboard"

  /organizations/{organizationId}/recruitment/reports/{reportKey}:
    get:
      operationId: runRecruitmentReport
      tags: [recruitment-reporting]
      summary: Run a recruitment report
      description: >-
        Computes a registered recruitment report (see GET /reports, category
        "recruitment") scoped to this organization and to the caller's
        recruitment visibility tier. Pass ?format=csv for a CSV download
        instead of JSON.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: reportKey
          in: path
          required: true
          schema:
            type: string
        - name: format
          in: query
          schema:
            type: string
            enum: [json, csv]
      responses:
        "200":
          description: Recruitment report result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportRunResult"
            text/csv:
              schema:
                type: string

  /organizations/{organizationId}/performance/rating-scales:
    get:
      operationId: listPerformanceRatingScales
      tags: [performance-rating-scales]
      summary: List an organization's Performance rating scales
      description: Broad read — any authenticated organization member holding performance.read.own may list rating scales.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Rating scales
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PerformanceRatingScale"
    post:
      operationId: createPerformanceRatingScale
      tags: [performance-rating-scales]
      summary: Create a Performance rating scale
      description: Requires performance.manage.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePerformanceRatingScaleInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceRatingScale"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/rating-scales/{id}:
    get:
      operationId: getPerformanceRatingScale
      tags: [performance-rating-scales]
      summary: Get a Performance rating scale with its levels
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Rating scale with levels
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceRatingScaleWithLevels"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updatePerformanceRatingScale
      tags: [performance-rating-scales]
      summary: Update a Performance rating scale's name, description, or status
      description: Requires performance.manage. name/description/status are always editable — the level set itself is protected separately (see the /levels endpoint).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePerformanceRatingScaleInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceRatingScale"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/rating-scales/{id}/levels:
    patch:
      operationId: replacePerformanceRatingScaleLevels
      tags: [performance-rating-scales]
      summary: Replace a rating scale's full level set
      description: >-
        Requires performance.manage. Replace-all semantics — the submitted
        array becomes the scale's complete level set. Structurally blocked
        (409) once any Performance review has used this scale; archive it
        and create a new scale instead.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplacePerformanceRatingScaleLevelsInput"
      responses:
        "200":
          description: The scale's new level set
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PerformanceRatingScaleLevel"
        "400":
          description: Invalid levels
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The scale's levels are locked because it has already been used by a review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/templates:
    get:
      operationId: listPerformanceReviewTemplates
      tags: [performance-templates]
      summary: List an organization's Performance review templates
      description: Broad read — any authenticated organization member holding performance.read.own may list templates.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Review templates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PerformanceReviewTemplate"
    post:
      operationId: createPerformanceReviewTemplate
      tags: [performance-templates]
      summary: Create a Performance review template
      description: Requires performance.manage. ratingScaleId must belong to the same organization. goalsWeight + competenciesWeight must sum to exactly 100.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePerformanceReviewTemplateInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReviewTemplate"
        "400":
          description: Invalid input, or ratingScaleId does not belong to this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/templates/{id}:
    get:
      operationId: getPerformanceReviewTemplate
      tags: [performance-templates]
      summary: Get a Performance review template with its competencies
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Template with competencies
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReviewTemplateWithCompetencies"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updatePerformanceReviewTemplate
      tags: [performance-templates]
      summary: Update a Performance review template
      description: >-
        Requires performance.manage. Templates carry no usage lock (a
        review snapshots competencies at creation time, so a later
        template edit never rewrites review history) — editing is blocked
        only while the template's own status is already "archived".
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePerformanceReviewTemplateInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReviewTemplate"
        "400":
          description: Invalid input, or ratingScaleId does not belong to this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The template is archived and cannot be edited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/templates/{id}/competencies:
    patch:
      operationId: replacePerformanceTemplateCompetencies
      tags: [performance-templates]
      summary: Replace a template's full competency set
      description: >-
        Requires performance.manage. Replace-all semantics. Competency
        weights must sum to exactly 100 when the template's
        competenciesWeight is greater than 0, and the set must be empty
        when it is 0. Blocked (409) while the template is archived.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplacePerformanceTemplateCompetenciesInput"
      responses:
        "200":
          description: The template's new competency set
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PerformanceTemplateCompetency"
        "400":
          description: Invalid competencies
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The template is archived and cannot be edited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/cycles:
    get:
      operationId: listPerformanceCycles
      tags: [performance-cycles]
      summary: List an organization's Performance cycles
      description: Requires performance.manage — cycle/assignment configuration is HR/admin territory (W75), unlike the broader read grant W74's rating-scales/templates routes use.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Performance cycles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PerformanceCycle"
    post:
      operationId: createPerformanceCycle
      tags: [performance-cycles]
      summary: Create a Performance cycle
      description: Requires performance.manage. templateId/ratingScaleId must belong to the same organization and be 'active'. Created in 'draft' status.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePerformanceCycleInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceCycle"
        "400":
          description: Invalid input, or template/rating scale not usable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/cycles/{id}:
    get:
      operationId: getPerformanceCycle
      tags: [performance-cycles]
      summary: Get a Performance cycle
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Performance cycle
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceCycle"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updatePerformanceCycle
      tags: [performance-cycles]
      summary: Update a Performance cycle, or transition its status
      description: >-
        Requires performance.manage. Configuration fields are only
        editable while the cycle is 'draft' (409 otherwise). status may
        only move draft->archived, open->closed, or closed->archived —
        'open' can never be set directly, only via generate-reviews.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePerformanceCycleInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceCycle"
        "400":
          description: Invalid input, or template/rating scale not usable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The cycle is not editable in its current status, or the requested status transition is invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/cycles/{id}/generate-reviews:
    post:
      operationId: generatePerformanceReviews
      tags: [performance-cycles]
      summary: Open a draft cycle and bulk-assign Performance reviews to its eligible employees
      description: >-
        Requires performance.manage. The cycle must be 'draft' (409
        otherwise — this action runs exactly once per cycle). Resolves
        eligible employees per the cycle's own applicabilityScope
        (all_active/department/position use active employmentStatus
        automatically; manual requires an explicit employeeIds list, each
        validated to belong to this organization and be active). Creates
        one performance_reviews row per eligible employee directly in
        self_assessment status, with competencies snapshotted from the
        cycle's template — never goals, which W76 owns entirely. Flips the
        cycle to 'open' in the same atomic transaction.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratePerformanceReviewsInput"
      responses:
        "201":
          description: Reviews generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratePerformanceReviewsResult"
        "400":
          description: Invalid employeeIds, or an ineligible/cross-organization employee was named
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The cycle is not in draft status — reviews were already generated, or the cycle is closed/archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/reviews:
    get:
      operationId: listPerformanceReviews
      tags: [performance-reviews]
      summary: List an organization's Performance reviews (W80 — internal HR workspace)
      description: >-
        Requires performance.manage, organization-wide. Optional cycleId/
        employeeId/status/departmentId/positionId/reviewerId query filters —
        department/position/reviewer filter the review's own historical
        SNAPSHOT columns, never live employee data, so a later transfer
        never changes which reviews a filter surfaces. Paginated
        (items/total/page/pageSize), matching listEmployees' own
        established convention. The manager's own scoped view remains
        GET .../team-reviews (W78) — deliberately not duplicated here.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: cycleId
          in: query
          required: false
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [draft, self_assessment, manager_review, hr_review, finalized, acknowledged]
        - name: departmentId
          in: query
          required: false
          description: Filters departmentIdSnapshot, not the employee's current department.
          schema:
            type: integer
        - name: positionId
          in: query
          required: false
          description: Filters positionIdSnapshot, not the employee's current position.
          schema:
            type: integer
        - name: reviewerId
          in: query
          required: false
          description: Filters the snapshotted reviewerEmployeeId.
          schema:
            type: integer
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          required: false
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated Performance reviews
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReviewListResponse"

  /organizations/{organizationId}/performance/reviews/{id}:
    get:
      operationId: getPerformanceReview
      tags: [performance-reviews]
      summary: Get a Performance review with its snapshotted competencies and goals
      description: >-
        Requires performance.read.own (own review, or reviewer-of-record
        via the review's own snapshotted reviewerEmployeeId) or
        performance.manage (organization-wide).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Review with competencies and goals
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReviewWithCompetencies"
        "403":
          description: Not authorized to view this review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/reviews/{id}/goals:
    post:
      operationId: createPerformanceReviewGoal
      tags: [performance-reviews]
      summary: Propose (own review, self_assessment) or create an official goal (reviewer of record, draft/manager_review)
      description: >-
        Requires performance.write.own (own review, self_assessment only —
        inserted approvalStatus 'proposed', not yet official) or
        performance.review.write (reviewer of record, draft or
        manager_review only — inserted approvalStatus 'accepted'
        immediately, no separate approval step). Which path applies is
        resolved entirely from the caller's own server-derived
        relationship to the review, never a client-supplied flag.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePerformanceReviewGoalInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceGoal"
        "400":
          description: Invalid input for the goal's measurementType
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not this review's own employee or reviewer of record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The review is not in a status that permits this action
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/reviews/{id}/goals/{goalId}:
    patch:
      operationId: updatePerformanceReviewGoal
      tags: [performance-reviews]
      summary: Edit a goal
      description: >-
        Two disjoint paths: the employee may edit only their own
        still-proposed goal, only while self_assessment
        (performance.write.own); the reviewer of record may edit any goal
        on the review, only while manager_review (performance.review.write).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: goalId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePerformanceReviewGoalInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceGoal"
        "400":
          description: Invalid input for the goal's measurementType
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not authorized to edit this goal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The review or goal is not in a status that permits this edit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/reviews/{id}/goals/{goalId}/accept:
    post:
      operationId: acceptPerformanceReviewGoal
      tags: [performance-reviews]
      summary: Reviewer of record accepts a proposed goal
      description: >-
        Requires performance.review.write (reviewer of record), review
        status manager_review. Optionally edits title/description/target/
        unit/weight/dueDate in the same atomic action. An atomic
        conditional update guards against a concurrent double-decision or
        a repeat/terminal-state redecision (409).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: goalId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcceptPerformanceReviewGoalInput"
      responses:
        "200":
          description: Accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceGoal"
        "400":
          description: Invalid input, or not an employee-proposed goal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not this review's reviewer of record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The review is not manager_review, or the goal was already decided
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/reviews/{id}/goals/{goalId}/reject:
    post:
      operationId: rejectPerformanceReviewGoal
      tags: [performance-reviews]
      summary: Reviewer of record rejects a proposed goal
      description: >-
        Requires performance.review.write (reviewer of record), review
        status manager_review. A reason is required (stored as
        managerComment). The goal is retained, never deleted, and
        permanently excluded from weighting/scoring. Same atomic
        conditional-update guard as accept.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: goalId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectPerformanceReviewGoalInput"
      responses:
        "200":
          description: Rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceGoal"
        "400":
          description: Missing reason, or not an employee-proposed goal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not this review's reviewer of record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The review is not manager_review, or the goal was already decided
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/my-reviews:
    get:
      operationId: listMyPerformanceReviews
      tags: [performance-reviews]
      summary: ESS — the caller's own Performance reviews
      description: Requires performance.read.own. employeeId is always server-resolved from the caller's own employee_user_links row, never client-supplied. Returns an empty array if the caller has no linked employee record.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own reviews
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PerformanceReview"

  /organizations/{organizationId}/performance/reviews/{id}/competencies/{competencyId}:
    patch:
      operationId: rateCompetency
      tags: [performance-reviews]
      summary: Employee self-rates a snapshotted competency
      description: >-
        Requires performance.write.own, own review only, self_assessment
        status only. employeeRatingValue must match one of the review's
        own configured rating-scale levels. Never touches
        managerRatingValue/managerComment.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: competencyId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RateCompetencyInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReviewCompetency"
        "400":
          description: employeeRatingValue does not match a configured rating-scale level
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not this review's own employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The review is not in self_assessment status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/reviews/{id}/self-assessment:
    post:
      operationId: submitSelfAssessment
      tags: [performance-reviews]
      summary: Employee submits their self-assessment — the authoritative self_assessment -> manager_review transition
      description: >-
        Requires performance.write.own, own review only. Readiness is
        validated first (400, listing every problem): every competency
        has an employeeRatingValue; every accepted, non-qualitative,
        non-N/A goal has a non-empty employeeComment; if any such goals
        exist, their weights sum to exactly 100. An atomic conditional
        UPDATE ... WHERE status = 'self_assessment' then performs the
        transition, setting selfAssessmentSubmittedAt — a concurrent or
        repeat submission affects zero rows and returns 409.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Submitted — review is now in manager_review status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReview"
        "400":
          description: Self-assessment is not ready to submit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SelfAssessmentNotReadyError"
        "403":
          description: Not this review's own employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The review is not (or is no longer) in self_assessment status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/team-reviews:
    get:
      operationId: listTeamPerformanceReviews
      tags: [performance-reviews]
      summary: Manager — reviews where the caller is the reviewer of record
      description: Requires performance.review.write. reviewerEmployeeId is always server-resolved from the caller's own employee_user_links row, never client-supplied. Returns an empty array if the caller has no linked employee record.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Reviews where the caller is reviewerEmployeeId
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PerformanceReview"

  /organizations/{organizationId}/performance/reviews/{id}/manager-review:
    post:
      operationId: submitManagerReview
      tags: [performance-reviews]
      summary: Reviewer of record submits the manager review — the authoritative manager_review -> hr_review transition
      description: >-
        Requires performance.review.write, reviewer of record only.
        Readiness is validated first (400, listing every problem): every
        proposed goal resolved (accepted or rejected); every accepted,
        non-qualitative, non-N/A goal has actualResult; every non-N/A
        competency has managerRatingValue; accepted, non-qualitative goal
        weights sum to 100; at least one section has a scoreable item. The
        official computedOverallScore is then computed deterministically
        (employee self-ratings never contribute) and an atomic conditional
        UPDATE ... WHERE status = 'manager_review' performs the
        transition, setting managerReviewSubmittedAt — a concurrent or
        repeat submission affects zero rows and returns 409.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Submitted — review is now in hr_review status, computedOverallScore set
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReview"
        "400":
          description: Manager review is not ready to submit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SelfAssessmentNotReadyError"
        "403":
          description: Not this review's reviewer of record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The review is not (or is no longer) in manager_review status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/reviews/{id}/finalize:
    post:
      operationId: finalizePerformanceReview
      tags: [performance-reviews]
      summary: HR finalizes the review, optionally overriding the score — the authoritative hr_review -> finalized transition
      description: >-
        Requires performance.finalize, organization-wide. computedOverallScore
        (set by the manager's own W78 submission) is read-only historical
        input here and is never recomputed or overwritten. hrOverrideScore
        and hrOverrideReason must both be supplied together, or neither —
        finalize with or without an override in the same call (§27 names no
        separate override-only route). An atomic conditional
        UPDATE ... WHERE status = 'hr_review' performs the transition,
        setting hrFinalizedAt — a concurrent or repeat finalization affects
        zero rows and returns 409.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinalizePerformanceReviewInput"
      responses:
        "200":
          description: Finalized — review is now in finalized status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReview"
        "400":
          description: hrOverrideScore/hrOverrideReason not supplied together, or the score is out of range
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not authorized to finalize this review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The review is not (or is no longer) in hr_review status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/reviews/{id}/reopen:
    post:
      operationId: reopenPerformanceReview
      tags: [performance-reviews]
      summary: HR reopens a review to an earlier explicit stage
      description: >-
        Requires performance.manage, organization-wide (not
        performance.finalize — §10.1 row 6 / §16 both explicitly assign
        reopen to performance.manage). Valid only from manager_review,
        hr_review, or finalized; targetStage must be one of
        self_assessment/manager_review/hr_review and strictly earlier than
        the review's current status; reason is mandatory. Clears only the
        review-row timestamp/decision fields downstream of the target stage
        (§10.4) — goal/competency/self-assessment content is never blanked.
        revisionNumber increments by 1 in the same atomic
        UPDATE ... WHERE status = '<current>' — a concurrent reopen affects
        zero rows and returns 409.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReopenPerformanceReviewInput"
      responses:
        "200":
          description: Reopened — review is now in the target status, revisionNumber incremented
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReview"
        "400":
          description: Missing reason, or an invalid/non-earlier targetStage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not authorized to reopen this review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The review's current status cannot be reopened
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/reviews/{id}/acknowledge:
    post:
      operationId: acknowledgePerformanceReview
      tags: [performance-reviews]
      summary: Employee acknowledges a finalized review — the authoritative finalized -> acknowledged transition
      description: >-
        Requires performance.write.own, own review only (§10.1 row 5). "Seen,"
        not "agreed" — never mutates computedOverallScore, hrOverrideScore,
        hrOverrideReason, or any manager/HR field. acknowledgementRequiredSnapshot
        does not gate this route: when false, acknowledgement is optional but
        still permitted (the snapshot is a display-only signal for the ESS UI,
        per §10.1 row 5's own parenthetical), so any own review in finalized
        status may be acknowledged. An optional employeeFinalComment may be
        recorded in the same call — informational only, never a formal appeal
        (Owner Decision 3, deferred) and never validated as a precondition for
        acknowledging. acknowledgedAt is always server-generated, never
        client-supplied. An atomic conditional
        UPDATE ... WHERE status = 'finalized' AND employee_id = <server-resolved>
        performs the transition — a concurrent or repeat acknowledgement
        affects zero rows and returns 409, never a silent double-transition.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcknowledgePerformanceReviewInput"
      responses:
        "200":
          description: Acknowledged — review is now in acknowledged status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReview"
        "403":
          description: Not this review's own employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The review is not (or is no longer) in finalized status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/dashboard:
    get:
      operationId: getPerformanceDashboard
      tags: [performance-reporting]
      summary: Performance dashboard — zero-filled lifecycle tile breakdown
      description: >-
        Requires performance.reports.read. Scope resolved in the service
        layer exactly like recruitment.reports.read/attendance.read.own:
        performance.manage holders see the whole organization; everyone
        else sees only reviews where they are the review's own employee or
        its snapshotted reviewerEmployeeId. Optional ?cycleId= narrows
        every review-scoped tile to one cycle; activeCycleCount is always
        organization-wide (cycle existence isn't sensitive). Plain counts
        only — no completion percentage or average-score tile, per the
        frozen plan's own "no invented rate/KPI" scope. A reopened review
        counts only under its current status.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: cycleId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Dashboard tile breakdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceDashboard"

  /organizations/{organizationId}/performance/reports/{reportKey}:
    get:
      operationId: runPerformanceReport
      tags: [performance-reporting]
      summary: Run a Performance report
      description: >-
        Computes a registered Performance report (see GET /reports,
        category "performance": performance_review_status,
        performance_scores, performance_goal_results) over the caller's
        own/reviewer-of-record/organization-wide visibility tier (identical
        to the dashboard). Filters mirror GET .../performance/reviews
        (W80) — cycleId/status/departmentId/positionId/reviewerId/
        employeeId, each filtering the review's own historical snapshot
        columns, narrowing the authorized scope but never broadening it.
        Pass ?format=csv for a CSV download instead of JSON.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: reportKey
          in: path
          required: true
          schema:
            type: string
        - name: cycleId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [draft, self_assessment, manager_review, hr_review, finalized, acknowledged]
        - name: departmentId
          in: query
          required: false
          description: Filters departmentIdSnapshot, not the employee's current department.
          schema:
            type: integer
        - name: positionId
          in: query
          required: false
          description: Filters positionIdSnapshot, not the employee's current position.
          schema:
            type: integer
        - name: reviewerId
          in: query
          required: false
          description: Filters the snapshotted reviewerEmployeeId.
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [json, csv]
      responses:
        "200":
          description: Performance report result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportRunResult"
            text/csv:
              schema:
                type: string
        "404":
          description: Unknown Performance report key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/reviews/{id}/evidence:
    get:
      operationId: listPerformanceReviewEvidence
      tags: [performance-evidence]
      summary: List a review's evidence
      description: >-
        Visible to the same own/reviewer-of-record/organization-wide tier as
        the review itself (§27 line 142's own scope model) — not stage-
        gated, since evidence remains part of the review's durable
        historical record after finalization.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Evidence attached to this review
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PerformanceReviewEvidence"
        "403":
          description: Not authorized to view this review's evidence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Review not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: addPerformanceReviewEvidence
      tags: [performance-evidence]
      summary: Attach evidence to a review (or one of its goals)
      description: >-
        multipart/form-data upload. Reuses the existing employee_documents
        storage layer verbatim (§8.9 — "a lightweight join table only, no
        new storage layer") — PDF, JPEG, PNG, DOCX, and XLSX only, validated
        by file signature, 10MB max, identical to the existing employee
        document upload. Server-derived actor identity decides which path
        applies, never a client-supplied flag: the review's own employee
        (performance.write.own) may attach evidence only while the review is
        self_assessment; the reviewer of record (performance.review.write)
        only while manager_review; HR (performance.manage) only while
        hr_review. Optional goalId attaches at a specific goal instead of
        the review as a whole; it must belong to this same review.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                goalId:
                  type: integer
                  description: Must belong to this same review.
      responses:
        "201":
          description: Evidence attached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceReviewEvidence"
        "400":
          description: Invalid file, or a goalId that isn't this review's own
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not authorized to add evidence to this review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Review or goal not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The review is not in a stage that allows this caller to add evidence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/performance/reviews/{id}/evidence/{evidenceId}/download:
    get:
      operationId: downloadPerformanceReviewEvidence
      tags: [performance-evidence]
      summary: Download a single evidence file
      description: >-
        Authorization-checked before any storage read (same own/reviewer-
        of-record/organization-wide tier as the review). No public or
        signed URL is ever generated — the file streams through this
        authenticated route on every request.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: evidenceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The file
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "403":
          description: Not authorized to access this review's evidence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Review or evidence not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/courses:
    get:
      operationId: listLearningCourses
      tags: [learning-courses]
      summary: List an organization's Learning courses
      description: Broad read — any authenticated organization member holding learning.read.own may list courses.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Courses
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LearningCourse"
    post:
      operationId: createLearningCourse
      tags: [learning-courses]
      summary: Create a Learning course
      description: >-
        Requires learning.manage. categoryCode is a free-text code from the
        training_category Master Data domain, not validated against the
        domain's item list (§8.1). certificateValidityMonths may only be
        supplied when issuesCertificate is true.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLearningCourseInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningCourse"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/courses/{id}:
    get:
      operationId: getLearningCourse
      tags: [learning-courses]
      summary: Get a Learning course
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Course
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningCourse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateLearningCourse
      tags: [learning-courses]
      summary: Update a Learning course
      description: >-
        Requires learning.manage. Courses carry no usage lock (an
        enrollment snapshots the course's own fields at creation time, so a
        later course edit never rewrites enrollment history) — editing is
        blocked only while the course's own status is already "archived".
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLearningCourseInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningCourse"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The course is archived and cannot be edited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/courses/{id}/sessions:
    get:
      operationId: listLearningCourseSessions
      tags: [learning-course-sessions]
      summary: List a course's scheduled sessions
      description: Broad read — any authenticated organization member holding learning.read.own may list sessions.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Sessions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LearningCourseSession"
        "404":
          description: Course not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createLearningCourseSession
      tags: [learning-course-sessions]
      summary: Schedule a session for a course
      description: >-
        Requires learning.manage. instructorEmployeeId, if supplied, must
        belong to the same organization.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLearningCourseSessionInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningCourseSession"
        "400":
          description: Invalid input, or instructorEmployeeId does not belong to this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Course not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/sessions/{id}:
    get:
      operationId: getLearningCourseSession
      tags: [learning-course-sessions]
      summary: Get a Learning course session
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningCourseSession"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateLearningCourseSession
      tags: [learning-course-sessions]
      summary: Edit a session, or transition it to completed/cancelled
      description: >-
        Requires learning.manage. A single atomic conditional
        UPDATE ... WHERE status = 'scheduled' performs both ordinary field
        edits and, optionally in the same call, the one-way transition to
        completed/cancelled (§10.2) — once a session leaves 'scheduled' it
        is permanently locked, no further edit or transition of any kind
        is ever permitted. A concurrent or repeat transition affects zero
        rows and returns 409.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLearningCourseSessionInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningCourseSession"
        "400":
          description: Invalid input, or instructorEmployeeId does not belong to this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The session is no longer scheduled and can no longer be edited or transitioned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/courses/{id}/enroll:
    post:
      operationId: requestLearningEnrollment
      tags: [learning-enrollments]
      summary: Employee self-enrolls or requests enrollment in a course
      description: >-
        Requires learning.write.own. Employee identity is always
        server-derived — never a client-supplied employeeId. sessionId is
        required for an instructor-led course, forbidden for a self-paced
        one. approvalStatus starts 'pending' if the course requires
        approval, else 'auto_approved'; status is always 'assigned'
        regardless (§10.3). A duplicate non-terminal enrollment for the
        same course/session is rejected 409.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RequestLearningEnrollmentInput"
      responses:
        "201":
          description: Enrollment created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningEnrollment"
        "400":
          description: Invalid input (e.g. missing/unexpected sessionId for this course's delivery mode)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Course not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Course/session not open for enrollment, session at capacity, or a duplicate non-terminal enrollment already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/courses/{id}/assign:
    post:
      operationId: assignLearningEnrollments
      tags: [learning-enrollments]
      summary: Assign a course to one or more employees (HR/L&D bulk-assign, or a manager assigning direct reports)
      description: >-
        Requires learning.manage (full audience targeting — all_active/
        department/position/manual, any employee in the organization) or
        learning.review.write (manager of record — scope must be 'manual',
        and every employeeId must be the caller's own direct report,
        server-verified). Both paths bypass approval entirely
        (approvalStatus = auto_approved) — the assigner's own authority is
        the approval. An eligible employee who already holds a
        non-terminal enrollment for the target course/session is skipped,
        not treated as a whole-batch failure; the result reports both
        counts.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignLearningEnrollmentsInput"
      responses:
        "201":
          description: Assignment result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssignLearningEnrollmentsResult"
        "400":
          description: Invalid input, a manager attempting audience-targeting scope, or a cross-organization employee/department/position reference
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: A manager attempting to assign an employee who is not their direct report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Course not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Course/session not open for enrollment, or session at capacity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/my-enrollments:
    get:
      operationId: listMyLearningEnrollments
      tags: [learning-enrollments]
      summary: List the caller's own Learning enrollments
      description: Requires learning.read.own. employeeId is always server-resolved from the caller's own employee_user_links row, never client-supplied. Returns an empty array if the caller has no linked employee record.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own enrollments
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LearningEnrollment"

  /organizations/{organizationId}/learning/team-enrollments:
    get:
      operationId: listTeamLearningEnrollments
      tags: [learning-enrollments]
      summary: Manager — enrollments where the caller is the manager of record
      description: Requires learning.review.write. managerEmployeeIdSnapshot is always server-resolved, never client-supplied. Returns an empty array if the caller has no linked employee record.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Enrollments where the caller is the manager of record
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LearningEnrollment"

  /organizations/{organizationId}/learning/enrollments:
    get:
      operationId: listLearningEnrollments
      tags: [learning-enrollments]
      summary: List an organization's Learning enrollments (internal HR/L&D workspace)
      description: >-
        Requires learning.manage, organization-wide. Optional courseId/
        employeeId/status/approvalStatus query filters. Paginated
        (items/total/page/pageSize). The manager's own scoped view remains
        GET .../team-enrollments — deliberately not duplicated here.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: courseId
          in: query
          required: false
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [assigned, in_progress, completed, failed, cancelled]
        - name: approvalStatus
          in: query
          required: false
          schema:
            type: string
            enum: [auto_approved, pending, approved, rejected]
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          required: false
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated Learning enrollments
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningEnrollmentListResponse"

  /organizations/{organizationId}/learning/enrollments/{id}:
    get:
      operationId: getLearningEnrollment
      tags: [learning-enrollments]
      summary: Get a Learning enrollment
      description: Requires learning.read.own (own, manager-of-record, or instructor-of-record) or learning.manage (organization-wide).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Enrollment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningEnrollment"
        "403":
          description: Not authorized to view this enrollment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/enrollments/{id}/approve:
    post:
      operationId: approveLearningEnrollment
      tags: [learning-enrollments]
      summary: Approve a pending employee-requested enrollment
      description: >-
        Requires learning.review.write (manager of record only) or
        learning.manage (organization-wide). The enrollment's own employee
        may never decide their own request. Atomic conditional
        UPDATE ... WHERE approvalStatus = 'pending' — a repeat or
        already-decided request returns 409, never a silent overwrite.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningEnrollment"
        "403":
          description: Not this enrollment's manager of record, or an attempt to decide one's own request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This enrollment is not awaiting approval
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/enrollments/{id}/reject:
    post:
      operationId: rejectLearningEnrollment
      tags: [learning-enrollments]
      summary: Reject a pending employee-requested enrollment
      description: >-
        Identical authorization/atomicity to the approve route. A rejected
        enrollment is permanently inert (status remains 'assigned' but can
        never progress) — never physically deleted, never silently
        converted into a cancellation.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningEnrollment"
        "403":
          description: Not this enrollment's manager of record, or an attempt to decide one's own request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This enrollment is not awaiting approval
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/enrollments/{id}/cancel:
    post:
      operationId: cancelLearningEnrollment
      tags: [learning-enrollments]
      summary: Cancel an enrollment
      description: >-
        Requires learning.write.own (the enrollment's own employee,
        non-mandatory, only while still 'assigned', no reason required) or
        learning.manage (any non-terminal enrollment, reason always
        required — §10.5). Atomic conditional
        UPDATE ... WHERE status IN ('assigned','in_progress').
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelLearningEnrollmentInput"
      responses:
        "200":
          description: Cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningEnrollment"
        "400":
          description: cancelReason missing where required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not this enrollment's own employee (without learning.manage), or a mandatory enrollment being self-cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This enrollment can no longer be cancelled (already terminal, or already in_progress for an employee's own non-mandatory cancel attempt)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/enrollments/{id}/progress:
    patch:
      operationId: advanceLearningEnrollmentProgress
      tags: [learning-enrollments]
      summary: Employee advances their own self-paced enrollment (assigned → in_progress → completed)
      description: >-
        Requires learning.write.own, own enrollment, self-paced delivery
        mode only (instructor-led completion belongs to the instructor of
        record or HR/L&D, a later workstream). There is no numeric
        "percentage complete" — only the two one-way transitions
        assigned→in_progress and in_progress→completed, each an atomic
        conditional update guarded by the enrollment's current `status`
        (§10.3.1) — a concurrent or repeat call against the same prior
        state returns a controlled 409, never a silent double-transition.
        The enrollment must also have `approvalStatus` of `auto_approved`
        or `approved` — approval gates actionability, not row existence
        (§0, §10.3) — a still-`pending` or `rejected` request 409s. This
        route never accepts or records an assessment result
        (`passed`/`score`) — an employee can never grade themselves,
        regardless of `hasAssessmentSnapshot` (§10.3.1, §11).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdvanceLearningEnrollmentProgressInput"
      responses:
        "200":
          description: Advanced
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningEnrollment"
        "400":
          description: Invalid target status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not this enrollment's own employee, or the enrollment is not self-paced
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Not yet approved, or the enrollment is not in the expected prior state for this transition (already advanced, already terminal, or a concurrent request already won)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/enrollments/{id}/attendance:
    post:
      operationId: markLearningEnrollmentAttendance
      tags: [learning-enrollments]
      summary: Record attendance for an instructor-led enrollment
      description: >-
        Requires learning.review.write (the session's own instructor of
        record, a live comparison against the session's instructorEmployeeId
        — never inferred from role or from holding the permission alone) or
        learning.manage (HR/L&D, organization-wide). Instructor-led only —
        a self-paced enrollment has no session to attend. Attendance is a
        separate fact from status (§10.3) and does not by itself transition
        it. Recorded once — an atomic conditional update guarded by both
        the enrollment's non-terminal status and attended still being null;
        a repeat or concurrent call, or a call against an already-terminal
        enrollment, returns a controlled 409.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkLearningEnrollmentAttendanceInput"
      responses:
        "200":
          description: Attendance recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningEnrollment"
        "400":
          description: This enrollment is self-paced — attendance does not apply
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not this session's own instructor, and not learning.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Attendance already recorded, or the enrollment is no longer in a non-terminal state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/enrollments/{id}/complete:
    post:
      operationId: completeLearningEnrollment
      tags: [learning-enrollments]
      summary: Complete an enrollment (instructor-led, or HR/L&D administrative completion of any enrollment)
      description: >-
        Requires learning.review.write (the session's own instructor of
        record, instructor-led enrollments only) or learning.manage (HR/L&D,
        any enrollment organization-wide — the only path for a self-paced
        enrollment, since no instructor-of-record relationship exists
        without a session). When the enrollment's own hasAssessmentSnapshot
        is true, `passed` is required and determines the resulting status
        server-side (completed if true, failed if false — never accepted
        directly from the caller, §10.3); when false, `passed`/`score` must
        not be supplied at all. The employee can never complete an assessed
        enrollment through their own `.../progress` route — this is the
        only route that can. Atomic conditional update guarded by
        non-terminal status — a concurrent or repeat completion, or one
        against an already-terminal enrollment, returns a controlled 409.
        Certificate issuance is not performed by this route — W90 owns
        issuance exclusively, per this workstream's own frozen STOP
        boundary.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteLearningEnrollmentInput"
      responses:
        "200":
          description: Completed (or failed, for an assessed course with passed=false)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningEnrollment"
        "400":
          description: passed missing for an assessed course, or passed/score supplied where no assessment applies
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not this session's own instructor and not learning.manage, or a self-paced enrollment without learning.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The enrollment is no longer in a non-terminal state (already completed/failed/cancelled, or a concurrent request already won)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/my-certificates:
    get:
      operationId: listMyLearningCertificates
      tags: [learning-certificates]
      summary: List the caller's own Learning certificates
      description: Requires learning.read.own. employeeId is always server-resolved from the caller's own employee_user_links row. "Expired" is never a stored status — compute it client-side from expiresAt < now(); status here is only ever active or revoked (§10.4 rule 9).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own certificates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LearningCertificate"

  /organizations/{organizationId}/learning/certificates:
    get:
      operationId: listLearningCertificates
      tags: [learning-certificates]
      summary: List an organization's Learning certificates (org-wide)
      description: >-
        Requires learning.manage, organization-wide. No manager-of-record
        or instructor-of-record certificate visibility route exists —
        §21's own frozen table names only own (my-certificates) and
        org-wide (this route) for certificates; none other is built.
        Paginated (items/total/page/pageSize), mirroring
        LearningEnrollmentListResponse's own established shape.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [active, revoked]
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          required: false
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated Learning certificates
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningCertificateListResponse"

  /organizations/{organizationId}/learning/certificates/{id}/revoke:
    post:
      operationId: revokeLearningCertificate
      tags: [learning-certificates]
      summary: Revoke an active certificate
      description: >-
        Requires learning.manage. Mandatory revokeReason. Permanently
        terminal — active → revoked only, no restore/un-revoke/reopen path
        exists anywhere (§10.4 rule 8). Atomic conditional update guarded
        by status = 'active' — a repeat or concurrent revocation returns a
        controlled 409.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RevokeLearningCertificateInput"
      responses:
        "200":
          description: Revoked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningCertificate"
        "400":
          description: revokeReason missing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Certificate not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Already revoked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/dashboard:
    get:
      operationId: getLearningDashboard
      tags: [learning-reporting]
      summary: Learning dashboard — zero-filled enrollment tile breakdown
      description: >-
        Requires learning.reports.read. Scope resolved in the service layer
        exactly like performance.reports.read: learning.manage holders see
        the whole organization; everyone else sees only enrollments where
        they are the enrollment's own employee or its snapshotted
        managerEmployeeIdSnapshot. activeCourseCount is always
        organization-wide (course catalog existence isn't sensitive
        enrollment data). overdueCount excludes every terminal status
        (completed/failed/cancelled), not merely "not yet completed".
        certificatesExpiringSoonCount uses a fixed 30-day window. Plain
        counts only — no completion percentage or average-score tile.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Dashboard tile breakdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningDashboard"

  /organizations/{organizationId}/learning/reports/{reportKey}:
    get:
      operationId: runLearningReport
      tags: [learning-reporting]
      summary: Run a Learning report
      description: >-
        Computes a registered Learning report (see GET /reports, category
        "learning": learning_enrollment_status, learning_completion_summary,
        learning_certificate_expiry) over the caller's own/manager-of-
        record/organization-wide visibility tier (identical to the
        dashboard). No instructor-of-record reporting tier exists.
        learning_completion_summary is a per-course aggregate; its
        completionPercentage excludes pending-approval and rejected
        enrollments from the denominator, and excludes cancelled
        enrollments, but includes failed. Pass ?format=csv for a CSV
        download instead of JSON — CSV scope is always identical to the
        already-authorized JSON scope.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: reportKey
          in: path
          required: true
          schema:
            type: string
        - name: courseId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          description: Enrollment status for learning_enrollment_status; certificate status (active/revoked) for learning_certificate_expiry.
          schema:
            type: string
        - name: approvalStatus
          in: query
          required: false
          schema:
            type: string
            enum: [auto_approved, pending, approved, rejected]
        - name: departmentId
          in: query
          required: false
          description: Filters departmentIdSnapshot, not the employee's current department.
          schema:
            type: integer
        - name: positionId
          in: query
          required: false
          description: Filters positionIdSnapshot, not the employee's current position.
          schema:
            type: integer
        - name: managerId
          in: query
          required: false
          description: Filters the snapshotted managerEmployeeIdSnapshot.
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [json, csv]
      responses:
        "200":
          description: Learning report result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportRunResult"
            text/csv:
              schema:
                type: string
        "404":
          description: Unknown Learning report key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/enrollments/{id}/evidence:
    get:
      operationId: listLearningEnrollmentEvidence
      tags: [learning-evidence]
      summary: List an enrollment's evidence
      description: >-
        Visible to the same own/manager-of-record/instructor-of-record/
        organization-wide tier as the enrollment itself (§21's own frozen
        row) — no stage/status restriction, at any enrollment status,
        including terminal.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Evidence attached to this enrollment
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LearningEnrollmentEvidence"
        "403":
          description: Not authorized to view this enrollment's evidence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Enrollment not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: addLearningEnrollmentEvidence
      tags: [learning-evidence]
      summary: Attach evidence to an enrollment
      description: >-
        multipart/form-data upload. Reuses the existing employee_documents
        storage layer verbatim (§18) — PDF, JPEG, PNG, DOCX, and XLSX
        only, validated by file signature, 10MB max, identical to every
        other document upload on this platform. Same own/manager-of-
        record/instructor-of-record/organization-wide tier as the
        enrollment itself, no stage/status restriction.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "201":
          description: Evidence attached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningEnrollmentEvidence"
        "400":
          description: Invalid file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not authorized to add evidence to this enrollment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Enrollment not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/learning/enrollments/{id}/evidence/{evidenceId}/download:
    get:
      operationId: downloadLearningEnrollmentEvidence
      tags: [learning-evidence]
      summary: Download a single evidence file
      description: >-
        Authorization-checked before any storage read (same own/manager-
        of-record/instructor-of-record/organization-wide tier as the
        enrollment). No public or signed URL is ever generated — the file
        streams through this authenticated route on every request.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: evidenceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The file
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "403":
          description: Not authorized to access this enrollment's evidence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Enrollment or evidence not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets:
    get:
      operationId: listAssets
      tags: [assets]
      summary: List an organization's assets (Asset Register)
      description: >-
        Requires asset_management.manage, organization-wide (§20 — the
        register itself is HR/Asset Officer only in V1, no broader own-scope
        catalog rule is frozen). Optional status/condition/categoryCode/
        branchId/search query filters. Paginated (items/total/page/pageSize).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [available, assigned, maintenance, lost, retired]
        - name: condition
          in: query
          required: false
          schema:
            type: string
            enum: [new, good, fair, poor, damaged]
        - name: categoryCode
          in: query
          required: false
          schema:
            type: string
        - name: branchId
          in: query
          required: false
          schema:
            type: integer
        - name: search
          in: query
          required: false
          schema:
            type: string
          description: Matches asset tag, name, serial number, manufacturer, or model.
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          required: false
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Paginated assets
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetListResponse"
    post:
      operationId: createAsset
      tags: [assets]
      summary: Register a new asset
      description: >-
        Requires asset_management.manage. assetTag is always server-generated
        (sequential, per-organization) — never client-supplied. status always
        starts "available" — never client-supplied. categoryCode is
        free-text from the asset_category Master Data domain, not validated
        against the domain's item list (§4, §5, Owner Decision 8). A
        submitted empty-string serialNumber is stored as null. branchId, when
        supplied, must belong to the caller's own organization.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAssetInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "400":
          description: Invalid input, or branchId does not belong to this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/{id}:
    get:
      operationId: getAsset
      tags: [assets]
      summary: Get an asset
      description: >-
        Requires asset_management.read.own — org-wide reach for a caller who
        additionally holds asset_management.manage, otherwise only if the
        caller currently holds this specific asset via an active assignment
        (§20's own frozen dual-floor; no assignment exists until W97 ships,
        so this narrower reach is presently unreachable in practice but is
        implemented exactly as frozen).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Asset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "403":
          description: Not authorized to view this asset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateAsset
      tags: [assets]
      summary: Update an asset's base register fields
      description: >-
        Requires asset_management.manage. Base register fields only — never
        status, never condition (each has its own dedicated, mandatory-
        reason, audited action: retire/mark-lost/recover and
        POST .../condition respectively). assetTag is never editable
        (server-generated only). branchId, when supplied, must belong to the
        caller's own organization.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAssetInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "400":
          description: Invalid input, or a referenced organization-scoped ID does not belong to this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Duplicate serial number
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/{id}/retire:
    post:
      operationId: retireAsset
      tags: [assets]
      summary: Retire an asset (permanently terminal)
      description: >-
        Requires asset_management.manage. Mandatory reason. Allowed only
        from available/maintenance/lost — never directly from assigned (the
        assign-side custody must close first, W97). Retired is permanently
        terminal — no reopen path exists anywhere (§7, Owner Decision 10).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RetireAssetInput"
      responses:
        "200":
          description: Retired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Currently assigned (return it first), or already retired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/{id}/mark-lost:
    post:
      operationId: markAssetLost
      tags: [assets]
      summary: Mark an asset lost
      description: >-
        Requires asset_management.manage. Mandatory reason. Allowed from
        available/assigned/maintenance. If the asset currently has an active
        assignment, that custody period is closed in the same transaction
        (endReason=lost) — the transition's own defined effect, not an
        assign/return action in its own right.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkAssetLostInput"
      responses:
        "200":
          description: Marked lost
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Already lost or retired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/{id}/recover:
    post:
      operationId: recoverAsset
      tags: [assets]
      summary: Recover a lost asset back to available
      description: Requires asset_management.manage. Mandatory reason. Allowed only from lost.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecoverAssetInput"
      responses:
        "200":
          description: Recovered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Not currently lost
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/{id}/condition:
    post:
      operationId: updateAssetCondition
      tags: [assets]
      summary: Record a condition change for an asset
      description: >-
        Requires asset_management.manage. Mandatory reason, always audited.
        Condition is kept structurally separate from status (§7) — never
        editable through the base-field PATCH.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAssetConditionInput"
      responses:
        "200":
          description: Condition updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/{id}/assign:
    post:
      operationId: assignAsset
      tags: [assets]
      summary: Assign an asset to an employee (issue custody)
      description: >-
        Requires asset_management.manage. available -> assigned only (§7).
        Atomic — the status transition and the new asset_assignments
        history row are created in one transaction. employeeId must belong
        to the caller's own organization. issueCondition defaults to the
        asset's own current condition when omitted. Snapshots
        (assetTag/name/category/department/position) are captured
        server-side at this moment and never later rewritten by an asset or
        employee edit (§14).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignAssetInput"
      responses:
        "201":
          description: Assigned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "400":
          description: Invalid input, or employeeId does not belong to this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This asset is not currently available for assignment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/{id}/return:
    post:
      operationId: returnAsset
      tags: [assets]
      summary: Return an asset (close active custody)
      description: >-
        Requires asset_management.manage. assigned -> available only (§7).
        Closes the currently active asset_assignments row
        (custodyEndedAt/endReason=returned/receivedByMembershipId/
        returnCondition/returnNotes) and atomically flips the asset back to
        available, one transaction. The historical assignment row is never
        deleted or reused — a later re-issue always creates a new row.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnAssetInput"
      responses:
        "200":
          description: Returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This asset has no active assignment to return
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/{id}/assignments:
    get:
      operationId: listAssetAssignments
      tags: [assets]
      summary: List an asset's custody history
      description: >-
        Requires asset_management.read.own. Organization-wide reach (via
        asset_management.manage) returns this asset's full custody history;
        otherwise only the caller's own rows for this asset (current +
        historical). No manager tier on this route — Decision 3's
        current-only manager reach is the separate team-assets route (W98).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Assignment history, newest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AssetAssignment"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/asset-assignments/{id}/acknowledge:
    post:
      operationId: acknowledgeAssetAssignment
      tags: [assets]
      summary: Acknowledge receipt of an asset (own currently-open assignment only)
      description: >-
        Requires asset_management.write.own. Decision 1. Identity is
        server-resolved from the caller's own linked employee record —
        never client-supplied. Only the caller's own currently-open
        (custodyEndedAt IS NULL) assignment may be acknowledged.
        acknowledgedAt is server-generated and never cleared by a later
        return. A repeat or concurrent call, or one against an already-
        closed assignment, returns a controlled 409. A non-owner receives
        the same 404 a nonexistent id would.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcknowledgeAssetAssignmentInput"
      responses:
        "200":
          description: Acknowledged
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetAssignment"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Already acknowledged, or no longer open
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/my-assets:
    get:
      operationId: listMyAssetAssignments
      tags: [assets]
      summary: List my own current and historical asset custody
      description: >-
        Requires asset_management.read.own. Identity is server-resolved from
        the caller's own linked employee record — never client-supplied.
        Returns the caller's own current + full historical assignments
        across every asset, newest first. A caller with no linked employee
        record gets an empty list, never an error.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own assignment history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AssetAssignment"

  /organizations/{organizationId}/assets/team-assets:
    get:
      operationId: listTeamAssetAssignments
      tags: [assets]
      summary: List current custody for my current direct reports (Decision 3)
      description: >-
        Requires asset_management.read.own. Current custody only, for
        current direct reports only — the reportingManagerId relationship is
        resolved live on every call, never snapshotted, never falling back
        to organization-wide reach. No closed/historical assignment is ever
        returned through this route. A caller with no linked employee record,
        or with zero current direct reports, gets an empty list.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Current custody for the caller's current direct reports
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AssetAssignment"

  /organizations/{organizationId}/assets/{id}/report-issue:
    post:
      operationId: reportAssetIssue
      tags: [assets]
      summary: Report a loss/damage issue on an asset currently assigned to me
      description: >-
        Requires asset_management.write.own. Decision 2 — report-only.
        Identity is server-resolved, never client-supplied. Only on an asset
        with an active assignment belonging to the caller. Creates exactly
        one asset_incidents row (status=open) — never mutates asset status,
        condition, or custody state. HR/Asset-Officer review (reviewed/
        dismissed) is a separate, later workstream's own routes.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReportAssetIssueInput"
      responses:
        "201":
          description: Reported
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIncident"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: This asset is not currently assigned to the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/asset-incidents:
    get:
      operationId: listAssetIncidents
      tags: [assets]
      summary: List an organization's asset incidents (HR/Asset-Officer)
      description: >-
        Requires asset_management.manage. Organization-wide, optionally
        filtered by status. Intentionally lightweight — no pagination, no
        case-management fields.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/AssetIncidentStatus"
      responses:
        "200":
          description: Incidents, newest-reported first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AssetIncident"

  /organizations/{organizationId}/asset-incidents/{id}/review:
    post:
      operationId: reviewAssetIncident
      tags: [assets]
      summary: Mark an open incident reviewed
      description: >-
        Requires asset_management.manage. open -> reviewed only, terminal —
        no reopen, no reviewed -> dismissed. Sets reviewedByMembershipId/
        reviewedAt server-side; resolutionNotes is optional. Deliberately
        independent of any asset-level action — reviewing an incident never
        itself changes assets.status/.condition or custody; if a resulting
        action (damage/loss/retirement) is warranted, the HR/Asset-Officer
        actor performs it separately through the existing asset routes.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewAssetIncidentInput"
      responses:
        "200":
          description: Reviewed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIncident"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This incident is no longer open
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/asset-incidents/{id}/dismiss:
    post:
      operationId: dismissAssetIncident
      tags: [assets]
      summary: Dismiss an open incident
      description: >-
        Requires asset_management.manage. open -> dismissed only, terminal —
        no reopen, no dismissed -> reviewed. Same actor/timestamp shape as
        review; resolutionNotes is optional.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DismissAssetIncidentInput"
      responses:
        "200":
          description: Dismissed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIncident"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This incident is no longer open
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/{id}/maintenance:
    get:
      operationId: listAssetMaintenance
      tags: [assets]
      summary: List an asset's maintenance history (HR/Asset-Officer)
      description: >-
        Requires asset_management.manage — org-wide only; no own/manager
        visibility is frozen for maintenance history (§20). Simple history,
        newest first.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Maintenance history for this asset
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AssetMaintenance"
        "404":
          description: Asset not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createAssetMaintenance
      tags: [assets]
      summary: Schedule a maintenance record for an asset (HR/Asset-Officer)
      description: >-
        Requires asset_management.manage. Creates a `scheduled` maintenance
        record — never touches the asset's own status (§7). Blocked only on
        a retired asset.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAssetMaintenanceInput"
      responses:
        "201":
          description: Scheduled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetMaintenance"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Asset not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This asset is retired and cannot be scheduled for maintenance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/asset-maintenance/{id}:
    patch:
      operationId: updateAssetMaintenance
      tags: [assets]
      summary: Transition a maintenance record (start / complete / cancel)
      description: >-
        Requires asset_management.manage. The single frozen route for every
        maintenance lifecycle transition (§20) — dispatched by the `action`
        field, never a raw target status. `start` atomically sets the
        asset's own status to `maintenance` (from `available`/`assigned`
        only). `complete`/`cancel` (from `in_progress`) atomically derive
        the asset's own post-maintenance status — `assigned` if an active
        custody row still exists, `available` otherwise — live, inside the
        same transaction; this is never caller-suppliable. `cancel` is also
        accepted from `scheduled`, which never touched the asset's own
        status in the first place.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAssetMaintenanceInput"
      responses:
        "200":
          description: Transitioned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetMaintenance"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This maintenance record is not in a state that permits this action, or the asset is no longer in a maintenance state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/{id}/evidence:
    get:
      operationId: listAssetEvidence
      tags: [assets]
      summary: List evidence attached to an asset
      description: >-
        Same visibility tier as the asset itself (§20) — organization-wide
        (asset_management.manage) or own-scope (the caller currently holds
        this specific asset).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Evidence attached to this asset
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AssetEvidence"
        "403":
          description: Not authorized to view this asset's evidence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Asset not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: addAssetEvidence
      tags: [assets]
      summary: Attach evidence to an asset
      description: >-
        multipart/form-data upload. Reuses the existing employee_documents
        storage layer verbatim (§13) — PDF, JPEG, PNG, DOCX, and XLSX only,
        validated by file signature, 10MB max, identical to every other
        document upload on this platform. Same visibility tier as the asset
        itself — organization-wide or own-scope (currently holds this
        asset). The created employee_documents row has employeeId=null (an
        asset has no natural single-employee owner, §13).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "201":
          description: Evidence attached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetEvidence"
        "400":
          description: Invalid file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Not authorized to add evidence to this asset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Asset not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/{id}/evidence/{evidenceId}/download:
    get:
      operationId: downloadAssetEvidence
      tags: [assets]
      summary: Download a single evidence file
      description: >-
        Authorization-checked before any storage read (same visibility tier
        as the asset itself). No public or signed URL is ever generated —
        the file streams through this authenticated route on every request.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: evidenceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The file
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "403":
          description: Not authorized to access this asset's evidence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Asset or evidence not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assets/dashboard:
    get:
      operationId: getAssetDashboard
      tags: [asset-reporting]
      summary: Asset dashboard — zero-filled deterministic tile breakdown
      description: >-
        Requires asset_management.reports.read. Scope resolved in the
        service layer: asset_management.manage holders see the whole
        organization; everyone else sees only assets currently under an
        active assignment to themselves or a CURRENT (live-resolved, never
        snapshotted) direct report. Plain zero-filled counts only — no
        financial/rate/KPI tile, per the frozen plan's own §18 scope.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Dashboard tile breakdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDashboard"

  /organizations/{organizationId}/assets/reports/{reportKey}:
    get:
      operationId: runAssetReport
      tags: [asset-reporting]
      summary: Run an Asset report
      description: >-
        Computes a registered Asset report (see GET /reports, category
        "asset_management": asset_register, asset_unreturned_by_employee,
        asset_maintenance_history). asset_register and
        asset_maintenance_history are organization-wide only (§17) — a
        non-asset_management.manage caller receives 403.
        asset_unreturned_by_employee follows the same own/manager-current-
        only/organization-wide tier as the dashboard. Pass ?format=csv for
        a CSV download instead of JSON, identical scope/filters/rows to
        JSON.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: reportKey
          in: path
          required: true
          schema:
            type: string
        - name: categoryCode
          in: query
          required: false
          description: asset_register only.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: asset_register's own asset status, or asset_maintenance_history's own maintenance status — whichever the selected report actually uses.
          schema:
            type: string
        - name: branchId
          in: query
          required: false
          description: asset_register only.
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          description: asset_unreturned_by_employee only.
          schema:
            type: integer
        - name: departmentId
          in: query
          required: false
          description: asset_unreturned_by_employee only — filters departmentIdSnapshot (department at issue time), not the employee's current department.
          schema:
            type: integer
        - name: assetId
          in: query
          required: false
          description: asset_maintenance_history only.
          schema:
            type: integer
        - name: dateFrom
          in: query
          required: false
          description: asset_maintenance_history only — inclusive lower bound on the maintenance record's own createdAt.
          schema:
            type: string
            format: date
        - name: dateTo
          in: query
          required: false
          description: asset_maintenance_history only — inclusive upper bound on the maintenance record's own createdAt.
          schema:
            type: string
            format: date
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [json, csv]
      responses:
        "200":
          description: Asset report result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportRunResult"
            text/csv:
              schema:
                type: string
        "403":
          description: This report requires organization-wide asset access
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Unknown Asset report key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-records/reports/{reportKey}:
    get:
      operationId: runPersonnelReport
      tags: [personnel-reporting]
      summary: Run a Personnel Records report (Phase 3H, W119)
      description: >-
        Computes one of the five frozen Personnel Records reports (see GET
        /reports, category "personnel_records": current/historical
        staff-number allocations, files by location, checked-out/overdue
        files, separated employees with unreleased numbers). Gated
        personnel_file.read — never the broad employee.read. Historical
        staff numbers are always resolved from employee_number_allocations,
        never a live employees.employeeNumber join; a reused number's
        separate allocations are never collapsed into one row. Pass
        ?format=csv for a CSV download instead of JSON, identical
        scope/filters/rows to JSON.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: reportKey
          in: path
          required: true
          schema:
            type: string
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [json, csv]
      responses:
        "200":
          description: Personnel report result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportRunResult"
            text/csv:
              schema:
                type: string
        "403":
          description: Forbidden — requires personnel_file.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Unknown Personnel report key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-records/import/template:
    get:
      operationId: getPersonnelImportTemplate
      tags: [personnel-import]
      summary: Download the legacy-import CSV template (Phase 3H, W119)
      description: >-
        A generic, platform-defined CSV template — not modeled on any
        specific organization's own legacy spreadsheet. See
        PersonnelImportRow for which fields are required, optional, or
        conditionally required (separationDate when employmentStatus is
        terminated).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: CSV template
          content:
            text/csv:
              schema:
                type: string
        "403":
          description: Forbidden — requires personnel_file.manage and employee_number.allocate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-records/import/preview:
    post:
      operationId: previewPersonnelImport
      tags: [personnel-import]
      summary: Validate a legacy-import CSV without writing anything (Phase 3H, W119)
      description: >-
        multipart/form-data upload, field "file", CSV only, 2MB max.
        Read-only — every row is validated against this organization's own
        reference data and the batch's own internal consistency, but nothing
        is written. Call /commit with the same file to actually import.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Row-level validation summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelImportValidationSummary"
        "400":
          description: The file itself is malformed/unsupported (not a per-row validation error)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires personnel_file.manage and employee_number.allocate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/personnel-records/import/commit:
    post:
      operationId: commitPersonnelImport
      tags: [personnel-import]
      summary: Commit a validated legacy-import CSV (Phase 3H, W119)
      description: >-
        multipart/form-data upload, field "file" — re-validates the file
        server-side (never trusts a prior /preview result) and, only if
        every row is valid, creates every employee/staff-number
        allocation/personnel-file in one all-or-nothing transaction through
        the same authoritative services live employee creation uses.
        Legacy staff/PIF numbers are preserved exactly as supplied, recorded
        with allocationMethod "migrated". A single invalid row rejects the
        whole file with the same row-level detail /preview returns — nothing
        is partially imported.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "201":
          description: Import committed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelImportCommitResult"
        "400":
          description: The file is malformed, or contains one or more invalid rows (response body includes the same row-level summary shape as /preview)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires personnel_file.manage and employee_number.allocate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A staff number or PIF number collided with an existing allocation during commit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/manager-portal/team:
    get:
      operationId: getManagerPortalTeam
      tags: [manager-portal]
      summary: The caller's own live direct reports (Manager Portal Team Overview, Phase 3G, W109)
      description: >-
        Requires the manager_portal module only — deliberately no
        permission key (frozen plan §13/§37: zero new permissions), mirroring
        GET /me/employee's own zero-permission precedent. Identity is
        server-resolved from the session, never a client-supplied
        employeeId/organizationId. Direct-report-based even for HR/admin
        callers (frozen plan §27, Decision 4/27) — this endpoint never
        returns an organization-wide employee list; Employee Management
        already serves that purpose. The reportingManagerId relationship is
        resolved live on every call, never snapshotted. A caller with no
        linked employee record, or with zero current direct reports, gets a
        valid empty result, never a 403/404. Excludes employmentStatus
        "terminated" direct reports (a former employee's reportingManagerId
        is never automatically cleared elsewhere in this system). Read-only,
        audit-silent. Returns a deliberately narrow DTO — never the full
        Employee shape.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own live direct reports, or the controlled not-linked/empty state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagerPortalTeamOverview"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No membership in this organization, or the manager_portal module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/manager-portal/dashboard:
    get:
      operationId: getManagerPortalDashboard
      tags: [manager-portal]
      summary: Six frozen deterministic tiles for the caller's own team (Manager Portal Dashboard, Phase 3G, W110)
      description: >-
        Requires the manager_portal module only — same zero-permission
        precedent as GET .../manager-portal/team. Direct Reports, Attendance
        "Absent or Late Today", Pending Leave Actions, Pending Performance
        Actions, Pending Learning Actions, Team Assets In Custody — no
        invented score/percentage/KPI tile. Each of the five module-backed
        tiles is `null` when its own underlying module is disabled for the
        organization, or when the caller lacks that module's own existing
        permission — silently omitted, never a 403 for the whole response.
        A real number (including 0) means the module is enabled, the caller
        is authorized, and the query genuinely found nothing. Attendance
        additionally returns `null` if the organization's timezone isn't
        configured yet (the same "nothing safe to show" signal, since
        Attendance's own read-model hard-errors on that precondition rather
        than risk a wrong "late" calculation). Direct Reports and Attendance
        share the caller's own live reportingManagerId scope (W109's
        listLiveDirectReports); Performance uses reviewer-of-record
        (snapshot); Learning uses manager-of-record (snapshot, excluding
        instructor-only work); Assets uses current-custody live direct-report
        scope; Leave is direct-reports-only for a plain manager or org-wide
        if the caller separately holds leave_request.manage. No tile ever
        widens authority beyond what its own module already grants.
        Read-only, audit-silent.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own dashboard, with per-tile null for a disabled/unauthorized/unavailable module
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagerPortalDashboard"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No membership in this organization, or the manager_portal module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/manager-portal/pending-actions:
    get:
      operationId: getManagerPortalPendingActions
      tags: [manager-portal]
      summary: Read-only aggregated pending work across Leave/Performance/Learning (Manager Portal Pending Actions, Phase 3G, W110)
      description: >-
        Requires the manager_portal module only — same zero-permission
        precedent as GET .../manager-portal/team. Aggregates ONLY Leave,
        Performance, and Learning — Attendance and Assets are dashboard/
        read-awareness tiles, not action queues, per the frozen plan.
        Recruitment, Workforce/Scheduling, and any other module are never
        included. Every item is recomputed live from that module's own
        existing service functions on every call — no persistent task table,
        no notifications engine, no mutation route exists anywhere in this
        response. Leave items are direct-reports-only for a plain manager or
        org-wide if the caller separately holds leave_request.manage;
        Performance items are strictly reviewer-of-record (snapshot) at
        status "manager_review"; Learning items are strictly
        manager-of-record (snapshot) at approvalStatus "pending" —
        instructor-only work is never included. Each item carries only a
        deliberately narrow, safe field set (source module, the underlying
        entity's own id, the target employee's id and raw first/last name,
        current authoritative status, a concise safe title, createdAt) —
        never a leave reason, never confidential Performance/Learning
        fields. Sorted by createdAt descending. Read-only, audit-silent.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own aggregated pending actions, or the controlled not-linked/empty state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagerPortalPendingActions"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: No membership in this organization, or the manager_portal module is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/statutory-rules:
    get:
      operationId: listPayrollStatutoryRuleVersions
      tags: [payroll-statutory-rules]
      summary: List Payroll statutory-rule versions (Payroll, Workstream 1)
      description: >-
        Platform-global statutory rule versions (not organization-scoped —
        Ghana law does not vary per organization), most recent effectiveFrom
        first. Gated payroll.statutory.manage or payroll.statutory.approve
        (either may read). No numeric Ghana figures are seeded by this
        workstream; rows returned here are whatever versions have been
        created for QA/verification purposes only.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: ruleType
          in: query
          required: false
          schema:
            type: string
            enum: [paye_bands, pension_rates, pension_earnings_ceiling]
      responses:
        "200":
          description: Statutory rule versions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PayrollStatutoryRuleVersion"
        "403":
          description: Forbidden — requires payroll.statutory.manage or payroll.statutory.approve
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createPayrollStatutoryRuleVersion
      tags: [payroll-statutory-rules]
      summary: Create a draft Payroll statutory-rule version (Payroll, Workstream 1)
      description: >-
        Creates a new version in "draft" status. Exactly one of payeBands
        (ruleType paye_bands), pensionRates (ruleType pension_rates), or
        pensionEarningsCeiling (ruleType pension_earnings_ceiling) must be
        supplied, matching ruleType. Gated payroll.statutory.manage.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePayrollStatutoryRuleVersionBody"
      responses:
        "201":
          description: Created draft version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollStatutoryRuleVersionDetail"
        "400":
          description: Invalid or ruleType-mismatched payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires payroll.statutory.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/statutory-rules/{id}:
    get:
      operationId: getPayrollStatutoryRuleVersion
      tags: [payroll-statutory-rules]
      summary: Get one Payroll statutory-rule version with its structured values (Payroll, Workstream 1)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Statutory rule version detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollStatutoryRuleVersionDetail"
        "403":
          description: Forbidden — requires payroll.statutory.manage or payroll.statutory.approve
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Statutory rule version not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/statutory-rules/{id}/validate:
    post:
      operationId: validatePayrollStatutoryRuleVersion
      tags: [payroll-statutory-rules]
      summary: Move a draft statutory-rule version to "validated" (Payroll, Workstream 1)
      description: Gated payroll.statutory.manage. Requires the version to currently be "draft".
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Validated version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollStatutoryRuleVersion"
        "403":
          description: Forbidden — requires payroll.statutory.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Statutory rule version not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Version is not currently "draft"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/statutory-rules/{id}/approve:
    post:
      operationId: approvePayrollStatutoryRuleVersion
      tags: [payroll-statutory-rules]
      summary: Approve a validated statutory-rule version (Payroll, Workstream 1)
      description: >-
        Gated payroll.statutory.approve. Requires the version to currently be
        "validated". Maker-checker enforced server-side: the approving
        membership must differ from the version's own createdByMembershipId,
        rejected with 409 otherwise. Approving closes the previously-open
        approved version of the same ruleType (if any) by setting its
        effectiveTo to this version's effectiveFrom. A concurrent approval of
        another overlapping version of the same ruleType is rejected with
        409 (database-enforced, not just a race in application code).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approved version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollStatutoryRuleVersion"
        "403":
          description: Forbidden — requires payroll.statutory.approve
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Statutory rule version not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Version is not currently "validated", the creator attempted to self-approve, or a collision with another overlapping approved version of the same ruleType
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/payroll/compensation:
    get:
      operationId: listEmployeeCompensation
      tags: [payroll-compensation]
      summary: List an employee's currently-open compensation components (Payroll, Workstream 2)
      description: >-
        Gated payroll.compensation.read — never employee.read, never
        personnel_file.*, never exposed through the generic employee GET/list
        or ESS/Manager Portal. Returns only currently-open (validTo null)
        rows; see .../history for the full effective-dated record.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Currently-open compensation components
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeCompensationComponent"
        "403":
          description: Forbidden — requires payroll.compensation.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createEmployeeCompensationComponent
      tags: [payroll-compensation]
      summary: Assign a new effective-dated compensation component (Payroll, Workstream 2)
      description: >-
        Gated payroll.compensation.manage. If an open component already
        exists for this (employee, category, componentTypeCode), it is
        closed (validTo set to the new component's validFrom) rather than
        overwritten — full history is always preserved. Rejected with 409 if
        the new validFrom does not strictly follow the existing open
        component's own validFrom, or if a genuine concurrent collision
        occurs.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEmployeeCompensationComponentBody"
      responses:
        "201":
          description: Created compensation component
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeCompensationComponent"
        "400":
          description: Unknown component type for this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires payroll.compensation.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Overlapping effective period with the existing open component
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/payroll/compensation/history:
    get:
      operationId: listEmployeeCompensationHistory
      tags: [payroll-compensation]
      summary: Full effective-dated compensation history for an employee (Payroll, Workstream 2)
      description: >-
        Gated payroll.compensation.read. Optional ?asOf= resolves only the
        components in force on that date (the same historical-resolution
        discipline already proven for staff numbers and statutory-rule
        versions) — omit for the full history, most recent first.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: asOf
          in: query
          required: false
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Compensation component history (or as-of resolution)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeCompensationComponent"
        "403":
          description: Forbidden — requires payroll.compensation.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/payroll/compensation/{id}/end:
    post:
      operationId: endEmployeeCompensationComponent
      tags: [payroll-compensation]
      summary: End an open compensation component with no successor (Payroll, Workstream 2)
      description: Gated payroll.compensation.manage. Sets validTo — never deletes the row.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [endDate]
              properties:
                endDate:
                  type: string
                  format: date-time
      responses:
        "200":
          description: Ended compensation component
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeCompensationComponent"
        "403":
          description: Forbidden — requires payroll.compensation.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Component not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Component already ended
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/payroll/banking:
    get:
      operationId: getEmployeeBankingDetail
      tags: [payroll-banking]
      summary: Get an employee's current banking details (Payroll, Workstream 2)
      description: >-
        Gated payroll.banking.read — narrower than payroll.compensation.read
        by design. Reads of this route are audit-logged (frozen plan
        Decision 9), a deliberate exception to the platform's usual
        read-silence convention.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Current banking detail, or null if none on file
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/EmployeeBankingDetail"
                  - type: "null"
        "403":
          description: Forbidden — requires payroll.banking.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createEmployeeBankingDetail
      tags: [payroll-banking]
      summary: Set an employee's banking details, effective-dated (Payroll, Workstream 2)
      description: Gated payroll.banking.manage. Closes any existing open record rather than overwriting it.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEmployeeBankingDetailBody"
      responses:
        "201":
          description: Created banking detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeBankingDetail"
        "400":
          description: Unknown bank for this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires payroll.banking.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Overlapping effective period with the existing open record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/payroll/banking/history:
    get:
      operationId: listEmployeeBankingHistory
      tags: [payroll-banking]
      summary: Full banking-detail history for an employee (Payroll, Workstream 2)
      description: Gated payroll.banking.read. Read-audited, same as the current-detail route.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Banking-detail history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeBankingDetail"
        "403":
          description: Forbidden — requires payroll.banking.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/payroll/statutory-identifiers:
    get:
      operationId: getEmployeeStatutoryIdentifier
      tags: [payroll-statutory-identifiers]
      summary: Get an employee's current statutory identifiers (SSNIT/TIN) (Payroll, Workstream 2)
      description: Gated payroll.statutory_identifiers.read. Reads are audit-logged (frozen plan Decision 9).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Current statutory identifier record, or null if none on file
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/EmployeeStatutoryIdentifier"
                  - type: "null"
        "403":
          description: Forbidden — requires payroll.statutory_identifiers.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createEmployeeStatutoryIdentifier
      tags: [payroll-statutory-identifiers]
      summary: Set an employee's statutory identifiers, effective-dated (Payroll, Workstream 2)
      description: Gated payroll.statutory_identifiers.manage. Closes any existing open record rather than overwriting it.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEmployeeStatutoryIdentifierBody"
      responses:
        "201":
          description: Created statutory identifier record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeStatutoryIdentifier"
        "403":
          description: Forbidden — requires payroll.statutory_identifiers.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Overlapping effective period with the existing open record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/payroll/statutory-identifiers/history:
    get:
      operationId: listEmployeeStatutoryIdentifierHistory
      tags: [payroll-statutory-identifiers]
      summary: Full statutory-identifier history for an employee (Payroll, Workstream 2)
      description: Gated payroll.statutory_identifiers.read. Read-audited, same as the current-record route.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Statutory-identifier history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeStatutoryIdentifier"
        "403":
          description: Forbidden — requires payroll.statutory_identifiers.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/periods:
    get:
      operationId: listPayrollPeriods
      tags: [payroll-periods]
      summary: List payroll periods for this organization (Payroll, Workstream 3)
      description: Gated payroll.run.prepare.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Payroll periods, ordered by startDate
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PayrollPeriod"
        "403":
          description: Forbidden — requires payroll.run.prepare
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createPayrollPeriod
      tags: [payroll-periods]
      summary: Create a payroll period (Payroll, Workstream 3)
      description: >-
        Gated payroll.run.prepare. periodKey is computed server-side from
        frequency + startDate — never client-supplied. Rejected with 409 if
        this organization/frequency already has a period covering an
        overlapping date range (enforced by an advisory lock plus a unique
        index, not merely a read-then-write check).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePayrollPeriodBody"
      responses:
        "201":
          description: Created payroll period
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollPeriod"
        "400":
          description: Invalid period dates
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires payroll.run.prepare
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Overlapping payroll period already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/periods/{id}:
    get:
      operationId: getPayrollPeriod
      tags: [payroll-periods]
      summary: Get one payroll period (Payroll, Workstream 3)
      description: Gated payroll.run.prepare.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Payroll period
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollPeriod"
        "403":
          description: Forbidden — requires payroll.run.prepare
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll period not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/periods/{periodId}/inputs:
    get:
      operationId: listPayrollInputReferences
      tags: [payroll-periods]
      summary: List one-off payroll inputs for a period (Payroll, Workstream 3)
      description: Gated payroll.run.prepare. Optional ?employeeId= narrows to one employee.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: periodId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: One-off payroll inputs for this period
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PayrollInputReference"
        "403":
          description: Forbidden — requires payroll.run.prepare
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createPayrollInputReference
      tags: [payroll-periods]
      summary: Add a one-off payroll input for one employee in this period (Payroll, Workstream 3)
      description: >-
        Gated payroll.run.prepare. componentTypeCode is validated against the
        same master-data component-type catalogue used for recurring
        compensation. Never automatically populated from Attendance/Leave or
        any other module — every row here is explicit and attributable.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: periodId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePayrollInputReferenceBody"
      responses:
        "201":
          description: Created one-off payroll input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollInputReference"
        "400":
          description: Unknown component type, or payroll period not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires payroll.run.prepare
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/periods/{periodId}/inputs/{id}:
    delete:
      operationId: deletePayrollInputReference
      tags: [payroll-periods]
      summary: Remove a one-off payroll input (Payroll, Workstream 3)
      description: Gated payroll.run.prepare. Deletion is audited with the removed row's before-state.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: periodId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Removed one-off payroll input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollInputReference"
        "403":
          description: Forbidden — requires payroll.run.prepare
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll input reference not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/runs:
    get:
      operationId: listPayrollRuns
      tags: [payroll-runs]
      summary: List payroll runs for this organization (Payroll, Workstream 3)
      description: Gated payroll.run.prepare.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Payroll runs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PayrollRun"
        "403":
          description: Forbidden — requires payroll.run.prepare
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createPayrollRun
      tags: [payroll-runs]
      summary: Create a draft payroll run for a period (Payroll, Workstream 3)
      description: >-
        Gated payroll.run.prepare. One run per (organization, period) —
        rejected with 409 if a run already exists for this period. Never
        reaches "approved"/"locked" in this workstream — those are reserved
        for Workstream 4.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePayrollRunBody"
      responses:
        "201":
          description: Created draft payroll run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRun"
        "403":
          description: Forbidden — requires payroll.run.prepare
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll period not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A payroll run already exists for this period
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/runs/{id}:
    get:
      operationId: getPayrollRun
      tags: [payroll-runs]
      summary: Get one payroll run (Payroll, Workstream 3)
      description: Gated payroll.run.prepare.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Payroll run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRun"
        "403":
          description: Forbidden — requires payroll.run.prepare
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll run not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/runs/{id}/lines:
    get:
      operationId: getPayrollRunLines
      tags: [payroll-runs]
      summary: Get the calculated lines and itemized trace for a payroll run (Payroll, Workstream 3)
      description: >-
        Gated payroll.run.prepare. Each line's components array is the exact
        calculation trace persisted at calculation time — never
        recomputed/re-joined live.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Payroll run lines with their itemized component trace
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PayrollRunLineWithTrace"
        "403":
          description: Forbidden — requires payroll.run.prepare
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/runs/{id}/calculate:
    post:
      operationId: calculatePayrollRun
      tags: [payroll-runs]
      summary: (Re)calculate every eligible employee for this payroll run (Payroll, Workstream 3)
      description: >-
        Gated payroll.run.prepare. Atomically discards and replaces any
        prior calculation for this run. If any included employee fails
        validation, the entire attempt is rolled back and every per-employee
        error is returned together (422) — a run never ends up partially
        calculated.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Calculation summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRunCalculationSummary"
        "400":
          description: No eligible employees for this period
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires payroll.run.prepare
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll run not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Run is approved/locked — calculation inputs can no longer be changed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: One or more employees failed calculation validation — nothing was persisted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRunValidationErrorBody"

  /organizations/{organizationId}/payroll/runs/{id}/approve:
    post:
      operationId: approvePayrollRun
      tags: [payroll-runs]
      summary: Approve a calculated payroll run (Payroll, Workstream 4)
      description: >-
        Gated payroll.run.approve. Requires the run to be exactly
        "calculated" with at least one line. The approving membership must
        differ from the run's own preparedByMembershipId (server-side
        maker-checker) — rejected with 409 on self-approval.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approved payroll run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRun"
        "403":
          description: Forbidden — requires payroll.run.approve
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll run not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Run is not "calculated", has no lines, or the approver is the same membership that prepared it
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/runs/{id}/lock:
    post:
      operationId: lockPayrollRun
      tags: [payroll-runs]
      summary: Finalize/lock an approved payroll run (Payroll, Workstream 4)
      description: >-
        Gated payroll.run.lock. Requires the run to be exactly "approved".
        The locking membership must differ from the run's own
        preparedByMembershipId. Locking is the terminal, immutable
        financial-integrity boundary — from this point the run's result may
        only be adjusted through a payroll_corrections record, never edited
        in place.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Locked payroll run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRun"
        "403":
          description: Forbidden — requires payroll.run.lock
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll run not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Run is not "approved", or the locking actor is the same membership that prepared it
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/runs/{runId}/corrections:
    get:
      operationId: listPayrollCorrectionsForRun
      tags: [payroll-corrections]
      summary: List corrections for a payroll run (Payroll, Workstream 4)
      description: Gated payroll.run.correct.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: runId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Corrections referencing this run
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PayrollCorrection"
        "403":
          description: Forbidden — requires payroll.run.correct
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createPayrollCorrection
      tags: [payroll-corrections]
      summary: Create a draft correction against a locked run's line (Payroll, Workstream 4)
      description: >-
        Gated payroll.run.correct. The original run must be "locked". The
        original run line is never edited — this re-invokes the calculation
        engine for the original period's payDate and persists the result as
        a new, separately-approved draft correction. Rejected with 409 if a
        draft correction is already open for this line.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: runId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePayrollCorrectionBody"
      responses:
        "201":
          description: Created draft correction
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollCorrection"
        "403":
          description: Forbidden — requires payroll.run.correct
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll run or run line not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Run is not locked, or a draft correction is already open for this line
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/corrections/{id}:
    get:
      operationId: getPayrollCorrection
      tags: [payroll-corrections]
      summary: Get one correction with its itemized component trace (Payroll, Workstream 4)
      description: Gated payroll.run.correct.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Correction with its component trace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollCorrectionWithTrace"
        "403":
          description: Forbidden — requires payroll.run.correct
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll correction not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/corrections/{id}/approve:
    post:
      operationId: approvePayrollCorrection
      tags: [payroll-corrections]
      summary: Approve a draft correction (Payroll, Workstream 4)
      description: >-
        Gated payroll.run.correct. The approving membership must differ
        from the correction's own createdByMembershipId — rejected with 409
        on self-approval, mirroring W1's statutory-rule maker-checker.
        Terminal: an approved correction is immutable.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approved correction
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollCorrection"
        "403":
          description: Forbidden — requires payroll.run.correct
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll correction not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Correction is not "draft", or the approver is the same membership that created it
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/runs/{runId}/lines/{lineId}/payslip:
    get:
      operationId: getPayslip
      tags: [payroll-payslips]
      summary: Get one employee's payslip for a locked payroll run (Payroll, Workstream 5)
      description: >-
        Gated payroll.payslip.read. Only available once the run is "locked"
        — a draft/calculated/approved run's payslip does not exist yet.
        Reads exclusively from the immutable payroll_run_lines/
        payroll_run_line_components snapshot, never recalculates. Approved
        corrections for this line appear in their own `corrections` array,
        never merged into `original`; `effective` is the most recently
        approved correction's own figures, or `original` if none exists.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: runId
          in: path
          required: true
          schema:
            type: integer
        - name: lineId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Payslip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Payslip"
        "403":
          description: Forbidden — requires payroll.payslip.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll run or run line not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Run is not locked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/me/payslips:
    get:
      operationId: listOwnPayslips
      tags: [payroll-payslips]
      summary: List the caller's own payslips across every locked payroll run (Payroll, Workstream 5)
      description: >-
        Gated payroll.payslip.read.own. Identity is resolved server-side
        from the caller's own employee_user_links — never a client-supplied
        employeeId. Returns an empty array if the caller has no linked
        employee record.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Own payslip summaries, newest pay date first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OwnPayslipSummary"
        "403":
          description: Forbidden — requires payroll.payslip.read.own
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/me/payslips/{runId}:
    get:
      operationId: getOwnPayslip
      tags: [payroll-payslips]
      summary: Get the caller's own payslip for one locked payroll run (Payroll, Workstream 5)
      description: >-
        Gated payroll.payslip.read.own. The run line is resolved from the
        caller's own identity — never a client-supplied employeeId or line
        ID — so no coworker's payslip is ever reachable through this route.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: runId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Own payslip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Payslip"
        "403":
          description: Forbidden — requires payroll.payslip.read.own
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No linked employee record, or no payslip for this run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Run is not locked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/runs/{runId}/reports/{reportKey}:
    get:
      operationId: getPayrollReport
      tags: [payroll-reports]
      summary: Get a payroll register/PAYE/pension report for one locked run (Payroll, Workstream 5)
      description: >-
        Gated payroll.report.read. reportKey is one of payroll_register,
        payroll_paye_schedule, payroll_pension_schedule. Only available once
        the run is "locked". Reads exclusively from stored run lines, never
        recalculates. ?format=csv returns a CSV download instead of JSON,
        using the exact same authorization and data. The pension schedule
        includes SSNIT numbers only for an actor who additionally holds
        payroll.statutory_identifiers.read — every such inclusion is
        audit-logged as a sensitive read.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: runId
          in: path
          required: true
          schema:
            type: integer
        - name: reportKey
          in: path
          required: true
          schema:
            type: string
            enum: [payroll_register, payroll_paye_schedule, payroll_pension_schedule]
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [csv]
      responses:
        "200":
          description: Report result (JSON), or a CSV file when ?format=csv
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollReportResult"
            text/csv:
              schema:
                type: string
        "403":
          description: Forbidden — requires payroll.report.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll run not found, or unknown reportKey
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Run is not locked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/runs/{runId}/payment-batches:
    get:
      operationId: listPaymentBatchesForRun
      tags: [payroll-payment-batches]
      summary: List payment batches for a payroll run (Frozen Workstream 8 — at most one ever exists)
      description: Gated payroll.payment.manage.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: runId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Payment batches for this run (0 or 1)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PayrollPaymentBatch"
        "403":
          description: Forbidden — requires payroll.payment.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createPaymentBatch
      tags: [payroll-payment-batches]
      summary: Prepare a payment batch for a locked payroll run (Frozen Workstream 8)
      description: >-
        Gated payroll.payment.manage. PAYMENT BATCH PREPARATION only — no
        payment is executed. Only permitted once the run is "locked"; exactly
        one batch is permitted per run for its entire lifetime. Each included
        line's amount is the effective net pay (original, or the latest
        approved correction's own net pay) snapshotted at this instant, with
        banking details snapshotted from the employee's currently-open
        banking record. Employees with zero or negative effective net pay are
        excluded from the batch and reported in `excludedLines`, never
        silently dropped. Fails with 422 if any employee eligible for payment
        has no banking details on file.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: runId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "201":
          description: Payment batch prepared
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollPaymentBatchCreateResult"
        "403":
          description: Forbidden — requires payroll.payment.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payroll run not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Run is not locked, or a payment batch already exists for this run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: No eligible employees, or one or more eligible employees have no banking details on file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/payment-batches/{id}:
    get:
      operationId: getPaymentBatch
      tags: [payroll-payment-batches]
      summary: Get one payment batch and its lines (Frozen Workstream 8)
      description: Gated payroll.payment.manage. Account numbers are masked; the full value is only ever present in the CSV export.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Payment batch detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollPaymentBatchDetail"
        "403":
          description: Forbidden — requires payroll.payment.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payment batch not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: deletePaymentBatch
      tags: [payroll-payment-batches]
      summary: Delete a draft payment batch (Frozen Workstream 8)
      description: Gated payroll.payment.manage. Only permitted while status is "draft" — nothing has been exported yet. Frees the run for a fresh batch attempt.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "403":
          description: Forbidden — requires payroll.payment.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payment batch not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Batch is already exported and can no longer be deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/payment-batches/{id}/export:
    post:
      operationId: exportPaymentBatch
      tags: [payroll-payment-batches]
      summary: Export a payment batch as a CSV payment instruction file (Frozen Workstream 8)
      description: >-
        Gated payroll.payment.manage. First call on a "draft" batch
        transitions it to "exported" (terminal, immutable) and is audited as
        a sensitive banking-data disclosure; a repeat call on an
        already-exported batch performs no further mutation and simply
        re-serves the identical, already-persisted content. Contains full,
        unmasked account numbers — the only surface in this workstream that
        does. This is a generic, neutral export format only; it is not a
        submission to any specific bank's proprietary file format, and no
        payment is executed by calling this route.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: CSV payment instruction file
          content:
            text/csv:
              schema:
                type: string
        "403":
          description: Forbidden — requires payroll.payment.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Payment batch not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/departments/{departmentId}/head:
    get:
      operationId: getCurrentDepartmentHead
      tags: [department-heads]
      summary: Get the current Department Head, if any — gated department.head.read
      description: >-
        General organizational-authority primitive
        (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §5) — not
        Inventory-specific. Returns 200 with a null body when the
        department is vacant; vacancy is never auto-resolved.
        Requires department.head.read (department.head.manage also grants it). Reading an assignment is not Department Head authority;
        assigning or revoking one still requires department.head.manage.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: departmentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Current Head, or null if vacant
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/DepartmentHead"
                  - type: "null"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Department not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: assignDepartmentHead
      tags: [department-heads]
      summary: Assign (or replace) the Department Head — gated department.head.manage
      description: >-
        Closes whatever assignment was previously open (if any) and opens a
        new one, in the same transaction. Works uniformly for first
        assignment or replacement.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: departmentId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignDepartmentHeadBody"
      responses:
        "201":
          description: New current Department Head assignment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DepartmentHead"
        "403":
          description: Forbidden — requires department.head.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Department not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: revokeDepartmentHead
      tags: [department-heads]
      summary: Revoke the current Department Head, leaving the department vacant — gated department.head.manage
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: departmentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The now-closed Department Head assignment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DepartmentHead"
        "403":
          description: Forbidden — requires department.head.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Department not found, or no current Head to revoke
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/departments/{departmentId}/head/history:
    get:
      operationId: listDepartmentHeadHistory
      tags: [department-heads]
      summary: Full Department Head assignment history, oldest first — gated department.head.read
      description: >-
        Requires department.head.read (department.head.manage also grants it).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: departmentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Assignment history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DepartmentHead"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Department not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/departments/{departmentId}/head/as-of:
    get:
      operationId: resolveDepartmentHeadAsOf
      tags: [department-heads]
      summary: "Who was the Department Head on a given date? (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §5.3)"
      description: >-
        Requires department.head.read (department.head.manage also grants it).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: departmentId
          in: path
          required: true
          schema:
            type: integer
        - name: date
          in: query
          required: true
          description: ISO 8601 date-time to resolve authority as of
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Head as of that date, or null if vacant at that time
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/DepartmentHead"
                  - type: "null"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Department not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/items:
    get:
      operationId: listOfficeInventoryItems
      tags: [office-inventory-catalog]
      summary: List Office Inventory catalog items
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryItem"
        "403":
          description: Forbidden — requires office_inventory.item.manage, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createOfficeInventoryItem
      tags: [office-inventory-catalog]
      summary: Create an Office Inventory catalog item — gated office_inventory.item.manage
      description: >-
        itemCode is always server-generated (never client-supplied),
        permanent, and never released or reused.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryItemBody"
      responses:
        "201":
          description: Created item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryItem"
        "403":
          description: Forbidden — requires office_inventory.item.manage, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/items/{id}:
    get:
      operationId: getOfficeInventoryItem
      tags: [office-inventory-catalog]
      summary: Get an Office Inventory catalog item
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryItem"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateOfficeInventoryItem
      tags: [office-inventory-catalog]
      summary: Update an Office Inventory catalog item — gated office_inventory.item.manage
      description: Never accepts itemCode (permanent) or classification (fixed at creation).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOfficeInventoryItemBody"
      responses:
        "200":
          description: Updated item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryItem"
        "403":
          description: Forbidden — requires office_inventory.item.manage, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/stores:
    get:
      operationId: listOfficeInventoryStores
      tags: [office-inventory-catalog]
      summary: List Office Inventory stores
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Stores
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryStore"
        "403":
          description: Forbidden — requires office_inventory.store.manage, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createOfficeInventoryStore
      tags: [office-inventory-catalog]
      summary: Create an Office Inventory store — gated office_inventory.store.manage
      description: Flat — no hierarchy in V1. `code` is stable and permanent.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryStoreBody"
      responses:
        "201":
          description: Created store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryStore"
        "403":
          description: Forbidden — requires office_inventory.store.manage, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A store with this code already exists for this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/stores/{id}:
    get:
      operationId: getOfficeInventoryStore
      tags: [office-inventory-catalog]
      summary: Get an Office Inventory store
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryStore"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Store not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateOfficeInventoryStore
      tags: [office-inventory-catalog]
      summary: Update an Office Inventory store — gated office_inventory.store.manage
      description: Never accepts `code` — the store's stable, permanent identity.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOfficeInventoryStoreBody"
      responses:
        "200":
          description: Updated store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryStore"
        "403":
          description: Forbidden — requires office_inventory.store.manage, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Store not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/receiving:
    get:
      operationId: listOfficeInventoryReceipts
      tags: [office-inventory-ledger]
      summary: List receiving history (Office Inventory Workstream 2)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Receipt summaries, newest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryReceiptSummary"
        "403":
          description: Forbidden — requires office_inventory.receive, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createOfficeInventoryReceipt
      tags: [office-inventory-ledger]
      summary: Receive stock — gated office_inventory.receive
      description: >-
        Not Procurement — stock has already entered organizational
        possession. A multi-item submission commits atomically: if any line
        is invalid, zero rows are written. Supports an optional
        idempotencyKey; resubmitting the same key returns the original
        receipt (`replay: true`) rather than creating a second one.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryReceiptBody"
      responses:
        "201":
          description: Created (or, on idempotency replay, the original) receipt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryReceipt"
        "400":
          description: No lines, invalid quantity, or store/items not found for this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.receive, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/receiving/{referenceNumber}:
    get:
      operationId: getOfficeInventoryReceipt
      tags: [office-inventory-ledger]
      summary: Get one receipt by its reference number
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: referenceNumber
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Receipt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryReceipt"
        "403":
          description: Forbidden — requires office_inventory.receive, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Receipt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/stock/balance:
    get:
      operationId: getOfficeInventoryStockBalance
      tags: [office-inventory-ledger]
      summary: Current stock balance for one item — one store, or the organization-wide total with a per-store breakdown
      description: >-
        Live-derived from the stock ledger (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md
        §8) — never a persisted balance column.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: itemId
          in: query
          required: true
          schema:
            type: integer
        - name: storeId
          in: query
          required: false
          description: If supplied, `total` is that one store's balance and `byStore` is empty. If omitted, `total` is the organization-wide total and `byStore` lists every store the item has moved through.
          schema:
            type: integer
      responses:
        "200":
          description: Balance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryItemBalance"
        "403":
          description: Forbidden — requires office_inventory.receive, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item (or store, if supplied) not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/stock/movements:
    get:
      operationId: listOfficeInventoryStockMovements
      tags: [office-inventory-ledger]
      summary: Stock movement history, optionally narrowed to one item and/or one store
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: itemId
          in: query
          required: false
          schema:
            type: integer
        - name: storeId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Movements, newest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryStockMovement"
        "403":
          description: Forbidden — requires office_inventory.receive, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/requestable-items:
    get:
      operationId: listOfficeInventoryRequestableItems
      tags: [office-inventory-requests]
      summary: Active catalogue items a requester may ask for — display identity only, gated office_inventory.request
      description: >-
        WWM Employee Access Remediation (2026-09-07). The New Request
        dialog needs item names, but the full catalogue (GET
        .../office-inventory/items) is a stock-management surface gated
        office_inventory.item.manage that an ordinary requester must not
        hold. This returns only active items and only their display
        identity (id, code, name, unit, classification) — no cost, reorder
        level or status management.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Requestable items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryRequestableItem"
        "403":
          description: Forbidden — requires office_inventory.request, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/requests:
    get:
      operationId: listOfficeInventoryMyRequests
      tags: [office-inventory-requests]
      summary: List the caller's own submitted Office Inventory requests (Workstream 3)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Requests submitted by the caller
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryRequest"
        "403":
          description: Forbidden — requires office_inventory.request, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createOfficeInventoryRequest
      tags: [office-inventory-requests]
      summary: Submit an employee or department stock request — gated office_inventory.request
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryRequestBody"
      responses:
        "201":
          description: Created request with its lines
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryRequestWithLines"
        "400":
          description: No lines, invalid quantity, item not found, no current department, or (department request) caller does not belong to the target department
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.request, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/requests/{id}:
    get:
      operationId: getOfficeInventoryRequest
      tags: [office-inventory-requests]
      summary: Get one request with its lines — own request, or a department the caller has approval authority over
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Request with lines
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryRequestWithLines"
        "403":
          description: Forbidden — not the requester and not an approval authority for this department
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Request not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/requests/{id}/cancel:
    post:
      operationId: cancelOfficeInventoryRequest
      tags: [office-inventory-requests]
      summary: Cancel a request — only the original requester, only while every line is still pending
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Cancelled request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryRequest"
        "403":
          description: Forbidden — requires office_inventory.request, or not the original requester
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Request not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A decision has already been recorded on at least one line — no longer cancellable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/requests/{id}/approval-context:
    get:
      operationId: getOfficeInventoryRequestApprovalContext
      tags: [office-inventory-requests]
      summary: Full approval-screen context — request, lines, current store availability, and the repeat-request warning per line
      description: Gated to the department's current Head or a currently-valid delegate — docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §34.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approval context
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryRequestApprovalContext"
        "403":
          description: Forbidden — requires office_inventory.approve and current Department Head/delegate authority for this request's department
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Request not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/departments/{departmentId}/requests:
    get:
      operationId: listOfficeInventoryDepartmentRequests
      tags: [office-inventory-requests]
      summary: List every request for a department — the approval queue, gated to its current Head or a currently-valid delegate
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: departmentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Requests for this department
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryRequest"
        "403":
          description: Forbidden — requires office_inventory.approve and current Department Head/delegate authority for this department
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/request-lines/{lineId}/approve:
    post:
      operationId: approveOfficeInventoryRequestLine
      tags: [office-inventory-requests]
      summary: Approve a request line, in full or in part — Department Head self-approval is permanently valid (§14)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: lineId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveOfficeInventoryRequestLineBody"
      responses:
        "200":
          description: Updated line and its parent request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryRequestWithLines"
        "400":
          description: Approved quantity must be > 0 and <= quantity requested
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.approve and current Department Head/delegate authority for this request's department
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Request line not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This line has already been decided
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/request-lines/{lineId}/reject:
    post:
      operationId: rejectOfficeInventoryRequestLine
      tags: [office-inventory-requests]
      summary: Reject a request line — a reason is required
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: lineId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectOfficeInventoryRequestLineBody"
      responses:
        "200":
          description: Updated line and its parent request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryRequestWithLines"
        "403":
          description: Forbidden — requires office_inventory.approve and current Department Head/delegate authority for this request's department
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Request line not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This line has already been decided
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/departments/{departmentId}/delegations:
    get:
      operationId: listOfficeInventoryDelegations
      tags: [office-inventory-delegations]
      summary: Current and historical approval delegations for a department — the current Head only
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: departmentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Delegations, oldest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryApprovalDelegation"
        "403":
          description: Forbidden — requires office_inventory.delegate.manage and being this department's current Head
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createOfficeInventoryDelegation
      tags: [office-inventory-delegations]
      summary: Delegate approval authority for this department — only the department's current Head may call this
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: departmentId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryDelegationBody"
      responses:
        "201":
          description: Created delegation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryApprovalDelegation"
        "403":
          description: Forbidden — requires office_inventory.delegate.manage and being this department's current Head
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Department not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/delegations/{id}/revoke:
    post:
      operationId: revokeOfficeInventoryDelegation
      tags: [office-inventory-delegations]
      summary: Revoke an open delegation — only the department's current Head may call this
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The now-closed delegation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryApprovalDelegation"
        "403":
          description: Forbidden — requires office_inventory.delegate.manage and being this department's current Head
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Delegation not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/requests/awaiting-fulfilment:
    get:
      operationId: listOfficeInventoryRequestsAwaitingFulfilment
      tags: [office-inventory-issuing]
      summary: The Store Officer's own queue — every request org-wide with at least one approved-but-not-fully-issued line (Workstream 4)
      description: Gated `office_inventory.issue` only — independent of departmental approval authority.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Requests awaiting fulfilment, with their lines
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryAwaitingFulfilmentEntry"
        "403":
          description: Forbidden — requires office_inventory.issue, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/request-lines/{lineId}/issue:
    post:
      operationId: issueOfficeInventoryRequestLine
      tags: [office-inventory-issuing]
      summary: Issue stock against an approved request line, in full or in part — gated office_inventory.issue
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: lineId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IssueRequestLineBody"
      responses:
        "201":
          description: Issue result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IssueResult"
        "400":
          description: Invalid quantity, expectedReturnDate on a consumable item, or store not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.issue, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Request line not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Line not approved, over-fulfilment, or insufficient stock
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/direct-issue:
    post:
      operationId: createOfficeInventoryDirectIssue
      tags: [office-inventory-issuing]
      summary: Direct issue — bypasses a prior request, gated office_inventory.issue.direct (distinct from office_inventory.issue)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DirectIssueBody"
      responses:
        "201":
          description: Direct issue result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DirectIssueResult"
        "400":
          description: Missing reason, invalid quantity, expectedReturnDate on a consumable item, or store/item/holder not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.issue.direct, or module not enabled, or direct issue disabled for this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Insufficient stock
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/movements/{movementId}/confirm:
    post:
      operationId: confirmOfficeInventoryReceipt
      tags: [office-inventory-issuing]
      summary: Confirm physical receipt of an issued item — non-gating (§18); the custody change already occurred when it was issued
      description: >-
        Confirmable by the recipient employee, or — for department custody
        — the original requester or any employee whose own current
        department matches the holder.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: movementId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Confirmed movement
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryStockMovement"
        "403":
          description: Forbidden — requires office_inventory.receipt.confirm.own, and eligibility (recipient, original requester, or department member)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Movement not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Already confirmed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/custody/employees/{employeeId}:
    get:
      operationId: getOfficeInventoryEmployeeCustody
      tags: [office-inventory-issuing]
      summary: What an employee currently holds, live-derived from the ledger — gated office_inventory.custody.read
      description: >-
        Item accountability only — never salary, banking, statutory
        identifiers, personnel-file documents, performance, or leave data.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Current custody
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryCustodyEntry"
        "403":
          description: Forbidden — requires office_inventory.custody.read, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/custody/departments/{departmentId}:
    get:
      operationId: getOfficeInventoryDepartmentCustody
      tags: [office-inventory-issuing]
      summary: What a department currently holds, live-derived from the ledger — gated office_inventory.custody.read
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: departmentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Current custody
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryCustodyEntry"
        "403":
          description: Forbidden — requires office_inventory.custody.read, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Department not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/returns:
    post:
      operationId: createOfficeInventoryReturn
      tags: [office-inventory-transfers]
      summary: Return from an employee or department's outstanding custody into a store — gated office_inventory.return
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryReturnBody"
      responses:
        "201":
          description: Return result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryReturnResult"
        "400":
          description: Invalid quantity, or the item is a consumable (not returnable)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.return, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item, store, or holder not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Insufficient outstanding custody (over-return)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/handovers:
    post:
      operationId: createOfficeInventoryHandover
      tags: [office-inventory-transfers]
      summary: Hand over outstanding custody from one holder to another (employee/department, any direction) — gated office_inventory.handover
      description: >-
        A department-to-department handover additionally requires the
        acting membership to resolve as the RECEIVING department's current
        Head or a currently-valid delegate (Owner Decision 17).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryHandoverBody"
      responses:
        "201":
          description: Handover result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryHandoverResult"
        "400":
          description: Invalid quantity, same source/destination holder, the item is a consumable, or expectedReturnDate on a consumable item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.handover, or module not enabled, or (department-to-department only) not the receiving department's current Head/delegate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item, source holder, or destination holder not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Insufficient outstanding custody on the source holder
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/my/custody:
    get:
      operationId: getOfficeInventoryMyCustody
      tags: [office-inventory-ess]
      summary: The caller's own current personal custody, live-derived — Workstream 8 ESS (§33)
      description: >-
        Resolved via resolveOwnEmployeeId exclusively — no client-supplied
        employee ID. No office_inventory.* permission is required beyond
        the module being enabled; owning your own data is not an
        operational grant. A caller with no linked employee record gets an
        empty list, never an error.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Current personal custody
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryCustodyEntry"
        "403":
          description: Forbidden — module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/my/history:
    get:
      operationId: getOfficeInventoryMyHistory
      tags: [office-inventory-ess]
      summary: Every ledger movement ever recorded against the caller's own personal custody, newest first — Workstream 8 ESS (§33)
      description: >-
        Resolved via resolveOwnEmployeeId exclusively. A caller with no
        linked employee record gets an empty list, never an error. Each row
        carries the item's own display identity so ESS never needs the
        catalogue grant.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Own movement history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryMyHistoryEntry"
        "403":
          description: Forbidden — module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/my/returns:
    post:
      operationId: createOfficeInventoryMyReturn
      tags: [office-inventory-ess]
      summary: Return from the caller's OWN outstanding custody into a store — Workstream 8 ESS (§33), gated office_inventory.return
      description: >-
        `holderType`/`holderId` must be the caller's own personal custody
        or their own current department's custody — verified server-side
        against resolveOwnEmployeeId, never a client-asserted "on behalf
        of" holder. Otherwise identical to POST .../office-inventory/returns.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryReturnBody"
      responses:
        "201":
          description: Return result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryReturnResult"
        "400":
          description: Invalid quantity, or the item is a consumable (not returnable)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.return, or the holder is not the caller's own custody, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item, store, or holder not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Insufficient outstanding custody (over-return)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/my/handovers:
    post:
      operationId: createOfficeInventoryMyHandover
      tags: [office-inventory-ess]
      summary: Hand over the caller's OWN outstanding custody to another holder — Workstream 8 ESS (§33), gated office_inventory.handover
      description: >-
        `fromHolderType`/`fromHolderId` must be the caller's own personal
        custody or their own current department's custody — verified
        server-side against resolveOwnEmployeeId. The destination holder is
        unrestricted (it is simply the recipient), subject to the same
        Owner Decision 17 department-to-department authority gate as
        POST .../office-inventory/handovers.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryHandoverBody"
      responses:
        "201":
          description: Handover result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryHandoverResult"
        "400":
          description: Invalid quantity, same source/destination holder, the item is a consumable, or expectedReturnDate on a consumable item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.handover, or the source holder is not the caller's own custody, or (department-to-department only) not the receiving department's current Head/delegate, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item, source holder, or destination holder not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Insufficient outstanding custody on the source holder
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/transfers:
    post:
      operationId: createOfficeInventoryTransfer
      tags: [office-inventory-transfers]
      summary: Transfer stock between two stores, atomically — gated office_inventory.transfer
      description: Single atomic paired transferred_out/transferred_in movement (§23) — organization-wide total is unchanged by construction.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryTransferBody"
      responses:
        "201":
          description: Transfer result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryTransferResult"
        "400":
          description: Invalid quantity, or source and destination store are the same
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.transfer, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item, source store, or destination store not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Insufficient stock in the source store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/incidents:
    post:
      operationId: reportOfficeInventoryIncident
      tags: [office-inventory-incidents]
      summary: Report a damage/missing incident on your own current custody — gated office_inventory.report_issue.own
      description: Zero ledger effect (§25) — a pure, self-service qualitative record.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReportOfficeInventoryIncidentBody"
      responses:
        "201":
          description: The created incident
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryIncident"
        "400":
          description: The specified holder has no outstanding custody of this item to report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.report_issue.own, module not enabled, or not your own custody/department
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item or holder not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    get:
      operationId: listOfficeInventoryIncidents
      tags: [office-inventory-incidents]
      summary: List incidents org-wide, optionally filtered by status — gated office_inventory.incident.review
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [open, reviewed, dismissed]
      responses:
        "200":
          description: Incidents
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryIncident"
        "403":
          description: Forbidden — requires office_inventory.incident.review, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/incidents/{id}:
    get:
      operationId: getOfficeInventoryIncident
      tags: [office-inventory-incidents]
      summary: Get one incident — gated office_inventory.incident.review
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The incident
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryIncident"
        "403":
          description: Forbidden — requires office_inventory.incident.review, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Incident not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/incidents/{id}/review:
    post:
      operationId: reviewOfficeInventoryIncident
      tags: [office-inventory-incidents]
      summary: Review or dismiss an open incident — gated office_inventory.incident.review
      description: Pure status transition, zero ledger effect regardless of outcome.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewOfficeInventoryIncidentBody"
      responses:
        "200":
          description: The updated incident
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryIncident"
        "403":
          description: Forbidden — requires office_inventory.incident.review, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Incident not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This incident has already been reviewed or dismissed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/incidents/{id}/mark-missing:
    post:
      operationId: markOfficeInventoryIncidentMissing
      tags: [office-inventory-incidents]
      summary: Mark a quantity of this incident's item as officially missing — gated office_inventory.incident.review
      description: Appends a holder-decreasing `missing` ledger row referencing the incident (§25) — the incident's report alone never does this.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkOfficeInventoryIncidentMissingBody"
      responses:
        "201":
          description: Mark-missing result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryMovementActionResult"
        "400":
          description: Invalid quantity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.incident.review, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Incident not found, or its holder no longer exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Incident is not open, or insufficient outstanding custody
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/incidents/{id}/recover:
    post:
      operationId: recoverOfficeInventoryIncident
      tags: [office-inventory-incidents]
      summary: Recover a quantity of this incident's already-missing item into a store — gated office_inventory.recover
      description: Validated against the incident's own outstanding-missing tally, never the holder's balance — a found item re-enters a STORE.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecoverOfficeInventoryIncidentBody"
      responses:
        "201":
          description: Recovery result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryMovementActionResult"
        "400":
          description: Invalid quantity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.recover, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Incident or destination store not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Requested quantity exceeds this incident's own outstanding-missing tally
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/incidents/{id}/write-off:
    post:
      operationId: writeOffOfficeInventoryIncident
      tags: [office-inventory-incidents]
      summary: Write off this incident's own already-missing quantity — gated office_inventory.writeoff
      description: An unscoped ledger row (no store, no holder) — the quantity already left holder custody when it was marked missing.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WriteOffOfficeInventoryIncidentBody"
      responses:
        "201":
          description: Write-off result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryMovementActionResult"
        "400":
          description: Invalid quantity, or missing reason
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.writeoff, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Incident not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Requested quantity exceeds this incident's own outstanding-missing tally
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/write-offs:
    post:
      operationId: createOfficeInventoryWriteOff
      tags: [office-inventory-incidents]
      summary: Directly write off quantity from a store or holder — gated office_inventory.writeoff
      description: Independent of any incident, though one may optionally be referenced purely for traceability (§11-§13).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryWriteOffBody"
      responses:
        "201":
          description: Write-off result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryMovementActionResult"
        "400":
          description: Invalid quantity, or missing reason
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.writeoff, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item, store, holder, or referenced incident not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Insufficient stock/custody at the source
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/adjustments:
    post:
      operationId: createOfficeInventoryAdjustment
      tags: [office-inventory-incidents]
      summary: Adjust a store's balance (in or out) — gated office_inventory.adjust
      description: STORE ONLY (§17) — an employee/department custody discrepancy is never resolved by adjustment.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryAdjustmentBody"
      responses:
        "201":
          description: Adjustment result (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryMovementActionResult"
        "400":
          description: Invalid quantity, or missing reason
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.adjust, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item or store not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Negative adjustment would make the store balance negative
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/stocktakes:
    post:
      operationId: createOfficeInventoryStocktake
      tags: [office-inventory-stocktakes]
      summary: Create a draft stocktake for one store — gated office_inventory.stocktake
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryStocktakeBody"
      responses:
        "201":
          description: The created draft stocktake
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryStocktake"
        "403":
          description: Forbidden — requires office_inventory.stocktake, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Store not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    get:
      operationId: listOfficeInventoryStocktakes
      tags: [office-inventory-stocktakes]
      summary: List stocktakes org-wide, optionally filtered by store/status — gated office_inventory.stocktake
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: storeId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [draft, counting, finalized]
      responses:
        "200":
          description: Stocktakes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficeInventoryStocktake"
        "403":
          description: Forbidden — requires office_inventory.stocktake, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/stocktakes/{id}:
    get:
      operationId: getOfficeInventoryStocktake
      tags: [office-inventory-stocktakes]
      summary: Get one stocktake with its lines, live-reconciled — gated office_inventory.stocktake
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The stocktake and its lines
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryStocktakeDetail"
        "403":
          description: Forbidden — requires office_inventory.stocktake, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Stocktake not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/stocktakes/{id}/start:
    post:
      operationId: startOfficeInventoryStocktake
      tags: [office-inventory-stocktakes]
      summary: Transition draft -> counting, capturing the expectedQuantitySnapshot for every in-scope item — gated office_inventory.stocktake
      description: Acquires the same advisory-lock domain an ordinary movement against this store's items would (§28) — serializes against an in-flight receipt/issue rather than racing silently.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The now-counting stocktake
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryStocktake"
        "403":
          description: Forbidden — requires office_inventory.stocktake, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Stocktake not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Stocktake is not draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/stocktakes/{id}/lines/{lineId}/count:
    post:
      operationId: recordOfficeInventoryStocktakeCount
      tags: [office-inventory-stocktakes]
      summary: Record (or recount) a line's physical counted quantity — gated office_inventory.stocktake
      description: Never mutates the stock ledger. A recount that brings a previously non-zero variance to exactly zero is automatically resolved as "recount", no stock movement.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: lineId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordOfficeInventoryStocktakeCountBody"
      responses:
        "201":
          description: The updated line, live-reconciled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryStocktakeLineView"
        "400":
          description: Invalid (negative) counted quantity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.stocktake, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Stocktake or line not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Stocktake is not counting
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/stocktakes/{id}/lines/{lineId}/resolve:
    post:
      operationId: resolveOfficeInventoryStocktakeLine
      tags: [office-inventory-stocktakes]
      summary: Resolve a counted line's non-zero variance via Workstream 6's own adjustment or missing domain service — gated office_inventory.stocktake
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: lineId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResolveOfficeInventoryStocktakeLineBody"
      responses:
        "201":
          description: The resolved line, live-reconciled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryStocktakeLineView"
        "400":
          description: Not yet counted, missing reason, or a "missing" resolution attempted on a surplus
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.stocktake, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Stocktake or line not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Stocktake is not counting, or the line has no variance to resolve
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/stocktakes/{id}/finalize:
    post:
      operationId: finalizeOfficeInventoryStocktake
      tags: [office-inventory-stocktakes]
      summary: Finalize a stocktake — gated office_inventory.stocktake
      description: Requires every line counted and every non-zero variance resolved; rejects with the exact blocking lines otherwise. Never mutates stock itself — every stock effect already happened through an explicit resolution action.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The finalized, immutable stocktake
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryStocktake"
        "403":
          description: Forbidden — requires office_inventory.stocktake, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Stocktake not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Stocktake is not counting, or one or more lines are not counted / have unresolved variance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/dashboard:
    get:
      operationId: getOfficeInventoryDashboard
      tags: [office-inventory-reporting]
      summary: Office Inventory dashboard — live-derived tiles, gated office_inventory.reports.read
      description: No persisted aggregate — every tile is a live, batched query (§43).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Dashboard tile breakdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryDashboard"
        "403":
          description: Forbidden — requires office_inventory.reports.read, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/reports/{reportKey}:
    get:
      operationId: runOfficeInventoryReport
      tags: [office-inventory-reporting]
      summary: Run an Office Inventory report — gated office_inventory.reports.read
      description: >-
        Computes one of the 13 frozen Office Inventory reports (§44; see GET
        /reports, category "office_inventory"). Organization-wide only — no
        own/manager visibility tiers; a Department Head's own scoped W8
        accountability context is never widened by this permission. Pass
        ?format=csv for a hardened (formula-injection-safe) CSV download
        instead of JSON, identical filters/rows to JSON.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: reportKey
          in: path
          required: true
          schema:
            type: string
        - name: itemId
          in: query
          required: false
          schema:
            type: integer
        - name: storeId
          in: query
          required: false
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: departmentId
          in: query
          required: false
          schema:
            type: integer
        - name: movementType
          in: query
          required: false
          description: office_inventory_movement_ledger only.
          schema:
            type: string
            enum: [received, issued, returned, transferred_out, transferred_in, adjustment_in, adjustment_out, written_off, missing, recovered, asset_handoff]
        - name: status
          in: query
          required: false
          description: office_inventory_missing_damaged (incident status) or office_inventory_stocktake_variances (stocktake status) only.
          schema:
            type: string
        - name: dateFrom
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: dateTo
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [json, csv]
      responses:
        "200":
          description: Office Inventory report result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportRunResult"
            text/csv:
              schema:
                type: string
        "403":
          description: Forbidden — requires office_inventory.reports.read, or module not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Unknown Office Inventory report key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/office-inventory/asset-handoff:
    post:
      operationId: createOfficeInventoryAssetHandoff
      tags: [office-inventory-asset-handoff]
      summary: Convert one unit of a store-held returnable item into an Asset — gated office_inventory.asset_handoff
      description: >-
        Workstream 10, §4 Owner Decision 1. Explicit, one-directional,
        one-unit-per-call conversion — calls Assets' own existing creation
        API directly. Requires BOTH office_inventory and asset_management to
        be enabled for the organization. The source unit must currently be
        store-held (not issued to an employee/department) and the item must
        be classification=returnable.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOfficeInventoryAssetHandoffBody"
      responses:
        "201":
          description: The created Asset and the office_inventory asset_handoff movement (or, on idempotency replay, the original)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfficeInventoryAssetHandoffResult"
        "400":
          description: The item is not returnable (not eligible for handoff)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires office_inventory.asset_handoff, or office_inventory/asset_management not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Item or store not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Insufficient store stock (fewer than 1 unit available)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/documents/categories:
    get:
      operationId: listDocumentCategories
      tags: [organization-documents]
      summary: List document categories with their configured behavior
      description: >-
        The organization's active "document_category" Master Data items,
        each merged with its resolved behavior (verification/expiry/
        sensitivity/retention). A category with no settings row resolves to
        safe defaults rather than an error.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Categories available to this organization
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentCategory"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/documents/categories/{categoryCode}:
    put:
      operationId: updateDocumentCategorySettings
      tags: [organization-documents]
      summary: Configure a document category's behavior
      description: >-
        Creates or updates this organization's own settings row for a
        category. Never modifies a platform-wide default row, so one
        organization's configuration can never change another's.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: categoryCode
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentCategorySettingsInput"
      responses:
        "200":
          description: Updated category settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentCategorySettings"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/documents:
    get:
      operationId: listOrganizationDocuments
      tags: [organization-documents]
      summary: List organization documents
      description: >-
        Metadata only — no file bytes are read. Each row carries its current
        version inline so the list needs no per-row follow-up query.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: categoryCode
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
            enum: [active, archived]
        - name: search
          in: query
          description: Case-insensitive substring match on the document title.
          schema:
            type: string
        - name: expiringBefore
          in: query
          description: Only documents whose current version expires on or before this date.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Documents in this organization
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OrganizationDocument"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createOrganizationDocument
      tags: [organization-documents]
      summary: Create an organization document with its first version
      description: >-
        multipart/form-data upload. PDF, JPEG, PNG, DOCX, and XLSX only,
        validated by file signature rather than the declared Content-Type,
        10MB max. Creates the document, its version 1, and its retention
        record in one transaction; a failure deletes the just-written
        storage object rather than leaving a dangling reference.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, categoryCode, title]
              properties:
                file:
                  type: string
                  format: binary
                categoryCode:
                  type: string
                  description: Must be an active category for this organization.
                title:
                  type: string
                description:
                  type: string
                effectiveDate:
                  type: string
                  format: date
                expiryDate:
                  type: string
                  format: date
      responses:
        "201":
          description: Document created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationDocument"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/documents/{documentId}:
    get:
      operationId: getOrganizationDocument
      tags: [organization-documents]
      summary: Get one organization document
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The document
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationDocument"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateOrganizationDocument
      tags: [organization-documents]
      summary: Update a document's title or description
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                description:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Updated document
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationDocument"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/documents/{documentId}/versions:
    get:
      operationId: listOrganizationDocumentVersions
      tags: [organization-documents]
      summary: List a document's full version history
      description: >-
        Newest first. Superseded versions are retained permanently and stay
        downloadable — a new upload never destroys a prior version.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Version history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OrganizationDocumentVersion"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: addOrganizationDocumentVersion
      tags: [organization-documents]
      summary: Upload a new version, superseding the current one
      description: >-
        The previous current version is marked superseded in the same
        transaction; its row and stored object are left intact. Returns 409
        if a concurrent upload won the race (retry against the new head) or
        if the document is archived.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                changeNote:
                  type: string
                effectiveDate:
                  type: string
                  format: date
                expiryDate:
                  type: string
                  format: date
      responses:
        "201":
          description: New current version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationDocumentVersion"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Document archived, or a concurrent version won the race
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/documents/{documentId}/versions/{versionId}/download:
    get:
      operationId: downloadOrganizationDocumentVersion
      tags: [organization-documents]
      summary: Download one version's file
      description: >-
        Authorization is re-proved before any storage read: the document must
        belong to this organization and the version to that document. If the
        category is marked confidential the caller additionally needs
        organization_document.sensitive.read, and the read is audited.
        Knowing an id or a storage key never grants access.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The stored file
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "403":
          description: Not authorized, or confidential without sensitive-read permission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-templates/merge-fields:
    get:
      operationId: listDocumentMergeFields
      tags: [document-templates]
      summary: List the allow-listed merge fields
      description: >-
        The complete set of fields a template may reference. A token naming
        anything outside this list is rejected when the template version is
        saved. Deliberately contains no payroll, banking, or statutory
        identifier field.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Available merge fields
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentMergeField"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-templates:
    get:
      operationId: listDocumentTemplates
      tags: [document-templates]
      summary: List document templates
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: categoryCode
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
            enum: [active, inactive]
      responses:
        "200":
          description: Templates in this organization
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentTemplate"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createDocumentTemplate
      tags: [document-templates]
      summary: Create a template with its first draft version
      description: >-
        Template content is plain text containing allow-listed
        {{merge.field}} tokens. Content referencing an unknown field is
        rejected here, at authoring time, rather than silently at generation
        time on an official letter.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, categoryCode, content]
              properties:
                name:
                  type: string
                categoryCode:
                  type: string
                description:
                  type: string
                  nullable: true
                content:
                  type: string
      responses:
        "201":
          description: Template created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentTemplate"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-templates/{templateId}:
    get:
      operationId: getDocumentTemplate
      tags: [document-templates]
      summary: Get one template
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentTemplate"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateDocumentTemplateStatus
      tags: [document-templates]
      summary: Activate or deactivate a template
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [active, inactive]
      responses:
        "200":
          description: Updated template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentTemplate"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-templates/{templateId}/versions:
    get:
      operationId: listDocumentTemplateVersions
      tags: [document-templates]
      summary: List a template's version history
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Version history, newest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentTemplateVersion"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createDocumentTemplateVersion
      tags: [document-templates]
      summary: Start a new draft version
      description: >-
        How an already-active template is edited. Prior versions keep their
        content permanently, so any artifact generated from one stays
        explainable by reading it.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content:
                  type: string
      responses:
        "201":
          description: New draft version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentTemplateVersion"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-templates/{templateId}/versions/{versionId}:
    patch:
      operationId: updateDocumentTemplateVersion
      tags: [document-templates]
      summary: Edit a draft version's content
      description: >-
        Draft versions only. Editing an active or superseded version returns
        409 — that immutability is what makes an already-generated artifact's
        provenance meaningful.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content:
                  type: string
      responses:
        "200":
          description: Updated draft version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentTemplateVersion"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Not a draft version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-templates/{templateId}/versions/{versionId}/activate:
    post:
      operationId: activateDocumentTemplateVersion
      tags: [document-templates]
      summary: Make a version the active one
      description: >-
        Supersedes whichever version was active. A partial unique index
        guarantees at most one active version per template, so concurrent
        activations cannot both succeed.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The now-active version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentTemplateVersion"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Another version was activated concurrently
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-templates/{templateId}/versions/{versionId}/preview:
    post:
      operationId: previewDocumentTemplateVersion
      tags: [document-templates]
      summary: Preview a version rendered with synthetic sample data
      description: >-
        Renders against obviously-fake sample values, never a real employee
        record — holding document_template.manage conveys no entitlement to
        any individual's data, so preview must not become a way to read one
        by guessing merge fields. Writes nothing: no storage object, no
        generated-document row, no audit event.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The rendered preview text
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentTemplatePreview"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-templates/{templateId}/generate:
    post:
      operationId: generateDocumentFromTemplate
      tags: [document-templates]
      summary: Generate an official document from a template's active version
      description: >-
        Renders the template's ACTIVE version against the named employee and
        stores the resulting PDF, recording exactly which template version
        produced it. The merge context is resolved server-side from the
        employee record inside the caller's own organization — never from
        request-body values — so another person's details cannot be injected
        and cross-organization generation is impossible. Later template edits
        never alter this artifact.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [employeeId]
              properties:
                employeeId:
                  type: integer
                effectiveDate:
                  type: string
                  format: date
                reference:
                  type: string
                  nullable: true
      responses:
        "201":
          description: The generated document's metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratedDocument"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Template or employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The template has no active version to generate from
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/generated-documents:
    get:
      operationId: listGeneratedDocuments
      tags: [document-templates]
      summary: List generated document artifacts
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: sourceType
          in: query
          schema:
            type: string
        - name: sourceId
          in: query
          schema:
            type: integer
        - name: templateId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Generated artifacts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeneratedDocument"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/generated-documents/{generatedId}/download:
    get:
      operationId: downloadGeneratedDocument
      tags: [document-templates]
      summary: Download a generated artifact
      description: >-
        Returns the exact bytes produced at generation time. Editing the
        template afterwards never changes this file.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: generatedId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The generated PDF
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-requirements:
    get:
      operationId: listDocumentRequirements
      tags: [document-requirements]
      summary: List document requirements
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: ownerType
          in: query
          schema:
            type: string
            enum: [employee, candidate, organization]
        - name: ownerId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Requirements
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentRequirement"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createDocumentRequirement
      tags: [document-requirements]
      summary: Declare that an owner must provide a document
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ownerType, ownerId, categoryCode]
              properties:
                ownerType:
                  type: string
                  enum: [employee, candidate, organization]
                ownerId:
                  type: integer
                categoryCode:
                  type: string
                required:
                  type: boolean
                notes:
                  type: string
                  nullable: true
      responses:
        "201":
          description: Requirement created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentRequirement"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This owner already has a requirement for that category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-requirements/expiry:
    get:
      operationId: getDocumentExpiryState
      tags: [document-requirements]
      summary: Documents that are expired, expiring soon, or still missing
      description: >-
        The read-only contract a future reminder workstream consumes. Sends
        no notification. The windows are disjoint: `expired` is strictly
        before asOf, `expiringSoon` is asOf through asOf+horizonDays
        inclusive, so a document expiring exactly on asOf appears once.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: asOf
          in: query
          description: YYYY-MM-DD. Defaults to today. Explicit so boundaries are deterministic.
          schema:
            type: string
            format: date
        - name: horizonDays
          in: query
          schema:
            type: integer
            default: 30
      responses:
        "200":
          description: Expiry state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentExpiryState"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-requirements/{requirementId}/provide:
    post:
      operationId: provideDocumentRequirement
      tags: [document-requirements]
      summary: Record that a document was supplied against a requirement
      description: >-
        Links the row that satisfied the requirement and sets status to
        "provided" — never "verified". Supplying a document is not the same
        act as accepting it.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requirementId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [fulfilledDocumentTable, fulfilledDocumentId]
              properties:
                fulfilledDocumentTable:
                  type: string
                fulfilledDocumentId:
                  type: integer
                expiryDate:
                  type: string
                  format: date
                  nullable: true
      responses:
        "200":
          description: Updated requirement
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentRequirement"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-requirements/{requirementId}/verify:
    post:
      operationId: verifyDocumentRequirement
      tags: [document-requirements]
      summary: Accept or reject a provided document
      description: >-
        The verification decision, gated by document.verify — deliberately a
        separate permission from the ability to upload. Records who decided
        and when.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requirementId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [approved]
              properties:
                approved:
                  type: boolean
                rejectionReason:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Updated requirement
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentRequirement"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-retention:
    get:
      operationId: listDocumentRetentionRecords
      tags: [document-retention]
      summary: List retention records
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: archiveStatus
          in: query
          schema:
            type: string
            enum: [active, archived]
        - name: disposalStatus
          in: query
          schema:
            type: string
            enum: [none, eligible, disposed]
        - name: legalHold
          in: query
          schema:
            type: boolean
      responses:
        "200":
          description: Retention records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentRetentionRecord"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-retention/disposal-eligible:
    get:
      operationId: listDisposalEligibleDocuments
      tags: [document-retention]
      summary: Documents whose retention has elapsed and which nothing blocks
      description: >-
        Reports candidates for a records officer to review. Never disposes
        anything. Records under legal hold are excluded, so a held document
        can never be presented as ready to dispose.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: asOf
          in: query
          description: YYYY-MM-DD. Defaults to today.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Disposal candidates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentRetentionRecord"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-retention/{documentTable}/{documentId}:
    get:
      operationId: getDocumentRetentionRecord
      tags: [document-retention]
      summary: Get one document instance's retention record
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: documentTable
          in: path
          required: true
          schema:
            type: string
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The retention record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentRetentionRecord"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-retention/{documentTable}/{documentId}/archive:
    post:
      operationId: archiveDocumentRetentionRecord
      tags: [document-retention]
      summary: Archive a document instance
      description: >-
        Removes the document from active circulation. Explicitly NOT
        deletion — the row and the stored file both survive untouched.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: documentTable
          in: path
          required: true
          schema:
            type: string
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated retention record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentRetentionRecord"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-retention/{documentTable}/{documentId}/legal-hold:
    put:
      operationId: setDocumentLegalHold
      tags: [document-retention]
      summary: Apply or lift a legal hold
      description: >-
        While a hold is in place the document can never become
        disposal-eligible and can never be disposed, regardless of elapsed
        retention. Both applying and lifting are audited.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: documentTable
          in: path
          required: true
          schema:
            type: string
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [legalHold]
              properties:
                legalHold:
                  type: boolean
                reason:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Updated retention record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentRetentionRecord"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-retention/{documentTable}/{documentId}/disposal-eligibility:
    post:
      operationId: markDocumentDisposalEligible
      tags: [document-retention]
      summary: Record the determination that a document may be disposed
      description: >-
        A reviewable decision, separate from and prior to actually disposing.
        Refused while a legal hold is in place, while retention has not
        elapsed, or when no retention deadline was ever recorded.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: documentTable
          in: path
          required: true
          schema:
            type: string
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated retention record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentRetentionRecord"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Legal hold active, retention not elapsed, or already disposed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /organizations/{organizationId}/document-retention/{documentTable}/{documentId}/dispose:
    post:
      operationId: disposeDocument
      tags: [document-retention]
      summary: Authorize and execute disposal of one document instance
      description: >-
        The irreversible act, requiring an explicit recorded reason. The
        record is marked disposed first and the storage object deleted
        afterwards, so a failed delete leaves a recoverable orphan rather
        than a destroyed file the record still claims is retained. Disposal
        is per instance — sibling versions of the same document are never
        touched. There is deliberately no bulk disposal operation.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: documentTable
          in: path
          required: true
          schema:
            type: string
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: The disposed record, with whether the storage object was removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisposedDocumentResult"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Legal hold active, retention not elapsed, or already disposed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  # --- WS-7 closure: Payroll Opening Balances -----------------------------
  /organizations/{organizationId}/payroll/opening-balances:
    get:
      operationId: listPayrollOpeningBalances
      tags: [payroll]
      summary: List imported payroll opening balances (WS-7 closure)
      description: >-
        Brought-forward payroll/statutory totals established at migration
        cutover. These are historical record only — they are never paid, never
        become compensation, and never produce a run, payslip or payment.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: taxYear
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Opening balances
          content:
            application/json:
              schema:
                type: object
                required: [balances]
                properties:
                  balances:
                    type: array
                    items:
                      $ref: "#/components/schemas/PayrollOpeningBalance"
        "403":
          description: Forbidden — requires payroll.opening_balance.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/opening-balances/{employeeId}/{taxYear}:
    get:
      operationId: getPayrollOpeningBalance
      tags: [payroll]
      summary: Get one employee's opening balance for a tax year (WS-7 closure)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: taxYear
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Opening balance
          content:
            application/json:
              schema:
                type: object
                required: [balance, locked]
                properties:
                  balance:
                    $ref: "#/components/schemas/PayrollOpeningBalance"
                  locked:
                    type: boolean
        "403":
          description: Forbidden — requires payroll.opening_balance.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    put:
      operationId: updatePayrollOpeningBalance
      tags: [payroll]
      summary: Amend an opening balance before it is locked (WS-7 closure)
      description: >-
        Refused once any payroll run for that employee and tax year has been
        locked — finalized payroll history is never silently rewritten.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: taxYear
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PayrollOpeningBalanceAmounts"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollOpeningBalance"
        "400":
          description: Invalid amounts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires payroll.opening_balance.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Locked by finalized payroll
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/payroll/year-to-date/{employeeId}/{taxYear}:
    get:
      operationId: getEmployeePayrollYearToDate
      tags: [payroll]
      summary: Year-to-date totals, brought-forward plus in-system (WS-7 closure)
      description: >-
        Sums brought-forward history and payroll finalized (locked) inside this
        system, keeping the two halves separately visible. Draft and merely
        calculated runs are excluded.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: taxYear
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Year-to-date totals
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollYearToDate"
        "403":
          description: Forbidden — requires payroll.report.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  # --- WS-9: Recruitment Completion ---------------------------------------
  /organizations/{organizationId}/recruitment/sources:
    get:
      operationId: listRecruitmentSources
      tags: [recruitment-completion]
      summary: List the organization's configured recruitment sources (WS-9)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Configured sources
          content:
            application/json:
              schema:
                type: object
                required: [sources]
                properties:
                  sources:
                    type: array
                    items:
                      $ref: "#/components/schemas/RecruitmentSource"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment/approval-stages:
    get:
      operationId: listRecruitmentApprovalStages
      tags: [recruitment-completion]
      summary: List configured approval stages for a purpose (WS-9)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: purpose
          in: query
          required: false
          schema:
            type: string
            enum: [requisition, hire]
      responses:
        "200":
          description: Configured stages
          content:
            application/json:
              schema:
                type: object
                required: [purpose, stages]
                properties:
                  purpose:
                    type: string
                  stages:
                    type: array
                    items:
                      $ref: "#/components/schemas/RecruitmentApprovalStage"
        "403":
          description: Forbidden — requires recruitment_settings.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createRecruitmentApprovalStage
      tags: [recruitment-completion]
      summary: Configure an approval stage (WS-9)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecruitmentApprovalStageInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecruitmentApprovalStage"
        "400":
          description: Invalid configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires recruitment_settings.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment/approval-stages/{stageId}:
    delete:
      operationId: deleteRecruitmentApprovalStage
      tags: [recruitment-completion]
      summary: Remove a configured approval stage (WS-9)
      description: Removing configuration never alters decisions already recorded — those carry their own snapshots.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: stageId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removed
        "403":
          description: Forbidden — requires recruitment_settings.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/applications/{applicationId}/hire-authorization:
    get:
      operationId: getHireAuthorization
      tags: [recruitment-completion]
      summary: Hire authorization state, decisions and configured stages (WS-9)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Authorization detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HireAuthorizationDetail"
        "403":
          description: Forbidden — requires application.read and the recruitment module
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: >-
            Application not found, or not visible to this caller — visible only
            with organization-wide application authority, as the requisition's
            recruiter or hiring manager, or as a configured approver of a raised
            hire authorization's stages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: requestHireAuthorization
      tags: [recruitment-completion]
      summary: Raise hire authorization for an application (WS-9)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: applicationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "201":
          description: Raised
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HireAuthorization"
        "400":
          description: No hire-approval stages are configured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires application.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This application already has a hire authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/hire-authorizations/{id}/decide:
    post:
      operationId: decideHireAuthorizationStage
      tags: [recruitment-completion]
      summary: Decide the current approval stage (WS-9)
      description: >-
        Authority is re-resolved live from the configured stage's resolver.
        Holding a role never confers authority; a Department Head stage resolves
        through the department_heads relationship.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                decision:
                  type: string
                  enum: [approved, rejected]
                reason:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Decision recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HireAuthorization"
        "403":
          description: Not authorized to decide this stage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Already decided or already settled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/recruitment/manual-candidates:
    post:
      operationId: captureManualCandidate
      tags: [recruitment-completion]
      summary: Capture a candidate without a published vacancy (WS-9)
      description: >-
        The authorized manual capture path. Public publication is not required;
        an authenticated, permission-checked actor, an organization-owned
        vacancy and a configured recruitment source are. The public careers
        path's own publication requirement is unchanged.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ManualCandidateInput"
      responses:
        "201":
          description: Captured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManualCaptureResult"
        "400":
          description: Invalid source, vacancy or candidate details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires candidate.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This candidate already has an application for this vacancy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offer-versions/{versionId}/state:
    get:
      operationId: getOfferVersionState
      tags: [recruitment-completion]
      summary: Derived lifecycle state of an offer version (WS-9)
      description: Expiry is computed from the authoritative expiry date, never from a scheduled job.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Offer version state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferVersionState"
        "403":
          description: Forbidden — requires offer.read and the recruitment module
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found, or not visible to this caller (org-wide offer authority or the requisition's recruiter/hiring manager only)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offer-versions/{versionId}/record-response:
    post:
      operationId: recordOfferResponse
      tags: [recruitment-completion]
      summary: Record a candidate's acceptance or decline (WS-9)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                responseType:
                  type: string
                  enum: [accepted, declined]
                reason:
                  type: string
                  nullable: true
                evidence:
                  nullable: true
      responses:
        "201":
          description: Recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferResponse"
        "403":
          description: Forbidden — requires offer.issue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Not respondable — already responded, superseded, withdrawn or expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offer-versions/{versionId}/withdraw-response:
    post:
      operationId: withdrawOfferWithReason
      tags: [recruitment-completion]
      summary: Withdraw an offer, recording a reason (WS-9)
      description: Refused once the candidate has already been converted to an employee.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
      responses:
        "201":
          description: Withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferResponse"
        "403":
          description: Forbidden — requires offer.withdraw
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Not withdrawable in the current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offer-versions/{versionId}/response-link:
    post:
      operationId: issueOfferResponseLink
      tags: [recruitment-completion]
      summary: Issue a single-purpose candidate response link (WS-9)
      description: >-
        The plaintext token is returned once and never stored — only its hash is
        persisted. The link is bound to this offer version, expires, is
        single-use and is revoked when the offer is responded to or withdrawn.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "201":
          description: Link issued
          content:
            application/json:
              schema:
                type: object
                required: [token, expiresAt]
                properties:
                  token:
                    type: string
                  expiresAt:
                    type: string
                    format: date-time
        "403":
          description: Forbidden — requires offer.issue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This offer is not in a respondable state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /offer-response/{token}:
    get:
      operationId: getPublicOfferResponse
      tags: [recruitment-completion]
      summary: Candidate view of an offer behind a response link (WS-9, public)
      description: >-
        Unauthenticated and rate-limited. Returns a minimal candidate-safe
        projection only — never internal approval data, scorecards, notes, other
        candidates or unpublished vacancies.
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Candidate-safe offer view
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicOfferView"
        "404":
          description: The link is not valid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: submitPublicOfferResponse
      tags: [recruitment-completion]
      summary: Accept or decline an offer via a response link (WS-9, public)
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                responseType:
                  type: string
                  enum: [accepted, declined]
                reason:
                  type: string
                  nullable: true
      responses:
        "201":
          description: Response recorded
          content:
            application/json:
              schema:
                type: object
                required: [responseType, respondedAt]
                properties:
                  responseType:
                    type: string
                  respondedAt:
                    type: string
                    format: date-time
        "404":
          description: The link is not valid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This offer is no longer respondable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offer-versions/{versionId}/particulars:
    get:
      operationId: getEmploymentParticulars
      tags: [recruitment-completion]
      summary: Employment particulars for an offer version (WS-9)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Stored particulars plus suggestions while still a draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmploymentParticularsDetail"
        "403":
          description: Forbidden — requires offer.read and the recruitment module
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found, or not visible to this caller (org-wide offer authority or the requisition's recruiter/hiring manager only)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    put:
      operationId: saveEmploymentParticulars
      tags: [recruitment-completion]
      summary: Save draft employment particulars (WS-9)
      description: Refused once the particulars have been issued.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmploymentParticularsInput"
      responses:
        "200":
          description: Saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmploymentParticulars"
        "400":
          description: Already issued and therefore immutable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires offer.issue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offer-versions/{versionId}/particulars/issue:
    post:
      operationId: issueEmploymentParticulars
      tags: [recruitment-completion]
      summary: Freeze particulars and optionally generate the document (WS-9)
      description: >-
        Freezing makes the record immutable, so a statement already furnished to
        a worker reproduces identically. When a template is supplied the
        artifact is generated through the shared document engine and bound to
        this offer version.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                templateId:
                  type: integer
                  nullable: true
      responses:
        "200":
          description: Issued
          content:
            application/json:
              schema:
                type: object
                required: [particulars]
                properties:
                  particulars:
                    $ref: "#/components/schemas/EmploymentParticulars"
                  generated:
                    nullable: true
        "403":
          description: Forbidden — requires offer.issue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  # --- WS-8: Custom Fields & Form Builder --------------------------------
  /organizations/{organizationId}/custom-fields/meta:
    get:
      operationId: getCustomFieldMeta
      tags: [custom-fields]
      summary: Field-type registry, approved scopes and visibility operators (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Registry metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldMeta"
        "403":
          description: Forbidden — requires custom_fields.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-fields:
    get:
      operationId: listCustomFields
      tags: [custom-fields]
      summary: List custom field definitions for a scope (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: scope
          in: query
          required: true
          schema:
            type: string
        - name: includeArchived
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Definitions with their current versions
          content:
            application/json:
              schema:
                type: object
                required: [fields]
                properties:
                  fields:
                    type: array
                    items:
                      $ref: "#/components/schemas/CustomFieldWithVersion"
        "400":
          description: Unknown scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires custom_fields.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createCustomField
      tags: [custom-fields]
      summary: Define a custom field (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomFieldInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldWithVersion"
        "400":
          description: Invalid scope, type or configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires custom_fields.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A field with this key already exists for this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-fields/{definitionId}:
    get:
      operationId: getCustomField
      tags: [custom-fields]
      summary: Get a custom field with its version history (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: definitionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Definition, current version, history and used choices
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldDetail"
        "403":
          description: Forbidden — requires custom_fields.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-fields/{definitionId}/versions:
    post:
      operationId: createCustomFieldVersion
      tags: [custom-fields]
      summary: Create the next version of a custom field (WS-8)
      description: >-
        Safe changes create a new version. A breaking change (field type,
        removing a choice already in use, changing a master data domain) is
        refused with 409 once values exist, so captured data is never
        reinterpreted.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: definitionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomFieldVersionInput"
      responses:
        "201":
          description: New version created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldWithVersion"
        "400":
          description: Invalid configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires custom_fields.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Breaking change refused
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-fields/{definitionId}/archive:
    post:
      operationId: archiveCustomField
      tags: [custom-fields]
      summary: Archive or restore a custom field (WS-8)
      description: Archiving removes a field from new data entry and never deletes stored values.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: definitionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                archived:
                  type: boolean
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldDefinition"
        "403":
          description: Forbidden — requires custom_fields.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-fields/{scope}/export.csv:
    get:
      operationId: exportCustomFieldValues
      tags: [custom-fields]
      summary: Export custom field values as CSV (WS-8)
      description: >-
        Sensitive columns are omitted entirely unless the caller holds
        custom_fields.sensitive.read. Output uses the platform's
        formula-injection-safe CSV writer.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: scope
          in: path
          required: true
          schema:
            type: string
        - name: entityIds
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: CSV export
          content:
            text/csv:
              schema:
                type: string
        "400":
          description: entityIds is required, or unknown scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-field-values/{scope}/{entityId}:
    get:
      operationId: getCustomFieldValues
      tags: [custom-fields]
      summary: Read custom field values for one record (WS-8)
      description: >-
        Returns every field in display order with server-evaluated visibility
        and missing-required status. Sensitive values are masked unless
        reveal=true and the caller holds custom_fields.sensitive.read.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: scope
          in: path
          required: true
          schema:
            type: string
        - name: entityId
          in: path
          required: true
          schema:
            type: integer
        - name: reveal
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Resolved values
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldValueList"
        "403":
          description: Forbidden — requires the target domain's read permission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Record not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    put:
      operationId: setCustomFieldValues
      tags: [custom-fields]
      summary: Set custom field values for one record (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: scope
          in: path
          required: true
          schema:
            type: string
        - name: entityId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [values]
              properties:
                values:
                  type: object
                  additionalProperties: true
      responses:
        "200":
          description: Values written
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldValueList"
        "400":
          description: Validation failed, or a field is not part of this record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires the target domain's write permission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Record not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: A field is not applicable to this record under its visibility conditions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-field-values/{scope}/{entityId}/{definitionId}/reveal:
    get:
      operationId: revealCustomFieldValue
      tags: [custom-fields]
      summary: Reveal one sensitive custom field value (WS-8)
      description: Audited as a sensitive read.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: scope
          in: path
          required: true
          schema:
            type: string
        - name: entityId
          in: path
          required: true
          schema:
            type: integer
        - name: definitionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The full value
          content:
            application/json:
              schema:
                type: object
                required: [definitionId, fieldKey, label]
                properties:
                  definitionId:
                    type: integer
                  fieldKey:
                    type: string
                  label:
                    type: string
                  value:
                    nullable: true
        "403":
          description: Forbidden — requires custom_fields.sensitive.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-forms:
    get:
      operationId: listCustomForms
      tags: [custom-forms]
      summary: List custom forms (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Forms
          content:
            application/json:
              schema:
                type: object
                required: [forms]
                properties:
                  forms:
                    type: array
                    items:
                      $ref: "#/components/schemas/CustomForm"
        "403":
          description: Forbidden — requires custom_forms.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createCustomForm
      tags: [custom-forms]
      summary: Create a custom form with its first draft version (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomFormInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFormWithVersion"
        "400":
          description: Invalid layout, scope or field reference
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires custom_forms.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A form with this key already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-forms/{formId}:
    get:
      operationId: getCustomForm
      tags: [custom-forms]
      summary: Get a form with its versions (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: formId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Form detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFormDetail"
        "403":
          description: Forbidden — requires custom_forms.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-forms/{formId}/versions:
    post:
      operationId: createCustomFormVersion
      tags: [custom-forms]
      summary: Create the next draft version of a form (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: formId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomFormVersionInput"
      responses:
        "201":
          description: Draft created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFormVersion"
        "400":
          description: Invalid layout
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires custom_forms.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-forms/{formId}/versions/{versionId}/publish:
    post:
      operationId: publishCustomFormVersion
      tags: [custom-forms]
      summary: Publish a draft version, retiring the previous one (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: formId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Published
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFormVersion"
        "400":
          description: Not publishable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires custom_forms.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-forms/{formId}/archive:
    post:
      operationId: archiveCustomForm
      tags: [custom-forms]
      summary: Archive or restore a form (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: formId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                archived:
                  type: boolean
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomForm"
        "403":
          description: Forbidden — requires custom_forms.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-forms/{formId}/render:
    get:
      operationId: renderCustomForm
      tags: [custom-forms]
      summary: Get the published version and its field definitions for rendering (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: formId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Renderable form
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFormRender"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No published version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-forms/{formId}/submissions:
    post:
      operationId: submitCustomForm
      tags: [custom-forms]
      summary: Submit a form (WS-8)
      description: >-
        The submitted formVersionId must match the currently published version.
        For employee_ess forms the target employee is resolved server-side from
        the caller's own identity and any supplied entityId is ignored.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: formId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [formVersionId, answers]
              properties:
                formVersionId:
                  type: integer
                entityId:
                  type: integer
                  nullable: true
                answers:
                  type: object
                  additionalProperties: true
      responses:
        "201":
          description: Submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFormSubmission"
        "400":
          description: Validation failed, form not published, or version superseded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: A field is not applicable under its visibility conditions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-form-submissions:
    get:
      operationId: listCustomFormSubmissions
      tags: [custom-forms]
      summary: List form submissions (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: formId
          in: query
          required: false
          schema:
            type: integer
        - name: scope
          in: query
          required: false
          schema:
            type: string
        - name: entityId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Submissions
          content:
            application/json:
              schema:
                type: object
                required: [submissions]
                properties:
                  submissions:
                    type: array
                    items:
                      $ref: "#/components/schemas/CustomFormSubmission"
        "403":
          description: Forbidden — requires custom_forms.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/custom-form-submissions/{submissionId}:
    get:
      operationId: getCustomFormSubmission
      tags: [custom-forms]
      summary: Get one submission with the form version it was captured under (WS-8)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: submissionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Submission and its captured version
          content:
            application/json:
              schema:
                type: object
                required: [submission, version]
                properties:
                  submission:
                    $ref: "#/components/schemas/CustomFormSubmission"
                  version:
                    $ref: "#/components/schemas/CustomFormVersion"
        "403":
          description: Forbidden — requires custom_forms.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  # --- WS-7: Bulk Import / Multi-Entity Migration -------------------------
  /organizations/{organizationId}/migrations/entity-types:
    get:
      operationId: listMigrationEntityTypes
      tags: [migrations]
      summary: List the entity types a migration can import, in dependency order (WS-7)
      description: >-
        The server-defined allow-list. Each entry carries its canonical
        fields (so a client can render a mapping UI) and its dependsOn list
        (so a client can explain why structure must accompany employees).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Supported entity types
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MigrationEntityTypeList"
        "403":
          description: Forbidden — requires migration.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations/templates/{entityType}:
    get:
      operationId: getMigrationTemplate
      tags: [migrations]
      summary: Download a CSV starter template for one entity type (WS-7)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: entityType
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: CSV template
          content:
            text/csv:
              schema:
                type: string
        "403":
          description: Forbidden — requires migration.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Unknown entity type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations:
    get:
      operationId: listMigrations
      tags: [migrations]
      summary: List migration batches (WS-7)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Migration batches
          content:
            application/json:
              schema:
                type: object
                required: [migrations]
                properties:
                  migrations:
                    type: array
                    items:
                      $ref: "#/components/schemas/MigrationBatch"
        "403":
          description: Forbidden — requires migration.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createMigration
      tags: [migrations]
      summary: Create a migration batch (WS-7)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MigrationBatch"
        "400":
          description: A migration name is required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires migration.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations/{migrationId}:
    get:
      operationId: getMigration
      tags: [migrations]
      summary: Get one migration batch and its sources (WS-7)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: migrationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Migration batch with sources
          content:
            application/json:
              schema:
                type: object
                required: [migration, sources, executionPolicy]
                properties:
                  migration:
                    $ref: "#/components/schemas/MigrationBatch"
                  sources:
                    type: array
                    items:
                      $ref: "#/components/schemas/MigrationSource"
                  executionPolicy:
                    $ref: "#/components/schemas/MigrationExecutionPolicy"
        "403":
          description: Forbidden — requires migration.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations/{migrationId}/sources:
    post:
      operationId: uploadMigrationSource
      tags: [migrations]
      summary: Upload a CSV/XLSX source file for one entity type (WS-7)
      description: >-
        multipart/form-data, field "file". CSV or XLSX, 20MB / 50,000 rows /
        200 columns maximum. Returns the parsed headers, a sample of rows and
        a conservative auto-mapping proposal. Re-uploading for an entity type
        already present replaces it and returns the batch to draft.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: migrationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, entityType]
              properties:
                file:
                  type: string
                  format: binary
                entityType:
                  type: string
                sheetName:
                  type: string
      responses:
        "201":
          description: Uploaded and parsed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MigrationSourceUploadResult"
        "400":
          description: Unsupported/malformed file, or unknown entity type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires migration.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Migration not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations/{migrationId}/sources/{sourceId}/mapping:
    put:
      operationId: setMigrationSourceMapping
      tags: [migrations]
      summary: Confirm a column mapping and stage every row (WS-7)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: migrationId
          in: path
          required: true
          schema:
            type: integer
        - name: sourceId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [mapping]
              properties:
                mapping:
                  type: object
                  additionalProperties:
                    type: string
      responses:
        "200":
          description: Mapping saved and rows staged
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MigrationMappingResult"
        "400":
          description: Invalid mapping (unknown field, duplicate target, missing required field)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires migration.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Migration or source not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The stored file no longer matches its recorded checksum
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations/{migrationId}/validate:
    post:
      operationId: validateMigration
      tags: [migrations]
      summary: Run the dry run (WS-7)
      description: >-
        Read-only. Re-plans every staged row against live data in dependency
        order; a reference satisfied by an earlier source in the same batch
        counts as resolvable. Safe to run repeatedly.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: migrationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Per-entity validation summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MigrationValidationResult"
        "400":
          description: Not in a validatable state, or sources still unmapped
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires migration.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Migration not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations/{migrationId}/issues:
    get:
      operationId: getMigrationIssues
      tags: [migrations]
      summary: List staged rows with dry-run errors or warnings (WS-7)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: migrationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Row-level issues
          content:
            application/json:
              schema:
                type: object
                required: [issues]
                properties:
                  issues:
                    type: array
                    items:
                      $ref: "#/components/schemas/MigrationRowIssue"
        "403":
          description: Forbidden — requires migration.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Migration not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations/{migrationId}/approve:
    post:
      operationId: approveMigration
      tags: [migrations]
      summary: Approve a validated migration, freezing its source checksums (WS-7)
      description: >-
        Refuses a batch that still has error rows. Snapshots every source's
        sha256 so execution can prove nothing changed after review.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: migrationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MigrationApprovalResult"
        "400":
          description: Not validated, or still has error rows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires migration.execute
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Migration not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations/{migrationId}/execute:
    post:
      operationId: executeMigration
      tags: [migrations]
      summary: Execute an approved migration (WS-7)
      description: >-
        Small migrations run synchronously and return a finished result.
        Larger ones are handed to the background worker and return 202 —
        poll the migration for progress. Re-running only ever picks up rows
        still pending, so this is safe to retry.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: migrationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Executed synchronously
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MigrationExecutionProgress"
        "202":
          description: Queued for background execution
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MigrationExecutionQueued"
        "400":
          description: Not in an executable state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires migration.execute
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Migration not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A source file changed after approval
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: An atomic migration was rolled back in full — nothing was written
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations/{migrationId}/reconciliation:
    get:
      operationId: getMigrationReconciliation
      tags: [migrations]
      summary: Per-entity reconciliation totals (WS-7)
      description: >-
        Every staged row lands in exactly one bucket and the buckets sum to
        the source row count, so a silently dropped row is arithmetically
        impossible to hide.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: migrationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Reconciliation summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MigrationReconciliation"
        "403":
          description: Forbidden — requires migration.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Migration not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations/{migrationId}/reconciliation.csv:
    get:
      operationId: downloadMigrationReconciliation
      tags: [migrations]
      summary: Download the reconciliation report as CSV (WS-7)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: migrationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: CSV reconciliation report
          content:
            text/csv:
              schema:
                type: string
        "403":
          description: Forbidden — requires migration.read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Migration not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/migrations/{migrationId}/cancel:
    post:
      operationId: cancelMigration
      tags: [migrations]
      summary: Cancel a migration that has not executed (WS-7)
      description: >-
        Only a draft/mapped/validated/approved migration can be cancelled.
        A running or finished one cannot, because cancelling would imply
        undoing committed rows, which this engine deliberately does not do.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: migrationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MigrationBatch"
        "400":
          description: Not in a cancellable state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires migration.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Migration not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"


  /organizations/{organizationId}/onboarding-templates:
    get:
      operationId: listOnboardingTemplates
      tags: [onboarding]
      summary: List onboarding templates
      description: >-
        Returns the organization's onboarding templates with their active
        version, if any. Gated by the onboarding module and onboarding.read.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Onboarding templates
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTemplateList"
    post:
      operationId: createOnboardingTemplate
      tags: [onboarding]
      summary: Create an onboarding template
      description: >-
        Creates a template together with its first draft version. Gated by
        onboarding.configure — configuration authority is deliberately
        separate from operating someone's onboarding.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOnboardingTemplateInput"
      responses:
        "201":
          description: Template created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTemplateWithVersion"
        "400":
          description: Invalid template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding-templates/{templateId}:
    patch:
      operationId: updateOnboardingTemplate
      tags: [onboarding]
      summary: Update an onboarding template's name or applicability
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOnboardingTemplateInput"
      responses:
        "200":
          description: Template updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTemplate"
        "404":
          description: Template not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding-templates/{templateId}/versions:
    get:
      operationId: listOnboardingTemplateVersions
      tags: [onboarding]
      summary: List a template's versions
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Template versions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTemplateVersionList"
    post:
      operationId: createOnboardingTemplateVersion
      tags: [onboarding]
      summary: Start a new draft version
      description: >-
        Creates a new draft, optionally copying an existing version's tasks.
        The source version is never modified — onboarding already issued from
        it keeps running unchanged.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOnboardingTemplateVersionInput"
      responses:
        "201":
          description: Draft version created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTemplateVersion"
        "404":
          description: Template not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding-template-versions/{versionId}/tasks:
    get:
      operationId: listOnboardingTemplateTasks
      tags: [onboarding]
      summary: List a version's task definitions
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Task definitions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTemplateTaskList"
    post:
      operationId: addOnboardingTemplateTask
      tags: [onboarding]
      summary: Add a task definition to a draft version
      description: >-
        Only a draft version may be edited. Publishing freezes the definition
        so instances that snapshotted it stay reproducible.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OnboardingTaskDefinitionInput"
      responses:
        "201":
          description: Task added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTemplateTask"
        "400":
          description: Invalid task definition, or the version is not a draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Version not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding-template-versions/{versionId}/tasks/{taskId}:
    delete:
      operationId: deleteOnboardingTemplateTask
      tags: [onboarding]
      summary: Remove a task definition from a draft version
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
        - name: taskId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Task removed
        "400":
          description: The version is not a draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding-template-versions/{versionId}/activate:
    post:
      operationId: activateOnboardingTemplateVersion
      tags: [onboarding]
      summary: Publish a draft version
      description: >-
        Makes this the version new onboarding starts from, archiving any
        previously active version of the same template. Existing instances
        retain the version they snapshotted.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Version activated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTemplateVersion"
        "400":
          description: The version cannot be activated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding-template-versions/{versionId}/archive:
    post:
      operationId: archiveOnboardingTemplateVersion
      tags: [onboarding]
      summary: Archive a template version
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Version archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTemplateVersion"
        "404":
          description: Version not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding:
    get:
      operationId: listOnboarding
      tags: [onboarding]
      summary: List onboarding across the organization
      description: >-
        HR view. Requires onboarding.read; ordinary employees use
        /my-onboarding instead and never see organization-wide data.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [not_started, in_progress, completed, cancelled]
        - name: overdueOnly
          in: query
          required: false
          schema:
            type: boolean
      responses:
        "200":
          description: Onboarding list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingSummaryList"
    post:
      operationId: startOnboarding
      tags: [onboarding]
      summary: Start onboarding for an existing employee
      description: >-
        Onboarding never creates an employee. The employee must already exist
        through Recruitment conversion, manual creation or import. A candidate
        record is not required, so legacy and manually created staff can be
        onboarded. Idempotent — an employee can hold only one open onboarding.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartOnboardingInput"
      responses:
        "201":
          description: Onboarding started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingInstance"
        "400":
          description: No applicable template, or the employee was not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This employee already has open onboarding
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding/{instanceId}:
    get:
      operationId: getOnboarding
      tags: [onboarding]
      summary: Get one onboarding with its tasks, progress and acknowledgements
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: instanceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Onboarding detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingDetail"
        "404":
          description: Onboarding not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding/{instanceId}/cancel:
    post:
      operationId: cancelOnboarding
      tags: [onboarding]
      summary: Cancel an onboarding
      description: >-
        Requires a reason. Preserves completed tasks, evidence and
        acknowledgements, and stops future reminders. This is not a
        separation — employment is unaffected.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: instanceId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelOnboardingInput"
      responses:
        "200":
          description: Onboarding cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingInstance"
        "400":
          description: A reason is required, or the onboarding is not open
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding/{instanceId}/supplementary-tasks:
    post:
      operationId: addSupplementaryOnboardingTask
      tags: [onboarding]
      summary: Add a supplementary task to an onboarding
      description: >-
        The controlled correction path for onboarding that has already
        completed. Appends a new task rather than reopening or rewriting
        completed history.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: instanceId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddSupplementaryTaskInput"
      responses:
        "201":
          description: Task added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTask"
        "400":
          description: A reason is required, or the onboarding is cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding-tasks/{taskId}/complete:
    post:
      operationId: completeOnboardingTask
      tags: [onboarding]
      summary: Complete an onboarding task
      description: >-
        A reference task (asset, inventory, access, personnel file) is refused
        unless the referenced module already shows the expected state, and a
        document task follows Documents & Records' own verification. Use a
        waiver where the authoritative state genuinely cannot be met.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: taskId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteOnboardingTaskInput"
      responses:
        "200":
          description: Task completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTask"
        "400":
          description: The task is already resolved, or the onboarding is closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Payroll authority is required for a payroll setup task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The referenced state does not yet exist
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding-tasks/{taskId}/waive:
    post:
      operationId: waiveOnboardingTask
      tags: [onboarding]
      summary: Waive an onboarding task
      description: >-
        Requires authority and a reason, and is audited. A waived required
        task no longer blocks completion.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: taskId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WaiveOnboardingTaskInput"
      responses:
        "200":
          description: Task waived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingTask"
        "400":
          description: A reason is required, or the task is already resolved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding-tasks/{taskId}/induction:
    patch:
      operationId: scheduleOnboardingInduction
      tags: [onboarding]
      summary: Schedule or reschedule an induction session
      description: >-
        Session logistics only — facilitator, time, delivery mode and venue.
        There is no course, curriculum or enrollment here; Learning remains a
        separate module.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: taskId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScheduleInductionInput"
      responses:
        "200":
          description: Induction updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingInductionDetail"
        "404":
          description: Induction task not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding-tasks/{taskId}/induction/attendance:
    post:
      operationId: recordInductionAttendance
      tags: [onboarding]
      summary: Record attendance at an induction session
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: taskId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordInductionAttendanceInput"
      responses:
        "200":
          description: Attendance recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingInductionDetail"
        "404":
          description: Induction task not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/document-acknowledgements:
    get:
      operationId: listOutstandingAcknowledgements
      tags: [onboarding]
      summary: List outstanding handbook and policy acknowledgements
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Outstanding acknowledgements
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentAcknowledgementList"
    post:
      operationId: assignDocumentAcknowledgement
      tags: [onboarding]
      summary: Assign a document to an audience for acknowledgement
      description: >-
        Raises one acknowledgement obligation per employee against the
        document's current version. The audience is a fixed set of attributes,
        not a query. Re-running is safe — existing obligations are not
        duplicated.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignAcknowledgementInput"
      responses:
        "200":
          description: Assignment result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssignAcknowledgementResult"
        "400":
          description: The document or audience is not valid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/documents/{documentId}/reacknowledge:
    post:
      operationId: raiseReacknowledgements
      tags: [onboarding]
      summary: Raise fresh acknowledgement obligations for a new document version
      description: >-
        Applies only to documents the organization configured to require
        re-acknowledgement. Earlier acknowledgements are preserved as
        historical evidence against their own version and are never rewritten.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: documentId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Re-acknowledgement result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RaiseReacknowledgementsResult"
        "400":
          description: The document has no current version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/my-onboarding:
    get:
      operationId: getMyOnboarding
      tags: [onboarding]
      summary: Get the caller's own onboarding
      description: >-
        Employee self-service. The employee is resolved server-side from the
        authenticated identity; any employee id in the request is ignored.
        Returns null when the caller has no onboarding.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own onboarding, or null
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyOnboarding"

  /organizations/{organizationId}/my-acknowledgements/{acknowledgementId}/acknowledge:
    post:
      operationId: acknowledgeDocument
      tags: [onboarding]
      summary: Confirm receipt of an assigned document
      description: >-
        Records that the caller acknowledged the exact assigned version. This
        is an acknowledgement of receipt, not an electronic signature.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: acknowledgementId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Acknowledged
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentAcknowledgement"
        "403":
          description: This acknowledgement belongs to someone else
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Acknowledgement not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/onboarding-responsibilities:
    get:
      operationId: listOnboardingResponsibilities
      tags: [onboarding]
      summary: List onboarding tasks the caller is currently responsible for
      description: >-
        Manager and Department Head view. Returns only tasks whose resolver
        currently points at the caller — holding a managerial position confers
        no organization-wide onboarding visibility.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Tasks the caller is responsible for
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnboardingResponsibilityList"

  /organizations/{organizationId}/employees/{employeeId}/employment-lifecycle:
    get:
      operationId: getEmploymentLifecycle
      tags: [employment-lifecycle]
      summary: Get an employee's current lifecycle state and full history
      description: >-
        Returns current state (active employment term, open acting appointment,
        open secondment, probation) separately from history, so a client never
        has to infer the present from unordered rows. History includes imported
        events, which are rendered as recorded and carry no system meaning.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Lifecycle state and history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmploymentLifecycleState"
        "404":
          description: Employee not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/employment-terms:
    get:
      operationId: listEmploymentTerms
      tags: [employment-lifecycle]
      summary: List an employee's employment terms, newest first
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Employment terms
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmploymentTermList"
    post:
      operationId: createEmploymentTerm
      tags: [employment-lifecycle]
      summary: Create an employment term
      description: >-
        An employee may hold at most one active term. A fixed term requires an
        end date; a permanent term must not have one.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEmploymentTermInput"
      responses:
        "201":
          description: Term created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmploymentTerm"
        "400":
          description: Invalid term, or the employee was not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: This employee already has an active term
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employment-terms/{termId}/renew:
    post:
      operationId: renewEmploymentTerm
      tags: [employment-lifecycle]
      summary: Renew an employment term
      description: >-
        Supersedes the current term and creates a new one linked back to it. The
        prior term is preserved unchanged, so the renewal chain stays readable
        end to end.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: termId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RenewEmploymentTermInput"
      responses:
        "200":
          description: Renewed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmploymentTermRenewal"
        "400":
          description: Invalid term dates, or the term is not active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Term not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employment-terms/{termId}/close:
    post:
      operationId: closeEmploymentTerm
      tags: [employment-lifecycle]
      summary: Close an employment term without renewing it
      description: >-
        Records that the term ended. This does NOT separate the employee and
        does not change employment status — the employment decision is a
        separate authorized action.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: termId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CloseEmploymentTermInput"
      responses:
        "200":
          description: Closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmploymentTerm"
        "400":
          description: The term is not active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employment-terms/expiring:
    get:
      operationId: listExpiringEmploymentTerms
      tags: [employment-lifecycle]
      summary: List terms approaching or past their end date
      description: >-
        The HR action queue. Expiry state is derived from dates and the given
        instant, never stored. Nothing here changes employment.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: asOf
          in: query
          required: false
          description: Evaluate expiry as at this instant instead of now.
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Expiring and expired terms
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExpiringTermList"

  /organizations/{organizationId}/employees/{employeeId}/probation/extend:
    post:
      operationId: extendProbation
      tags: [employment-lifecycle]
      summary: Extend an employee's probation
      description: >-
        Records an effective-dated extension event and moves the expected
        probation end. The previous expected end is preserved in history.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExtendProbationInput"
      responses:
        "200":
          description: Probation extended
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProbationState"
        "400":
          description: Not on probation, invalid date, extensions not permitted, or the limit was reached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/probation/unsuccessful:
    post:
      operationId: recordUnsuccessfulProbation
      tags: [employment-lifecycle]
      summary: Record an unsuccessful probation outcome
      description: >-
        Records the outcome and surfaces it for HR action. This deliberately
        does NOT separate the employee and does not change employment status —
        if employment is to end, that is a separate authorized separation.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordProbationOutcomeInput"
      responses:
        "200":
          description: Outcome recorded; employment unchanged
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProbationState"
        "400":
          description: Not on probation, or a reason is missing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/assignments:
    get:
      operationId: listEmploymentAssignments
      tags: [employment-lifecycle]
      summary: List an employee's acting appointments and secondments
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: assignmentType
          in: query
          required: false
          schema:
            type: string
            enum: [acting, secondment]
      responses:
        "200":
          description: Assignments
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmploymentAssignmentList"
    post:
      operationId: startEmploymentAssignment
      tags: [employment-lifecycle]
      summary: Start an acting appointment or secondment
      description: >-
        The employee's substantive position is never changed by this action. An
        acting appointment records the position being acted in alongside the
        substantive one; a secondment records a descriptive destination.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartAssignmentInput"
      responses:
        "201":
          description: Assignment started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmploymentAssignment"
        "400":
          description: Invalid assignment, or this capability is not enabled for the organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: An assignment of this type is already open for this employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/assignments/{assignmentId}/end:
    post:
      operationId: endEmploymentAssignment
      tags: [employment-lifecycle]
      summary: End an acting appointment or secondment
      description: >-
        Closes the temporary record. Nothing is restored, because the
        substantive position was never overwritten.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: assignmentId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EndAssignmentInput"
      responses:
        "200":
          description: Assignment ended
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmploymentAssignment"
        "400":
          description: Already ended, or invalid end date
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Assignment not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/separation-readiness:
    get:
      operationId: getSeparationReadiness
      tags: [employment-lifecycle]
      summary: Factual warnings about outstanding accountability before separation
      description: >-
        Reports outstanding asset custody, inventory custody and personnel-file
        state so an authorized user can see them before separating someone.
        These are WARNINGS ONLY — nothing here returns an asset, moves stock,
        closes a file, or blocks separation.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Outstanding accountability warnings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SeparationReadiness"

  # --- WS-12 — Employee Relations & Offboarding Clearance (§28) ---

  /organizations/{organizationId}/disciplinary-cases:
    get:
      operationId: listDisciplinaryCases
      tags: [employee-relations]
      summary: List structured disciplinary cases
      description: >-
        Structured cases only. The legacy `employee_disciplinary_records` log is
        preserved separately and unchanged (§28.2); it is read through its own
        existing endpoint and its own `employee.disciplinary.read` permission.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/DisciplinaryCaseStatus"
      responses:
        "200":
          description: Disciplinary cases
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisciplinaryCaseList"
    post:
      operationId: openDisciplinaryCase
      tags: [employee-relations]
      summary: Open a structured disciplinary case
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpenDisciplinaryCaseInput"
      responses:
        "201":
          description: Case opened
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisciplinaryCase"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Invalid case
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/disciplinary-cases/{caseId}:
    get:
      operationId: getDisciplinaryCase
      tags: [employee-relations]
      summary: Read one disciplinary case with its chronology
      description: >-
        A sensitive read. Owner Decision #18 auditing fires here (§28.12); it
        deliberately does not fire on the list endpoint.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Disciplinary case detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisciplinaryCaseDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/disciplinary-cases/{caseId}/events:
    post:
      operationId: recordDisciplinaryEvent
      tags: [employee-relations]
      summary: Append one chronology event to a disciplinary case
      description: Append-only. No update or delete path exists for a case event.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordDisciplinaryEventInput"
      responses:
        "201":
          description: Event recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisciplinaryCaseEvent"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The case is closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/disciplinary-cases/{caseId}/stage:
    post:
      operationId: changeDisciplinaryStage
      tags: [employee-relations]
      summary: Move a disciplinary case to another organization-defined stage
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangeDisciplinaryStageInput"
      responses:
        "200":
          description: Stage changed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisciplinaryCase"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The case is closed, or the stage is invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/disciplinary-cases/{caseId}/outcome:
    post:
      operationId: recordDisciplinaryOutcome
      tags: [employee-relations]
      summary: Record the outcome an authorized human decided
      description: >-
        Records a fact and separates nobody. Employment status, separation date
        and the separation service are untouched (§28.7). If employment ends it
        ends through WS-11's separation service as its own authorized act.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordDisciplinaryOutcomeInput"
      responses:
        "200":
          description: Outcome recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisciplinaryCase"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The case is closed, or the outcome is invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/disciplinary-cases/{caseId}/close:
    post:
      operationId: closeDisciplinaryCase
      tags: [employee-relations]
      summary: Close a disciplinary case
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CloseDisciplinaryCaseInput"
      responses:
        "200":
          description: Case closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisciplinaryCase"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The case is already closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/disciplinary-cases/{caseId}/reopen:
    post:
      operationId: reopenDisciplinaryCase
      tags: [employee-relations]
      summary: Reopen a closed disciplinary case
      description: Every prior event stays exactly where it is; nothing is erased.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReopenDisciplinaryCaseInput"
      responses:
        "200":
          description: Case reopened
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisciplinaryCase"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Only a closed case can be reopened
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/disciplinary-cases/{caseId}/evidence:
    post:
      operationId: attachDisciplinaryEvidence
      tags: [employee-relations]
      summary: Attach an existing WS-5 document to a disciplinary case
      description: >-
        WS-12 stores no documents of its own (§28.11). The document must already
        exist in this organization.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AttachCaseEvidenceInput"
      responses:
        "201":
          description: Evidence attached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisciplinaryCaseEvent"
        "404":
          description: Case or document not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/grievances:
    get:
      operationId: listGrievances
      tags: [employee-relations]
      summary: List grievance cases
      description: >-
        Requires the explicit `grievance.read` permission. Organization
        Administrator status alone does not grant it (§28.17).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/GrievanceCaseStatus"
      responses:
        "200":
          description: Grievance cases
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceCaseList"
    post:
      operationId: submitGrievance
      tags: [employee-relations]
      summary: Record a grievance on an employee's behalf
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitGrievanceInput"
      responses:
        "201":
          description: Grievance submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceCase"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Invalid grievance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/grievances/{caseId}:
    get:
      operationId: getGrievance
      tags: [employee-relations]
      summary: Read one grievance with its full chronology
      description: A sensitive read; Owner Decision #18 auditing fires here (§28.12).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Grievance detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceCaseDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/grievances/{caseId}/acknowledge:
    post:
      operationId: acknowledgeGrievance
      tags: [employee-relations]
      summary: Acknowledge receipt of a grievance
      description: The acknowledgement is visible to the complainant (§28.5).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcknowledgeGrievanceInput"
      responses:
        "200":
          description: Acknowledged
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceCase"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Already acknowledged, closed or withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/grievances/{caseId}/assign:
    post:
      operationId: assignGrievance
      tags: [employee-relations]
      summary: Assign a grievance to an investigator
      description: Who is investigating is internal and is not shown to the complainant.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignGrievanceInput"
      responses:
        "200":
          description: Assigned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceCase"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The grievance is closed or withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/grievances/{caseId}/events:
    post:
      operationId: recordGrievanceEvent
      tags: [employee-relations]
      summary: Append one chronology event to a grievance
      description: >-
        `visibleToComplainant` defaults to false. An investigator's working note
        is invisible to the complainant unless somebody deliberately marks it as
        communicated (§28.5).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordGrievanceEventInput"
      responses:
        "201":
          description: Event recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceCaseEvent"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The grievance is closed or withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/grievances/{caseId}/resolve:
    post:
      operationId: resolveGrievance
      tags: [employee-relations]
      summary: Record the resolution communicated to the complainant
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResolveGrievanceInput"
      responses:
        "200":
          description: Resolved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceCase"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The grievance is closed or withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/grievances/{caseId}/close:
    post:
      operationId: closeGrievance
      tags: [employee-relations]
      summary: Close a grievance
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CloseGrievanceInput"
      responses:
        "200":
          description: Closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceCase"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The grievance is already closed or withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/grievances/{caseId}/evidence:
    post:
      operationId: attachGrievanceEvidence
      tags: [employee-relations]
      summary: Attach an existing WS-5 document to a grievance
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AttachCaseEvidenceInput"
      responses:
        "201":
          description: Evidence attached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceCaseEvent"
        "404":
          description: Grievance or document not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/my-grievances:
    get:
      operationId: listMyGrievances
      tags: [employee-relations]
      summary: An employee's own grievances (Employee Self-Service)
      description: >-
        Gated by the caller's own employee link, not by a permission key (§28.17
        mints none for self-service). Returns the explicit §28.5 allow-list view:
        confidential HR notes, investigator working notes, internal
        deliberations, draft findings and protected audit information are never
        included.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own grievances
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceEssViewList"
    post:
      operationId: submitMyGrievance
      tags: [employee-relations]
      summary: Submit a grievance as the signed-in employee
      description: >-
        The complainant is resolved from the caller's own employee link and is
        never accepted from the request body, so an employee cannot file a
        grievance in a colleague's name.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitMyGrievanceInput"
      responses:
        "201":
          description: Grievance submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceEssView"
        "403":
          description: No employee record is linked to this account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/my-grievances/{caseId}:
    get:
      operationId: getMyGrievance
      tags: [employee-relations]
      summary: One of the caller's own grievances (Employee Self-Service)
      description: >-
        A grievance belonging to somebody else is reported as 404 rather than
        403: a 403 would confirm the case exists, which is itself a disclosure
        about a colleague's complaint.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own grievance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceEssView"
        "404":
          description: Not found, or not the caller's own
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/my-grievances/{caseId}/withdraw:
    post:
      operationId: withdrawMyGrievance
      tags: [employee-relations]
      summary: Withdraw the caller's own grievance
      description: Never deletes the record.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: caseId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WithdrawGrievanceInput"
      responses:
        "200":
          description: Withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceEssView"
        "404":
          description: Not found, or not the caller's own
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/clearance-templates:
    get:
      operationId: listClearanceTemplates
      tags: [offboarding]
      summary: List the organization's clearance templates
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Clearance templates
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearanceTemplateList"
    post:
      operationId: createClearanceTemplate
      tags: [offboarding]
      summary: Create a clearance template
      description: >-
        Requires `offboarding.configure`. Defining what every departing person
        must clear is a configuration act, separated from day-to-day offboarding
        authority (§28.17).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateClearanceTemplateInput"
      responses:
        "201":
          description: Template created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearanceTemplate"
        "422":
          description: Invalid template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/clearance-templates/{templateId}:
    get:
      operationId: getClearanceTemplate
      tags: [offboarding]
      summary: Read one clearance template with its items
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Template detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearanceTemplateDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateClearanceTemplate
      tags: [offboarding]
      summary: Update a clearance template
      description: >-
        Editing a template never alters clearance already instantiated from it —
        instance items are snapshots, not references (§28.8).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateClearanceTemplateInput"
      responses:
        "200":
          description: Template updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearanceTemplate"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/clearance-templates/{templateId}/items:
    post:
      operationId: addClearanceTemplateItem
      tags: [offboarding]
      summary: Add an item to a clearance template
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddClearanceTemplateItemInput"
      responses:
        "201":
          description: Item added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearanceTemplateItem"
        "404":
          description: Template not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/clearance-templates/{templateId}/items/{itemId}:
    delete:
      operationId: removeClearanceTemplateItem
      tags: [offboarding]
      summary: Remove an item from a clearance template
      description: >-
        Live clearance instantiated from this item survives; its
        `sourceTemplateItemId` simply becomes null (§28.8).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
        - name: itemId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Item removed
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/offboarding-eligibility:
    get:
      operationId: getOffboardingEligibility
      tags: [offboarding]
      summary: Whether an offboarding may begin, and on what authoritative basis
      description: >-
        Offboarding may start before separation, but only against an
        authoritative recorded basis (§28.6) — the employee already separated, or
        an active fixed-term employment term with an end date. An offboarding
        case can never be its own basis.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Eligibility
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OffboardingEligibility"

  /organizations/{organizationId}/offboarding:
    get:
      operationId: listOffboarding
      tags: [offboarding]
      summary: List offboarding processes
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Offboarding processes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OffboardingList"

  /organizations/{organizationId}/offboarding/{exitProcessId}:
    get:
      operationId: getOffboarding
      tags: [offboarding]
      summary: Read one offboarding with clearance, progress and outstanding custody
      description: >-
        Outstanding custody is READ from Assets and Office Inventory and never
        altered (§28.9, §28.10). Only `returnable` inventory is reported;
        consumables are never clearance liabilities.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: exitProcessId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Offboarding detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OffboardingDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/offboarding:
    post:
      operationId: initiateOffboarding
      tags: [offboarding]
      summary: Start an offboarding and snapshot its clearance items
      description: >-
        Starting an offboarding never terminates employment (§28.7). Clearance
        items are COPIED from the chosen template, so revising the template later
        cannot rewrite clearance already in progress.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InitiateOffboardingInput"
      responses:
        "201":
          description: Offboarding initiated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InitiateOffboardingResult"
        "409":
          description: An offboarding is already running for this employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: No authoritative separation basis exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offboarding/{exitProcessId}/final-clearance:
    post:
      operationId: grantFinalClearance
      tags: [offboarding]
      summary: Grant final HR clearance
      description: >-
        A distinct terminal act, not the arithmetic of the item list. It refuses
        while a required item is outstanding, and it changes nobody's employment
        status (§28.7, §28.8).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: exitProcessId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Final clearance granted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalClearanceResult"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Required clearance is still outstanding
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offboarding/{exitProcessId}/cancel:
    post:
      operationId: cancelOffboarding
      tags: [offboarding]
      summary: Cancel a running offboarding
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: exitProcessId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelOffboardingInput"
      responses:
        "200":
          description: Offboarding cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeExitProcess"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Only a running offboarding can be cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offboarding/{exitProcessId}/clearance-items:
    post:
      operationId: addClearanceItem
      tags: [offboarding]
      summary: Add an ad-hoc clearance item to a running offboarding
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: exitProcessId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddClearanceItemInput"
      responses:
        "201":
          description: Item added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearanceItem"
        "404":
          description: Offboarding not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/clearance-items/{itemId}/complete:
    post:
      operationId: completeClearanceItem
      tags: [offboarding]
      summary: Mark a clearance item complete
      description: >-
        Completing an `asset_return` item does NOT end an asset assignment or
        alter custody, and an `inventory_return` item does NOT move stock. Those
        transitions belong to Assets and Office Inventory (§28.9, §28.10).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: itemId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteClearanceItemInput"
      responses:
        "200":
          description: Item completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearanceItem"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: A waived item cannot be completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/clearance-items/{itemId}/return:
    post:
      operationId: returnClearanceItem
      tags: [offboarding]
      summary: Send a clearance item back for more work
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: itemId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnClearanceItemInput"
      responses:
        "200":
          description: Item returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearanceItem"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/clearance-items/{itemId}/waive:
    post:
      operationId: waiveClearanceItem
      tags: [offboarding]
      summary: Waive a clearance item
      description: >-
        The deliberate escape hatch, and it costs a reason (§28.8). Gated on
        `offboarding.manage` rather than `clearance.act`, so the desk that cannot
        recover an item is not the same party that excuses it.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: itemId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WaiveClearanceItemInput"
      responses:
        "200":
          description: Item waived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearanceItem"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: A waiver requires a reason
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/offboarding/{exitProcessId}/exit-interview:
    post:
      operationId: scheduleExitInterview
      tags: [offboarding]
      summary: Schedule the exit interview for an offboarding
      description: >-
        Questions and answers are WS-8 Custom Fields bound to the
        `exit_interview` scope; no second questionnaire engine exists (§28.19).
        No rehire-eligibility field exists anywhere in WS-12 (§28.15).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: exitProcessId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScheduleExitInterviewInput"
      responses:
        "201":
          description: Interview scheduled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExitInterview"
        "409":
          description: An exit interview already exists for this offboarding
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Offboarding not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/exit-interviews/{interviewId}/complete:
    post:
      operationId: completeExitInterview
      tags: [offboarding]
      summary: Record an exit interview as completed
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: interviewId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteExitInterviewInput"
      responses:
        "200":
          description: Interview completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExitInterview"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/exit-interviews/{interviewId}/cancel:
    post:
      operationId: cancelExitInterview
      tags: [offboarding]
      summary: Cancel a scheduled exit interview
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: interviewId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelExitInterviewInput"
      responses:
        "200":
          description: Interview cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExitInterview"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employee-relations/reports/disciplinary:
    get:
      operationId: getDisciplinaryReport
      tags: [employee-relations]
      summary: Open disciplinary cases with ageing
      description: >-
        Counts and ageing only — no subject, description, finding, outcome or
        note (§28.23). Ageing is derived against the current instant, never
        stored.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Disciplinary read model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisciplinaryReport"

  /organizations/{organizationId}/employee-relations/reports/grievances:
    get:
      operationId: getGrievanceReport
      tags: [employee-relations]
      summary: Open grievances with ageing
      description: >-
        Gated on `grievance.read`, deliberately a separate endpoint from the
        disciplinary read model so one key cannot span both (§28.17).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Grievance read model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrievanceReport"

  /organizations/{organizationId}/employee-relations/reports/offboarding:
    get:
      operationId: getOffboardingReport
      tags: [offboarding]
      summary: Employees currently offboarding, completed offboarding, outstanding assets
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Offboarding read model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OffboardingReport"

  /organizations/{organizationId}/employee-relations/reports/clearance-queue:
    get:
      operationId: getClearanceQueue
      tags: [offboarding]
      summary: Outstanding clearance items across running offboarding
      description: >-
        Readable by `clearance.act` holders as well as offboarding readers — an
        approver who cannot see their own queue cannot do the job the key exists
        for.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: responsibleDepartmentId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Clearance queue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearanceQueue"
        "403":
          description: Neither clearance nor offboarding authority
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  # --- WS-13 — Employee Data Change Approval & HR Service Requests (§29) ---

  /organizations/{organizationId}/data-change-fields:
    get:
      operationId: listDataChangeFields
      tags: [employee-requests]
      summary: The eligible-field registry with this organization's effective policy
      description: >-
        Only fields the PRODUCT allows (§29.3). There is deliberately no endpoint
        that lists employee columns: configuration chooses whether an eligible
        field needs approval, and can never introduce a target.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Eligible fields and policy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataChangeFieldPolicyList"

  /organizations/{organizationId}/data-change-fields/{fieldKey}:
    put:
      operationId: setDataChangeFieldPolicy
      tags: [employee-requests]
      summary: Set whether an eligible field requires approval
      description: >-
        The key is validated against the registry before anything is stored, so
        a specialist-module-owned field can never be turned into a generic
        WS-13 field (§29.4).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: fieldKey
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetDataChangeFieldPolicyInput"
      responses:
        "200":
          description: Policy updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataChangeFieldPolicy"
        "422":
          description: Not an eligible field
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/data-change-field-registry:
    get:
      operationId: getDataChangeFieldRegistry
      tags: [employee-requests]
      summary: Field labels without policy, for surfaces that only need naming
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Registry
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DataChangeFieldSummary"

  /organizations/{organizationId}/request-approval-stages:
    get:
      operationId: listRequestApprovalStages
      tags: [employee-requests]
      summary: WS-13's own approval-stage configuration
      description: >-
        WS-13-namespaced, modelled on WS-9 but not shared with it (§29.8).
        Recruitment is neither refactored nor made to depend on this.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: purpose
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/RequestApprovalPurpose"
      responses:
        "200":
          description: Stages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RequestApprovalStage"
    post:
      operationId: createRequestApprovalStage
      tags: [employee-requests]
      summary: Add an approval stage
      description: >-
        A stage NAMES one of three server-defined resolvers and supplies data; it
        cannot supply code. Authority is never inferred from a role name.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRequestApprovalStageInput"
      responses:
        "201":
          description: Stage created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestApprovalStage"
        "422":
          description: Invalid stage or resolver configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/request-approval-stages/{stageId}:
    delete:
      operationId: deleteRequestApprovalStage
      tags: [employee-requests]
      summary: Remove an approval stage
      description: >-
        In-flight requests are unaffected: each froze its own stage count when it
        was raised (§29.8).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: stageId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Stage removed
        "422":
          description: Not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/data-change-requests:
    get:
      operationId: listDataChangeRequests
      tags: [employee-requests]
      summary: The pending data-change queue
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/DataChangeStatus"
      responses:
        "200":
          description: Requests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DataChangeRequest"

  /organizations/{organizationId}/data-change-requests/{requestId}:
    get:
      operationId: getDataChangeRequest
      tags: [employee-requests]
      summary: The approval view of one request
      description: >-
        Deliberately not the employee record (§29.7). It carries the field, its
        previous and requested values and the decision context, and sensitive
        values arrive masked. Being an approver expands no data visibility.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approval view
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataChangeApprovalView"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/data-change-requests:
    post:
      operationId: createDataChangeRequest
      tags: [employee-requests]
      summary: Raise an HR-originated data-change request
      description: >-
        Origin is fixed by the route as `hr_originated` and never taken from the
        body (§29.2).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDataChangeRequestInput"
      responses:
        "201":
          description: Request raised
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateDataChangeRequestResult"
        "409":
          description: A request is already open for one of these fields
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Not an eligible field, or the value is invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/data-change-requests/{requestId}/approve:
    post:
      operationId: decideDataChangeRequest
      tags: [employee-requests]
      summary: Approve a data-change request (one stage)
      description: >-
        Maker-checker is enforced server-side: the requester may never approve
        their own request (§29.6). Stale is re-checked here, so approving against
        a basis that no longer holds is refused with 409.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DecideDataChangeRequestInput"
      responses:
        "200":
          description: Stage approved, or the request approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataChangeRequest"
        "403":
          description: Self-approval, or the caller lacks this stage's authority
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The record changed after the request was raised
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/data-change-requests/{requestId}/reject:
    post:
      operationId: rejectDataChangeRequest
      tags: [employee-requests]
      summary: Reject a data-change request
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectDataChangeRequestInput"
      responses:
        "200":
          description: Rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataChangeRequest"
        "403":
          description: Self-approval, or missing authority
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/data-change-requests/{requestId}/return:
    post:
      operationId: returnDataChangeRequest
      tags: [employee-requests]
      summary: Return a request for more information
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnDataChangeRequestInput"
      responses:
        "200":
          description: Returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataChangeRequest"

  /organizations/{organizationId}/data-change-requests/{requestId}/reconfirm:
    post:
      operationId: reconfirmDataChangeRequest
      tags: [employee-requests]
      summary: Re-confirm a stale request against the values as they now stand
      description: >-
        The audited re-confirmation §29.10 requires. Captured previous values are
        refreshed to live, the request returns to pending, and the act is
        recorded. It applies nothing.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Re-confirmed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataChangeRequest"
        "422":
          description: Only a stale request can be re-confirmed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/data-change-requests/{requestId}/apply:
    post:
      operationId: applyDataChangeRequest
      tags: [employee-requests]
      summary: Write an approved change to the authoritative record
      description: >-
        A separate explicit human act, transactional and idempotent, and
        deliberately unreachable from any scheduled job (§29.10, §29.11). A
        future effective date does not licence a job to do this later.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataChangeRequest"
        "409":
          description: The record changed after the request was raised
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The request is not in an appliable state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/my-data-change-fields:
    get:
      operationId: listMyDataChangeFields
      tags: [employee-requests]
      summary: Fields an employee may request on their own record
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Eligible fields
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataChangeFieldPolicyList"

  /organizations/{organizationId}/my-data-change-requests:
    get:
      operationId: listMyDataChangeRequests
      tags: [employee-requests]
      summary: An employee's own data-change requests
      description: >-
        Gated by the caller's own employee link, not by a permission key (§29.18
        mints none for self-service).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own requests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DataChangeEssView"
    post:
      operationId: submitMyDataChangeRequest
      tags: [employee-requests]
      summary: Request a change to your own record
      description: >-
        The subject is derived from the caller's own employee link; the body
        carries no employee identifier at all, so a browser cannot claim
        authority over somebody else's record (§29.2).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitMyDataChangeRequestInput"
      responses:
        "201":
          description: Request raised
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataChangeEssView"
        "403":
          description: No employee record is linked to this account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A request is already open for one of these fields
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/my-data-change-requests/{requestId}/withdraw:
    post:
      operationId: withdrawMyDataChangeRequest
      tags: [employee-requests]
      summary: Withdraw your own data-change request
      description: >-
        Never undoes an already-applied change; a new request is the route for
        that. Somebody else's request returns 404 rather than 403.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DataChangeEssView"
        "404":
          description: Not found, or not the caller's own
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/service-request-types:
    get:
      operationId: listServiceRequestTypes
      tags: [employee-requests]
      summary: The organization's HR service-request catalogue
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Types
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceRequestType"
    post:
      operationId: createServiceRequestType
      tags: [employee-requests]
      summary: Add a service-request type
      description: >-
        A closed set of behaviours, not a process designer (§29.12). Questions
        come from a WS-8 form where one is configured; WS-13 builds no second
        form engine.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServiceRequestTypeInput"
      responses:
        "201":
          description: Type created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequestType"
        "422":
          description: Invalid type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/service-request-types/{typeId}:
    patch:
      operationId: updateServiceRequestType
      tags: [employee-requests]
      summary: Update a service-request type
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: typeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateServiceRequestTypeInput"
      responses:
        "200":
          description: Type updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequestType"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/service-requests:
    get:
      operationId: listServiceRequests
      tags: [employee-requests]
      summary: The HR service-request queue
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ServiceRequestStatus"
        - name: assignedToMe
          in: query
          required: false
          description: Restricts to requests assigned to the caller. A caller cannot ask about somebody else's queue.
          schema:
            type: string
            enum: ["true"]
      responses:
        "200":
          description: Requests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceRequest"

  /organizations/{organizationId}/service-requests/{requestId}:
    get:
      operationId: getServiceRequest
      tags: [employee-requests]
      summary: One service request with its chronology
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Request detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequestDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/service-requests:
    post:
      operationId: createServiceRequest
      tags: [employee-requests]
      summary: Raise a service request on an employee's behalf
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServiceRequestInput"
      responses:
        "201":
          description: Request raised
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequest"
        "422":
          description: Invalid request or inactive type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/service-requests/{requestId}/acknowledge:
    post:
      operationId: acknowledgeServiceRequest
      tags: [employee-requests]
      summary: Acknowledge receipt of a service request
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcknowledgeServiceRequestInput"
      responses:
        "200":
          description: Acknowledged
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequest"

  /organizations/{organizationId}/service-requests/{requestId}/assign:
    post:
      operationId: assignServiceRequest
      tags: [employee-requests]
      summary: Assign a service request for fulfilment
      description: >-
        Routing work to a desk. This is NOT approval delegation (§29.9), and
        assignment confers no authority to decide.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignServiceRequestInput"
      responses:
        "200":
          description: Assigned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequest"

  /organizations/{organizationId}/service-requests/{requestId}/approve:
    post:
      operationId: approveServiceRequest
      tags: [employee-requests]
      summary: Approve a service request
      description: Maker-checker applies here too — the raiser may not approve it.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveServiceRequestInput"
      responses:
        "200":
          description: Approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequest"
        "403":
          description: Self-approval, or missing authority
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/service-requests/{requestId}/reject:
    post:
      operationId: rejectServiceRequest
      tags: [employee-requests]
      summary: Reject a service request
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectServiceRequestInput"
      responses:
        "200":
          description: Rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequest"

  /organizations/{organizationId}/service-requests/{requestId}/request-information:
    post:
      operationId: requestServiceRequestInformation
      tags: [employee-requests]
      summary: Ask the employee for more information
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RequestServiceRequestInformationInput"
      responses:
        "200":
          description: Information requested
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequest"

  /organizations/{organizationId}/service-requests/{requestId}/fulfil:
    post:
      operationId: fulfilServiceRequest
      tags: [employee-requests]
      summary: Record fulfilment, attaching the resulting document where applicable
      description: >-
        WS-13 owns no document generation (§29.13). HR generates through WS-5 and
        points this at the resulting `generated_documents` record. A type whose
        fulfilment is a document cannot be fulfilled without one.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FulfilServiceRequestInput"
      responses:
        "200":
          description: Fulfilled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequest"
        "422":
          description: Still awaiting approval, rejected, or a document is required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/service-requests/{requestId}/close:
    post:
      operationId: closeServiceRequest
      tags: [employee-requests]
      summary: Close a service request
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CloseServiceRequestInput"
      responses:
        "200":
          description: Closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequest"

  /organizations/{organizationId}/my-service-request-types:
    get:
      operationId: listMyServiceRequestTypes
      tags: [employee-requests]
      summary: Request types an employee may raise
      description: >-
        Active and employee-visible only. A type hidden from self-service is not
        merely unlisted — submitting it is refused too.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Types
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceRequestType"

  /organizations/{organizationId}/my-service-requests:
    get:
      operationId: listMyServiceRequests
      tags: [employee-requests]
      summary: An employee's own service requests
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The caller's own requests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceRequestEssView"
    post:
      operationId: submitMyServiceRequest
      tags: [employee-requests]
      summary: Raise a service request as the signed-in employee
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitMyServiceRequestInput"
      responses:
        "201":
          description: Request raised
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequestEssView"
        "403":
          description: No employee record is linked to this account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/my-service-requests/{requestId}/respond:
    post:
      operationId: respondToServiceRequest
      tags: [employee-requests]
      summary: Reply to HR's request for information
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RespondToServiceRequestInput"
      responses:
        "200":
          description: Response recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequestEssView"
        "404":
          description: Not found, or not the caller's own
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/my-service-requests/{requestId}/withdraw:
    post:
      operationId: withdrawMyServiceRequest
      tags: [employee-requests]
      summary: Withdraw your own service request
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequestEssView"
        "404":
          description: Not found, or not the caller's own
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/requests/reports:
    get:
      operationId: getRequestReports
      tags: [employee-requests]
      summary: Pending data changes and open service requests, with derived ageing
      description: >-
        Counts and ageing only — no requested value, previous value or note
        (§29.21). Each half appears only if the caller may read that half, so a
        service-request reader does not learn how many data changes are pending.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Read models
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestReports"
        "403":
          description: Neither read permission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  # --- WS-14 — Skills, Competency Framework & Succession (§30) ---------------

  /organizations/{organizationId}/skills:
    get:
      operationId: listSkills
      tags: [skills]
      summary: The organization's skills catalogue
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: activeOnly
          in: query
          required: false
          schema:
            type: boolean
      responses:
        "200":
          description: Skills
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Skill"
    post:
      operationId: createSkill
      tags: [skills]
      summary: Add a skill to the catalogue
      description: >-
        One catalogue serves every organization type (§30.2). The code is stable
        and organization-unique; renaming never rewrites history.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSkillInput"
      responses:
        "201":
          description: Skill created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Skill"
        "409":
          description: That code already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/skills/{skillId}:
    patch:
      operationId: updateSkill
      tags: [skills]
      summary: Amend or retire a catalogue skill
      description: >-
        Retiring sets `active` false. It never deletes the skill, because
        existing employee records and position requirements point at it (§30.2).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: skillId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSkillInput"
      responses:
        "200":
          description: Skill updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Skill"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/skills/import-master-data:
    post:
      operationId: importSkillsFromMasterData
      tags: [skills]
      summary: Import the existing skill Master Data domain into the catalogue
      description: >-
        Idempotent and one-way (§30.4). Master Data is read here and never
        written, and re-running imports nothing already imported.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Import result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillImportResult"

  /organizations/{organizationId}/proficiency-scale:
    get:
      operationId: getProficiencyScale
      tags: [skills]
      summary: The active proficiency scale and its levels
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Scale and levels; scale is null when none is configured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProficiencyScaleView"
    post:
      operationId: createProficiencyScale
      tags: [skills]
      summary: Define a new proficiency scale
      description: >-
        Publishing a new scale archives the incumbent rather than editing it
        (§30.3). Historical assessments keep pointing at the levels they were
        recorded against, so a past judgement is never silently reinterpreted.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProficiencyScaleInput"
      responses:
        "201":
          description: Scale created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProficiencyScaleView"
        "422":
          description: A scale needs a name and at least two levels
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/proficiency-levels/{levelId}:
    patch:
      operationId: relabelProficiencyLevel
      tags: [skills]
      summary: Relabel a proficiency level
      description: >-
        Labels may change; ORDER MAY NOT. There is no ordinal in this payload,
        because reordering would reinterpret every assessment already recorded
        against the level (§30.3).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: levelId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RelabelProficiencyLevelInput"
      responses:
        "200":
          description: Level updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProficiencyLevel"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employee-skills:
    get:
      operationId: listEmployeeSkillRecords
      tags: [skills]
      summary: Employee skill records
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
        - name: skillId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeSkillRecord"

  /organizations/{organizationId}/employee-skills/{recordId}:
    get:
      operationId: getEmployeeSkillRecord
      tags: [skills]
      summary: One record with its full assessment history
      description: >-
        The history is append-only (§30.7): an assessment is never edited or
        deleted, so a reader can always see what was judged, by whom and when.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: recordId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Record and history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeSkillDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/skill-records:
    post:
      operationId: claimEmployeeSkill
      tags: [skills]
      summary: Record a skill against an employee (HR entry)
      description: >-
        Source is fixed by the route as hr_entry and never taken from the body.
        A record starts CLAIMED — recording it verifies nothing (§30.5).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClaimEmployeeSkillInput"
      responses:
        "201":
          description: Record created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeSkillRecord"
        "422":
          description: Unknown, inactive or non-proficiency skill
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employee-skills/{recordId}/assess:
    post:
      operationId: assessEmployeeSkill
      tags: [skills]
      summary: Record an assessment
      description: >-
        HR holding skill_assessment.record, or the employee's authoritative
        reporting manager, may assess (§30.8). Manager authority is resolved from
        the employee record, never from a role name and never from the body.
        Recording an assessment does NOT verify the claim.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: recordId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssessEmployeeSkillInput"
      responses:
        "201":
          description: Assessment recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeSkillDecisionResult"
        "403":
          description: Not an authorised assessor for this employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employee-skills/{recordId}/verify:
    post:
      operationId: verifyEmployeeSkill
      tags: [skills]
      summary: Verify a claimed skill
      description: >-
        The only path that sets a verified level (§30.6). Self-verification is
        refused on the actor's own employee link.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: recordId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerifyEmployeeSkillInput"
      responses:
        "200":
          description: Verified
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeSkillDecisionResult"
        "403":
          description: An employee may not verify their own skill
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employee-skills/{recordId}/reject:
    post:
      operationId: rejectEmployeeSkill
      tags: [skills]
      summary: Reject a claimed skill
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: recordId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectEmployeeSkillInput"
      responses:
        "200":
          description: Rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeSkillRecord"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/my-skills:
    get:
      operationId: listMySkills
      tags: [skills]
      summary: My own skill records
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: My records; empty when no employee record is linked
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeSkillRecord"
    post:
      operationId: claimMySkill
      tags: [skills]
      summary: Claim a skill for myself
      description: >-
        No permission key gates this: the right comes from the employee link,
        resolved server-side (§30.18, §30.22). The body carries no employee
        identifier, so nobody can claim onto another employee's record.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClaimMySkillInput"
      responses:
        "201":
          description: Claim recorded, awaiting verification
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeSkillRecord"
        "403":
          description: No employee record is linked to this account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/my-skill-options:
    get:
      operationId: listMySkillOptions
      tags: [skills]
      summary: The skills I may claim, and the active level labels
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Claim options
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MySkillOptions"

  /organizations/{organizationId}/my-skill-gaps:
    get:
      operationId: getMySkillGaps
      tags: [skills]
      summary: My gaps against my own current position
      description: >-
        The position comes from my employee record, never from the query string
        (§30.18). This surface carries no succession content of any kind.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: My gaps; positionId is null when I hold no position
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MySkillGaps"

  /organizations/{organizationId}/positions/{positionId}/skill-requirements:
    get:
      operationId: listPositionSkillRequirements
      tags: [skills]
      summary: A position's skill requirements
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: positionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Requirements
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PositionSkillRequirement"
    post:
      operationId: addPositionRequirement
      tags: [skills]
      summary: Require a skill for a position
      description: >-
        Adding a requirement changes nobody's employment and blocks nothing
        (§30.9). It makes a gap visible; a human decides what follows.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: positionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddPositionRequirementInput"
      responses:
        "201":
          description: Requirement added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PositionSkillRequirement"
        "422":
          description: Unknown skill or level
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/position-skill-requirements/{requirementId}:
    delete:
      operationId: removePositionRequirement
      tags: [skills]
      summary: Withdraw a position skill requirement
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: requirementId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Withdrawn
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/skill-gaps:
    get:
      operationId: getEmployeeSkillGaps
      tags: [skills]
      summary: One employee measured against one position
      description: >-
        ONLY VERIFIED CAPABILITY COUNTS (§30.6). A claimed-but-unverified skill
        is reported through hasUnverifiedClaim and never satisfies a
        requirement, and no_verified_evidence is a distinct state from
        below_requirement — a caller must not render the first as lacking the
        skill.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
        - name: positionId
          in: query
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Gaps
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillGapResult"

  /organizations/{organizationId}/readiness-levels:
    get:
      operationId: listReadinessLevels
      tags: [succession]
      summary: The organization's readiness bands
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Readiness levels, nearest-term first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReadinessLevel"
    post:
      operationId: createReadinessLevel
      tags: [succession]
      summary: Define a readiness band
      description: >-
        A readiness band is a human judgement of how soon somebody could step up.
        It is not a score and nothing computes it (§30.13).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReadinessLevelInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessLevel"
        "422":
          description: A label and a positive ordinal are required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/succession-plans:
    get:
      operationId: listSuccessionPlans
      tags: [succession]
      summary: Which positions are succession-managed
      description: >-
        NOT a sensitive read: no candidate, rationale or confidential note is
        returned here (§30.17).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Plan summaries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SuccessionPlanSummary"
    post:
      operationId: createSuccessionPlan
      tags: [succession]
      summary: Open a succession plan for a position
      description: >-
        Marks the position as succession-managed. It changes nothing about the
        position itself and appoints nobody (§30.11, §30.16).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSuccessionPlanInput"
      responses:
        "201":
          description: Plan opened
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessionPlan"
        "409":
          description: An open plan already exists for that position
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/succession-plans/{planId}:
    get:
      operationId: getSuccessionPlan
      tags: [succession]
      summary: One plan, including its confidential criticality notes
      description: >-
        Requires succession.confidential.read, deliberately narrower than
        succession.read, and recorded as a sensitive read under Owner Decision
        #18 (§30.17).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: planId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Plan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessionPlan"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateSuccessionPlan
      tags: [succession]
      summary: Amend or close a succession plan
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: planId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSuccessionPlanInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessionPlan"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/succession-plans/{planId}/candidates:
    get:
      operationId: listSuccessionCandidates
      tags: [succession]
      summary: The candidate pool for a plan
      description: >-
        Confidential, and a sensitive read (§30.17). Grouped by readiness band
        for presentation only — there is NO rank, score or ordering position,
        and none may be added (§30.12).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: planId
          in: path
          required: true
          schema:
            type: integer
        - name: includeRemoved
          in: query
          required: false
          schema:
            type: boolean
      responses:
        "200":
          description: Candidates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SuccessionCandidate"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: nominateSuccessionCandidate
      tags: [succession]
      summary: Nominate a candidate
      description: >-
        Nominating records a view. It appoints nobody, promises nothing and
        changes no employment state (§30.16).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: planId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NominateSuccessionCandidateInput"
      responses:
        "201":
          description: Nominated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessionCandidate"
        "409":
          description: Already an active candidate on this plan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/succession-candidates/{candidateId}/readiness:
    post:
      operationId: setSuccessionReadiness
      tags: [succession]
      summary: Set a candidate's readiness band
      description: >-
        A human chooses this band. Nothing computes it, and no scheduled job can
        reach this path (§30.13, §30.21).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: candidateId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetSuccessionReadinessInput"
      responses:
        "200":
          description: Readiness changed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessionCandidate"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/succession-candidates/{candidateId}/remove:
    post:
      operationId: removeSuccessionCandidate
      tags: [succession]
      summary: Remove a candidate from a plan
      description: >-
        The candidate row is retained and marked removed; the chronology keeps
        the reason (§30.12).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: candidateId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RemoveSuccessionCandidateInput"
      responses:
        "200":
          description: Removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessionCandidate"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/succession-candidates/{candidateId}/history:
    get:
      operationId: listSuccessionCandidateEvents
      tags: [succession]
      summary: A candidate's append-only readiness chronology
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: candidateId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Events, oldest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SuccessionCandidateEvent"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/succession/coverage:
    get:
      operationId: getSuccessionCoverage
      tags: [succession]
      summary: Succession coverage counts
      description: >-
        Counts only. No candidate identity, rationale or note — a coverage report
        must never become the route by which confidential succession content
        reaches a caller who could not read the plan itself (§30.25).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Coverage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessionCoverageResult"

  /organizations/{organizationId}/development-actions:
    get:
      operationId: listDevelopmentActions
      tags: [skills]
      summary: Recorded development actions
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Actions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DevelopmentAction"
    post:
      operationId: createDevelopmentAction
      tags: [skills]
      summary: Record a development action
      description: >-
        Learning references are validated for organization ownership and then
        left alone (§30.14). Nothing enrols anybody, and no enrolment happens
        automatically because a gap exists.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDevelopmentActionInput"
      responses:
        "201":
          description: Recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevelopmentAction"
        "422":
          description: Unknown employee, skill or Learning reference
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/development-actions/{actionId}:
    patch:
      operationId: updateDevelopmentAction
      tags: [skills]
      summary: Progress or close a development action
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: actionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateDevelopmentActionInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevelopmentAction"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  # --- WS-15 — HR Action Centre (§31) ---------------------------------------

  /organizations/{organizationId}/action-centre:
    get:
      operationId: listActionCentre
      tags: [action-centre]
      summary: The federated cross-module action queue
      description: >-
        Runtime federation (§31.4). Every request recomputes current work from
        the source modules; nothing is stored, cached or reconciled. Each source
        enforces its own permission and its own live authority resolver, so a
        source the caller cannot read is omitted entirely — no row, no count and
        no zero, indistinguishably from the module being disabled (§31.19).
        Sources the caller IS authorized for whose work could not be loaded are
        named in `unavailableSources` (§31.22).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: scope
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ActionScope"
        - name: sourceModule
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ActionSourceModule"
        - name: actionKind
          in: query
          required: false
          schema:
            type: string
        - name: status
          in: query
          required: false
          schema:
            type: string
        - name: dueState
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ActionDueState"
        - name: employeeId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: The permission-filtered queue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActionCentreResult"

  /organizations/{organizationId}/action-centre/counts:
    get:
      operationId: getActionCentreCounts
      tags: [action-centre]
      summary: Permission-filtered action counts
      description: >-
        Computed from the same providers as the rows (§31.19). There is no
        separate counting query that could drift from the row query's
        permissions, and a source the caller cannot read is absent from
        `byModule` rather than reported as zero — a zero would assert that the
        module exists and is empty, which is itself a disclosure.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: scope
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ActionScope"
        - name: sourceModule
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ActionSourceModule"
        - name: dueState
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ActionDueState"
      responses:
        "200":
          description: Counts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActionCentreCounts"

  /organizations/{organizationId}/my-action-centre:
    get:
      operationId: listMyActionCentre
      tags: [action-centre]
      summary: An employee's own actionable work
      description: >-
        The three allow-listed ESS sources (§31.13): onboarding tasks the
        employee owns themselves, documents awaiting their acknowledgement, and
        service requests put back to them. The subject is derived server-side
        from the employee link; no employee identifier is accepted, and none is
        read if supplied. Carries no grievance, succession, Payroll or case
        content of any kind.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: My actions; `linked` is false when no employee record is linked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EssActionCentreResult"

  /organizations/{organizationId}/action-centre/actions/{command}:
    post:
      operationId: executeActionCentreCommand
      tags: [action-centre]
      summary: Invoke one allow-listed inline command
      description: >-
        The command name is validated against the closed §31.8 vocabulary before
        anything else happens, so a client can never name a module, a table or a
        method (§31.33). The handler then re-checks live authority at action
        time and calls the owning module's existing service — preserving its
        transaction, its atomic state guard, its audit event, its notifications
        and its maker-checker (§31.8). No approval logic is reimplemented here,
        and a repeated request fails at the source's own guard (§31.23).
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: command
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/InlineActionCommand"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExecuteActionCentreCommandInput"
      responses:
        "200":
          description: The source's own fresh truth
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActionCommandResult"
        "403":
          description: Not currently authorized for this action
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Unknown command, or the item no longer exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The source refused — already decided, not open, or a reason is required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/employees/{employeeId}/360-sections:
    get:
      operationId: getEmployee360Sections
      tags: [employees]
      summary: Bounded cross-module summary sections for one employee
      description: >-
        WS-15 P2/P3 (§31.29) — read composition, not a data model. Each section
        is a small summary from the module that owns it, plus a deep link;
        detail stays in the owning module and re-gates there. A section the
        caller may not read is OMITTED, indistinguishably from a disabled module
        or an employee with nothing recorded — never returned empty, because an
        empty section asserts the module exists and holds nothing about this
        person. Succession never appears here at all (§30.17), and Payroll has
        no section. Sections the caller IS authorized for whose data could not
        be loaded are named in `unavailableSections`.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: employeeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: The authorized sections
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee360Result"
        "404":
          description: Employee not found in this organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  # ------------------------------------------------------------------
  # WS-26 — Tenant Form, Workflow & Signature Engine (WS-26A foundation)
  # ------------------------------------------------------------------
  /organizations/{organizationId}/form-templates:
    get:
      operationId: listFormTemplates
      tags: [forms]
      summary: List form templates (WS-26)
      description: Any active member sees active templates with a published version; form_template.manage or form.read sees every template and version.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Templates
          content:
            application/json:
              schema:
                type: object
                required: [templates]
                properties:
                  templates:
                    type: array
                    items:
                      $ref: "#/components/schemas/FormTemplateSummary"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createFormTemplate
      tags: [forms]
      summary: Create a form template with its first draft version (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFormTemplateBody"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                required: [template, version]
                properties:
                  template:
                    $ref: "#/components/schemas/FormTemplate"
                  version:
                    $ref: "#/components/schemas/FormTemplateVersion"
        "400":
          description: Invalid definition or stages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Forbidden — requires form_template.manage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Template key already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-templates/{templateId}:
    get:
      operationId: getFormTemplate
      tags: [forms]
      summary: Get a form template with its versions (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Template
          content:
            application/json:
              schema:
                type: object
                required: [template, versions]
                properties:
                  template:
                    $ref: "#/components/schemas/FormTemplate"
                  versions:
                    type: array
                    items:
                      $ref: "#/components/schemas/FormTemplateVersion"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-templates/{templateId}/versions:
    post:
      operationId: createFormTemplateVersion
      tags: [forms]
      summary: Create a new draft version of a template (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFormTemplateVersionBody"
      responses:
        "201":
          description: Draft version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormTemplateVersion"
        "400":
          description: Invalid definition or stages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A draft already exists or the template is archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-templates/{templateId}/archive:
    post:
      operationId: archiveFormTemplate
      tags: [forms]
      summary: Archive a form template (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormTemplate"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-template-versions/{versionId}:
    get:
      operationId: getFormTemplateVersion
      tags: [forms]
      summary: Get a template version with its definition and stages (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormTemplateVersion"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateFormTemplateVersion
      tags: [forms]
      summary: Update a draft version (WS-26). Published versions are immutable.
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateFormTemplateVersionBody"
      responses:
        "200":
          description: Updated draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormTemplateVersion"
        "409":
          description: Version is not an editable draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-template-versions/{versionId}/publish:
    post:
      operationId: publishFormTemplateVersion
      tags: [forms]
      summary: Publish a draft version, superseding the current one (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Published version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormTemplateVersion"
        "403":
          description: Forbidden — requires form_template.publish
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Not a draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-template-versions/{versionId}/blank.pdf:
    get:
      operationId: downloadFormTemplateBlank
      tags: [forms]
      summary: Download the blank official form as PDF (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: versionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: PDF
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-submissions:
    get:
      operationId: listFormSubmissions
      tags: [forms]
      summary: List form submissions visible to the caller (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: templateId
          in: query
          required: false
          schema:
            type: integer
        - name: subjectEmployeeId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/FormSubmissionStatus"
      responses:
        "200":
          description: Submissions
          content:
            application/json:
              schema:
                type: object
                required: [submissions]
                properties:
                  submissions:
                    type: array
                    items:
                      $ref: "#/components/schemas/FormSubmissionSummary"
    post:
      operationId: createFormSubmission
      tags: [forms]
      summary: Start a form (WS-26) — for oneself via the employee link, or on behalf of another employee with form_submission.create_on_behalf where the template version permits it
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFormSubmissionBody"
      responses:
        "201":
          description: Draft submission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormSubmissionDetail"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Template not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-submissions/{submissionId}:
    get:
      operationId: getFormSubmission
      tags: [forms]
      summary: Get a submission with its definition, current revision, history and the caller's allowed actions (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: submissionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Submission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormSubmissionDetail"
        "404":
          description: Not found (or not visible to the caller)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-submissions/{submissionId}/draft:
    put:
      operationId: saveFormSubmissionDraft
      tags: [forms]
      summary: Save draft answers (WS-26) — appends a revision
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: submissionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveFormSubmissionDraftBody"
      responses:
        "200":
          description: Submission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormSubmissionDetail"
        "400":
          description: Invalid answers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormAnswersError"
        "409":
          description: Not editable in this state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-submissions/{submissionId}/submit:
    post:
      operationId: submitFormSubmission
      tags: [forms]
      summary: Submit (or resubmit) a form into its workflow (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: submissionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitFormSubmissionBody"
      responses:
        "200":
          description: Submission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormSubmissionDetail"
        "400":
          description: Required answers missing or invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormAnswersError"
        "409":
          description: Not submittable in this state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-submissions/{submissionId}/stage-action:
    post:
      operationId: actOnFormSubmissionStage
      tags: [forms]
      summary: Complete, approve, return or reject the current workflow stage (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: submissionId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FormSubmissionStageActionBody"
      responses:
        "200":
          description: Submission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormSubmissionDetail"
        "400":
          description: Invalid answers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormAnswersError"
        "403":
          description: Not the actor for this stage, or maker-checker violation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Not awaiting a stage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-submissions/{submissionId}/finalize:
    post:
      operationId: finalizeFormSubmission
      tags: [forms]
      summary: Generate the immutable final document and finalize (WS-26) — requires form.finalize
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: submissionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Submission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormSubmissionDetail"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Not approved, or already finalized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-submissions/{submissionId}/archive:
    post:
      operationId: archiveFormSubmission
      tags: [forms]
      summary: Archive a finalized or rejected submission (WS-26)
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: submissionId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Submission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormSubmissionDetail"
        "409":
          description: Not archivable in this state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/form-submissions/{submissionId}/document.pdf:
    get:
      operationId: downloadFormSubmissionDocument
      tags: [forms]
      summary: Download the form at a state (WS-26) — blank, draft, submitted, returned, rejected, approved, or the immutable final snapshot
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
        - name: submissionId
          in: path
          required: true
          schema:
            type: integer
        - name: kind
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/FormDocumentKind"
        - name: revisionId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: PDF
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Final document not yet generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /organizations/{organizationId}/signature-assets:
    get:
      operationId: listSignatureAssets
      tags: [forms]
      summary: List the caller's own stored signature assets (WS-26B)
      parameters:
        - { name: organizationId, in: path, required: true, schema: { type: integer } }
      responses:
        "200":
          description: The caller's stored signatures
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SignatureAssetList" }
    post:
      operationId: uploadSignatureAsset
      tags: [forms]
      summary: Upload a stored signature image owned by the caller (WS-26B)
      description: >-
        multipart/form-data. PNG/JPEG/WebP, validated by file signature, 5MB
        max. Stored as a signature_assets row owned by the uploader; applying it
        to a document is a separate explicit action.
      parameters:
        - { name: organizationId, in: path, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file: { type: string, format: binary }
      responses:
        "201":
          description: Stored signature
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SignatureAssetView" }
        "400":
          description: Invalid file
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }
  /organizations/{organizationId}/signature-assets/{assetId}/revoke:
    post:
      operationId: revokeSignatureAsset
      tags: [forms]
      summary: Revoke one of the caller's stored signature assets (WS-26B)
      parameters:
        - { name: organizationId, in: path, required: true, schema: { type: integer } }
        - { name: assetId, in: path, required: true, schema: { type: integer } }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RevokeSignatureBody" }
      responses:
        "200":
          description: Revoked
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SignatureAssetView" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }
  /organizations/{organizationId}/form-submissions/{submissionId}/signatures:
    get:
      operationId: listFormSubmissionSignatures
      tags: [forms]
      summary: List signatures applied to a submission the caller may view (WS-26B)
      parameters:
        - { name: organizationId, in: path, required: true, schema: { type: integer } }
        - { name: submissionId, in: path, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Applied signatures
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FormSignatureList" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }
    post:
      operationId: applyFormSubmissionSignature
      tags: [forms]
      summary: Apply a signature to a slot on a submission (WS-26B)
      description: >-
        multipart/form-data. Authorization is checked INDEPENDENTLY — the caller
        must resolve as the participant of the workflow stage that owns the slot;
        possession of an image is never authorization. drawn/device carry a
        binary "file"; uploaded carries "sourceAssetId" (a stored asset owned by
        the caller) and no file.
      parameters:
        - { name: organizationId, in: path, required: true, schema: { type: integer } }
        - { name: submissionId, in: path, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [slotKey, method]
              properties:
                slotKey: { type: string }
                method: { type: string, enum: [drawn, uploaded, device] }
                file: { type: string, format: binary }
                sourceAssetId: { type: integer }
                deviceProvider: { type: string }
                deviceMetadata: { type: string }
      responses:
        "201":
          description: Signature applied
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FormSignatureView" }
        "400":
          description: Invalid
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }
        "403":
          description: Not authorized to sign this slot
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }
        "409":
          description: State conflict
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }
  /organizations/{organizationId}/form-submissions/{submissionId}/signatures/{signatureId}/revoke:
    post:
      operationId: revokeFormSubmissionSignature
      tags: [forms]
      summary: Revoke an applied signature — a new fact, never a delete (WS-26B)
      parameters:
        - { name: organizationId, in: path, required: true, schema: { type: integer } }
        - { name: submissionId, in: path, required: true, schema: { type: integer } }
        - { name: signatureId, in: path, required: true, schema: { type: integer } }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RevokeSignatureBody" }
      responses:
        "200":
          description: Revoked
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FormSignatureView" }
        "403":
          description: Forbidden
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }
        "404":
          description: Not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }
        "409":
          description: Finalized
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }

components:
  schemas:
    SignatureAssetView:
      type: object
      required: [id, status, mimeType, sha256, uploadedAt]
      properties:
        id: { type: integer }
        status: { type: string, enum: [active, revoked] }
        mimeType: { type: string }
        widthPx: { type: integer, nullable: true }
        heightPx: { type: integer, nullable: true }
        byteSize: { type: integer, nullable: true }
        sha256: { type: string }
        uploadedAt: { type: string, format: date-time }
        revokedAt: { type: string, format: date-time, nullable: true }
    SignatureAssetList:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/SignatureAssetView" } }
    FormSignatureView:
      type: object
      required: [id, submissionId, revisionId, slotKey, signerUserId, signerMembershipId, authority, method, sha256, mimeType, signedAt]
      properties:
        id: { type: integer }
        submissionId: { type: integer }
        revisionId: { type: integer }
        slotKey: { type: string }
        signerUserId: { type: integer }
        signerMembershipId: { type: integer }
        representedEmployeeId: { type: integer, nullable: true }
        authority: { type: string }
        stageOrder: { type: integer, nullable: true }
        method: { type: string, enum: [drawn, uploaded, device] }
        sourceAssetId: { type: integer, nullable: true }
        deviceProvider: { type: string, nullable: true }
        sha256: { type: string }
        mimeType: { type: string }
        widthPx: { type: integer, nullable: true }
        heightPx: { type: integer, nullable: true }
        signedAt: { type: string, format: date-time }
        revokedAt: { type: string, format: date-time, nullable: true }
        revokeReason: { type: string, nullable: true }
    FormSignatureList:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/FormSignatureView" } }
    RevokeSignatureBody:
      type: object
      properties:
        reason: { type: string }

    # --- WS-15 P2/P3 — Employee 360 (§31.29) ---------------------------------

    Employee360SectionKey:
      type: string
      description: >-
        The modules §31.29 names as absent from the employee page. Succession
        and Payroll are deliberately not among them.
      enum:
        [
          employment_lifecycle,
          employee_relations,
          employee_requests,
          skills,
          leave,
          learning,
          attendance,
          onboarding,
        ]

    Employee360Provenance:
      type: string
      description: >-
        `legacy` marks genuine history from a superseded model, shown beside the
        current model rather than migrated into it or hidden (§31.29, §31.37).
      enum: [current, legacy]

    Employee360Stat:
      type: object
      required: [label, value]
      properties:
        label:
          type: string
        value:
          type: string

    Employee360Row:
      type: object
      description: >-
        A label, a status and a date. No narrative, no evidence, no proposed
        value, and no free-text body of any kind.
      required: [id, label, provenance]
      properties:
        id:
          type: integer
        label:
          type: string
        status:
          type: string
          nullable: true
        occurredAt:
          type: string
          nullable: true
        provenance:
          $ref: "#/components/schemas/Employee360Provenance"

    Employee360Section:
      type: object
      required: [key, title, provenance, stats, rows, truncated, deepLink]
      properties:
        key:
          $ref: "#/components/schemas/Employee360SectionKey"
        title:
          type: string
        provenance:
          $ref: "#/components/schemas/Employee360Provenance"
        stats:
          type: array
          items:
            $ref: "#/components/schemas/Employee360Stat"
        rows:
          type: array
          items:
            $ref: "#/components/schemas/Employee360Row"
        truncated:
          type: boolean
          description: The module holds more than this summary shows.
        deepLink:
          type: string
        note:
          type: string
          description: Shown beside a legacy section so a reader is never left to guess why a superseded model is present.

    Employee360Result:
      type: object
      required: [sections, unavailableSections]
      properties:
        sections:
          type: array
          items:
            $ref: "#/components/schemas/Employee360Section"
        unavailableSections:
          type: array
          description: >-
            Sections the caller IS authorized for whose data could not be
            loaded. Never contains a section the caller may not see.
          items:
            $ref: "#/components/schemas/Employee360SectionKey"

    # --- WS-15 — HR Action Centre (§31) ---------------------------------------

    ActionScope:
      type: string
      description: >-
        `assigned` exists only for sources with a genuine assignment concept
        (§31.10); assignment is never fabricated for dynamic-authority sources.
      enum: [my_actions, assigned, oversight]

    ActionSourceModule:
      type: string
      description: Fixed vocabulary (§31.6). Never free text, and never a table or module path from a client.
      enum:
        [
          leave,
          learning,
          onboarding,
          skills,
          performance,
          recruitment,
          employee_requests,
          employee_relations,
          succession,
          employment_lifecycle,
        ]

    ActionKind:
      type: string
      enum: [approve, complete, verify, review, decide, fulfil, acknowledge]

    ActionDueState:
      type: string
      enum: [overdue, due_soon, undated]

    InlineActionCommand:
      type: string
      description: The closed §31.8 allow-list. Exactly four inline actions, and nothing else.
      enum:
        [
          leave.approve,
          leave.reject,
          learning.approve,
          learning.reject,
          onboarding.complete,
          skill.verify,
          skill.reject,
        ]

    ActionItem:
      type: object
      required:
        [sourceModule, sourceType, sourceId, actionKind, title, status, createdAt, deepLink, inlineCommands]
      description: >-
        A pointer plus enough to triage, never a copy (§31.6). Carries no
        metadata blob, no source payload, no narrative and no priority.
      properties:
        sourceModule:
          $ref: "#/components/schemas/ActionSourceModule"
        sourceType:
          type: string
          description: The source's own resource kind, e.g. `leave_request`.
        sourceId:
          type: integer
          description: The source record's own existing id. Never a WS-15-minted identifier.
        actionKind:
          $ref: "#/components/schemas/ActionKind"
        title:
          type: string
          description: A safe, generic operational label. Never narrative, evidence or a sensitive value (§31.14).
        employeeId:
          type: integer
          nullable: true
          description: Present only where the actor may already see that employee through the source.
        employeeFirstName:
          type: string
          nullable: true
        employeeLastName:
          type: string
          nullable: true
        status:
          type: string
          description: The source's own status string, passed through and never re-mapped.
        createdAt:
          type: string
        dueAt:
          type: string
          nullable: true
          description: The source's own authoritative date. Null where the source has none (§31.16).
        overdue:
          type: boolean
          nullable: true
          description: >-
            Null where `dueAt` is null — never false. "Not overdue" and "no
            concept of overdue" are different statements (§31.16).
        deepLink:
          type: string
          description: Route into the owning module's own surface, which re-gates at the destination.
        inlineCommands:
          type: array
          description: Empty for every deep-link-only item (§31.9).
          items:
            $ref: "#/components/schemas/InlineActionCommand"

    ActionCentreResult:
      type: object
      required: [items, unavailableSources]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ActionItem"
        unavailableSources:
          type: array
          description: >-
            Sources the caller IS authorized for whose work could not be loaded
            (§31.22). Never contains a source the caller may not see, so it
            cannot disclose that a protected module exists.
          items:
            $ref: "#/components/schemas/ActionSourceModule"

    ActionCentreCounts:
      type: object
      required: [total, overdue, byModule, unavailableSources]
      properties:
        total:
          type: integer
        overdue:
          type: integer
        byModule:
          type: array
          description: One entry per source that answered. A source the caller cannot read is absent, never zero.
          items:
            type: object
            required: [sourceModule, count]
            properties:
              sourceModule:
                $ref: "#/components/schemas/ActionSourceModule"
              count:
                type: integer
        unavailableSources:
          type: array
          items:
            $ref: "#/components/schemas/ActionSourceModule"

    EssActionCentreResult:
      type: object
      required: [linked, items]
      properties:
        linked:
          type: boolean
          description: False when no employee record is linked to the account — a legitimate state, not an error.
        items:
          type: array
          items:
            $ref: "#/components/schemas/ActionItem"

    ExecuteActionCentreCommandInput:
      type: object
      required: [sourceId]
      description: >-
        Carries no module, table or method name — the command identifier in the
        path is the only selector, and it comes from a closed vocabulary.
      properties:
        sourceId:
          type: integer
          description: The source record's own id, re-resolved and organization-scoped server-side.
        reason:
          type: string
          nullable: true
          description: Required by the source for a leave rejection and a skill rejection.
        notes:
          type: string
          nullable: true
        levelId:
          type: integer
          nullable: true
          description: Skill verification only; WS-14 requires one for a proficiency-applicable skill.
        assessedAt:
          type: string
          nullable: true

    ActionCommandResult:
      type: object
      required: [ok]
      properties:
        ok:
          type: boolean
        result:
          description: The owning module's own fresh record, returned unchanged.

    # --- WS-14 — Skills, Competency Framework & Succession (§30) -------------

    SkillCategory:
      type: string
      enum: [technical, behavioural, leadership, functional, compliance, other]

    Skill:
      type: object
      required: [id, organizationId, code, name, category, active]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        code:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        category:
          $ref: "#/components/schemas/SkillCategory"
        active:
          type: boolean
        proficiencyApplicable:
          type: boolean
        evidenceExpected:
          type: boolean
        certificationApplicable:
          type: boolean
        sourceMasterDataCode:
          type: string
          nullable: true
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string

    CreateSkillInput:
      type: object
      required: [code, name]
      properties:
        code:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        description:
          type: string
          nullable: true
        category:
          $ref: "#/components/schemas/SkillCategory"
        proficiencyApplicable:
          type: boolean
        evidenceExpected:
          type: boolean
        certificationApplicable:
          type: boolean

    UpdateSkillInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
          nullable: true
        category:
          $ref: "#/components/schemas/SkillCategory"
        active:
          type: boolean
        proficiencyApplicable:
          type: boolean
        evidenceExpected:
          type: boolean
        certificationApplicable:
          type: boolean

    SkillImportResult:
      type: object
      required: [imported, skipped]
      properties:
        imported:
          type: integer
        skipped:
          type: integer
          description: Already present from an earlier import — the operation is idempotent.

    ProficiencyScale:
      type: object
      required: [id, organizationId, name, active]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        description:
          type: string
          nullable: true
        active:
          type: boolean
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string

    ProficiencyLevel:
      type: object
      required: [id, organizationId, scaleId, ordinal, label]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        scaleId:
          type: integer
        ordinal:
          type: integer
          description: Position in the scale. Immutable once levels exist — see §30.3.
        label:
          type: string
        description:
          type: string
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string

    ProficiencyScaleView:
      type: object
      required: [scale, levels]
      properties:
        scale:
          oneOf:
            - $ref: "#/components/schemas/ProficiencyScale"
            - type: "null"
        levels:
          type: array
          items:
            $ref: "#/components/schemas/ProficiencyLevel"

    CreateProficiencyScaleInput:
      type: object
      required: [name, levels]
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
          nullable: true
        levels:
          type: array
          minItems: 2
          description: In order, lowest first. The order is fixed at creation.
          items:
            type: object
            required: [label]
            properties:
              label:
                type: string
                minLength: 1
              description:
                type: string
                nullable: true

    RelabelProficiencyLevelInput:
      type: object
      description: No ordinal — a level's order may never be changed (§30.3).
      properties:
        label:
          type: string
          minLength: 1
        description:
          type: string
          nullable: true

    EmployeeSkillStatus:
      type: string
      enum: [claimed, assessed, verified, rejected]

    EmployeeSkillSource:
      type: string
      enum: [employee_self_service, hr_entry, assessment, import]

    EmployeeSkillRecord:
      type: object
      required: [id, organizationId, employeeId, skillId, status, source]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        skillId:
          type: integer
        status:
          $ref: "#/components/schemas/EmployeeSkillStatus"
        source:
          $ref: "#/components/schemas/EmployeeSkillSource"
        claimedLevelId:
          type: integer
          nullable: true
        verifiedLevelId:
          type: integer
          nullable: true
          description: Set only by the verification path (§30.6).
        verifiedAt:
          type: string
          nullable: true
        verifiedByUserId:
          type: integer
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true
        certificationId:
          type: integer
          nullable: true
        notes:
          type: string
          nullable: true
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string

    EmployeeSkillAssessmentKind:
      type: string
      enum: [assessment, verification, rejection]

    AssessorRole:
      type: string
      enum: [hr, reporting_manager]

    EmployeeSkillAssessment:
      type: object
      required: [id, organizationId, recordId, kind, assessorRole, assessedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        recordId:
          type: integer
        kind:
          $ref: "#/components/schemas/EmployeeSkillAssessmentKind"
        levelId:
          type: integer
          nullable: true
        assessorRole:
          $ref: "#/components/schemas/AssessorRole"
        assessorUserId:
          type: integer
          nullable: true
        assessorMembershipId:
          type: integer
          nullable: true
        assessedAt:
          type: string
        notes:
          type: string
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true
        createdAt:
          type: string

    EmployeeSkillDetail:
      allOf:
        - $ref: "#/components/schemas/EmployeeSkillRecord"
        - type: object
          required: [assessments]
          properties:
            assessments:
              type: array
              items:
                $ref: "#/components/schemas/EmployeeSkillAssessment"

    EmployeeSkillDecisionResult:
      type: object
      required: [record, assessment]
      properties:
        record:
          $ref: "#/components/schemas/EmployeeSkillRecord"
        assessment:
          $ref: "#/components/schemas/EmployeeSkillAssessment"

    ClaimEmployeeSkillInput:
      type: object
      required: [skillId]
      description: No source field — the route fixes it as hr_entry.
      properties:
        skillId:
          type: integer
        claimedLevelId:
          type: integer
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true
        certificationId:
          type: integer
          nullable: true
        notes:
          type: string
          nullable: true

    ClaimMySkillInput:
      type: object
      required: [skillId]
      description: >-
        No employeeId — the subject is my own employee link, resolved
        server-side (§30.18).
      properties:
        skillId:
          type: integer
        claimedLevelId:
          type: integer
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true
        certificationId:
          type: integer
          nullable: true
        notes:
          type: string
          nullable: true

    AssessEmployeeSkillInput:
      type: object
      required: [levelId, assessedAt]
      description: No assessorRole — the server resolves the actor's authority (§30.8).
      properties:
        levelId:
          type: integer
        assessedAt:
          type: string
        notes:
          type: string
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true

    VerifyEmployeeSkillInput:
      type: object
      required: [assessedAt]
      properties:
        levelId:
          type: integer
          nullable: true
          description: >-
            The level being confirmed. Required when the skill uses proficiency
            levels — "verified, at some unstated standard" is not recordable.
            Omit only for a skill that carries no proficiency.
        assessedAt:
          type: string
        notes:
          type: string
          nullable: true

    RejectEmployeeSkillInput:
      type: object
      required: [reason, assessedAt]
      properties:
        reason:
          type: string
          minLength: 1
        assessedAt:
          type: string

    MySkillOptions:
      type: object
      required: [skills, levels]
      properties:
        skills:
          type: array
          items:
            type: object
            required: [id, name, category, proficiencyApplicable]
            properties:
              id:
                type: integer
              name:
                type: string
              category:
                $ref: "#/components/schemas/SkillCategory"
              proficiencyApplicable:
                type: boolean
        levels:
          type: array
          items:
            type: object
            required: [id, label]
            properties:
              id:
                type: integer
              label:
                type: string

    PositionSkillRequirement:
      type: object
      required: [id, organizationId, positionId, skillId, mandatory, active]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        positionId:
          type: integer
        skillId:
          type: integer
        minimumLevelId:
          type: integer
          nullable: true
        mandatory:
          type: boolean
        active:
          type: boolean
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string

    AddPositionRequirementInput:
      type: object
      required: [skillId]
      properties:
        skillId:
          type: integer
        minimumLevelId:
          type: integer
          nullable: true
        mandatory:
          type: boolean

    SkillGapState:
      type: string
      enum: [no_verified_evidence, below_requirement, meets_requirement, exceeds_requirement]

    SkillGap:
      type: object
      required: [skillId, skillName, mandatory, hasUnverifiedClaim, evidenceExpired, state]
      properties:
        skillId:
          type: integer
        skillName:
          type: string
        mandatory:
          type: boolean
        requiredLevelOrdinal:
          type: integer
          nullable: true
        requiredLevelLabel:
          type: string
          nullable: true
        verifiedLevelOrdinal:
          type: integer
          nullable: true
        verifiedLevelLabel:
          type: string
          nullable: true
        hasUnverifiedClaim:
          type: boolean
          description: Shown separately and never counted toward a requirement (§30.6).
        evidenceExpired:
          type: boolean
          description: The backing certification has lapsed, so the evidence is no longer current (§30.20).
        state:
          $ref: "#/components/schemas/SkillGapState"

    SkillGapResult:
      type: object
      required: [gaps]
      properties:
        gaps:
          type: array
          items:
            $ref: "#/components/schemas/SkillGap"

    MySkillGaps:
      type: object
      required: [positionId, gaps]
      properties:
        positionId:
          type: integer
          nullable: true
        gaps:
          type: array
          items:
            $ref: "#/components/schemas/SkillGap"

    ReadinessLevel:
      type: object
      required: [id, organizationId, ordinal, label, active]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        ordinal:
          type: integer
          description: Nearest-term first. A band, not a score (§30.13).
        label:
          type: string
        description:
          type: string
          nullable: true
        active:
          type: boolean
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string

    CreateReadinessLevelInput:
      type: object
      required: [ordinal, label]
      properties:
        ordinal:
          type: integer
          minimum: 1
        label:
          type: string
          minLength: 1
        description:
          type: string
          nullable: true

    SuccessionPlanStatus:
      type: string
      enum: [active, under_review, closed]

    SuccessionPlanSummary:
      type: object
      required: [id, positionId, status]
      description: Carries no confidential note — see §30.17.
      properties:
        id:
          type: integer
        positionId:
          type: integer
        status:
          $ref: "#/components/schemas/SuccessionPlanStatus"
        reviewDueAt:
          type: string
          nullable: true
        createdAt:
          type: string

    SuccessionPlan:
      type: object
      required: [id, organizationId, positionId, status]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        positionId:
          type: integer
        status:
          $ref: "#/components/schemas/SuccessionPlanStatus"
        criticalityNotes:
          type: string
          nullable: true
          description: Confidential. Returned only on the sensitive-read path.
        reviewDueAt:
          type: string
          nullable: true
        closedAt:
          type: string
          nullable: true
        closedBy:
          type: integer
          nullable: true
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string

    CreateSuccessionPlanInput:
      type: object
      required: [positionId]
      properties:
        positionId:
          type: integer
        criticalityNotes:
          type: string
          nullable: true
        reviewDueAt:
          type: string
          nullable: true

    UpdateSuccessionPlanInput:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/SuccessionPlanStatus"
        criticalityNotes:
          type: string
          nullable: true
        reviewDueAt:
          type: string
          nullable: true

    SuccessionCandidateStatus:
      type: string
      enum: [active, removed, appointed]

    SuccessionCandidate:
      type: object
      required: [id, organizationId, planId, employeeId, status]
      description: >-
        There is deliberately no rank, score or ordering position on this object
        (§30.12). readinessOrdinal groups candidates into bands; it does not
        order them within one.
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        planId:
          type: integer
        employeeId:
          type: integer
        status:
          $ref: "#/components/schemas/SuccessionCandidateStatus"
        readinessLevelId:
          type: integer
          nullable: true
        readinessLabel:
          type: string
          nullable: true
        readinessOrdinal:
          type: integer
          nullable: true
        rationale:
          type: string
          nullable: true
          description: Confidential.
        nominatedByUserId:
          type: integer
          nullable: true
        nominatedAt:
          type: string
          nullable: true
        removedAt:
          type: string
          nullable: true
        removedBy:
          type: integer
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string

    NominateSuccessionCandidateInput:
      type: object
      required: [employeeId]
      properties:
        employeeId:
          type: integer
        readinessLevelId:
          type: integer
          nullable: true
        rationale:
          type: string
          nullable: true

    SetSuccessionReadinessInput:
      type: object
      required: [readinessLevelId]
      properties:
        readinessLevelId:
          type: integer
        notes:
          type: string
          nullable: true

    RemoveSuccessionCandidateInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1

    SuccessionCandidateEventType:
      type: string
      enum:
        [
          nominated,
          readiness_changed,
          rationale_updated,
          removed,
          reinstated,
          appointed_elsewhere,
        ]

    SuccessionCandidateEvent:
      type: object
      required: [id, organizationId, candidateId, eventType, occurredAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        candidateId:
          type: integer
        eventType:
          $ref: "#/components/schemas/SuccessionCandidateEventType"
        previousReadinessLevelId:
          type: integer
          nullable: true
        newReadinessLevelId:
          type: integer
          nullable: true
        notes:
          type: string
          nullable: true
        actorUserId:
          type: integer
          nullable: true
        actorMembershipId:
          type: integer
          nullable: true
        occurredAt:
          type: string
        createdAt:
          type: string

    SuccessionCoverageRow:
      type: object
      required: [planId, positionId, positionTitle, status, candidateCount, nearestTermReadyCount, hasNoCandidates]
      properties:
        planId:
          type: integer
        positionId:
          type: integer
        positionTitle:
          type: string
        status:
          type: string
        candidateCount:
          type: integer
        nearestTermReadyCount:
          type: integer
          description: Candidates in the nearest-term readiness band. Never a rank.
        hasNoCandidates:
          type: boolean

    SuccessionCoverageResult:
      type: object
      required: [coverage]
      properties:
        coverage:
          type: array
          items:
            $ref: "#/components/schemas/SuccessionCoverageRow"

    DevelopmentActionStatus:
      type: string
      enum: [open, in_progress, completed, cancelled]

    DevelopmentAction:
      type: object
      required: [id, organizationId, employeeId, action, status]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        skillId:
          type: integer
          nullable: true
        successionCandidateId:
          type: integer
          nullable: true
        action:
          type: string
        status:
          $ref: "#/components/schemas/DevelopmentActionStatus"
        targetDate:
          type: string
          nullable: true
        learningCourseId:
          type: integer
          nullable: true
        learningEnrollmentId:
          type: integer
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true
        responsibleMembershipId:
          type: integer
          nullable: true
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string

    CreateDevelopmentActionInput:
      type: object
      required: [employeeId, action]
      properties:
        employeeId:
          type: integer
        action:
          type: string
          minLength: 1
        skillId:
          type: integer
          nullable: true
        successionCandidateId:
          type: integer
          nullable: true
        targetDate:
          type: string
          nullable: true
        learningCourseId:
          type: integer
          nullable: true
        learningEnrollmentId:
          type: integer
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true

    UpdateDevelopmentActionInput:
      type: object
      properties:
        action:
          type: string
          minLength: 1
        status:
          $ref: "#/components/schemas/DevelopmentActionStatus"
        targetDate:
          type: string
          nullable: true

    EmploymentTermType:
      type: string
      enum: [permanent, fixed_term]
    EmploymentTermStatus:
      type: string
      enum: [active, superseded, closed]
    TermExpiryState:
      type: string
      enum: [not_applicable, current, expiring_soon, expired]
    EmploymentAssignmentType:
      type: string
      enum: [acting, secondment]
    SecondmentDestinationType:
      type: string
      enum: [internal, external]

    EmploymentTerm:
      type: object
      required: [id, organizationId, employeeId, termType, startDate, status]
      properties:
        id: { type: integer }
        organizationId: { type: integer }
        employeeId: { type: integer }
        termType: { $ref: "#/components/schemas/EmploymentTermType" }
        startDate: { type: string, format: date-time }
        endDate: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/EmploymentTermStatus" }
        renewedFromTermId: { type: integer, nullable: true }
        reason: { type: string, nullable: true }
        closedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        expiryState:
          allOf:
            - $ref: "#/components/schemas/TermExpiryState"
          nullable: true
          description: Derived from the term's dates and the current instant — never stored.

    EmploymentTermList:
      type: object
      required: [terms]
      properties:
        terms:
          type: array
          items:
            $ref: "#/components/schemas/EmploymentTerm"

    EmploymentTermRenewal:
      type: object
      required: [previous, renewed]
      properties:
        previous: { $ref: "#/components/schemas/EmploymentTerm" }
        renewed: { $ref: "#/components/schemas/EmploymentTerm" }

    ExpiringTermItem:
      type: object
      required: [term, state]
      properties:
        term: { $ref: "#/components/schemas/EmploymentTerm" }
        state: { $ref: "#/components/schemas/TermExpiryState" }
        employeeName: { type: string, nullable: true }

    ExpiringTermList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ExpiringTermItem"

    CreateEmploymentTermInput:
      type: object
      required: [termType, startDate]
      properties:
        termType: { $ref: "#/components/schemas/EmploymentTermType" }
        startDate: { type: string, format: date-time }
        endDate: { type: string, format: date-time, nullable: true }
        reason: { type: string, nullable: true, maxLength: 1000 }

    RenewEmploymentTermInput:
      type: object
      required: [termType, startDate]
      properties:
        termType: { $ref: "#/components/schemas/EmploymentTermType" }
        startDate: { type: string, format: date-time }
        endDate: { type: string, format: date-time, nullable: true }
        reason: { type: string, nullable: true, maxLength: 1000 }

    CloseEmploymentTermInput:
      type: object
      properties:
        reason: { type: string, nullable: true, maxLength: 1000 }

    EmploymentAssignment:
      type: object
      required: [id, organizationId, employeeId, assignmentType, startDate]
      properties:
        id: { type: integer }
        organizationId: { type: integer }
        employeeId: { type: integer }
        assignmentType: { $ref: "#/components/schemas/EmploymentAssignmentType" }
        actingPositionId: { type: integer, nullable: true }
        actingPositionTitle: { type: string, nullable: true }
        actingDepartmentId: { type: integer, nullable: true }
        destinationDescription: { type: string, nullable: true }
        destinationType:
          allOf:
            - $ref: "#/components/schemas/SecondmentDestinationType"
          nullable: true
        startDate: { type: string, format: date-time }
        expectedEndDate: { type: string, format: date-time, nullable: true }
        actualEndDate: { type: string, format: date-time, nullable: true }
        reason: { type: string, nullable: true }
        endReason: { type: string, nullable: true }
        overdue:
          type: boolean
          description: Derived — open past its expected end. Never stored, and never acted on automatically.
        createdAt: { type: string, format: date-time }

    EmploymentAssignmentList:
      type: object
      required: [assignments]
      properties:
        assignments:
          type: array
          items:
            $ref: "#/components/schemas/EmploymentAssignment"

    StartAssignmentInput:
      type: object
      required: [assignmentType, startDate]
      properties:
        assignmentType: { $ref: "#/components/schemas/EmploymentAssignmentType" }
        actingPositionId: { type: integer, nullable: true }
        actingDepartmentId: { type: integer, nullable: true }
        destinationDescription: { type: string, nullable: true, maxLength: 500 }
        destinationType:
          allOf:
            - $ref: "#/components/schemas/SecondmentDestinationType"
          nullable: true
        startDate: { type: string, format: date-time }
        expectedEndDate: { type: string, format: date-time, nullable: true }
        reason: { type: string, nullable: true, maxLength: 1000 }

    EndAssignmentInput:
      type: object
      required: [actualEndDate]
      properties:
        actualEndDate: { type: string, format: date-time }
        endReason: { type: string, nullable: true, maxLength: 1000 }

    ProbationState:
      type: object
      required: [employeeId, onProbation, extensionCount, unsuccessfulOutcomeRecorded]
      properties:
        employeeId: { type: integer }
        onProbation: { type: boolean }
        probationEndDate: { type: string, format: date-time, nullable: true }
        probationEndSource:
          type: string
          enum: [extension_event, employee_record, none]
        extensionCount: { type: integer }
        unsuccessfulOutcomeRecorded:
          type: boolean
          description: An outcome was recorded. Employment status is unaffected by it.

    ExtendProbationInput:
      type: object
      required: [newProbationEndDate, effectiveDate, reason]
      properties:
        newProbationEndDate: { type: string, format: date-time }
        effectiveDate: { type: string, format: date-time }
        reason: { type: string, minLength: 1, maxLength: 1000 }
        probationReviewId: { type: integer, nullable: true }

    RecordProbationOutcomeInput:
      type: object
      required: [effectiveDate, reason]
      properties:
        effectiveDate: { type: string, format: date-time }
        reason: { type: string, minLength: 1, maxLength: 1000 }
        probationReviewId: { type: integer, nullable: true }

    EmploymentLifecycleEvent:
      type: object
      required: [id, eventType, effectiveDate, isSystemEvent, label]
      properties:
        id: { type: integer }
        eventType:
          type: string
          description: Free text. Imported history may carry any value.
        label:
          type: string
          description: Friendly label for a registered event, or the raw string for imported history.
        isSystemEvent:
          type: boolean
          description: >-
            False for imported/legacy history. Such events are displayed as
            recorded and never participate in state derivation.
        effectiveDate: { type: string, format: date-time }
        previousState: { nullable: true }
        newState: { nullable: true }
        createdAt: { type: string, format: date-time }

    EmploymentLifecycleState:
      type: object
      required: [employeeId, history, probation]
      properties:
        employeeId: { type: integer }
        substantivePositionId: { type: integer, nullable: true }
        substantivePositionTitle: { type: string, nullable: true }
        currentTerm:
          allOf:
            - $ref: "#/components/schemas/EmploymentTerm"
          nullable: true
        currentActingAssignment:
          allOf:
            - $ref: "#/components/schemas/EmploymentAssignment"
          nullable: true
        currentSecondment:
          allOf:
            - $ref: "#/components/schemas/EmploymentAssignment"
          nullable: true
        probation: { $ref: "#/components/schemas/ProbationState" }
        history:
          type: array
          items:
            $ref: "#/components/schemas/EmploymentLifecycleEvent"

    SeparationWarning:
      type: object
      required: [kind, count, message]
      properties:
        kind:
          type: string
          enum: [assets, office_inventory, personnel_file]
        count: { type: integer }
        message: { type: string }

    SeparationReadiness:
      type: object
      required: [employeeId, warnings, blocksSeparation]
      properties:
        employeeId: { type: integer }
        warnings:
          type: array
          items:
            $ref: "#/components/schemas/SeparationWarning"
        blocksSeparation:
          type: boolean
          description: Always false — these are advisory only and never prevent separation.

    OnboardingTemplateStatus:
      type: string
      enum: [draft, active, archived]
    OnboardingInstanceStatus:
      type: string
      enum: [not_started, in_progress, completed, cancelled]
    OnboardingTaskStatus:
      type: string
      enum: [pending, completed, waived, cancelled]
    OnboardingTaskKind:
      type: string
      enum:
        - general
        - document
        - acknowledgement
        - induction
        - asset_reference
        - inventory_reference
        - access_reference
        - payroll_reference
        - personnel_file_reference
    OnboardingResponsibilityResolver:
      type: string
      enum: [employee_self, reporting_manager, department_head, permission_holder, specific_membership]
    OnboardingDueBasis:
      type: string
      enum: [onboarding_start, commencement_date, dependency_completion]
    InductionDeliveryMode:
      type: string
      enum: [in_person, virtual, hybrid]
    AcknowledgementStatus:
      type: string
      enum: [pending, acknowledged]
    AcknowledgementAudience:
      type: string
      enum: [all_employees, branch, department, position, employment_type, specific_employees, onboarding]

    OnboardingTemplate:
      type: object
      required: [id, organizationId, name, createdAt]
      properties:
        id: { type: integer }
        organizationId: { type: integer }
        name: { type: string }
        description: { type: string, nullable: true }
        branchId: { type: integer, nullable: true }
        departmentId: { type: integer, nullable: true }
        positionId: { type: integer, nullable: true }
        employmentType: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }

    OnboardingTemplateVersion:
      type: object
      required: [id, organizationId, templateId, versionNumber, status]
      properties:
        id: { type: integer }
        organizationId: { type: integer }
        templateId: { type: integer }
        versionNumber: { type: integer }
        status: { $ref: "#/components/schemas/OnboardingTemplateStatus" }
        changeNote: { type: string, nullable: true }
        activatedAt: { type: string, format: date-time, nullable: true }
        archivedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }

    OnboardingTemplateVersionList:
      type: object
      required: [versions]
      properties:
        versions:
          type: array
          items:
            $ref: "#/components/schemas/OnboardingTemplateVersion"

    OnboardingTemplateWithVersion:
      type: object
      required: [template, version]
      properties:
        template: { $ref: "#/components/schemas/OnboardingTemplate" }
        version: { $ref: "#/components/schemas/OnboardingTemplateVersion" }

    OnboardingTemplateListItem:
      type: object
      required: [template, versionCount]
      properties:
        template: { $ref: "#/components/schemas/OnboardingTemplate" }
        activeVersion:
          allOf:
            - $ref: "#/components/schemas/OnboardingTemplateVersion"
          nullable: true
        versionCount: { type: integer }

    OnboardingTemplateList:
      type: object
      required: [templates]
      properties:
        templates:
          type: array
          items:
            $ref: "#/components/schemas/OnboardingTemplateListItem"

    OnboardingTemplateTask:
      type: object
      required: [id, organizationId, templateVersionId, displayOrder, title, taskKind, required, responsibleResolver]
      properties:
        id: { type: integer }
        organizationId: { type: integer }
        templateVersionId: { type: integer }
        displayOrder: { type: integer }
        title: { type: string }
        description: { type: string, nullable: true }
        taskKind: { $ref: "#/components/schemas/OnboardingTaskKind" }
        required: { type: boolean }
        responsibleResolver: { $ref: "#/components/schemas/OnboardingResponsibilityResolver" }
        responsiblePermissionKey: { type: string, nullable: true }
        responsibleMembershipId: { type: integer, nullable: true }
        dueBasis:
          allOf:
            - $ref: "#/components/schemas/OnboardingDueBasis"
          nullable: true
        dueOffsetDays: { type: integer, nullable: true }
        dependsOnTemplateTaskId: { type: integer, nullable: true }
        documentCategoryCode: { type: string, nullable: true }
        acknowledgementDocumentId: { type: integer, nullable: true }

    OnboardingTemplateTaskList:
      type: object
      required: [tasks]
      properties:
        tasks:
          type: array
          items:
            $ref: "#/components/schemas/OnboardingTemplateTask"

    CreateOnboardingTemplateInput:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 160 }
        description: { type: string, nullable: true }
        branchId: { type: integer, nullable: true }
        departmentId: { type: integer, nullable: true }
        positionId: { type: integer, nullable: true }
        employmentType: { type: string, nullable: true }

    UpdateOnboardingTemplateInput:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 160 }
        description: { type: string, nullable: true }
        branchId: { type: integer, nullable: true }
        departmentId: { type: integer, nullable: true }
        positionId: { type: integer, nullable: true }
        employmentType: { type: string, nullable: true }

    CreateOnboardingTemplateVersionInput:
      type: object
      properties:
        copyFromVersionId: { type: integer, nullable: true }
        changeNote: { type: string, nullable: true }

    OnboardingTaskDefinitionInput:
      type: object
      required: [title]
      properties:
        title: { type: string, minLength: 1, maxLength: 200 }
        description: { type: string, nullable: true }
        taskKind: { $ref: "#/components/schemas/OnboardingTaskKind" }
        required: { type: boolean }
        responsibleResolver: { $ref: "#/components/schemas/OnboardingResponsibilityResolver" }
        responsiblePermissionKey: { type: string, nullable: true }
        responsibleMembershipId: { type: integer, nullable: true }
        dueBasis:
          allOf:
            - $ref: "#/components/schemas/OnboardingDueBasis"
          nullable: true
        dueOffsetDays: { type: integer, nullable: true }
        dependsOnTemplateTaskId: { type: integer, nullable: true }
        documentCategoryCode: { type: string, nullable: true }
        acknowledgementDocumentId: { type: integer, nullable: true }

    OnboardingInstance:
      type: object
      required: [id, organizationId, employeeId, templateId, templateVersionId, status, startDate]
      properties:
        id: { type: integer }
        organizationId: { type: integer }
        employeeId: { type: integer }
        templateId: { type: integer }
        templateVersionId: { type: integer }
        status: { $ref: "#/components/schemas/OnboardingInstanceStatus" }
        startDate: { type: string, format: date-time }
        commencementDate: { type: string, format: date-time, nullable: true }
        candidateId: { type: integer, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancellationReason: { type: string, nullable: true }

    OnboardingTask:
      type: object
      required: [id, organizationId, instanceId, displayOrder, title, taskKind, required, status, overdue]
      properties:
        id: { type: integer }
        organizationId: { type: integer }
        instanceId: { type: integer }
        displayOrder: { type: integer }
        title: { type: string }
        description: { type: string, nullable: true }
        taskKind: { $ref: "#/components/schemas/OnboardingTaskKind" }
        required: { type: boolean }
        status: { $ref: "#/components/schemas/OnboardingTaskStatus" }
        dueAt: { type: string, format: date-time, nullable: true }
        overdue:
          type: boolean
          description: Derived from the due date and current state — never stored.
        completedAt: { type: string, format: date-time, nullable: true }
        completedByName: { type: string, nullable: true }
        completionNotes: { type: string, nullable: true }
        waivedAt: { type: string, format: date-time, nullable: true }
        waivedByName: { type: string, nullable: true }
        waiverReason: { type: string, nullable: true }
        documentRequirementId: { type: integer, nullable: true }
        responsibleResolver: { $ref: "#/components/schemas/OnboardingResponsibilityResolver" }
        responsibleBasis:
          type: string
          description: Why the current responsible party holds this task.
        responsibleMembershipIds:
          type: array
          items: { type: integer }

    OnboardingProgress:
      type: object
      required: [total, required, completed, waived, pending, cancelled, overdue, completionPercentage, status]
      properties:
        total: { type: integer }
        required: { type: integer }
        completed: { type: integer }
        waived: { type: integer }
        pending: { type: integer }
        cancelled: { type: integer }
        overdue: { type: integer }
        completionPercentage:
          type: integer
          description: Informational only — completion is decided by required-task state.
        status: { $ref: "#/components/schemas/OnboardingInstanceStatus" }

    OnboardingSummary:
      type: object
      required: [instance, progress]
      properties:
        instance: { $ref: "#/components/schemas/OnboardingInstance" }
        employeeName: { type: string, nullable: true }
        employeeNumber: { type: string, nullable: true }
        templateName: { type: string, nullable: true }
        progress: { $ref: "#/components/schemas/OnboardingProgress" }

    OnboardingSummaryList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/OnboardingSummary"

    OnboardingInductionDetail:
      type: object
      required: [id, taskId, rescheduleCount]
      properties:
        id: { type: integer }
        taskId: { type: integer }
        facilitatorMembershipId: { type: integer, nullable: true }
        scheduledAt: { type: string, format: date-time, nullable: true }
        deliveryMode:
          allOf:
            - $ref: "#/components/schemas/InductionDeliveryMode"
          nullable: true
        location: { type: string, nullable: true }
        meetingDetails: { type: string, nullable: true }
        attendedAt: { type: string, format: date-time, nullable: true }
        attendanceNotes: { type: string, nullable: true }
        rescheduleCount: { type: integer }
        lastRescheduledAt: { type: string, format: date-time, nullable: true }
        lastRescheduleReason: { type: string, nullable: true }

    OnboardingTaskDetail:
      allOf:
        - $ref: "#/components/schemas/OnboardingTask"
        - type: object
          properties:
            induction:
              allOf:
                - $ref: "#/components/schemas/OnboardingInductionDetail"
              nullable: true
            referenceSatisfied: { type: boolean, nullable: true }
            referenceDetail: { type: string, nullable: true }

    OnboardingDetail:
      type: object
      required: [instance, tasks, progress, acknowledgements]
      properties:
        instance: { $ref: "#/components/schemas/OnboardingInstance" }
        employeeName: { type: string, nullable: true }
        employeeNumber: { type: string, nullable: true }
        templateName: { type: string, nullable: true }
        templateVersionNumber: { type: integer, nullable: true }
        progress: { $ref: "#/components/schemas/OnboardingProgress" }
        tasks:
          type: array
          items:
            $ref: "#/components/schemas/OnboardingTaskDetail"
        acknowledgements:
          type: array
          items:
            $ref: "#/components/schemas/DocumentAcknowledgement"

    StartOnboardingInput:
      type: object
      required: [employeeId]
      properties:
        employeeId: { type: integer }
        templateVersionId:
          type: integer
          nullable: true
          description: Optional override; otherwise the most specific applicable active template is used.

    CancelOnboardingInput:
      type: object
      required: [reason]
      properties:
        reason: { type: string, minLength: 1, maxLength: 1000 }

    AddSupplementaryTaskInput:
      type: object
      required: [title, reason]
      properties:
        title: { type: string, minLength: 1, maxLength: 200 }
        description: { type: string, nullable: true }
        required: { type: boolean }
        reason: { type: string, minLength: 1, maxLength: 1000 }

    CompleteOnboardingTaskInput:
      type: object
      properties:
        notes: { type: string, nullable: true, maxLength: 1000 }

    WaiveOnboardingTaskInput:
      type: object
      required: [reason]
      properties:
        reason: { type: string, minLength: 1, maxLength: 1000 }

    ScheduleInductionInput:
      type: object
      properties:
        facilitatorMembershipId: { type: integer, nullable: true }
        scheduledAt: { type: string, format: date-time, nullable: true }
        deliveryMode:
          allOf:
            - $ref: "#/components/schemas/InductionDeliveryMode"
          nullable: true
        location: { type: string, nullable: true, maxLength: 300 }
        meetingDetails: { type: string, nullable: true, maxLength: 1000 }
        rescheduleReason: { type: string, nullable: true, maxLength: 500 }

    RecordInductionAttendanceInput:
      type: object
      required: [attendedAt]
      properties:
        attendedAt: { type: string, format: date-time }
        attendanceNotes: { type: string, nullable: true, maxLength: 1000 }

    DocumentAcknowledgement:
      type: object
      required: [id, organizationId, employeeId, documentId, documentVersionId, status, audience, assignedAt]
      properties:
        id: { type: integer }
        organizationId: { type: integer }
        employeeId: { type: integer }
        documentId: { type: integer }
        documentVersionId: { type: integer }
        documentTitle: { type: string, nullable: true }
        versionNumber: { type: integer, nullable: true }
        status: { $ref: "#/components/schemas/AcknowledgementStatus" }
        audience: { $ref: "#/components/schemas/AcknowledgementAudience" }
        onboardingInstanceId: { type: integer, nullable: true }
        assignedAt: { type: string, format: date-time }
        dueAt: { type: string, format: date-time, nullable: true }
        acknowledgedAt: { type: string, format: date-time, nullable: true }
        acknowledgedByName: { type: string, nullable: true }

    DocumentAcknowledgementList:
      type: object
      required: [acknowledgements]
      properties:
        acknowledgements:
          type: array
          items:
            $ref: "#/components/schemas/DocumentAcknowledgement"

    AssignAcknowledgementInput:
      type: object
      required: [documentId, audience]
      properties:
        documentId: { type: integer }
        audience: { $ref: "#/components/schemas/AcknowledgementAudience" }
        branchId: { type: integer, nullable: true }
        departmentId: { type: integer, nullable: true }
        positionId: { type: integer, nullable: true }
        employmentType: { type: string, nullable: true }
        employeeIds:
          type: array
          nullable: true
          items: { type: integer }
        dueAt: { type: string, format: date-time, nullable: true }

    AssignAcknowledgementResult:
      type: object
      required: [assigned, alreadyAssigned]
      properties:
        assigned: { type: integer }
        alreadyAssigned: { type: integer }

    RaiseReacknowledgementsResult:
      type: object
      required: [raised]
      properties:
        raised: { type: integer }

    MyOnboarding:
      type: object
      required: [acknowledgements]
      properties:
        onboarding:
          allOf:
            - $ref: "#/components/schemas/OnboardingDetail"
          nullable: true
        acknowledgements:
          type: array
          items:
            $ref: "#/components/schemas/DocumentAcknowledgement"

    OnboardingResponsibilityItem:
      type: object
      required: [task, instanceId, employeeId]
      properties:
        task: { $ref: "#/components/schemas/OnboardingTask" }
        instanceId: { type: integer }
        employeeId: { type: integer }
        employeeName: { type: string, nullable: true }

    OnboardingResponsibilityList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/OnboardingResponsibilityItem"
    HealthStatus:
      type: object
      properties:
        status:
          type: string
        version:
          type: string
      required:
        - status

    ReadinessStatus:
      type: object
      properties:
        status:
          type: string
          enum: [ready, not_ready]
        database:
          type: string
          enum: [ok, error]
      required:
        - status
        - database

    ApiError:
      type: object
      properties:
        error:
          type: string
      required:
        - error

    MessageResponse:
      type: object
      properties:
        message:
          type: string
      required:
        - message

    LoginInput:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
        password:
          type: string
          minLength: 6

    ForgotPasswordInput:
      type: object
      required: [email]
      properties:
        email:
          type: string

    AuthSession:
      type: object
      required: [user, token]
      properties:
        user:
          $ref: "#/components/schemas/UserProfile"
        token:
          type: string

    UserProfile:
      type: object
      required: [id, email, firstName, lastName, role, organizationId]
      properties:
        id:
          type: integer
        email:
          type: string
        firstName:
          type: string
        lastName:
          type: string
        role:
          type: string
          enum: [super_admin, org_admin, hr_manager, employee]
        organizationId:
          type: integer
        activeOrganizationId:
          type: ["integer", "null"]
          description: >-
            The organization currently in scope for this session, resolved
            server-side from the session's switched organization (falling
            back to organizationId, then to any other active membership).
            Org-scoped requests should use this value, not organizationId.
        avatarUrl:
          type: ["string", "null"]
        jobTitle:
          type: ["string", "null"]
        department:
          type: ["string", "null"]
        phoneNumber:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    UserProfileUpdate:
      type: object
      properties:
        firstName:
          type: string
        lastName:
          type: string
        jobTitle:
          type: ["string", "null"]
        department:
          type: ["string", "null"]
        phoneNumber:
          type: ["string", "null"]

    Organization:
      type: object
      required: [id, tenantUuid, name, slug, type, status]
      properties:
        id:
          type: integer
          description: Internal primary key. The security boundary every tenant-owned row references. Immutable.
        tenantUuid:
          type: string
          description: Immutable, globally unique tenant identity. Quote this in support, maintenance and change requests. Generated by the database, never client-supplied, never changed.
        name:
          type: string
          description: Display name only. Never a security boundary; renaming changes nothing about data ownership.
        slug:
          type: string
          description: Human-readable tenant code. Unique; immutable after creation.
        type:
          type: string
          enum: [business, church, ngo, school, hospital, hotel, government, other]
        status:
          type: string
          enum: [active, suspended, trial]
        logoUrl:
          type: ["string", "null"]
        industry:
          type: ["string", "null"]
        employeeCount:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    CreateOrganizationInput:
      type: object
      required: [name, slug, type]
      properties:
        name:
          type: string
          minLength: 1
        slug:
          type: string
          minLength: 2
          maxLength: 63
          pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
          description: Tenant code. Lowercase letters, digits and single hyphens; unique across the platform; immutable after creation.
        type:
          type: string
          enum: [business, church, ngo, school, hospital, hotel, government, other]

    UpdateOrganizationInput:
      type: object
      description: The slug (tenant code), id and tenantUuid are immutable and are not accepted here.
      properties:
        name:
          type: string
          minLength: 1
        type:
          type: string
          enum: [business, church, ngo, school, hospital, hotel, government, other]
        logoUrl:
          type: ["string", "null"]
        industry:
          type: ["string", "null"]
        employeeCount:
          type: ["integer", "null"]

    OrganizationDomain:
      type: object
      required: [id, organizationId, hostname, domainType, status, isPrimary, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        hostname:
          type: string
        domainType:
          type: string
          enum: [platform_subdomain, custom_domain]
        status:
          type: string
          enum: [pending, active, disabled]
        isPrimary:
          type: boolean
        verifiedAt:
          type: ["string", "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateOrganizationDomainInput:
      type: object
      required: [hostname, domainType]
      properties:
        hostname:
          type: string
          minLength: 1
        domainType:
          type: string
          enum: [platform_subdomain, custom_domain]

    PlatformUserSummary:
      type: object
      required: [id, email, firstName, lastName, disabledAt, disabledBy, disabledReason]
      properties:
        id:
          type: integer
        email:
          type: string
        firstName:
          type: string
        lastName:
          type: string
        disabledAt:
          type: ["string", "null"]
          format: date-time
        disabledBy:
          type: ["integer", "null"]
        disabledReason:
          type: ["string", "null"]

    DisablePlatformUserInput:
      type: object
      properties:
        reason:
          type: string
          minLength: 1

    OrganizationStatusChangeInput:
      type: object
      required: [confirmSlug]
      properties:
        confirmSlug:
          type: string
          minLength: 1
          description: Must equal the target organization's slug (tenant code). A typed confirmation of the target.
        reason:
          type: string
          maxLength: 2000
          description: Why the status is being changed. Recorded in the audit event.

    TenantIdentity:
      type: object
      required: [organization, runtime, installations, domains, modules, featureFlags]
      properties:
        organization:
          type: object
          required: [id, tenantUuid, slug, name, type, status, createdAt]
          properties:
            id:
              type: integer
            tenantUuid:
              type: string
            slug:
              type: string
            name:
              type: string
            type:
              type: string
              enum: [business, church, ngo, school, hospital, hotel, government, other]
            status:
              type: string
              enum: [active, suspended, trial]
            createdAt:
              type: string
              format: date-time
        runtime:
          type: object
          description: Release identity of the process that served this request.
          required: [environment, appVersion, installationKey]
          properties:
            environment:
              type: string
            appVersion:
              type: string
            installationKey:
              type: ["string", "null"]
        installations:
          type: array
          items:
            $ref: "#/components/schemas/TenantIdentityInstallation"
        domains:
          type: array
          items:
            type: object
            required: [id, hostname, domainType, status, isPrimary]
            properties:
              id:
                type: integer
              hostname:
                type: string
              domainType:
                type: string
                enum: [platform_subdomain, custom_domain]
              status:
                type: string
                enum: [pending, active, disabled]
              isPrimary:
                type: boolean
        modules:
          type: array
          items:
            type: object
            required: [key, name, status, enabled]
            properties:
              key:
                type: string
              name:
                type: string
              status:
                type: string
                enum: [active, beta, hidden, deprecated]
              enabled:
                type: boolean
        featureFlags:
          type: array
          items:
            $ref: "#/components/schemas/FeatureFlagState"

    TenantIdentityInstallation:
      type: object
      required: [id, installationKey, name, environmentType, hostingModel, hostingProvider, primaryDomain, applicationVersion, gitCommit, migrationVersion, deployedAt, status, linkedAt]
      properties:
        id:
          type: integer
        installationKey:
          type: string
        name:
          type: string
        environmentType:
          type: string
          enum: [development, staging, demo, production]
        hostingModel:
          type: string
          enum: [shared, dedicated_owner_managed, dedicated_customer_managed, other]
        hostingProvider:
          type: ["string", "null"]
        primaryDomain:
          type: ["string", "null"]
        applicationVersion:
          type: ["string", "null"]
        gitCommit:
          type: ["string", "null"]
        migrationVersion:
          type: ["string", "null"]
        deployedAt:
          type: ["string", "null"]
          format: date-time
        status:
          type: string
          enum: [active, inactive, decommissioned]
        linkedAt:
          type: string
          format: date-time

    FeatureFlagState:
      type: object
      required: [key, level, description, moduleKey, enabled]
      properties:
        key:
          type: string
        level:
          type: string
          enum: [feature, tenant_extension]
          description: feature = a platform capability enabled for selected tenants (Level 2); tenant_extension = a controlled tenant-specific behaviour (Level 3).
        description:
          type: string
        moduleKey:
          type: ["string", "null"]
        enabled:
          type: boolean

    SetFeatureFlagInput:
      type: object
      required: [enabled, reason]
      properties:
        enabled:
          type: boolean
        reason:
          type: string
          minLength: 1
          maxLength: 2000
          description: Why this flag is being changed for this tenant. Recorded in the audit event.

    Installation:
      type: object
      required:
        [
          id,
          installationKey,
          name,
          environmentType,
          hostingModel,
          hostingProvider,
          primaryDomain,
          applicationVersion,
          gitCommit,
          migrationVersion,
          deployedAt,
          healthUrl,
          extensionProfile,
          status,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: integer
        installationKey:
          type: string
        name:
          type: string
        environmentType:
          type: string
          enum: [development, staging, demo, production]
        hostingModel:
          type: string
          enum: [shared, dedicated_owner_managed, dedicated_customer_managed, other]
        hostingProvider:
          type: ["string", "null"]
        primaryDomain:
          type: ["string", "null"]
        applicationVersion:
          type: ["string", "null"]
        gitCommit:
          type: ["string", "null"]
        migrationVersion:
          type: ["string", "null"]
        deployedAt:
          type: ["string", "null"]
          format: date-time
        healthUrl:
          type: ["string", "null"]
        extensionProfile:
          type: ["string", "null"]
        status:
          type: string
          enum: [active, inactive, decommissioned]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    RestoreRequest:
      type: object
      required: [id, installationId, environmentSnapshot, purpose, status, restorePointType, completeness, preRestoreCheckpointState, quiescenceState, affectedOrganizationIdsSnapshot, reason, requestedByUserId, submittedAt]
      properties:
        id: { type: integer }
        installationId: { type: integer }
        environmentSnapshot: { type: string }
        purpose:
          type: string
          enum: [recovery, test]
        status:
          type: string
          enum: [submitted, approved, rejected, dispatched, executing, succeeded, failed, validated, validation_failed, cancelled, expired]
        restorePointType:
          type: string
          enum: [backup_run, pitr, provider_snapshot]
        backupRunId: { type: integer, nullable: true }
        pitrTimestamp: { type: string, format: date-time, nullable: true }
        providerReference: { type: string, nullable: true }
        recoveryPointAt: { type: string, format: date-time, nullable: true }
        completeness:
          type: string
          enum: [complete, partial, unknown]
          description: Derived from backup evidence. Unknown is never complete.
        incompleteComponents: { nullable: true }
        incompleteAcknowledgedAt: { type: string, format: date-time, nullable: true }
        incompleteAcknowledgementNote: { type: string, nullable: true }
        preRestoreCheckpointState:
          type: string
          enum: [not_required, required, satisfied, unsupported_acknowledged]
        preRestoreBackupRunId: { type: integer, nullable: true }
        preRestoreExceptionNote: { type: string, nullable: true }
        quiescenceState:
          type: string
          enum: [not_required, required, requested, confirmed, unavailable, released]
        quiescenceEvidence: { type: string, nullable: true }
        affectedOrganizationIdsSnapshot:
          description: The blast radius the requester was shown, frozen at submission.
        reason: { type: string }
        requestedByUserId: { type: integer }
        submittedAt: { type: string, format: date-time }
        statusChangedAt: { type: string, format: date-time, nullable: true }
        statusDetail: { type: string, nullable: true }
        applicationVersionAtRequest: { type: string, nullable: true }
        migrationVersionAtRequest: { type: string, nullable: true }
        driftAcknowledgedAt: { type: string, format: date-time, nullable: true }
    RestoreApproval:
      type: object
      required: [id, requestId, decision, approverUserId, decidedAt, affectedOrganizationIdsAtDecision, completenessAtDecision, preRestoreCheckpointAtDecision]
      properties:
        id: { type: integer }
        requestId: { type: integer }
        decision:
          type: string
          enum: [approved, rejected]
        approverUserId: { type: integer }
        decidedAt: { type: string, format: date-time }
        comment: { type: string, nullable: true }
        affectedOrganizationIdsAtDecision:
          description: What the approver actually saw, frozen.
        completenessAtDecision:
          type: string
          enum: [complete, partial, unknown]
        preRestoreCheckpointAtDecision:
          type: string
          enum: [not_required, required, satisfied, unsupported_acknowledged]
        expiresAt: { type: string, format: date-time, nullable: true }
        invalidatedAt: { type: string, format: date-time, nullable: true }
        invalidationReason: { type: string, nullable: true }
    RestoreExecution:
      type: object
      required: [id, requestId, attemptNumber, status, idempotencyKey, dispatchedAt]
      properties:
        id: { type: integer }
        requestId: { type: integer }
        attemptNumber: { type: integer }
        status:
          type: string
          enum: [dispatched, accepted, started, succeeded, failed]
        idempotencyKey: { type: string }
        dispatchedByUserId: { type: integer, nullable: true }
        dispatchedAt: { type: string, format: date-time }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        executorType: { type: string, nullable: true }
        externalReference: { type: string, nullable: true }
        failureCategory: { type: string, nullable: true }
        statusDetail: { type: string, nullable: true }
    RestoreValidation:
      type: object
      required: [id, requestId, checkKey, result, source, observedAt]
      properties:
        id: { type: integer }
        requestId: { type: integer }
        executionId: { type: integer, nullable: true }
        checkKey: { type: string }
        result:
          type: string
          enum: [passed, failed, unknown, not_applicable]
        source:
          type: string
          enum: [automated, operator_attestation]
        detail: { type: string, nullable: true }
        observedAt: { type: string, format: date-time }
    RestoreDispatchPrecheck:
      type: object
      required: [ok, blockers, driftRequiringAcknowledgement]
      properties:
        ok: { type: boolean }
        blockers:
          type: array
          items: { type: string }
        driftRequiringAcknowledgement:
          type: array
          items: { type: string }
    RestoreObservedMetrics:
      type: object
      description: Facts about an execution. No SLA compliance is asserted.
      properties:
        recoveryPointAgeMs: { type: integer, nullable: true }
        executionDurationMs: { type: integer, nullable: true }
        validationCompletedAt: { type: string, format: date-time, nullable: true }
    RestoreRequestDetail:
      type: object
      required: [request, approvals, executions, validations, precheck, metrics]
      properties:
        request:
          $ref: "#/components/schemas/RestoreRequest"
        approvals:
          type: array
          items:
            $ref: "#/components/schemas/RestoreApproval"
        executions:
          type: array
          items:
            $ref: "#/components/schemas/RestoreExecution"
        validations:
          type: array
          items:
            $ref: "#/components/schemas/RestoreValidation"
        precheck:
          $ref: "#/components/schemas/RestoreDispatchPrecheck"
        metrics:
          $ref: "#/components/schemas/RestoreObservedMetrics"
    SubmitRestoreRequestInput:
      type: object
      required: [purpose, restorePointType, reason, confirmationPhrase]
      properties:
        purpose:
          type: string
          enum: [recovery, test]
        restorePointType:
          type: string
          enum: [backup_run, pitr, provider_snapshot]
        backupRunId: { type: integer, nullable: true }
        pitrTimestamp: { type: string, format: date-time, nullable: true }
        providerReference: { type: string, nullable: true }
        reason: { type: string }
        confirmationPhrase:
          type: string
          description: Must equal "RESTORE <installationKey> <ENVIRONMENT>" exactly.
        incompleteAcknowledgementNote: { type: string, nullable: true }
        preRestoreExceptionNote: { type: string, nullable: true }
        preRestoreBackupRunId: { type: integer, nullable: true }
        quiescenceRequired: { type: boolean }
    RestoreDecisionInput:
      type: object
      required: [decision]
      properties:
        decision:
          type: string
          enum: [approved, rejected]
        comment: { type: string, nullable: true }
        expiresAt:
          type: string
          format: date-time
          nullable: true
          description: Policy-driven only. There is no default expiry.
    RestoreCancelInput:
      type: object
      properties:
        reason: { type: string }
    RestoreQuiescenceInput:
      type: object
      required: [state]
      properties:
        state:
          type: string
          enum: [not_required, required, requested, confirmed, unavailable, released]
        evidence: { type: string, nullable: true }
    RestoreDispatchInput:
      type: object
      required: [idempotencyKey]
      properties:
        idempotencyKey: { type: string }
        executorType: { type: string, nullable: true }
    RestoreExecutionEvidenceInput:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [accepted, started, succeeded, failed]
        externalReference: { type: string, nullable: true }
        failureCategory: { type: string, nullable: true }
        statusDetail: { type: string, nullable: true }
    RestoreValidationInput:
      type: object
      required: [checkKey, result, source]
      properties:
        executionId: { type: integer, nullable: true }
        checkKey: { type: string }
        result:
          type: string
          enum: [passed, failed, unknown, not_applicable]
        source:
          type: string
          enum: [automated, operator_attestation]
        detail: { type: string, nullable: true }
    HealthSignal:
      type: object
      required: [key, state, detail]
      properties:
        key: { type: string }
        state:
          type: string
          enum: [healthy, degraded, unhealthy, unknown, stale]
        detail:
          type: string
          description: Plain operator-facing reason, so state never depends on colour alone.
    InstallationHealth:
      type: object
      required: [installationId, installationKey, name, overall, signals, affectedOrganizations]
      properties:
        installationId: { type: integer }
        installationKey: { type: string }
        name: { type: string }
        environmentType: { type: string }
        hostingModel: { type: string }
        status: { type: string }
        currentApplicationVersion: { type: string, nullable: true }
        currentGitCommit: { type: string, nullable: true }
        currentMigrationVersion: { type: string, nullable: true }
        deployedAt: { type: string, format: date-time, nullable: true }
        overall:
          type: string
          enum: [healthy, degraded, unhealthy, unknown, stale]
          description: The worst signal present. Deliberately not a numeric score.
        signals:
          type: array
          items:
            $ref: "#/components/schemas/HealthSignal"
        affectedOrganizations:
          type: array
          description: Derived through installation_organizations, never stored per event.
          items:
            type: object
            required: [organizationId, name]
            properties:
              organizationId: { type: integer }
              name: { type: string }
    InstallationDeployment:
      type: object
      required: [id, installationId, result, executorType, recordedAt]
      properties:
        id: { type: integer }
        installationId: { type: integer }
        applicationVersion: { type: string, nullable: true }
        gitCommit: { type: string, nullable: true }
        migrationVersion: { type: string, nullable: true }
        previousApplicationVersion: { type: string, nullable: true }
        previousGitCommit: { type: string, nullable: true }
        result:
          type: string
          enum: [in_progress, succeeded, failed, rolled_back]
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        executorType:
          type: string
          enum: [operator, ci_cd, installation_agent, provider, imported_evidence]
        externalReference: { type: string, nullable: true }
        rolledBackFromDeploymentId: { type: integer, nullable: true }
        notes: { type: string, nullable: true }
        recordedByUserId: { type: integer, nullable: true }
        recordedAt: { type: string, format: date-time }
    RecordDeploymentInput:
      type: object
      required: [result, executorType]
      properties:
        applicationVersion: { type: string, nullable: true }
        gitCommit: { type: string, nullable: true }
        migrationVersion: { type: string, nullable: true }
        result:
          type: string
          enum: [in_progress, succeeded, failed, rolled_back]
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        executorType:
          type: string
          enum: [operator, ci_cd, installation_agent, provider, imported_evidence]
        externalReference: { type: string, nullable: true }
        notes: { type: string, nullable: true }
    InstallationBackupPolicy:
      type: object
      required: [id, installationId, enabled, databaseCoverage, binaryStorageCoverage]
      properties:
        id: { type: integer }
        installationId: { type: integer }
        enabled: { type: integer }
        strategy:
          type: string
          nullable: true
          enum: [provider_managed, logical_dump, volume_snapshot, mixed]
        expectedFrequencyHours: { type: integer, nullable: true }
        targetRpoMinutes:
          type: integer
          nullable: true
          description: Configured target only. Null means unconfigured; there is no default SLA.
        targetRtoMinutes: { type: integer, nullable: true }
        retentionPolicyReference: { type: string, nullable: true }
        databaseCoverage:
          type: string
          enum: [covered, not_covered, unknown]
        binaryStorageCoverage:
          type: string
          enum: [covered, not_covered, unknown]
        executorType: { type: string, nullable: true }
        supportsBackupRequests: { type: integer }
        updatedByUserId: { type: integer, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    UpsertBackupPolicyInput:
      type: object
      properties:
        enabled: { type: boolean }
        strategy:
          type: string
          enum: [provider_managed, logical_dump, volume_snapshot, mixed]
        expectedFrequencyHours: { type: integer, nullable: true }
        targetRpoMinutes: { type: integer, nullable: true }
        targetRtoMinutes: { type: integer, nullable: true }
        retentionPolicyReference: { type: string, nullable: true }
        databaseCoverage:
          type: string
          enum: [covered, not_covered, unknown]
        binaryStorageCoverage:
          type: string
          enum: [covered, not_covered, unknown]
        executorType:
          type: string
          enum: [operator, ci_cd, installation_agent, provider, imported_evidence]
        supportsBackupRequests: { type: boolean }
    InstallationBackupRequest:
      type: object
      required: [id, installationId, status, reason, requestedAt]
      properties:
        id: { type: integer }
        installationId: { type: integer }
        status:
          type: string
          enum: [requested, accepted, executing, succeeded, failed, rejected, cancelled]
        backupType: { type: string, nullable: true }
        reason: { type: string }
        requestedByUserId: { type: integer, nullable: true }
        requestedAt: { type: string, format: date-time }
        statusChangedAt: { type: string, format: date-time, nullable: true }
        externalReference: { type: string, nullable: true }
        statusDetail: { type: string, nullable: true }
    RequestBackupInput:
      type: object
      required: [reason]
      properties:
        reason: { type: string }
        backupType:
          type: string
          enum: [provider_managed, logical_dump, volume_snapshot, mixed]
    UpdateBackupRequestStatusInput:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [accepted, executing, succeeded, failed, rejected, cancelled]
        externalReference: { type: string, nullable: true }
        statusDetail: { type: string, nullable: true }
    InstallationBackupRun:
      type: object
      required: [id, installationId, result, databaseResult, binaryStorageResult, executorType, evidenceReceivedAt]
      properties:
        id: { type: integer }
        installationId: { type: integer }
        requestId: { type: integer, nullable: true }
        backupType: { type: string, nullable: true }
        result:
          type: string
          enum: [succeeded, partial, failed]
          description: Derived from the component results, never supplied by the caller.
        databaseResult:
          type: string
          enum: [succeeded, failed, not_attempted, unknown]
        binaryStorageResult:
          type: string
          enum: [succeeded, failed, not_attempted, unknown]
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        recoveryPointAt: { type: string, format: date-time, nullable: true }
        sizeBytes: { type: integer, nullable: true }
        executorType: { type: string }
        externalReference: { type: string, nullable: true }
        failureCategory: { type: string, nullable: true }
        evidenceReceivedAt: { type: string, format: date-time }
        recordedByUserId: { type: integer, nullable: true }
        supersedesRunId: { type: integer, nullable: true }
    RecordBackupRunInput:
      type: object
      required: [databaseResult, binaryStorageResult]
      properties:
        requestId: { type: integer, nullable: true }
        backupType:
          type: string
          enum: [provider_managed, logical_dump, volume_snapshot, mixed]
        databaseResult:
          type: string
          enum: [succeeded, failed, not_attempted, unknown]
        binaryStorageResult:
          type: string
          enum: [succeeded, failed, not_attempted, unknown]
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        recoveryPointAt: { type: string, format: date-time, nullable: true }
        sizeBytes: { type: integer, nullable: true }
        executorType:
          type: string
          enum: [operator, ci_cd, installation_agent, provider, imported_evidence]
        externalReference: { type: string, nullable: true }
        failureCategory: { type: string, nullable: true }
        supersedesRunId: { type: integer, nullable: true }
    InstallationTelemetry:
      type: object
      required: [id, installationId, observedAt, executorType]
      properties:
        id: { type: integer }
        installationId: { type: integer }
        observedAt: { type: string, format: date-time }
        reportedApplicationVersion: { type: string, nullable: true }
        reportedGitCommit: { type: string, nullable: true }
        reportedMigrationVersion: { type: string, nullable: true }
        applicationHealthy: { type: integer, nullable: true }
        databaseReady: { type: integer, nullable: true }
        storageBackend: { type: string, nullable: true }
        storageHealthy: { type: integer, nullable: true }
        executorType: { type: string }
        recordedByUserId: { type: integer, nullable: true }
        updatedAt: { type: string, format: date-time }
    RecordTelemetryInput:
      type: object
      required: [observedAt]
      properties:
        observedAt: { type: string, format: date-time }
        reportedApplicationVersion: { type: string, nullable: true }
        reportedGitCommit: { type: string, nullable: true }
        reportedMigrationVersion: { type: string, nullable: true }
        applicationHealthy: { type: boolean, nullable: true }
        databaseReady: { type: boolean, nullable: true }
        storageBackend: { type: string, nullable: true }
        storageHealthy: { type: boolean, nullable: true }
        executorType:
          type: string
          enum: [operator, ci_cd, installation_agent, provider, imported_evidence]
    CreateInstallationInput:
      type: object
      required: [name, environmentType, hostingModel]
      properties:
        installationKey:
          type: string
          description: Stable identifier the deployed application's own environment configuration will carry. Server-generated if omitted.
          minLength: 1
        name:
          type: string
          minLength: 1
        environmentType:
          type: string
          enum: [development, staging, demo, production]
        hostingModel:
          type: string
          enum: [shared, dedicated_owner_managed, dedicated_customer_managed, other]
        hostingProvider:
          type: string
        primaryDomain:
          type: string
        applicationVersion:
          type: string
        gitCommit:
          type: string
        migrationVersion:
          type: string
        deployedAt:
          type: string
          format: date-time
        healthUrl:
          type: string
        extensionProfile:
          type: string

    UpdateInstallationInput:
      type: object
      description: installationKey is immutable and cannot be changed here.
      properties:
        name:
          type: string
          minLength: 1
        environmentType:
          type: string
          enum: [development, staging, demo, production]
        hostingModel:
          type: string
          enum: [shared, dedicated_owner_managed, dedicated_customer_managed, other]
        hostingProvider:
          type: ["string", "null"]
        primaryDomain:
          type: ["string", "null"]
        applicationVersion:
          type: ["string", "null"]
        gitCommit:
          type: ["string", "null"]
        migrationVersion:
          type: ["string", "null"]
        deployedAt:
          type: ["string", "null"]
          format: date-time
        healthUrl:
          type: ["string", "null"]
        extensionProfile:
          type: ["string", "null"]
        status:
          type: string
          enum: [active, inactive, decommissioned]

    InstallationOrganizationLink:
      type: object
      required: [id, installationId, organizationId, linkedAt, unlinkedAt]
      properties:
        id:
          type: integer
        installationId:
          type: integer
        organizationId:
          type: integer
        linkedAt:
          type: string
          format: date-time
        unlinkedAt:
          type: ["string", "null"]
          format: date-time

    LinkInstallationOrganizationInput:
      type: object
      required: [organizationId]
      properties:
        organizationId:
          type: integer

    BreakGlassGrant:
      type: object
      required:
        [
          id,
          actorUserId,
          targetOrganizationId,
          targetInstallationId,
          reason,
          scope,
          requestedAt,
          activatedAt,
          expiresAt,
          revokedAt,
          revokedBy,
          status,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: integer
        actorUserId:
          type: integer
        targetOrganizationId:
          type: integer
        targetInstallationId:
          type: ["integer", "null"]
        reason:
          type: string
        scope:
          type: array
          items:
            type: string
        requestedAt:
          type: string
          format: date-time
        activatedAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        revokedAt:
          type: ["string", "null"]
          format: date-time
        revokedBy:
          type: ["integer", "null"]
        status:
          type: string
          enum: [active, revoked]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateBreakGlassGrantInput:
      type: object
      required: [targetOrganizationId, reason, scope, expiresAt, confirm]
      properties:
        targetOrganizationId:
          type: integer
        targetInstallationId:
          type: integer
        reason:
          type: string
          minLength: 1
        scope:
          type: array
          minItems: 1
          items:
            type: string
          description: Explicit, real, read-only permission keys only (e.g. "employee.read"). Never "all permissions".
        expiresAt:
          type: string
          format: date-time
          description: Must be in the future and within the platform's configured maximum break-glass grant duration.
        confirm:
          type: boolean
          enum: [true]
          description: Explicit confirmation that this grant is being knowingly activated.
        metadata:
          type: object
          additionalProperties: true
          description: Safe, non-sensitive context only (e.g. a support-ticket reference) — never customer HR data.

    TenantContext:
      type: object
      required: [resolved]
      properties:
        resolved:
          type: boolean
        organizationId:
          type: integer
        organizationName:
          type: string
        organizationSlug:
          type: string
        organizationType:
          type: string
          enum: [business, church, ngo, school, hospital, hotel, government, other]
        logoUrl:
          type: ["string", "null"]
        systemDisplayName:
          type: ["string", "null"]
        theme:
          description: >-
            A small, fixed set of theme tokens mapped directly onto the
            application's existing CSS custom properties. Every key is
            optional; null (or an absent key) means "use the platform's
            shared default theme" — never a fabricated fallback color.
          anyOf:
            - $ref: "#/components/schemas/TenantThemeTokens"
            - type: "null"

    TenantThemeTokens:
      type: object
      description: Each value is an HSL triple string, e.g. "217 45% 17%" (no hsl() wrapper, no leading #).
      properties:
        sidebar:
          type: string
        sidebarForeground:
          type: string
        sidebarAccent:
          type: string
        sidebarAccentForeground:
          type: string
        primary:
          type: string
        primaryForeground:
          type: string
        accent:
          type: string
        accentForeground:
          type: string
        ring:
          type: string

    MembershipSummary:
      type: object
      required: [organizationId, organizationName, organizationSlug, logoUrl, systemDisplayName, status, roles, permissions, isPrimaryHr]
      properties:
        organizationId:
          type: integer
        organizationName:
          type: string
        organizationSlug:
          type: string
        logoUrl:
          type: ["string", "null"]
        systemDisplayName:
          type: ["string", "null"]
        status:
          type: string
          enum: [invited, active, suspended, expired, revoked]
        roles:
          type: array
          items:
            type: string
        permissions:
          type: array
          description: >-
            The caller's own EFFECTIVE permission keys for this membership —
            the union of every permission granted through the membership's
            roles, exactly what the server evaluates in requirePermission.
            Self-scoped (never another member's grants) and informational:
            it lets the client show an administrative entry only when the
            caller holds a permission its destination actually requires,
            rather than inferring authority from role names. The server
            remains authoritative on every request.
          items:
            type: string
        isPrimaryHr:
          type: boolean
        isDepartmentHead:
          type: boolean
          description: >-
            True when this membership is the CURRENT head of at least one
            department. Structural authority, not a permission: a department head
            acts on their department because of a live department_heads row, while
            leave_request.approve is held by every employee and gates may-attempt,
            never may-act. Self-scoped and informational — it lets the client offer
            a manager surface only to someone who actually holds one. The server
            re-resolves the same primitive on every request and stays authoritative.
        hasDirectReports:
          type: boolean
          description: >-
            True when at least one employee currently reports to this membership's
            employee record. The same structural signal as isDepartmentHead, for the
            reporting-manager tier. No ids, counts or names are exposed — only
            whether the authority exists.

    SwitchOrganizationInput:
      type: object
      required: [organizationId]
      properties:
        organizationId:
          type: integer

    SelfServiceEmployeeProfile:
      type: object
      description: >-
        A deliberately restricted view of Employee (W39) — omits notes
        (employee.notes.read is HR-only), nationalId/passportNumber
        (sensitive identifiers with no self-service read need),
        linkedApplicationUserId/createdBy/updatedBy/createdAt/updatedAt
        (internal bookkeeping), and organizationId (redundant). Read-only:
        no field here is editable through Employee Self-Service.
      required: [id, firstName, lastName, employmentStatus]
      properties:
        id:
          type: integer
        employeeNumber:
          type: ["string", "null"]
        hasProfilePicture:
          type: boolean
        firstName:
          type: string
        middleName:
          type: ["string", "null"]
        lastName:
          type: string
        preferredName:
          type: ["string", "null"]
        gender:
          type: ["string", "null"]
          enum: [male, female, other, prefer_not_to_say, null]
        dateOfBirth:
          type: ["string", "null"]
          format: date-time
        maritalStatus:
          type: ["string", "null"]
          enum: [single, married, divorced, widowed, other, null]
        nationality:
          type: ["string", "null"]
        personalEmail:
          type: ["string", "null"]
        workEmail:
          type: ["string", "null"]
        phoneNumber:
          type: ["string", "null"]
        alternatePhoneNumber:
          type: ["string", "null"]
        residentialAddress:
          anyOf:
            - $ref: "#/components/schemas/Address"
            - type: "null"
        emergencyContacts:
          type: ["array", "null"]
          items:
            $ref: "#/components/schemas/EmergencyContact"
        departmentId:
          type: ["integer", "null"]
        departmentName:
          type: ["string", "null"]
        branchId:
          type: ["integer", "null"]
        branchName:
          type: ["string", "null"]
        positionId:
          type: ["integer", "null"]
        positionName:
          type: ["string", "null"]
        reportingManagerId:
          type: ["integer", "null"]
        reportingManagerName:
          type: ["string", "null"]
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        hireDate:
          type: ["string", "null"]
          format: date-time
        probationEndDate:
          type: ["string", "null"]
          format: date-time
        employmentStatus:
          type: string
          enum: [active, probation, on_leave, suspended, terminated]
        workLocation:
          type: ["string", "null"]
        separationDate:
          type: ["string", "null"]
          format: date-time
        separationReason:
          type: ["string", "null"]
          description: Code from the "separation_reason" Master Data domain.

    MyEmployeeResponse:
      type: object
      required: [linked, employee]
      properties:
        linked:
          type: boolean
          description: False when the caller's login isn't linked to an employee record (or that link no longer resolves) — an intentional, safe state, not an error.
        employee:
          anyOf:
            - $ref: "#/components/schemas/SelfServiceEmployeeProfile"
            - type: "null"

    InternalVacancyQuestion:
      type: object
      description: Deliberately excludes isKnockout/expectedAnswer (W52) — an applicant must never learn which questions are knockout-screened or what answer is "correct".
      required: [id, questionText, questionType]
      properties:
        id:
          type: integer
        questionText:
          type: string
        questionType:
          type: string
          enum: [text, yes_no, multiple_choice, numeric]

    InternalVacancySummary:
      type: object
      description: >-
        Deliberately restricted — no recruiter, hiring-manager,
        requisition, approval, or audit detail is ever included, the same
        discipline as the public careers DTO.
      required: [publicId, title, departmentName, employmentType, workplaceType, openingsCount, openDate, closeDate, jobDescription, responsibilities, requirements, preferredQualifications, questions]
      properties:
        publicId:
          type: string
        title:
          type: string
        departmentName:
          type: ["string", "null"]
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        workplaceType:
          type: ["string", "null"]
          enum: [onsite, remote, hybrid, null]
        openingsCount:
          type: integer
        openDate:
          type: ["string", "null"]
          format: date-time
        closeDate:
          type: ["string", "null"]
          format: date-time
        jobDescription:
          type: ["string", "null"]
        responsibilities:
          type: ["string", "null"]
        requirements:
          type: ["string", "null"]
        preferredQualifications:
          type: ["string", "null"]
        questions:
          type: array
          items:
            $ref: "#/components/schemas/InternalVacancyQuestion"

    InternalVacanciesResponse:
      type: object
      required: [linked, active, items]
      properties:
        linked:
          type: boolean
          description: False when the caller's login isn't linked to an employee record — the same intentional, safe state as MyEmployeeResponse.linked.
        active:
          type: boolean
          description: Meaningful only when linked is true — false when the linked employee is not currently active (probation/on_leave/suspended/terminated).
        items:
          type: array
          items:
            $ref: "#/components/schemas/InternalVacancySummary"

    SubmitInternalApplicationAnswerInput:
      type: object
      required: [vacancyQuestionId, answerText]
      properties:
        vacancyQuestionId:
          type: integer
        answerText:
          type: string

    SubmitInternalApplicationInput:
      type: object
      properties:
        answers:
          type: array
          items:
            $ref: "#/components/schemas/SubmitInternalApplicationAnswerInput"

    MyInternalApplicationSubmitResult:
      type: object
      required: [id, isNew, status]
      properties:
        id:
          type: integer
        isNew:
          type: boolean
          description: False when a prior application to this vacancy already existed and was returned as-is (idempotent repeat submission).
        status:
          type: string
          enum: [submitted]

    MyInternalApplicationSummary:
      type: object
      required: [id, vacancyTitle, currentStageCategory, submittedAt]
      properties:
        id:
          type: integer
        vacancyTitle:
          type: string
        currentStageCategory:
          type: string
          enum: [applied, screening, interview, assessment, offer, hired, rejected, withdrawn]
        submittedAt:
          type: string
          format: date-time

    MyInternalApplicationsResponse:
      type: object
      required: [linked, active, items]
      properties:
        linked:
          type: boolean
        active:
          type: boolean
        items:
          type: array
          items:
            $ref: "#/components/schemas/MyInternalApplicationSummary"

    Address:
      type: object
      properties:
        line1:
          type: ["string", "null"]
        line2:
          type: ["string", "null"]
        city:
          type: ["string", "null"]
        state:
          type: ["string", "null"]
        postalCode:
          type: ["string", "null"]
        country:
          type: ["string", "null"]

    EmergencyContact:
      type: object
      required: [name, relationship, phone]
      properties:
        name:
          type: string
        relationship:
          type: string
        phone:
          type: string

    LeaveRequest:
      type: object
      required:
        [
          id,
          organizationId,
          employeeId,
          leaveTypeId,
          leavePolicyId,
          startDate,
          endDate,
          daysRequested,
          status,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        leaveTypeId:
          type: integer
        leavePolicyId:
          type: integer
          description: Resolved server-side from the employee's eligibility — never client-supplied.
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
        daysRequested:
          type: string
          description: Decimal string, computed server-side.
        status:
          type: string
          description: >-
            "pending" = awaiting the employee's current Department Head;
            "pending_hr" = Department Head approved, awaiting HR's final
            decision. See `workflowStage` for the 6 owner-facing labels
            (also distinguishes which stage a "rejected" request was
            rejected at).
          enum: [pending, pending_hr, approved, rejected, cancelled]
        workflowStage:
          type: string
          description: Derived, never stored — the exact stage label HR/ESS should render.
          enum:
            [
              awaiting_department_head,
              awaiting_hr,
              rejected_by_department_head,
              approved,
              rejected_by_hr,
              cancelled,
            ]
        reason:
          type: ["string", "null"]
        attachmentDocumentId:
          type: ["integer", "null"]
        cancelledAt:
          type: ["string", "null"]
          format: date-time
        cancelledBy:
          type: ["integer", "null"]
        departmentHeadApprovedBy:
          type: ["integer", "null"]
          description: The user id of the Department Head who approved this request's first stage. Never overwritten by a later HR action or a subsequent Department Head replacement.
        departmentHeadApprovedAt:
          type: ["string", "null"]
          format: date-time
        departmentHeadRejectedBy:
          type: ["integer", "null"]
        departmentHeadRejectedAt:
          type: ["string", "null"]
          format: date-time
        departmentHeadRejectionReason:
          type: ["string", "null"]
        approvedBy:
          type: ["integer", "null"]
          description: The user id who gave FINAL (HR-stage) approval.
        approvedAt:
          type: ["string", "null"]
          format: date-time
        rejectedBy:
          type: ["integer", "null"]
          description: The user id who gave the FINAL (HR-stage) rejection. Null when the request was instead rejected at the Department Head stage — see departmentHeadRejectedBy.
        rejectedAt:
          type: ["string", "null"]
          format: date-time
        rejectionReason:
          type: ["string", "null"]
          description: The HR-stage rejection reason. See departmentHeadRejectionReason for a Department-Head-stage rejection.
        createdBy:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    LeaveCalendarEntry:
      type: object
      description: >-
        The minimum identity fields the calendar UI needs — deliberately not
        the full LeaveRequest shape. Never includes reason,
        attachmentDocumentId, or any approval/ledger detail.
      required: [id, employeeId, employeeName, leaveTypeId, leaveTypeName, startDate, endDate, daysRequested]
      properties:
        id:
          type: integer
        employeeId:
          type: integer
        employeeName:
          type: string
        leaveTypeId:
          type: integer
        leaveTypeName:
          type: string
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
        daysRequested:
          type: string

    PublicHolidayOccurrence:
      type: object
      description: >-
        One concrete occurrence of a holiday within a requested range — a
        recurring holiday's template expanded for the relevant year(s), or a
        one-off holiday's date/observedDate. A read-only calendar overlay,
        never a leave request.
      required: [id, name, date]
      properties:
        id:
          type: integer
        name:
          type: string
        date:
          type: string
          format: date

    LeaveCalendarResponse:
      type: object
      required: [leaveEntries, holidays]
      properties:
        leaveEntries:
          type: array
          items:
            $ref: "#/components/schemas/LeaveCalendarEntry"
        holidays:
          type: array
          items:
            $ref: "#/components/schemas/PublicHolidayOccurrence"

    RejectLeaveRequestInput:
      type: object
      description: >-
        Rejecting a leave request (at either the Department Head or HR
        stage) always requires a non-empty reason — Leave Approval Workflow
        Reconciliation binding rules #5/#6.
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1

    CreateLeaveRequestInput:
      type: object
      required: [leaveTypeId, startDate, endDate]
      properties:
        leaveTypeId:
          type: integer
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
        reason:
          type: string
        attachmentDocumentId:
          type: integer

    AttendanceEvent:
      type: object
      required: [id, organizationId, employeeId, eventType, occurredAt, source, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        eventType:
          type: string
          enum: [clock_in, clock_out]
        occurredAt:
          type: string
          format: date-time
          description: Server-authoritative instant — never client-supplied for a self-service event.
        source:
          type: string
          enum: [self_service, hr_manual, biometric, import]
          description: biometric and import are schema-reserved values; no W65 route writes either.
        recordedByMembershipId:
          type: ["integer", "null"]
        branchId:
          type: ["integer", "null"]
        deviceReference:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    RecordAttendanceEventInput:
      type: object
      required: [eventType]
      properties:
        eventType:
          type: string
          enum: [clock_in, clock_out]

    AttendanceAdjustment:
      type: object
      required: [id, organizationId, employeeId, date, adjustmentType, reason, status, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        date:
          type: string
          format: date
        adjustmentType:
          type: string
          enum: [manual_clock_in, manual_clock_out, mark_present, mark_absent, excuse_absence]
        correctedClockIn:
          type: ["string", "null"]
          format: date-time
        correctedClockOut:
          type: ["string", "null"]
          format: date-time
        reason:
          type: string
        status:
          type: string
          enum: [pending, approved, rejected]
          description: W65's HR direct-entry path always creates this as "approved" immediately. "pending" is reachable only by a later workstream's employee-initiated request path.
        requestedByMembershipId:
          type: ["integer", "null"]
        decidedByMembershipId:
          type: ["integer", "null"]
        decidedAt:
          type: ["string", "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time

    RecordAttendanceAdjustmentInput:
      type: object
      required: [employeeId, date, adjustmentType, reason]
      properties:
        employeeId:
          type: integer
        date:
          type: string
          format: date
        adjustmentType:
          type: string
          enum: [manual_clock_in, manual_clock_out, mark_present, mark_absent, excuse_absence]
        correctedClockIn:
          type: string
          format: date-time
          description: Required when adjustmentType is manual_clock_in.
        correctedClockOut:
          type: string
          format: date-time
          description: Required when adjustmentType is manual_clock_out.
        reason:
          type: string

    DailyAttendanceSummary:
      type: object
      required: [organizationId, employeeId, date, status]
      properties:
        organizationId:
          type: integer
        employeeId:
          type: integer
        date:
          type: string
          format: date
          description: Organization-local civil date, never a browser-local or UTC-literal date.
        status:
          type: ["string", "null"]
          enum: [present, late, partial, absent, on_leave, holiday, non_working_day, null]
          description: >-
            null means no summary is generated for this date — before
            hireDate, on/after separationDate, or an otherwise-absent day
            suppressed because the employee's current employmentStatus is
            on_leave or suspended.
        firstClockIn:
          type: ["string", "null"]
          format: date-time
          description: Effective first clock-in for this civil date — an approved manual_clock_in adjustment overrides the raw event when present.
        lastClockOut:
          type: ["string", "null"]
          format: date-time
          description: Effective last clock-out for this civil date — an approved manual_clock_out adjustment overrides the raw event when present.
        workedMinutes:
          type: ["integer", "null"]
          description: firstClockIn to lastClockOut only (first-in/last-out, no multi-segment net-duration math). null unless status is present or late.
        lateMinutes:
          type: ["integer", "null"]
          description: Minutes past workStartTime + gracePeriodMinutes. 0 when on time. null unless status is present or late.
        earlyDepartureMinutes:
          type: ["integer", "null"]
          description: Minutes before workEndTime. Informational only — never its own status value. null unless status is present or late.

    AttendanceRegisterRow:
      type: object
      required: [employeeId, summaries]
      properties:
        employeeId:
          type: integer
        summaries:
          type: array
          description: One DailyAttendanceSummary per date in the requested range, for this employee. Employee identity/department/branch display names are resolved client-side from the already-loaded employee list, not duplicated here.
          items:
            $ref: "#/components/schemas/DailyAttendanceSummary"

    AttendanceRegisterResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AttendanceRegisterRow"
        total:
          type: integer
          description: Employee count matching the caller's authorized scope and department/branch/employeeId filters — computed before any status filter is applied.
        page:
          type: integer
        pageSize:
          type: integer

    AttendanceDashboardBreakdownItem:
      type: object
      required: [status, count]
      properties:
        status:
          type: ["string", "null"]
          enum: [present, late, partial, absent, on_leave, holiday, non_working_day, null]
          description: A DailyAttendanceSummary status, or null for "not applicable" (no summary generated for that day).
        count:
          type: integer

    AttendanceDashboard:
      type: object
      required: [date, totalEmployeesCount, statusBreakdown]
      properties:
        date:
          type: string
          format: date
        totalEmployeesCount:
          type: integer
          description: Employees in the caller's authorized scope (organization-wide, or self + direct reports).
        statusBreakdown:
          type: array
          description: All 8 buckets (7 statuses + null), zero-filled, in a fixed display order.
          items:
            $ref: "#/components/schemas/AttendanceDashboardBreakdownItem"

    LeaveBalanceSummary:
      type: object
      required: [leaveTypeId, leaveTypeName, available]
      properties:
        leaveTypeId:
          type: integer
        leaveTypeName:
          type: string
        available:
          type: string
          description: Decimal string, computed live as SUM(amount) over the immutable ledger — never a stored, directly editable value.

    LeaveBalanceEntry:
      type: object
      required:
        [
          id,
          organizationId,
          employeeId,
          leaveTypeId,
          leavePolicyId,
          entryType,
          amount,
          effectiveDate,
          createdAt,
        ]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        leaveTypeId:
          type: integer
        leavePolicyId:
          type: integer
          description: Resolved server-side at posting time and never reinterpreted by a later policy change.
        entryType:
          type: string
          enum: [opening_balance, accrual, carry_forward, usage, reversal, expiry, manual_adjustment]
        amount:
          type: string
          description: Signed decimal string — positive credits the balance, negative debits it.
        effectiveDate:
          type: string
          format: date
        reason:
          type: ["string", "null"]
        relatedLeaveRequestId:
          type: ["integer", "null"]
        sourceReference:
          type: ["string", "null"]
          description: Idempotency key for postings not tied to a specific leave request.
        createdBy:
          type: ["integer", "null"]
        approvedBy:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    AdjustLeaveBalanceInput:
      type: object
      required: [leaveTypeId, amount, effectiveDate, reason]
      properties:
        leaveTypeId:
          type: integer
        amount:
          type: number
          description: Signed — positive credits the balance, negative debits it. Cannot be zero.
        effectiveDate:
          type: string
          format: date
        reason:
          type: string
          minLength: 1

    PublicHoliday:
      type: object
      required: [id, organizationId, name, date, scope, recurring, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        date:
          type: string
          format: date
          description: For a recurring holiday, a month/day template — the year is only its first occurrence.
        scope:
          type: string
          enum: [organization, branch, region, national]
          description: Always "organization" in this phase — branch/region/national are schema-ready but not implemented.
        recurring:
          type: boolean
        observedDate:
          type: ["string", "null"]
          format: date
          description: Only valid for a one-off holiday shifted to a weekday. Always null for a recurring holiday.
        effectiveYear:
          type: ["integer", "null"]
          description: Required for a one-off holiday (must match date's year); always null for a recurring holiday.
        description:
          type: ["string", "null"]
        status:
          type: string
          enum: [active, inactive]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreatePublicHolidayInput:
      type: object
      required: [name, date, recurring]
      properties:
        name:
          type: string
        date:
          type: string
          format: date
        recurring:
          type: boolean
        observedDate:
          type: string
          format: date
        effectiveYear:
          type: integer
        description:
          type: string

    UpdatePublicHolidayInput:
      type: object
      properties:
        name:
          type: string
        date:
          type: string
          format: date
        recurring:
          type: boolean
        observedDate:
          type: ["string", "null"]
          format: date
        effectiveYear:
          type: ["integer", "null"]
        description:
          type: string

    RecruitmentSettings:
      type: object
      description: >-
        One row per organization. `updatedAt` is null when nothing has ever
        been saved — the response is still safe defaults, not an error.
      required:
        - organizationId
        - enabled
        - internalRecruitmentEnabled
        - externalRecruitmentEnabled
        - requireCandidateAccount
        - defaultWorkflowId
        - candidateDataRetentionMonths
        - reapplicationWaitingDays
        - duplicateCandidatePolicy
        - defaultOfferExpiryDays
        - defaultDocumentRequirements
        - applicationLimitPerCandidate
        - updatedAt
      properties:
        organizationId:
          type: integer
        enabled:
          type: boolean
        internalRecruitmentEnabled:
          type: boolean
        externalRecruitmentEnabled:
          type: boolean
        requireCandidateAccount:
          type: boolean
        defaultWorkflowId:
          type: ["integer", "null"]
        candidateDataRetentionMonths:
          type: ["integer", "null"]
        reapplicationWaitingDays:
          type: integer
        duplicateCandidatePolicy:
          type: string
          enum: [allow, flag, block]
        defaultOfferExpiryDays:
          type: ["integer", "null"]
        defaultDocumentRequirements:
          type: array
          items:
            type: string
          description: Master Data categoryCode strings, free text — not validated against a domain's item list.
        applicationLimitPerCandidate:
          type: ["integer", "null"]
        updatedAt:
          type: ["string", "null"]
          format: date-time

    UpdateRecruitmentSettingsInput:
      type: object
      description: All fields optional — merged into the organization's existing settings (or defaults) before validation.
      properties:
        enabled:
          type: boolean
        internalRecruitmentEnabled:
          type: boolean
        externalRecruitmentEnabled:
          type: boolean
        requireCandidateAccount:
          type: boolean
        defaultWorkflowId:
          type: ["integer", "null"]
        candidateDataRetentionMonths:
          type: ["integer", "null"]
        reapplicationWaitingDays:
          type: integer
          minimum: 0
        duplicateCandidatePolicy:
          type: string
          enum: [allow, flag, block]
        defaultOfferExpiryDays:
          type: integer
          minimum: 1
        defaultDocumentRequirements:
          type: array
          items:
            type: string
        applicationLimitPerCandidate:
          type: integer
          minimum: 1

    RecruitmentWorkflow:
      type: object
      required: [id, organizationId, name, description, isActive, isDefault, displayOrder, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        description:
          type: ["string", "null"]
        isActive:
          type: boolean
        isDefault:
          type: boolean
        displayOrder:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateRecruitmentWorkflowInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: ["string", "null"]
        displayOrder:
          type: integer

    UpdateRecruitmentWorkflowInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: ["string", "null"]
        displayOrder:
          type: integer

    RecruitmentStage:
      type: object
      required:
        - id
        - organizationId
        - workflowId
        - name
        - category
        - displayOrder
        - color
        - icon
        - isRequired
        - isTerminal
        - isActive
        - createdAt
        - updatedAt
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        workflowId:
          type: integer
        name:
          type: string
        category:
          type: string
          enum: [applied, screening, interview, assessment, offer, hired, rejected, withdrawn]
        displayOrder:
          type: integer
        color:
          type: ["string", "null"]
        icon:
          type: ["string", "null"]
        isRequired:
          type: boolean
        isTerminal:
          type: boolean
          description: Derived server-side from category (hired/rejected/withdrawn are terminal) — never independently settable to a contradictory value.
        isActive:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateRecruitmentStageInput:
      type: object
      required: [name, category, displayOrder]
      properties:
        name:
          type: string
          minLength: 1
        category:
          type: string
          enum: [applied, screening, interview, assessment, offer, hired, rejected, withdrawn]
        displayOrder:
          type: integer
        color:
          type: ["string", "null"]
        icon:
          type: ["string", "null"]
        isRequired:
          type: boolean

    UpdateRecruitmentStageInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        category:
          type: string
          enum: [applied, screening, interview, assessment, offer, hired, rejected, withdrawn]
        displayOrder:
          type: integer
        color:
          type: ["string", "null"]
        icon:
          type: ["string", "null"]
        isRequired:
          type: boolean

    JobRequisition:
      type: object
      description: >-
        Foundation only (W45) — no approval execution, vacancy, candidate,
        or application linkage exists yet. filledCount is always 0 in this
        workstream; nothing increments it.
      required:
        - id
        - organizationId
        - title
        - requisitionType
        - positionId
        - departmentId
        - branchId
        - hiringManagerEmployeeId
        - recruiterEmployeeId
        - requestedHeadcount
        - filledCount
        - employmentType
        - workplaceType
        - expectedStartDate
        - salaryRangeMin
        - salaryRangeMax
        - salaryCurrency
        - justification
        - replacementEmployeeId
        - status
        - cancellationReason
        - createdBy
        - updatedBy
        - createdAt
        - updatedAt
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        title:
          type: string
        requisitionType:
          type: string
          enum: [new_role, replacement, temporary, internship, volunteer, contract, ministry]
        positionId:
          type: ["integer", "null"]
        departmentId:
          type: ["integer", "null"]
        branchId:
          type: ["integer", "null"]
        hiringManagerEmployeeId:
          type: ["integer", "null"]
        recruiterEmployeeId:
          type: ["integer", "null"]
        requestedHeadcount:
          type: integer
        filledCount:
          type: integer
          description: Never client-settable. Always 0 in this workstream.
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        workplaceType:
          type: ["string", "null"]
          enum: [onsite, remote, hybrid, null]
        expectedStartDate:
          type: ["string", "null"]
          format: date
        salaryRangeMin:
          type: ["string", "null"]
          description: Recruitment-scoped only — not a payroll/compensation field.
        salaryRangeMax:
          type: ["string", "null"]
        salaryCurrency:
          type: ["string", "null"]
        justification:
          type: ["string", "null"]
        replacementEmployeeId:
          type: ["integer", "null"]
          description: Required when requisitionType is "replacement"; disallowed otherwise.
        status:
          type: string
          enum: [draft, pending_approval, approved, rejected, partially_filled, filled, cancelled, closed]
        cancellationReason:
          type: ["string", "null"]
        createdBy:
          type: ["integer", "null"]
        updatedBy:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    JobRequisitionListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/JobRequisition"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    CreateJobRequisitionInput:
      type: object
      required: [title, requisitionType, requestedHeadcount]
      properties:
        title:
          type: string
          minLength: 1
        requisitionType:
          type: string
          enum: [new_role, replacement, temporary, internship, volunteer, contract, ministry]
        positionId:
          type: ["integer", "null"]
        departmentId:
          type: ["integer", "null"]
        branchId:
          type: ["integer", "null"]
        hiringManagerEmployeeId:
          type: ["integer", "null"]
        recruiterEmployeeId:
          type: ["integer", "null"]
        requestedHeadcount:
          type: integer
          minimum: 1
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        workplaceType:
          type: ["string", "null"]
          enum: [onsite, remote, hybrid, null]
        expectedStartDate:
          type: ["string", "null"]
          format: date
        salaryRangeMin:
          type: ["string", "null"]
        salaryRangeMax:
          type: ["string", "null"]
        salaryCurrency:
          type: ["string", "null"]
        justification:
          type: ["string", "null"]
        replacementEmployeeId:
          type: ["integer", "null"]

    UpdateJobRequisitionInput:
      type: object
      description: Only accepted while the requisition is in draft — status, filledCount, and audit fields are never accepted here.
      properties:
        title:
          type: string
          minLength: 1
        requisitionType:
          type: string
          enum: [new_role, replacement, temporary, internship, volunteer, contract, ministry]
        positionId:
          type: ["integer", "null"]
        departmentId:
          type: ["integer", "null"]
        branchId:
          type: ["integer", "null"]
        hiringManagerEmployeeId:
          type: ["integer", "null"]
        recruiterEmployeeId:
          type: ["integer", "null"]
        requestedHeadcount:
          type: integer
          minimum: 1
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        workplaceType:
          type: ["string", "null"]
          enum: [onsite, remote, hybrid, null]
        expectedStartDate:
          type: ["string", "null"]
          format: date
        salaryRangeMin:
          type: ["string", "null"]
        salaryRangeMax:
          type: ["string", "null"]
        salaryCurrency:
          type: ["string", "null"]
        justification:
          type: ["string", "null"]
        replacementEmployeeId:
          type: ["integer", "null"]

    CancelJobRequisitionInput:
      type: object
      properties:
        reason:
          type: ["string", "null"]

    RequisitionApproval:
      type: object
      description: >-
        A single approval step's decision record. This workstream writes
        only sequence 1 (a single required approval step) — the column
        exists so a later workstream can extend to a genuine multi-step
        chain without a schema change. Immutable once decided: approve/reject
        are the only operations that ever change a row, and only while
        decision is "pending".
      required:
        - id
        - organizationId
        - requisitionId
        - sequence
        - approverMembershipId
        - decision
        - decidedAt
        - comment
        - createdAt
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        requisitionId:
          type: integer
        sequence:
          type: integer
        approverMembershipId:
          type: ["integer", "null"]
          description: Resolved at decision time (who actually decided) — null while pending.
        decision:
          type: string
          enum: [pending, approved, rejected]
        decidedAt:
          type: ["string", "null"]
          format: date-time
        comment:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    ApproveJobRequisitionInput:
      type: object
      properties:
        comment:
          type: ["string", "null"]

    RejectJobRequisitionInput:
      type: object
      properties:
        comment:
          type: ["string", "null"]

    VacancyLocation:
      type: object
      required: [id, organizationId, vacancyId, branchId, label, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        vacancyId:
          type: integer
        branchId:
          type: ["integer", "null"]
        label:
          type: ["string", "null"]
          description: Free-text location (e.g. "Remote") used when branchId is unset.
        createdAt:
          type: string
          format: date-time

    VacancyLocationInput:
      type: object
      properties:
        branchId:
          type: ["integer", "null"]
        label:
          type: ["string", "null"]

    VacancyQuestion:
      type: object
      required: [id, organizationId, vacancyId, questionText, questionType, isKnockout, expectedAnswer, displayOrder, isActive, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        vacancyId:
          type: integer
        questionText:
          type: string
        questionType:
          type: string
          enum: [text, yes_no, multiple_choice, numeric]
        isKnockout:
          type: boolean
        expectedAnswer:
          type: ["string", "null"]
          description: Only meaningful for knockout auto-scoring.
        displayOrder:
          type: integer
        isActive:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    VacancyQuestionInput:
      type: object
      required: [questionText]
      properties:
        questionText:
          type: string
          minLength: 1
        questionType:
          type: string
          enum: [text, yes_no, multiple_choice, numeric]
        isKnockout:
          type: boolean
        expectedAnswer:
          type: ["string", "null"]
        displayOrder:
          type: integer

    Vacancy:
      type: object
      description: >-
        The postable unit produced from an approved job requisition
        (Phase 3A W48 in the frozen plan's own numbering). Internal
        lifecycle only — no public careers exposure exists yet. filledCount
        is always 0 in this workstream; nothing increments it.
      required:
        - id
        - organizationId
        - requisitionId
        - workflowId
        - publicId
        - title
        - visibility
        - status
        - openingsCount
        - filledCount
        - openDate
        - closeDate
        - jobDescription
        - responsibilities
        - requirements
        - preferredQualifications
        - seoTitle
        - seoDescription
        - featured
        - createdBy
        - updatedBy
        - createdAt
        - updatedAt
        - locations
        - questions
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        requisitionId:
          type: integer
        workflowId:
          type: ["integer", "null"]
        publicId:
          type: string
          description: Reserved for a later public careers workstream — not consumed by anything in this workstream.
        title:
          type: string
        visibility:
          type: string
          enum: [internal, external, both]
        status:
          type: string
          enum: [draft, scheduled, published, paused, closed, archived]
        openingsCount:
          type: integer
        filledCount:
          type: integer
          description: Never client-settable. Always 0 in this workstream.
        openDate:
          type: ["string", "null"]
          format: date-time
        closeDate:
          type: ["string", "null"]
          format: date-time
        jobDescription:
          type: ["string", "null"]
        responsibilities:
          type: ["string", "null"]
        requirements:
          type: ["string", "null"]
        preferredQualifications:
          type: ["string", "null"]
        seoTitle:
          type: ["string", "null"]
        seoDescription:
          type: ["string", "null"]
        featured:
          type: boolean
        createdBy:
          type: ["integer", "null"]
        updatedBy:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        locations:
          type: array
          items:
            $ref: "#/components/schemas/VacancyLocation"
        questions:
          type: array
          items:
            $ref: "#/components/schemas/VacancyQuestion"

    VacancyListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Vacancy"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    CreateVacancyInput:
      type: object
      required: [requisitionId, title]
      properties:
        requisitionId:
          type: integer
        workflowId:
          type: ["integer", "null"]
        title:
          type: string
          minLength: 1
        visibility:
          type: string
          enum: [internal, external, both]
        openingsCount:
          type: integer
          minimum: 1
        openDate:
          type: ["string", "null"]
          format: date-time
        closeDate:
          type: ["string", "null"]
          format: date-time
        jobDescription:
          type: ["string", "null"]
        responsibilities:
          type: ["string", "null"]
        requirements:
          type: ["string", "null"]
        preferredQualifications:
          type: ["string", "null"]
        seoTitle:
          type: ["string", "null"]
        seoDescription:
          type: ["string", "null"]
        featured:
          type: boolean
        locations:
          type: array
          items:
            $ref: "#/components/schemas/VacancyLocationInput"
        questions:
          type: array
          items:
            $ref: "#/components/schemas/VacancyQuestionInput"

    UpdateVacancyInput:
      type: object
      description: Only accepted while the vacancy is in draft — status, filledCount, publicId, and audit fields are never accepted here.
      properties:
        workflowId:
          type: ["integer", "null"]
        title:
          type: string
          minLength: 1
        visibility:
          type: string
          enum: [internal, external, both]
        openingsCount:
          type: integer
          minimum: 1
        openDate:
          type: ["string", "null"]
          format: date-time
        closeDate:
          type: ["string", "null"]
          format: date-time
        jobDescription:
          type: ["string", "null"]
        responsibilities:
          type: ["string", "null"]
        requirements:
          type: ["string", "null"]
        preferredQualifications:
          type: ["string", "null"]
        seoTitle:
          type: ["string", "null"]
        seoDescription:
          type: ["string", "null"]
        featured:
          type: boolean
        locations:
          type: array
          items:
            $ref: "#/components/schemas/VacancyLocationInput"
        questions:
          type: array
          items:
            $ref: "#/components/schemas/VacancyQuestionInput"

    ApplicationSummary:
      type: object
      description: >-
        Internal ATS view — unlike the public careers DTOs, this freely
        exposes internal ids (this route requires authentication and
        application.read).
      required:
        - id
        - vacancyId
        - vacancyTitle
        - candidateId
        - candidateName
        - candidateEmail
        - currentStageId
        - currentStageName
        - currentStageCategory
        - submittedAt
      properties:
        id:
          type: integer
        vacancyId:
          type: integer
        vacancyTitle:
          type: string
        candidateId:
          type: integer
        candidateName:
          type: string
        candidateEmail:
          type: string
        currentStageId:
          type: ["integer", "null"]
        currentStageName:
          type: ["string", "null"]
        currentStageCategory:
          type: string
          description: The current stage's fixed category, or "applied" for a never-triaged application (currentStageId null) — computed for display, never stored.
          enum: [applied, screening, interview, assessment, offer, hired, rejected, withdrawn]
        submittedAt:
          type: string
          format: date-time

    ApplicationListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationSummary"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    ApplicationDocumentSummary:
      type: object
      description: Metadata only — this workstream does not add a document download/streaming endpoint.
      required: [id, categoryCode, fileName, mimeType, fileSize]
      properties:
        id:
          type: integer
        categoryCode:
          type: string
        fileName:
          type: string
        mimeType:
          type: string
        fileSize:
          type: integer

    ApplicationStageHistoryEntry:
      type: object
      description: Immutable — never updated or deleted through any application API.
      required: [id, organizationId, applicationId, fromStageId, toStageId, movedByMembershipId, reason, movedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        applicationId:
          type: integer
        fromStageId:
          type: ["integer", "null"]
          description: Null for an application's first recorded movement.
        toStageId:
          type: integer
        movedByMembershipId:
          type: ["integer", "null"]
        reason:
          type: ["string", "null"]
          description: Optional free-text comment supplied with the movement.
        movedAt:
          type: string
          format: date-time

    ApplicationAnswerSummary:
      type: object
      description: A candidate's response to one of the vacancy's screening questions (W52), captured at apply time — write-once, never edited.
      required: [id, vacancyQuestionId, questionText, answerText, knockoutFailed, createdAt]
      properties:
        id:
          type: integer
        vacancyQuestionId:
          type: integer
        questionText:
          type: string
        answerText:
          type: string
        knockoutFailed:
          type: boolean
          description: Computed server-side at submission for yes_no/multiple_choice knockout questions only. A failed knockout flags this row for review — it never auto-rejects the application (§12).
        createdAt:
          type: string
          format: date-time

    ApplicationScoreEntry:
      type: object
      description: Append-only — no update/delete path exists. Multiple entries per scoreType are expected as different reviewers score independently.
      required: [id, organizationId, applicationId, scoredByMembershipId, scoreType, score, notes, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        applicationId:
          type: integer
        scoredByMembershipId:
          type: ["integer", "null"]
        scoreType:
          type: string
          enum: [screening, interview, overall]
        score:
          type: string
          description: Decimal string (numeric(5,2) column) — parse to a number for display/arithmetic.
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    SubmitApplicationScoreInput:
      type: object
      required: [scoreType, score]
      properties:
        scoreType:
          type: string
          enum: [screening, interview, overall]
        score:
          type: number
        notes:
          type: ["string", "null"]

    ApplicationDetail:
      allOf:
        - $ref: "#/components/schemas/ApplicationSummary"
        - type: object
          required: [candidatePhone, rejectionReasonCode, withdrawalReasonCode, documents, history, answers, scores, scoreRollup]
          properties:
            candidatePhone:
              type: ["string", "null"]
            rejectionReasonCode:
              type: ["string", "null"]
            withdrawalReasonCode:
              type: ["string", "null"]
            documents:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationDocumentSummary"
            history:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationStageHistoryEntry"
            answers:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationAnswerSummary"
            scores:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationScoreEntry"
              description: Newest first.
            scoreRollup:
              type: ["number", "null"]
              description: >-
                Computed on read from `scores` — an explicit "overall"
                entry wins outright if one exists, otherwise the average of
                whichever of screening/interview have a latest entry. Null
                when no scores exist. Never applications.score (that column
                stays permanently unused — §12).

    MoveApplicationStageInput:
      type: object
      required: [toStageId]
      properties:
        toStageId:
          type: integer
        comment:
          type: ["string", "null"]

    RejectApplicationInput:
      type: object
      properties:
        reasonCode:
          type: ["string", "null"]
        comment:
          type: ["string", "null"]

    WithdrawApplicationInput:
      type: object
      properties:
        reasonCode:
          type: ["string", "null"]
        comment:
          type: ["string", "null"]

    ReopenApplicationInput:
      type: object
      properties:
        comment:
          type: ["string", "null"]

    Candidate:
      type: object
      description: >-
        Internal ATS view of a W49 candidate record (this route requires
        authentication and candidate.read). Visibility-filtered per caller
        the same way as Applications — organization-wide for candidate.manage
        holders, otherwise scoped to candidates reachable via an application
        to a requisition that assigns the caller as recruiter or hiring
        manager. This workstream (W53) never redesigns the candidate record
        itself, only adds this internal read path.
      required: [id, organizationId, firstName, lastName, email, phone, source, isActive, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        firstName:
          type: string
        lastName:
          type: string
        email:
          type: string
        phone:
          type: ["string", "null"]
        source:
          type: string
        isActive:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CandidateListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Candidate"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    CandidateNote:
      type: object
      description: >-
        Create-only — no update/delete path exists (a correction is a new
        note, never an edit of a past one). Never exposed to an applicant —
        no candidate-session read path exists anywhere in this codebase (W50
        deferred).
      required: [id, organizationId, candidateId, applicationId, authorMembershipId, note, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        candidateId:
          type: integer
        applicationId:
          type: ["integer", "null"]
          description: Null for a candidate-level note; set for a note scoped to one specific application.
        authorMembershipId:
          type: ["integer", "null"]
        note:
          type: string
        createdAt:
          type: string
          format: date-time

    CreateCandidateNoteInput:
      type: object
      required: [note]
      properties:
        note:
          type: string
          minLength: 1
        applicationId:
          type: ["integer", "null"]
          description: Must belong to this candidate if supplied.

    CandidateTag:
      type: object
      description: Free-text label, never a fixed enum — tag values are never hard-coded.
      required: [id, organizationId, candidateId, tag, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        candidateId:
          type: integer
        tag:
          type: string
        createdAt:
          type: string
          format: date-time

    AddCandidateTagInput:
      type: object
      required: [tag]
      properties:
        tag:
          type: string
          minLength: 1

    TalentPool:
      type: object
      description: Reusable, org-scoped candidate grouping — never tied to a single vacancy or requisition.
      required: [id, organizationId, name, description, isActive, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        description:
          type: ["string", "null"]
        isActive:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateTalentPoolInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: ["string", "null"]

    UpdateTalentPoolInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: ["string", "null"]

    TalentPoolMember:
      type: object
      required: [id, organizationId, talentPoolId, candidateId, addedByMembershipId, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        talentPoolId:
          type: integer
        candidateId:
          type: integer
        addedByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    AddTalentPoolMemberInput:
      type: object
      required: [candidateId]
      properties:
        candidateId:
          type: integer

    InterviewPanelMember:
      type: object
      description: >-
        Panel composition only, independent from interview results.
        `interviewerMembershipId` is null for an external interviewer,
        captured by name/email only (no login, no external-interviewer
        portal).
      required: [id, organizationId, interviewId, interviewerMembershipId, externalInterviewerName, externalInterviewerEmail, role, conflictDeclared, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        interviewId:
          type: integer
        interviewerMembershipId:
          type: ["integer", "null"]
        externalInterviewerName:
          type: ["string", "null"]
        externalInterviewerEmail:
          type: ["string", "null"]
        role:
          type: string
          enum: [lead, member]
        conflictDeclared:
          type: boolean
          description: A plain self-declared flag — no automated behavior is attached to it in this workstream.
        createdAt:
          type: string
          format: date-time

    PanelMemberInput:
      type: object
      description: Either interviewerMembershipId (internal) or both externalInterviewerName and externalInterviewerEmail (external) must be supplied, never both, never neither.
      properties:
        interviewerMembershipId:
          type: ["integer", "null"]
        externalInterviewerName:
          type: ["string", "null"]
        externalInterviewerEmail:
          type: ["string", "null"]
        role:
          type: string
          enum: [lead, member]
        conflictDeclared:
          type: boolean

    Interview:
      type: object
      description: >-
        Scheduling only — never evaluation. Column list is exactly §9's
        `interviews` row; no notes/timezone/separate-end-time field exists.
        `outcome` is a short free-text summary settable only once the
        interview is marked completed, never a score/recommendation
        (W55's `interview_scorecards` is a separate table).
      required: [id, organizationId, applicationId, interviewType, scheduledAt, durationMinutes, location, meetingLink, status, outcome, createdAt, updatedAt, panelMembers]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        applicationId:
          type: integer
        interviewType:
          type: string
          enum: [phone, virtual, in_person]
        scheduledAt:
          type: string
          format: date-time
        durationMinutes:
          type: integer
        location:
          type: ["string", "null"]
        meetingLink:
          type: ["string", "null"]
        status:
          type: string
          enum: [scheduled, completed, cancelled, no_show]
        outcome:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        panelMembers:
          type: array
          items:
            $ref: "#/components/schemas/InterviewPanelMember"

    InterviewListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Interview"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    ScheduleInterviewInput:
      type: object
      required: [interviewType, scheduledAt, durationMinutes]
      properties:
        interviewType:
          type: string
          enum: [phone, virtual, in_person]
        scheduledAt:
          type: string
          format: date-time
        durationMinutes:
          type: integer
        location:
          type: ["string", "null"]
        meetingLink:
          type: ["string", "null"]
        panelMembers:
          type: array
          items:
            $ref: "#/components/schemas/PanelMemberInput"

    UpdateInterviewInput:
      type: object
      description: >-
        Supplying scheduledAt reschedules — cancels the current row and
        creates a brand new one (§4.5), never mutating a scheduled
        interview's date in place. Omitting scheduledAt performs an ordinary
        in-place update, including a status transition to completed/no_show
        and/or a full panel replace.
      properties:
        interviewType:
          type: string
          enum: [phone, virtual, in_person]
        scheduledAt:
          type: string
          format: date-time
        durationMinutes:
          type: integer
        location:
          type: ["string", "null"]
        meetingLink:
          type: ["string", "null"]
        status:
          type: string
          enum: [completed, no_show]
        outcome:
          type: ["string", "null"]
        panelMembers:
          type: array
          items:
            $ref: "#/components/schemas/PanelMemberInput"

    InterviewScorecardResponse:
      type: object
      description: >-
        One per-criterion answer within a scorecard. `criterion` is plain
        free text supplied per submission — no organization-configurable
        criteria/template table exists in this frozen scope. `rating` has no
        defined scale in the frozen plan and is kept an unconstrained
        integer, the same unconstrained-numeric posture application_scores.score
        (W52) already established for a comparable field. Immutable once the
        parent scorecard is submitted (in practice) / finalized (formally).
      required: [id, organizationId, scorecardId, criterion, rating, comment, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        scorecardId:
          type: integer
        criterion:
          type: string
        rating:
          type: ["integer", "null"]
        comment:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    ResponseInput:
      type: object
      required: [criterion]
      properties:
        criterion:
          type: string
          minLength: 1
        rating:
          type: ["integer", "null"]
        comment:
          type: ["string", "null"]

    InterviewScorecard:
      type: object
      description: >-
        One per panel member per interview. Deliberately never exposes
        externalInterviewerToken/expiry (reserved for a later workstream,
        never consumed by any route here — an internal-only column, never a
        DTO field, so a raw token can never leak through this API even once
        another workstream starts populating it). No weighting/rollup number
        exists in the frozen model — recommendation is the only structured
        summary field.
      required: [id, organizationId, interviewId, interviewerMembershipId, recommendation, overallComment, submittedAt, finalizedAt, createdAt, updatedAt, responses]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        interviewId:
          type: integer
        interviewerMembershipId:
          type: ["integer", "null"]
        recommendation:
          type: ["string", "null"]
          enum: [strong_yes, yes, no, strong_no]
        overallComment:
          type: ["string", "null"]
        submittedAt:
          type: ["string", "null"]
          format: date-time
          description: Null while still a draft. Set exactly once — a submitted scorecard is immutable from the owning interviewer's side.
        finalizedAt:
          type: ["string", "null"]
          format: date-time
          description: Set only by an HR/recruiter administrator (scorecard.finalize) — never by the submitting interviewer themselves.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        responses:
          type: array
          items:
            $ref: "#/components/schemas/InterviewScorecardResponse"

    SaveInterviewScorecardInput:
      type: object
      properties:
        recommendation:
          type: ["string", "null"]
          enum: [strong_yes, yes, no, strong_no]
        overallComment:
          type: ["string", "null"]
        responses:
          type: array
          items:
            $ref: "#/components/schemas/ResponseInput"
          description: Full replace of the response set on every save.
        submit:
          type: boolean
          description: When true, transitions this draft to submitted in the same call.

    RecommendationCounts:
      type: object
      required: [strong_yes, yes, no, strong_no]
      properties:
        strong_yes:
          type: integer
        yes:
          type: integer
        no:
          type: integer
        strong_no:
          type: integer

    InterviewScorecardListResponse:
      type: object
      required: [scorecards, panelSummary]
      properties:
        scorecards:
          type: array
          items:
            $ref: "#/components/schemas/InterviewScorecard"
        panelSummary:
          type: ["object", "null"]
          description: >-
            Computed on every read, never stored — the same discipline as
            W52's scoreRollup. Null unless the caller holds scorecard.read_all.
          required: [totalPanelMembers, submittedCount, pendingCount, recommendationCounts]
          properties:
            totalPanelMembers:
              type: integer
            submittedCount:
              type: integer
            pendingCount:
              type: integer
            recommendationCounts:
              $ref: "#/components/schemas/RecommendationCounts"

    ReferenceBackgroundCheckStatus:
      type: string
      description: Shared status model (§4.7) for both reference and background checks. No "cancelled"/"expired" state exists — a check that can't be completed is recorded as unable_to_complete.
      enum: [requested, in_progress, completed, flagged, unable_to_complete]

    ReferenceCheck:
      type: object
      description: >-
        Referee contact + outcome, status-tracked, no vendor integration.
        Column list is exactly §9's `reference_checks` row. No dedicated
        reference-check permission exists — visibility/write reuse the
        application's own read/manage pair.
      required: [id, organizationId, applicationId, refereeName, refereeContact, refereeRelationship, status, notes, completedAt, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        applicationId:
          type: integer
        refereeName:
          type: string
        refereeContact:
          type: string
        refereeRelationship:
          type: ["string", "null"]
        status:
          $ref: "#/components/schemas/ReferenceBackgroundCheckStatus"
        notes:
          type: ["string", "null"]
        completedAt:
          type: ["string", "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateReferenceCheckInput:
      type: object
      required: [refereeName, refereeContact]
      properties:
        refereeName:
          type: string
          minLength: 1
        refereeContact:
          type: string
          minLength: 1
        refereeRelationship:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]

    UpdateReferenceCheckStatusInput:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [in_progress, completed, flagged, unable_to_complete]
        notes:
          type: ["string", "null"]

    BackgroundCheck:
      type: object
      description: >-
        Background check tracking, status-tracked, no vendor integration —
        vendorReference is a plain, manually-entered string, never a real
        provider API call. Column list is exactly §9's `background_checks`
        row, minus the raw documentStorageKey (never exposed — hasEvidence
        signals its presence instead). Gated by its own dedicated
        background_check.read/.manage pair, organization-wide only.
      required: [id, organizationId, applicationId, checkType, status, vendorReference, resultSummary, hasEvidence, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        applicationId:
          type: integer
        checkType:
          type: string
          description: Free text — no organization-configurable criteria/type table exists in this frozen scope.
        status:
          $ref: "#/components/schemas/ReferenceBackgroundCheckStatus"
        vendorReference:
          type: ["string", "null"]
        resultSummary:
          type: ["string", "null"]
        hasEvidence:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateBackgroundCheckInput:
      type: object
      required: [checkType]
      properties:
        checkType:
          type: string
          minLength: 1
        vendorReference:
          type: ["string", "null"]

    UpdateBackgroundCheckStatusInput:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [in_progress, completed, flagged, unable_to_complete]
        resultSummary:
          type: ["string", "null"]
        vendorReference:
          type: ["string", "null"]

    OfferVersion:
      type: object
      description: >-
        One immutable revision of an offer's content — column list is
        exactly §9's `offer_versions` row, minus the raw
        generatedDocumentStorageKey (a reserved, never-populated internal
        file-storage reference, never exposed — same DTO-shaping discipline
        as W55/W56's own stripped internal-only columns). letterTemplateId
        is a plain, unpopulated reserved FK-shaped integer — no offer-letter
        template mechanism exists in this workstream's scope.
      required:
        - id
        - organizationId
        - offerId
        - versionNumber
        - proposedStartDate
        - employmentType
        - workplaceType
        - location
        - compensationSummary
        - conditions
        - expiryDate
        - letterTemplateId
        - status
        - createdAt
        - updatedAt
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        offerId:
          type: integer
        versionNumber:
          type: integer
        proposedStartDate:
          type: ["string", "null"]
          format: date
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        workplaceType:
          type: ["string", "null"]
          enum: [onsite, remote, hybrid, null]
        location:
          type: ["string", "null"]
        compensationSummary:
          description: Recruitment-scoped fields only (base salary, currency, bonus, benefits summary) — never a full payroll/compensation structure. Caller-supplied, unvalidated shape.
          type: ["object", "null"]
          additionalProperties: true
        conditions:
          type: ["string", "null"]
        expiryDate:
          type: ["string", "null"]
          format: date
        letterTemplateId:
          type: ["integer", "null"]
          description: Reserved — no offer-letter template mechanism exists in this workstream's scope. Always null.
        status:
          type: string
          enum: [draft, pending_approval, approved, issued, accepted, declined, expired, withdrawn, superseded]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    Offer:
      type: object
      description: >-
        A stable envelope per application — the actual content lives in
        OfferVersion, never here (§14). currentVersionId always points at
        the latest non-superseded version.
      required: [id, organizationId, applicationId, currentVersionId, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        applicationId:
          type: integer
        currentVersionId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    OfferDetail:
      type: object
      required: [offer, versions]
      properties:
        offer:
          $ref: "#/components/schemas/Offer"
        versions:
          type: array
          description: Every version of this offer, oldest first (versionNumber ascending) — including superseded ones, for full history.
          items:
            $ref: "#/components/schemas/OfferVersion"

    OfferListItem:
      type: object
      required: [offer, currentVersion, vacancyTitle, applicationId]
      properties:
        offer:
          $ref: "#/components/schemas/Offer"
        currentVersion:
          description: >-
            Null only for the brief instant between creating the offer
            envelope and its first version within the same transaction —
            never observable via this list endpoint in practice. Inlined
            (not $ref'd) because a nullable $ref/allOf combination breaks
            this repo's orval codegen (see the `offers` tag description /
            W55's own documented fix for the same issue).
          type: ["object", "null"]
          required:
            - id
            - organizationId
            - offerId
            - versionNumber
            - proposedStartDate
            - employmentType
            - workplaceType
            - location
            - compensationSummary
            - conditions
            - expiryDate
            - letterTemplateId
            - status
            - createdAt
            - updatedAt
          properties:
            id:
              type: integer
            organizationId:
              type: integer
            offerId:
              type: integer
            versionNumber:
              type: integer
            proposedStartDate:
              type: ["string", "null"]
              format: date
            employmentType:
              type: ["string", "null"]
              enum: [full_time, part_time, contract, intern, temporary, null]
            workplaceType:
              type: ["string", "null"]
              enum: [onsite, remote, hybrid, null]
            location:
              type: ["string", "null"]
            compensationSummary:
              type: ["object", "null"]
              additionalProperties: true
            conditions:
              type: ["string", "null"]
            expiryDate:
              type: ["string", "null"]
              format: date
            letterTemplateId:
              type: ["integer", "null"]
            status:
              type: string
              enum: [draft, pending_approval, approved, issued, accepted, declined, expired, withdrawn, superseded]
            createdAt:
              type: string
              format: date-time
            updatedAt:
              type: string
              format: date-time
        vacancyTitle:
          type: string
        applicationId:
          type: integer

    OfferListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/OfferListItem"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    OfferApproval:
      type: object
      description: >-
        A single approval step's decision record — mirrors
        RequisitionApproval exactly (§9's own "mirrors requisition_approvals"
        instruction). This workstream writes only sequence 1. No reject
        route exists (see the `offers` tag description) — "rejected" is a
        structurally valid but currently unreachable decision value.
      required: [id, organizationId, offerVersionId, sequence, approverMembershipId, decision, decidedAt, comment, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        offerVersionId:
          type: integer
        sequence:
          type: integer
        approverMembershipId:
          type: ["integer", "null"]
          description: Resolved at decision time (who actually decided) — null while pending.
        decision:
          type: string
          enum: [pending, approved, rejected]
        decidedAt:
          type: ["string", "null"]
          format: date-time
        comment:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    CreateOfferInput:
      type: object
      description: Fields for the offer's first (draft) version. Every field is optional — an empty draft can be created and filled in later via PATCH.
      properties:
        proposedStartDate:
          type: ["string", "null"]
          format: date
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        workplaceType:
          type: ["string", "null"]
          enum: [onsite, remote, hybrid, null]
        location:
          type: ["string", "null"]
        compensationSummary:
          type: ["object", "null"]
          additionalProperties: true
        conditions:
          type: ["string", "null"]
        expiryDate:
          type: ["string", "null"]
          format: date

    UpdateOfferVersionInput:
      type: object
      description: Used both for PATCH .../offers/{id} (edit the current draft in place) and POST .../offers/{id}/versions (create a new draft from an approved/issued version) — same field shape either way.
      properties:
        proposedStartDate:
          type: ["string", "null"]
          format: date
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        workplaceType:
          type: ["string", "null"]
          enum: [onsite, remote, hybrid, null]
        location:
          type: ["string", "null"]
        compensationSummary:
          type: ["object", "null"]
          additionalProperties: true
        conditions:
          type: ["string", "null"]
        expiryDate:
          type: ["string", "null"]
          format: date

    ApproveOfferVersionInput:
      type: object
      properties:
        comment:
          type: ["string", "null"]

    WithdrawOfferVersionInput:
      type: object
      properties:
        reason:
          type: ["string", "null"]

    PreEmploymentRequirement:
      type: object
      description: >-
        A single checklist item tracked against an application before
        employee conversion. Column list is exactly §9's
        `pre_employment_requirements` row. No dedicated permission exists —
        visibility/write reuse the application's own read/manage pair.
        requirementCode is a Master Data reference (free text, not
        FK-validated), the same convention as candidate_documents.categoryCode.
      required: [id, organizationId, applicationId, requirementCode, status, satisfiedAt, notes, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        applicationId:
          type: integer
        requirementCode:
          type: string
          description: Free text (e.g. right-to-work doc, medical, reference-complete) — no fixed/Ghana-specific list is hard-coded.
        status:
          type: string
          enum: [pending, satisfied, waived]
        satisfiedAt:
          type: ["string", "null"]
          format: date-time
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    PreEmploymentRequirementSummary:
      type: object
      description: Computed on every read, never stored — the same discipline as W52's scoreRollup/W55's panelSummary.
      required: [totalCount, pendingCount, satisfiedCount, waivedCount, readyForConversion]
      properties:
        totalCount:
          type: integer
        pendingCount:
          type: integer
        satisfiedCount:
          type: integer
        waivedCount:
          type: integer
        readyForConversion:
          type: boolean
          description: Mirrors §13's own convert-to-employee readiness rule ("every non-waived row is satisfied"). Vacuously true when no requirements are tracked. Informational only — does not itself gate or trigger conversion.

    PreEmploymentRequirementListResponse:
      type: object
      required: [items, summary]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/PreEmploymentRequirement"
        summary:
          $ref: "#/components/schemas/PreEmploymentRequirementSummary"

    CreatePreEmploymentRequirementInput:
      type: object
      required: [requirementCode]
      properties:
        requirementCode:
          type: string
          minLength: 1
        notes:
          type: ["string", "null"]

    UpdatePreEmploymentRequirementStatusInput:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [pending, satisfied, waived]
        notes:
          type: ["string", "null"]

    CandidateEmployeeLink:
      type: object
      description: >-
        Immutable provenance record left behind once an application
        converts to a real employee. Column list is exactly §9's
        `candidate_employee_links` row. Unique on applicationId (one
        conversion per application) and on employeeId (one recruitment
        provenance per employee).
      required: [id, organizationId, candidateId, applicationId, employeeId, convertedAt, convertedByMembershipId]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        candidateId:
          type: integer
        applicationId:
          type: integer
        employeeId:
          type: integer
        convertedAt:
          type: string
          format: date-time
        convertedByMembershipId:
          type: ["integer", "null"]

    ConversionResult:
      type: object
      required: [link, employeeId, reusedExistingEmployee]
      properties:
        link:
          $ref: "#/components/schemas/CandidateEmployeeLink"
        employeeId:
          type: integer
        reusedExistingEmployee:
          type: boolean
          description: True only when an existing employee (an internal candidate, via candidates.linkedInternalEmployeeId) was reused rather than a new one created.

    PublicOrganization:
      type: object
      description: Deliberately narrow — only what a careers page header needs. Never the internal Organization DTO (no status, no type, no internal id).
      required: [slug, name, logoUrl]
      properties:
        slug:
          type: string
        name:
          type: string
        logoUrl:
          type: ["string", "null"]

    PublicVacancySummary:
      type: object
      description: >-
        Public-safe projection only — never requisitionId, the vacancy's
        own internal serial id, hiring manager/recruiter identity, salary,
        or any internal note. Looked up and referenced only by publicId.
      required:
        - publicId
        - title
        - departmentName
        - locations
        - employmentType
        - workplaceType
        - openingsCount
        - openDate
        - closeDate
        - summary
        - featured
      properties:
        publicId:
          type: string
        title:
          type: string
        departmentName:
          type: ["string", "null"]
        locations:
          type: array
          items:
            type: string
        employmentType:
          type: ["string", "null"]
        workplaceType:
          type: ["string", "null"]
        openingsCount:
          type: integer
        openDate:
          type: ["string", "null"]
          format: date-time
        closeDate:
          type: ["string", "null"]
          format: date-time
        summary:
          type: ["string", "null"]
          description: Truncated jobDescription (220 chars) for card display.
        featured:
          type: boolean

    PublicVacancyListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/PublicVacancySummary"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    PublicVacancyQuestion:
      type: object
      description: >-
        Deliberately excludes isKnockout/expectedAnswer (W52) — a candidate
        must never learn which questions are knockout-screened or what
        answer is "correct."
      required: [id, questionText, questionType]
      properties:
        id:
          type: integer
        questionText:
          type: string
        questionType:
          type: string
          enum: [text, yes_no, multiple_choice, numeric]

    PublicVacancyDetail:
      allOf:
        - $ref: "#/components/schemas/PublicVacancySummary"
        - type: object
          required: [jobDescription, responsibilities, requirements, preferredQualifications, seoTitle, seoDescription, questions]
          properties:
            jobDescription:
              type: ["string", "null"]
            responsibilities:
              type: ["string", "null"]
            requirements:
              type: ["string", "null"]
            preferredQualifications:
              type: ["string", "null"]
            seoTitle:
              type: ["string", "null"]
            seoDescription:
              type: ["string", "null"]
            questions:
              type: array
              items:
                $ref: "#/components/schemas/PublicVacancyQuestion"

    ApplicationSubmittedResponse:
      type: object
      required: [submitted, publicId]
      properties:
        submitted:
          type: boolean
        publicId:
          type: string
          description: The new (or pre-existing, on an idempotent repeat) application's public reference. Never the internal serial id.

    ApplicationStatusResponse:
      type: object
      required: [vacancyTitle, submittedAt, status]
      properties:
        vacancyTitle:
          type: string
        submittedAt:
          type: string
          format: date-time
        status:
          type: string
          description: Always "submitted" in this workstream — pipeline-derived status begins with W51 (Application Pipeline & Stage Movement).

    LeaveType:
      type: object
      required: [id, organizationId, name, code, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        code:
          type: string
        status:
          type: string
          enum: [active, inactive]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateLeaveTypeInput:
      type: object
      required: [name, code]
      properties:
        name:
          type: string
          minLength: 1
        code:
          type: string
          minLength: 1

    UpdateLeaveTypeInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        code:
          type: string
          minLength: 1

    LeavePolicy:
      type: object
      required:
        [
          id,
          organizationId,
          leaveTypeId,
          name,
          probationRestricted,
          annualEntitlementDays,
          isPaid,
          accrualMethod,
          entitlementPeriod,
          carryForwardAllowed,
          attachmentRequired,
          countWeekends,
          countPublicHolidays,
          allowNegativeBalance,
          effectiveFrom,
          status,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        leaveTypeId:
          type: integer
        name:
          type: string
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        branchId:
          type: ["integer", "null"]
        departmentId:
          type: ["integer", "null"]
        positionId:
          type: ["integer", "null"]
        gender:
          type: ["string", "null"]
          enum: [male, female, other, prefer_not_to_say, null]
        minimumServiceMonths:
          type: ["integer", "null"]
        probationRestricted:
          type: boolean
        annualEntitlementDays:
          type: string
          description: Decimal string (e.g. "21.00").
        isPaid:
          type: boolean
        accrualMethod:
          type: string
          enum: [annual, monthly, per_pay_period, none]
        accrualRate:
          type: ["string", "null"]
        entitlementPeriod:
          type: string
          enum: [calendar_year, anniversary_year]
        carryForwardAllowed:
          type: boolean
        maxCarryForwardDays:
          type: ["string", "null"]
        carryForwardExpiryMonths:
          type: ["integer", "null"]
        minRequestDurationDays:
          type: ["string", "null"]
        maxRequestDurationDays:
          type: ["string", "null"]
        noticePeriodDays:
          type: ["integer", "null"]
        noticePeriodCountsWorkingDaysOnly:
          type: ["boolean", "null"]
          description: >-
            Phase 3H, W117. Null/false (the default) means the existing
            calendar-day notice-period behavior — completely unchanged. True
            means noticePeriodDays counts only working days (weekends
            skipped, organization holidays optionally skipped).
        attachmentRequired:
          type: boolean
        countWeekends:
          type: boolean
        countPublicHolidays:
          type: boolean
        allowNegativeBalance:
          type: boolean
        effectiveFrom:
          type: string
          format: date-time
        effectiveTo:
          type: ["string", "null"]
          format: date-time
        status:
          type: string
          enum: [active, inactive]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateLeavePolicyInput:
      type: object
      required: [name, annualEntitlementDays, effectiveFrom]
      properties:
        name:
          type: string
          minLength: 1
        employmentType:
          type: string
          enum: [full_time, part_time, contract, intern, temporary]
        branchId:
          type: integer
        departmentId:
          type: integer
        positionId:
          type: integer
        gender:
          type: string
          enum: [male, female, other, prefer_not_to_say]
        minimumServiceMonths:
          type: integer
        probationRestricted:
          type: boolean
        annualEntitlementDays:
          type: number
        isPaid:
          type: boolean
        accrualMethod:
          type: string
          enum: [annual, monthly, per_pay_period, none]
        accrualRate:
          type: number
        entitlementPeriod:
          type: string
          enum: [calendar_year, anniversary_year]
        carryForwardAllowed:
          type: boolean
        maxCarryForwardDays:
          type: number
        carryForwardExpiryMonths:
          type: integer
        minRequestDurationDays:
          type: number
        maxRequestDurationDays:
          type: number
        noticePeriodDays:
          type: integer
        noticePeriodCountsWorkingDaysOnly:
          type: boolean
        attachmentRequired:
          type: boolean
        countWeekends:
          type: boolean
        countPublicHolidays:
          type: boolean
        allowNegativeBalance:
          type: boolean
        effectiveFrom:
          type: string
          format: date-time
        effectiveTo:
          type: string
          format: date-time

    UpdateLeavePolicyInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        branchId:
          type: ["integer", "null"]
        departmentId:
          type: ["integer", "null"]
        positionId:
          type: ["integer", "null"]
        gender:
          type: ["string", "null"]
          enum: [male, female, other, prefer_not_to_say, null]
        minimumServiceMonths:
          type: ["integer", "null"]
        probationRestricted:
          type: boolean
        annualEntitlementDays:
          type: number
        isPaid:
          type: boolean
        accrualMethod:
          type: string
          enum: [annual, monthly, per_pay_period, none]
        accrualRate:
          type: ["number", "null"]
        entitlementPeriod:
          type: string
          enum: [calendar_year, anniversary_year]
        carryForwardAllowed:
          type: boolean
        maxCarryForwardDays:
          type: ["number", "null"]
        carryForwardExpiryMonths:
          type: ["integer", "null"]
        minRequestDurationDays:
          type: ["number", "null"]
        maxRequestDurationDays:
          type: ["number", "null"]
        noticePeriodDays:
          type: ["integer", "null"]
        noticePeriodCountsWorkingDaysOnly:
          type: ["boolean", "null"]
        attachmentRequired:
          type: boolean
        countWeekends:
          type: boolean
        countPublicHolidays:
          type: boolean
        allowNegativeBalance:
          type: boolean
        effectiveFrom:
          type: string
          format: date-time
        effectiveTo:
          type: ["string", "null"]
          format: date-time

    Branch:
      type: object
      required: [id, organizationId, name, code, status, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        code:
          type: string
        status:
          type: string
          enum: [active, inactive]
        createdAt:
          type: string
          format: date-time

    CreateBranchInput:
      type: object
      required: [name, code]
      properties:
        name:
          type: string
          minLength: 1
        code:
          type: string
          minLength: 1

    UpdateBranchInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        code:
          type: string
          minLength: 1

    Department:
      type: object
      required: [id, organizationId, name, code, status, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        branchId:
          type: ["integer", "null"]
        parentDepartmentId:
          type: ["integer", "null"]
        name:
          type: string
        code:
          type: string
        status:
          type: string
          enum: [active, inactive]
        createdAt:
          type: string
          format: date-time

    CreateDepartmentInput:
      type: object
      required: [name, code]
      properties:
        name:
          type: string
          minLength: 1
        code:
          type: string
          minLength: 1
        branchId:
          type: ["integer", "null"]
        parentDepartmentId:
          type: ["integer", "null"]

    UpdateDepartmentInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        code:
          type: string
          minLength: 1

    RestructureDepartmentInput:
      type: object
      properties:
        branchId:
          type: ["integer", "null"]
        parentDepartmentId:
          type: ["integer", "null"]

    Position:
      type: object
      required: [id, organizationId, title, status, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        title:
          type: string
        departmentId:
          type: ["integer", "null"]
        status:
          type: string
          enum: [active, inactive]
        createdAt:
          type: string
          format: date-time

    CreatePositionInput:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
        departmentId:
          type: ["integer", "null"]

    UpdatePositionInput:
      type: object
      properties:
        title:
          type: string
          minLength: 1

    RestructurePositionInput:
      type: object
      properties:
        departmentId:
          type: ["integer", "null"]

    Employee:
      type: object
      description: >-
        WWM Employee Access Remediation (2026-09-07): `gender`,
        `dateOfBirth`, `maritalStatus`, `nationality`, `nationalId`,
        `passportNumber`, `personalEmail`, `phoneNumber`,
        `alternatePhoneNumber`, `residentialAddress`, `emergencyContacts`
        and `separationReason` are returned only to a caller holding
        employee.sensitive.read or viewing their own record; otherwise they are
        null and `sensitiveFieldsRedacted` is true. `phoneNumber` joined this
        set on 2026-09-15 because the field is not constrained to a work line;
        `workEmail` remains the directory contact field. `notes` keeps its own
        employee.notes.read gate.
      required: [id, organizationId, firstName, lastName, employmentStatus, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeNumber:
          type: ["string", "null"]
        hasProfilePicture:
          type: boolean
        firstName:
          type: string
        middleName:
          type: ["string", "null"]
        lastName:
          type: string
        preferredName:
          type: ["string", "null"]
        gender:
          type: ["string", "null"]
          enum: [male, female, other, prefer_not_to_say, null]
        dateOfBirth:
          type: ["string", "null"]
          format: date-time
        maritalStatus:
          type: ["string", "null"]
          enum: [single, married, divorced, widowed, other, null]
        nationality:
          type: ["string", "null"]
        nationalId:
          type: ["string", "null"]
        passportNumber:
          type: ["string", "null"]
        personalEmail:
          type: ["string", "null"]
        workEmail:
          type: ["string", "null"]
        phoneNumber:
          type: ["string", "null"]
        alternatePhoneNumber:
          type: ["string", "null"]
        residentialAddress:
          anyOf:
            - $ref: "#/components/schemas/Address"
            - type: "null"
        emergencyContacts:
          type: ["array", "null"]
          items:
            $ref: "#/components/schemas/EmergencyContact"
        departmentId:
          type: ["integer", "null"]
        departmentName:
          type: ["string", "null"]
        branchId:
          type: ["integer", "null"]
        branchName:
          type: ["string", "null"]
        positionId:
          type: ["integer", "null"]
        positionName:
          type: ["string", "null"]
        reportingManagerId:
          type: ["integer", "null"]
        reportingManagerName:
          type: ["string", "null"]
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        hireDate:
          type: ["string", "null"]
          format: date-time
        probationEndDate:
          type: ["string", "null"]
          format: date-time
        employmentStatus:
          type: string
          enum: [active, probation, on_leave, suspended, terminated]
        workLocation:
          type: ["string", "null"]
        separationDate:
          type: ["string", "null"]
          format: date-time
        separationReason:
          type: ["string", "null"]
          description: Code from the "separation_reason" Master Data domain.
        notes:
          type: ["string", "null"]
        sensitiveFieldsRedacted:
          type: boolean
          description: True when the personal identity fields above were withheld from this caller (directory-only visibility).
        linkedApplicationUserId:
          type: ["integer", "null"]
          description: Set when this employee record is linked to a login account (see POST/DELETE .../link-user).
        createdBy:
          type: ["integer", "null"]
        updatedBy:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateEmployeeInput:
      type: object
      required: [firstName, lastName]
      properties:
        employeeNumber:
          type: ["string", "null"]
        firstName:
          type: string
          minLength: 1
        middleName:
          type: ["string", "null"]
        lastName:
          type: string
          minLength: 1
        preferredName:
          type: ["string", "null"]
        gender:
          type: ["string", "null"]
          enum: [male, female, other, prefer_not_to_say, null]
        dateOfBirth:
          type: ["string", "null"]
          format: date-time
        maritalStatus:
          type: ["string", "null"]
          enum: [single, married, divorced, widowed, other, null]
        nationality:
          type: ["string", "null"]
        nationalId:
          type: ["string", "null"]
        passportNumber:
          type: ["string", "null"]
        personalEmail:
          type: ["string", "null"]
        workEmail:
          type: ["string", "null"]
        phoneNumber:
          type: ["string", "null"]
        alternatePhoneNumber:
          type: ["string", "null"]
        residentialAddress:
          anyOf:
            - $ref: "#/components/schemas/Address"
            - type: "null"
        emergencyContacts:
          type: ["array", "null"]
          items:
            $ref: "#/components/schemas/EmergencyContact"
        departmentId:
          type: ["integer", "null"]
        branchId:
          type: ["integer", "null"]
        positionId:
          type: ["integer", "null"]
        reportingManagerId:
          type: ["integer", "null"]
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        hireDate:
          type: ["string", "null"]
          format: date-time
        probationEndDate:
          type: ["string", "null"]
          format: date-time
        employmentStatus:
          type: string
          enum: [active, probation, on_leave, suspended, terminated]
        workLocation:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]

    UpdateEmployeeInput:
      type: object
      description: >-
        Phase 3H, W114: employeeNumber is deliberately absent — a staff
        number is never mutated through the generic employee update path.
        Use POST .../employees/:employeeId/number/allocate,
        .../number/release, or the deliberate-reuse path instead (all gated
        by employee_number.allocate, all fully audited via
        employee_number_allocations) — closing what was previously an
        unaudited, arbitrary employeeNumber mutation reachable through this
        endpoint.
      properties:
        firstName:
          type: string
          minLength: 1
        middleName:
          type: ["string", "null"]
        lastName:
          type: string
          minLength: 1
        preferredName:
          type: ["string", "null"]
        gender:
          type: ["string", "null"]
          enum: [male, female, other, prefer_not_to_say, null]
        dateOfBirth:
          type: ["string", "null"]
          format: date-time
        maritalStatus:
          type: ["string", "null"]
          enum: [single, married, divorced, widowed, other, null]
        nationality:
          type: ["string", "null"]
        nationalId:
          type: ["string", "null"]
        passportNumber:
          type: ["string", "null"]
        personalEmail:
          type: ["string", "null"]
        workEmail:
          type: ["string", "null"]
        phoneNumber:
          type: ["string", "null"]
        alternatePhoneNumber:
          type: ["string", "null"]
        residentialAddress:
          anyOf:
            - $ref: "#/components/schemas/Address"
            - type: "null"
        emergencyContacts:
          type: ["array", "null"]
          items:
            $ref: "#/components/schemas/EmergencyContact"
        departmentId:
          type: ["integer", "null"]
        branchId:
          type: ["integer", "null"]
        positionId:
          type: ["integer", "null"]
        reportingManagerId:
          type: ["integer", "null"]
        employmentType:
          type: ["string", "null"]
          enum: [full_time, part_time, contract, intern, temporary, null]
        hireDate:
          type: ["string", "null"]
          format: date-time
        probationEndDate:
          type: ["string", "null"]
          format: date-time
        employmentStatus:
          type: string
          enum: [active, probation, on_leave, suspended, terminated]
        workLocation:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]

    AllocateEmployeeNumberInput:
      type: object
      description: >-
        Phase 3H, W114. "generate" uses the organization's numbering-format
        engine (config namespace "numbering", key employeeNumber). "manual"
        requires employeeNumber and is validated identically to a generated
        value — including the deliberate-reuse check against this
        organization's allocation history and reuse policy.
      required: [mode]
      properties:
        mode:
          type: string
          enum: [generate, manual]
        employeeNumber:
          type: ["string", "null"]
          description: Required when mode is "manual"; ignored when mode is "generate".

    EmployeeNumberAllocation:
      type: object
      description: >-
        One row of the authoritative staff-number allocation history
        (frozen plan Decision 1) — append-only, never edited. validTo null
        means this is the currently-active allocation for this employee.
      required: [id, organizationId, employeeId, employeeNumber, allocationMethod, validFrom, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        employeeNumber:
          type: string
        allocationMethod:
          type: string
          enum: [generated, manual, reused, migrated]
          description: >-
            "migrated" is written only by the one-time backfill script for
            employees that already had an employeeNumber before this
            workstream — never produced by the allocate/release/reuse routes.
        validFrom:
          type: string
          format: date-time
        validTo:
          type: ["string", "null"]
          format: date-time
        allocatedByMembershipId:
          type: ["integer", "null"]
        releasedByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    EmployeeNumberAllocationResult:
      type: object
      required: [employeeNumber, allocation]
      properties:
        employeeNumber:
          type: ["string", "null"]
        allocation:
          $ref: "#/components/schemas/EmployeeNumberAllocation"

    CreatePersonnelFileInput:
      type: object
      description: >-
        Phase 3H, W115. "generate" uses the organization's PIF numbering-
        format engine (config namespace "numbering", key pifNumber —
        independent from employeeNumber's own config/sequence). "manual"
        requires pifNumber, validated for organization-scoped uniqueness
        identically to a generated value.
      required: [mode]
      properties:
        mode:
          type: string
          enum: [generate, manual]
        pifNumber:
          type: ["string", "null"]
          description: Required when mode is "manual"; ignored when mode is "generate".

    PersonnelFile:
      type: object
      description: >-
        A permanent, organization-owned personnel-record identity, strictly
        1:1 with an employee (frozen plan §7a) — unlike EmployeeNumberAllocation,
        there is no history: pifNumber is never released, reassigned, or
        reused (Decision 4). currentLocationId/currentCustodyState (W116)
        are used only when this organization does not use volumes for this
        file — see PersonnelFileVolume otherwise.
      required: [id, organizationId, employeeId, pifNumber, allocationMethod, currentCustodyState, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        pifNumber:
          type: string
        allocationMethod:
          type: string
          enum: [generated, manual]
        allocatedByMembershipId:
          type: ["integer", "null"]
        currentLocationId:
          type: ["integer", "null"]
        currentCustodyState:
          type: string
          enum: [in_registry, checked_out, missing]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    PersonnelSearchResult:
      type: object
      description: >-
        One HR/records search match (frozen plan §10) — a reused staff
        number never collapses to "the current holder": a historical and a
        current allocation matching the same term appear as two distinct,
        clearly-labeled results.
      required: [employeeId, firstName, lastName, employmentStatus, matchType, matchedValue, isCurrentHolder, currentEmployeeNumber, pifNumber]
      properties:
        employeeId:
          type: integer
        firstName:
          type: string
        lastName:
          type: string
        employmentStatus:
          type: string
          enum: [active, probation, on_leave, suspended, terminated]
        matchType:
          type: string
          enum: [name, employee_number, pif_number]
        matchedValue:
          type: string
        isCurrentHolder:
          type: boolean
          description: For matchType employee_number, false means this is a historical (released/reused-away) allocation, not the current holder.
        validFrom:
          type: ["string", "null"]
          format: date-time
        validTo:
          type: ["string", "null"]
          format: date-time
        currentEmployeeNumber:
          type: ["string", "null"]
        pifNumber:
          type: ["string", "null"]

    Vehicle:
      type: object
      description: >-
        VR-01 — one organizational vehicle, identified by the registration
        ("car") number the organization already uses. Its own register, not an
        asset-custody record: asset custody is open-ended possession, while a
        vehicle is booked for a period by the VR-02 flow.
      required: [id, organizationId, registrationNumber, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        registrationNumber:
          type: string
          description: Normalized (trimmed, single-spaced, upper-cased) and unique within the organization.
        make:
          type: ["string", "null"]
        model:
          type: ["string", "null"]
        description:
          type: ["string", "null"]
        defaultDriverEmployeeId:
          type: ["integer", "null"]
          description: Optional. A live reference — every movement record snapshots its own driver, so history is never rewritten by a change here.
        branchId:
          type: ["integer", "null"]
        status:
          type: string
          enum: [available, in_use, maintenance, inactive]
          description: in_use is set only by the VR-02 release/return flow, never through the register.
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateVehicleInput:
      type: object
      required: [registrationNumber]
      properties:
        registrationNumber:
          type: string
          minLength: 1
          maxLength: 32
        make:
          type: ["string", "null"]
          maxLength: 120
        model:
          type: ["string", "null"]
          maxLength: 120
        description:
          type: ["string", "null"]
          maxLength: 500
        defaultDriverEmployeeId:
          type: ["integer", "null"]
        branchId:
          type: ["integer", "null"]
        notes:
          type: ["string", "null"]
          maxLength: 500

    UpdateVehicleInput:
      type: object
      description: Every field is optional; only the fields supplied are changed.
      properties:
        registrationNumber:
          type: string
          minLength: 1
          maxLength: 32
        make:
          type: ["string", "null"]
          maxLength: 120
        model:
          type: ["string", "null"]
          maxLength: 120
        description:
          type: ["string", "null"]
          maxLength: 500
        defaultDriverEmployeeId:
          type: ["integer", "null"]
        branchId:
          type: ["integer", "null"]
        notes:
          type: ["string", "null"]
          maxLength: 500
        status:
          type: string
          enum: [available, maintenance, inactive]

    RecordsLocation:
      type: object
      description: >-
        A dedicated, self-referencing, organization-owned physical-storage
        hierarchy (Phase 3H, W116, Decision 6) — not a Master Data
        extension. Every level is optional and organization-defined.
      required: [id, organizationId, parentId, name, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        parentId:
          type: ["integer", "null"]
        name:
          type: string
        description:
          type: ["string", "null"]
        status:
          type: string
          enum: [active, retired]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateRecordsLocationInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: ["string", "null"]
        parentId:
          type: ["integer", "null"]

    UpdateRecordsLocationInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: ["string", "null"]
        parentId:
          type: ["integer", "null"]

    PersonnelFileVolume:
      type: object
      description: >-
        A physical volume of a personnel file (Phase 3H, W116, Decision 8) —
        a child of the personnel-file identity, never a sibling; never a new
        employee, PIF, or personnel-file identity of its own.
      required: [id, organizationId, personnelFileId, volumeNumber, status, currentCustodyState, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        personnelFileId:
          type: integer
        volumeNumber:
          type: integer
        status:
          type: string
          enum: [open, closed]
        currentLocationId:
          type: ["integer", "null"]
        currentCustodyState:
          type: string
          enum: [in_registry, checked_out, missing]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    PersonnelFileMovement:
      type: object
      description: >-
        One immutable, append-only custody event (frozen plan §7b) — never
        edited or deleted. "Overdue" is deliberately absent here: it is
        always derived live (see PersonnelFileCustodyDetail), never stored.
      required: [id, organizationId, personnelFileId, volumeId, eventType, occurredAt, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        personnelFileId:
          type: integer
        volumeId:
          type: ["integer", "null"]
        eventType:
          type: string
          enum: [checked_out, returned, marked_missing, recovered]
        occurredAt:
          type: string
          format: date-time
        actorMembershipId:
          type: ["integer", "null"]
        purpose:
          type: ["string", "null"]
        destination:
          type: ["string", "null"]
        expectedReturnDate:
          type: ["string", "null"]
          format: date-time
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    PersonnelFileCustodyDetail:
      type: object
      description: Live-derived current custody snapshot for a personnel file (no volumes) — overdue is computed, never stored.
      required: [currentCustodyState, currentLocationId, overdue]
      properties:
        currentCustodyState:
          type: string
          enum: [in_registry, checked_out, missing]
        currentLocationId:
          type: ["integer", "null"]
        overdue:
          type: boolean

    CheckoutPersonnelFileInput:
      type: object
      required: [destination]
      properties:
        volumeId:
          type: ["integer", "null"]
        purpose:
          type: ["string", "null"]
        destination:
          type: string
          minLength: 1
        expectedReturnDate:
          type: ["string", "null"]
          format: date-time
        notes:
          type: ["string", "null"]

    ReturnPersonnelFileInput:
      type: object
      properties:
        volumeId:
          type: ["integer", "null"]
        locationId:
          type: ["integer", "null"]
        notes:
          type: ["string", "null"]

    MarkPersonnelFileMissingInput:
      type: object
      required: [notes]
      properties:
        volumeId:
          type: ["integer", "null"]
        notes:
          type: string
          minLength: 1
          description: Mandatory reason for marking the file missing.

    RecoverPersonnelFileInput:
      type: object
      properties:
        volumeId:
          type: ["integer", "null"]
        locationId:
          type: ["integer", "null"]
        notes:
          type: ["string", "null"]

    EmployeeListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Employee"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    LinkEmployeeUserInput:
      type: object
      required: [applicationUserId]
      properties:
        applicationUserId:
          type: integer

    SeparateEmployeeInput:
      type: object
      required: [separationDate]
      properties:
        separationDate:
          type: string
          format: date-time
        separationReason:
          type: string
          description: Code from the "separation_reason" Master Data domain.

    TransferEmployeeInput:
      type: object
      required: [effectiveDate]
      properties:
        effectiveDate:
          type: string
          format: date-time
        departmentId:
          type: ["integer", "null"]
        branchId:
          type: ["integer", "null"]
        positionId:
          type: ["integer", "null"]

    PromoteEmployeeInput:
      type: object
      required: [positionId, effectiveDate]
      properties:
        positionId:
          type: integer
        effectiveDate:
          type: string
          format: date-time

    ConfirmEmployeeInput:
      type: object
      required: [effectiveDate]
      properties:
        effectiveDate:
          type: string
          format: date-time
        probationReviewId:
          type: ["integer", "null"]
          description: >-
            Phase 3H, W117. Optional — when supplied, must reference a
            performance review belonging to this employee and to a
            cycleType='probation' cycle in this organization. Recorded
            inside this confirmation's own employment_periods.newState
            (frozen plan Decision 13) — no schema change, no second review
            engine. Review completion never auto-confirms; this remains
            HR's own deliberate decision.

    EmployeeDisciplinaryRecord:
      type: object
      required: [id, organizationId, employeeId, actionType, description, actionDate, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        actionType:
          type: string
        description:
          type: string
        actionDate:
          type: string
          format: date-time
        recordedBy:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    AddEmployeeDisciplinaryRecordInput:
      type: object
      required: [actionType, description, actionDate]
      properties:
        actionType:
          type: string
        description:
          type: string
        actionDate:
          type: string
          format: date-time

    EmployeeExitProcess:
      type: object
      required:
        [
          id,
          organizationId,
          employeeId,
          checklistCompleted,
          clearanceCompleted,
          exitInterviewCompleted,
          status,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        separationDate:
          type: ["string", "null"]
          format: date-time
          description: >-
            The ACTUAL separation instant, snapshotted from employees.separationDate —
            it identifies which separation cycle this row belongs to. WS-12 (§28.6)
            made it nullable: an offboarding may now begin against an authoritative
            recorded basis BEFORE separation, and stays null until separation happens.
        checklistCompleted:
          type: boolean
        clearanceCompleted:
          type: boolean
          description: >-
            WS-12 (§28.8): derived from real clearance items once any exist, and never
            independently settable through the API. Rows predating WS-12 keep this
            boolean as their only record.
        exitInterviewCompleted:
          type: boolean
        exitInterviewNotes:
          type: ["string", "null"]
        initiatedBy:
          type: ["integer", "null"]
        status:
          $ref: "#/components/schemas/ExitProcessStatus"
        separationBasis:
          oneOf:
            - $ref: "#/components/schemas/ExitSeparationBasis"
            - type: "null"
        separationBasisRecordedAt:
          type: ["string", "null"]
          format: date-time
        expectedSeparationDate:
          type: ["string", "null"]
          format: date-time
        clearanceTemplateId:
          type: ["integer", "null"]
        finalClearedAt:
          type: ["string", "null"]
          format: date-time
        finalClearedBy:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    UpdateEmployeeExitProcessInput:
      type: object
      properties:
        checklistCompleted:
          type: boolean
        clearanceCompleted:
          type: boolean
        exitInterviewCompleted:
          type: boolean
        exitInterviewNotes:
          type: ["string", "null"]

    EmployeeDocument:
      type: object
      required: [id, organizationId, employeeId, categoryCode, fileName, mimeType, fileSize, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        categoryCode:
          type: string
          description: Code from the "document_category" Master Data domain.
        fileName:
          type: string
          description: Original client-supplied filename, for display only.
        mimeType:
          type: string
        fileSize:
          type: integer
        uploadedBy:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    EmployeeSkill:
      type: object
      required: [id, organizationId, employeeId, skillCode, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        skillCode:
          type: string
          description: Code from the "skill" Master Data domain.
        proficiencyLevel:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    AddEmployeeSkillInput:
      type: object
      required: [skillCode]
      properties:
        skillCode:
          type: string
        proficiencyLevel:
          type: string

    UpdateEmployeeSkillInput:
      type: object
      properties:
        skillCode:
          type: string
        proficiencyLevel:
          type: ["string", "null"]

    EmployeeQualification:
      type: object
      required: [id, organizationId, employeeId, qualificationTypeCode, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        qualificationTypeCode:
          type: string
          description: Code from the "qualification_type" Master Data domain.
        institution:
          type: ["string", "null"]
        fieldOfStudy:
          type: ["string", "null"]
        startDate:
          type: ["string", "null"]
          format: date-time
        endDate:
          type: ["string", "null"]
          format: date-time
        grade:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    AddEmployeeQualificationInput:
      type: object
      required: [qualificationTypeCode]
      properties:
        qualificationTypeCode:
          type: string
        institution:
          type: string
        fieldOfStudy:
          type: string
        startDate:
          type: string
          format: date-time
        endDate:
          type: string
          format: date-time
        grade:
          type: string

    UpdateEmployeeQualificationInput:
      type: object
      properties:
        qualificationTypeCode:
          type: string
        institution:
          type: ["string", "null"]
        fieldOfStudy:
          type: ["string", "null"]
        startDate:
          type: ["string", "null"]
          format: date-time
        endDate:
          type: ["string", "null"]
          format: date-time
        grade:
          type: ["string", "null"]

    EmployeeCertification:
      type: object
      required: [id, organizationId, employeeId, certificationTypeCode, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        certificationTypeCode:
          type: string
          description: Code from the "certification_type" Master Data domain.
        issuingOrganization:
          type: ["string", "null"]
        issueDate:
          type: ["string", "null"]
          format: date-time
        expiryDate:
          type: ["string", "null"]
          format: date-time
        credentialId:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    AddEmployeeCertificationInput:
      type: object
      required: [certificationTypeCode]
      properties:
        certificationTypeCode:
          type: string
        issuingOrganization:
          type: string
        issueDate:
          type: string
          format: date-time
        expiryDate:
          type: string
          format: date-time
        credentialId:
          type: string

    UpdateEmployeeCertificationInput:
      type: object
      properties:
        certificationTypeCode:
          type: string
        issuingOrganization:
          type: ["string", "null"]
        issueDate:
          type: ["string", "null"]
          format: date-time
        expiryDate:
          type: ["string", "null"]
          format: date-time
        credentialId:
          type: ["string", "null"]

    Notification:
      type: object
      required: [id, title, message, type, read, createdAt]
      properties:
        id:
          type: integer
        title:
          type: string
        message:
          type: string
        type:
          type: string
          enum: [info, success, warning, alert]
        read:
          type: boolean
        createdAt:
          type: string
          format: date-time
        organizationId:
          type: integer
          nullable: true
          description: WS-6. Null for a platform-scoped notification.
        sourceReferenceType:
          type: string
          nullable: true
        sourceReferenceId:
          type: integer
          nullable: true
        actionPath:
          type: string
          nullable: true
        dismissedAt:
          type: string
          format: date-time
          nullable: true

    DashboardSummary:
      type: object
      required: [totalEmployees, activeEmployees, activeModules, unreadNotifications, leaveMetrics, attendanceMetrics, assetMetrics, inventoryMetrics]
      properties:
        activeEmployees:
          description: >-
            Employees whose employment status is active, probation or on_leave
            (suspended and terminated excluded). Same employee.write gate as
            totalEmployees — null, never zero, without it.
          anyOf:
            - type: integer
            - type: "null"
        totalEmployees:
          description: >-
            Null when the caller lacks employee.write (the same
            org_admin/hr_manager-only tier that gates every employee-record
            mutation) — the broad employee.read every role holds only
            implies "may view the directory," never "may see an aggregate
            organization headcount."
          anyOf:
            - type: integer
            - type: "null"
        activeModules:
          type: integer
        unreadNotifications:
          type: integer
        leaveMetrics:
          description: >-
            Null when the "leave" module is disabled for the caller's active
            organization — never a zero-filled placeholder. When present,
            scoped to the viewer's own access tier: org-wide for an HR admin
            (leave_request.manage), own + direct reports otherwise.
          anyOf:
            - $ref: "#/components/schemas/LeaveDashboardMetrics"
            - type: "null"
        attendanceMetrics:
          description: >-
            Null when the "attendance" module is disabled, or when the
            organization has not configured a timezone yet — never a
            zero-filled placeholder. Reuses the existing Attendance Dashboard
            aggregation and its own own/team/organization-wide visibility
            scope, never a second business-rules engine.
          anyOf:
            - $ref: "#/components/schemas/AttendanceDashboardMetrics"
            - type: "null"
        assetMetrics:
          description: >-
            Null when the "asset_management" module is disabled for the
            caller's active organization, or when the caller lacks
            asset_management.reports.read (the same permission
            assets-dashboard.tsx's own endpoint requires).
          anyOf:
            - $ref: "#/components/schemas/AssetDashboardMetrics"
            - type: "null"
        inventoryMetrics:
          description: >-
            Null when the "office_inventory" module is disabled for the
            caller's active organization, or when the caller lacks
            office_inventory.reports.read.
          anyOf:
            - $ref: "#/components/schemas/InventoryDashboardMetrics"
            - type: "null"

    HrTask:
      type: object
      required: [sourceModule, sourceType, sourceId, actionKind, title, status, createdAt, deepLink, context]
      description: >-
        A task the caller can perform now. The Action Centre's safe pointer
        shape (§31.6) plus a short context line; read-only (no inline
        commands). No narrative, no priority.
      properties:
        sourceModule:
          type: string
          enum:
            [
              leave,
              learning,
              onboarding,
              skills,
              performance,
              recruitment,
              employee_requests,
              employee_relations,
              succession,
              employment_lifecycle,
              forms,
            ]
        sourceType:
          type: string
        sourceId:
          type: integer
        actionKind:
          $ref: "#/components/schemas/ActionKind"
        title:
          type: string
        employeeId:
          type: integer
          nullable: true
        employeeFirstName:
          type: string
          nullable: true
        employeeLastName:
          type: string
          nullable: true
        status:
          type: string
        createdAt:
          type: string
        dueAt:
          type: string
          nullable: true
        overdue:
          type: boolean
          nullable: true
          description: Null where dueAt is null — never false (§31.16).
        context:
          type: string
          nullable: true
          description: Safe context such as the current workflow stage or a probation end date.
        deepLink:
          type: string

    HrTaskList:
      type: object
      required: [items, total, overdue, unavailableSources]
      properties:
        items:
          type: array
          description: At most 25 tasks, overdue first, then due soon, then undated oldest first.
          items:
            $ref: "#/components/schemas/HrTask"
        total:
          type: integer
        overdue:
          type: integer
        unavailableSources:
          type: array
          description: Authorized task sources whose work could not be loaded.
          items:
            type: string

    HrAttentionCard:
      type: object
      required: [key, count, secondaryCount, deepLink]
      properties:
        key:
          type: string
          enum:
            [
              attendance_exceptions,
              probation_reviews_due,
              performance_reviews_due,
              personnel_files_attention,
              assets_awaiting_return,
              forms_awaiting_review,
            ]
        count:
          type: integer
        secondaryCount:
          type: integer
          nullable: true
          description: Monitoring figure where the source has one (forms waiting at other stages).
        deepLink:
          type: string

    HrUpcomingHoliday:
      type: object
      required: [id, name, date]
      properties:
        id:
          type: integer
        name:
          type: string
        date:
          type: string
          description: ISO calendar date (YYYY-MM-DD).

    HrActivityItem:
      type: object
      required: [id, occurredAt, eventType, targetType, actorName]
      description: HR-category audit event, redacted to what a dashboard needs — no state, IP, user agent or metadata.
      properties:
        id:
          type: integer
        occurredAt:
          type: string
        eventType:
          type: string
        targetType:
          type: string
          nullable: true
        actorName:
          type: string
          nullable: true

    HrCommandCentre:
      type: object
      required: [organizationId, generatedAt, tasks, attention, upcomingHolidays, recentActivity, unavailableSections]
      properties:
        organizationId:
          type: integer
        generatedAt:
          type: string
        tasks:
          description: Null when the caller is authorized for no task source at all.
          anyOf:
            - $ref: "#/components/schemas/HrTaskList"
            - type: "null"
        attention:
          type: array
          description: Only cards whose module is enabled and whose permission the caller holds.
          items:
            $ref: "#/components/schemas/HrAttentionCard"
        upcomingHolidays:
          description: Next 60 days (max 5). Null without public_holiday.read or with Leave disabled.
          anyOf:
            - type: array
              items:
                $ref: "#/components/schemas/HrUpcomingHoliday"
            - type: "null"
        recentActivity:
          description: Latest HR-category audit events. Null without audit.read or audit.read.hr.
          anyOf:
            - type: array
              items:
                $ref: "#/components/schemas/HrActivityItem"
            - type: "null"
        unavailableSections:
          type: array
          description: Authorized sections whose data could not be loaded.
          items:
            type: string

    AttendanceDashboardMetrics:
      type: object
      required: [presentToday, totalEmployeesInScope]
      properties:
        presentToday:
          type: integer
          description: Employees (in the viewer's scope) whose today's attendance status is "present".
        totalEmployeesInScope:
          type: integer

    AssetDashboardMetrics:
      type: object
      required: [activeAssets]
      properties:
        activeAssets:
          type: integer
          description: Assets whose status is not "retired" or "lost".

    InventoryDashboardMetrics:
      type: object
      required: [totalItems]
      properties:
        totalItems:
          type: integer
          description: Office Inventory items whose status is "active".

    LeaveRequestsByStatusCounts:
      type: object
      required: [pending, approved, rejected, cancelled]
      properties:
        pending:
          type: integer
        approved:
          type: integer
        rejected:
          type: integer
        cancelled:
          type: integer

    LeaveDashboardMetrics:
      type: object
      description: >-
        W40 — HR Operations Dashboard. Every figure is computed live from
        leave_requests/leave_balance_entries/public_holidays (W33/W34/W37) —
        no cache, no new table. "Upcoming"/"expiring" figures use a shared
        30-day window from today.
      required:
        - scope
        - employeesOnLeave
        - upcomingApprovedLeave
        - pendingApprovalCount
        - awaitingMyActionCount
        - awaitingOtherStageCount
        - upcomingPublicHolidays
        - leaveUtilizationPercent
        - expiringCarryForwardBalances
        - requestsByStatus
      properties:
        scope:
          type: string
          enum: [organization, own_and_reports]
          description: >-
            Which population the figures cover (WWM Employee Access
            Remediation, 2026-09-07). `organization` when the caller holds
            leave_request.manage; otherwise `own_and_reports` — the
            caller's own record plus live direct reports, so an ordinary
            employee sees only their own leave. Lets the dashboard label
            the section honestly.
        employeesOnLeave:
          type: integer
          description: Distinct employees (in the viewer's scope) with an approved leave request spanning today.
        upcomingApprovedLeave:
          type: integer
          description: Approved leave requests (in scope) starting within the next 30 days, not yet started.
        pendingApprovalCount:
          type: integer
          description: Pending leave requests the viewer is authorized to approve — same scope GET .../leave-requests/pending-approvals (W35) uses.
        awaitingMyActionCount:
          type: integer
          description: >-
            Of pendingApprovalCount, requests at a stage this viewer can decide
            now — `pending` only for the employee's current Department Head,
            `pending_hr` only for a leave_request.manage holder, never the
            viewer's own request.
        awaitingOtherStageCount:
          type: integer
          description: >-
            Of pendingApprovalCount, requests the viewer can see but which wait
            on another approver (e.g. HR monitoring a request still with the
            Department Head). Monitoring only — not an action for this viewer.
        upcomingPublicHolidays:
          type: integer
          description: Organization-wide public holiday occurrences within the next 30 days.
        leaveUtilizationPercent:
          type: number
          description: 100 * (ledger usage) / (ledger opening_balance + accrual + carry_forward credits) across the viewer's scope; 0 when nothing has been credited yet.
        expiringCarryForwardBalances:
          type: integer
          description: Carry-forward ledger entries (in scope) expiring within the next 30 days, counted only under a policy whose carryForwardExpiryMonths is actually set.
        requestsByStatus:
          $ref: "#/components/schemas/LeaveRequestsByStatusCounts"

    Role:
      type: object
      required: [id, key, label, isSystemRole]
      properties:
        id:
          type: integer
        key:
          type: string
        label:
          type: string
        description:
          type: ["string", "null"]
        isSystemRole:
          type: boolean

    OrganizationRole:
      type: object
      required: [id, key, organizationId, label, isSystemRole, permissionKeys]
      properties:
        id:
          type: integer
        key:
          type: string
        organizationId:
          type: ["integer", "null"]
          description: Null for a system role template shared by every organization.
        label:
          type: string
        description:
          type: ["string", "null"]
        isSystemRole:
          type: boolean
        permissionKeys:
          type: array
          items:
            type: string
        delegable:
          type: boolean
          description: Whether the CALLING user may assign this role (server-derived from the role's actual permission set and the caller's delegation authority — organization admin, or Primary HR with hr_team.manage). UI convenience only; every assignment is re-checked server-side.

    CopyRoleTemplateInput:
      type: object
      required: [templateRoleId, key, label]
      properties:
        templateRoleId:
          type: integer
        key:
          type: string
          minLength: 1
        label:
          type: string
          minLength: 1
        description:
          type: string

    GrantRolePermissionInput:
      type: object
      required: [permissionId]
      properties:
        permissionId:
          type: integer

    Permission:
      type: object
      required: [id, key, resource, action]
      properties:
        id:
          type: integer
        key:
          type: string
        resource:
          type: string
        action:
          type: string
        description:
          type: ["string", "null"]

    Module:
      type: object
      required: [id, key, name, description, category, version, status, defaultEnabled, requiredModuleKeys, optionalModuleKeys]
      properties:
        id:
          type: integer
        key:
          type: string
        name:
          type: string
        description:
          type: string
        category:
          type: string
        version:
          type: string
        status:
          type: string
          enum: [active, beta, hidden, deprecated]
        defaultEnabled:
          type: boolean
        requiredModuleKeys:
          type: array
          items:
            type: string
        optionalModuleKeys:
          type: array
          items:
            type: string

    OrganizationModule:
      type: object
      required:
        [id, key, name, description, category, version, status, defaultEnabled, requiredModuleKeys, optionalModuleKeys, enabled]
      properties:
        id:
          type: integer
        key:
          type: string
        name:
          type: string
        description:
          type: string
        category:
          type: string
        version:
          type: string
        status:
          type: string
          enum: [active, beta, hidden, deprecated]
        defaultEnabled:
          type: boolean
        requiredModuleKeys:
          type: array
          items:
            type: string
        optionalModuleKeys:
          type: array
          items:
            type: string
        enabled:
          type: boolean
          description: Effective enablement for this organization (override row if present, otherwise defaultEnabled).

    UpdateOrganizationModuleInput:
      type: object
      required: [enabled]
      properties:
        enabled:
          type: boolean

    MasterDataDomain:
      type: object
      required: [key, label, classification]
      properties:
        key:
          type: string
        label:
          type: string
        classification:
          type: string
          enum: [system-defined, organization-overridable, organization-defined]

    MasterDataItem:
      type: object
      required: [id, domain, organizationId, code, label, sortOrder, status]
      properties:
        id:
          type: integer
        domain:
          type: string
        organizationId:
          type: ["integer", "null"]
          description: Null for a system-defined item shared by every organization.
        code:
          type: string
        label:
          type: string
        sortOrder:
          type: integer
        status:
          type: string
          enum: [active, inactive]

    CreateMasterDataItemInput:
      type: object
      required: [code, label]
      properties:
        code:
          type: string
        label:
          type: string
        sortOrder:
          type: integer

    OrganizationMember:
      type: object
      required: [membershipId, applicationUserId, email, firstName, lastName, status, roles, isPrimaryHr]
      properties:
        membershipId:
          type: integer
        applicationUserId:
          type: integer
        email:
          type: string
        firstName:
          type: string
        lastName:
          type: string
        status:
          type: string
          enum: [invited, active, suspended, expired, revoked]
        roles:
          type: array
          items:
            type: string
        isPrimaryHr:
          type: boolean
        joinedAt:
          type: ["string", "null"]
          format: date-time

    AddMemberInput:
      type: object
      required: [email]
      properties:
        email:
          type: string
          minLength: 1

    AssignRoleInput:
      type: object
      required: [roleId]
      properties:
        roleId:
          type: integer

    CreateInvitationInput:
      type: object
      required: [email]
      properties:
        email:
          type: string
          minLength: 1
        roleId:
          type: integer

    InvitationCreated:
      type: object
      required: [membershipId, applicationUserId, organizationId, status, inviteToken, inviteUrl, inviteUrlSource, reinvited]
      properties:
        membershipId:
          type: integer
        applicationUserId:
          type: integer
        organizationId:
          type: integer
        status:
          type: string
          enum: [invited, active, suspended, expired, revoked]
        inviteToken:
          type: string
          description: The raw accept token (returned exactly once). Prefer inviteUrl; share it with the invitee out-of-band -- no email is sent.
        inviteUrl:
          type: string
          description: >-
            Complete accept link, built SERVER-SIDE from the invited organization's
            governed domain configuration (primary active domain, else its single
            active domain, else APP_BASE_URL). Never derived from the request Host or
            any forwarded header.
        inviteUrlSource:
          type: string
          enum: [primary_domain, single_active_domain, app_base_url, app_base_url_ambiguous_domains]
          description: >-
            Which rule produced inviteUrl. app_base_url_ambiguous_domains means the
            organization has several active domains and no primary, so the platform
            host was used -- designate a primary domain to get tenant-specific links.
        reinvited:
          type: boolean
          description: True when an existing revoked or expired-pending membership was re-issued a fresh invitation (new token, fresh expiry, roles replaced) instead of a new membership being created.

    InvitationPreview:
      type: object
      required: [organizationName, email, status, organization]
      properties:
        organizationName:
          type: string
        email:
          type: string
        status:
          type: string
          enum: [pending, expired, accepted, revoked]
        organization:
          description: >-
            Public branding of the INVITED organization (the invitation decides the
            organization; the hostname the link is opened on never does). Null only
            when the organization is not currently presentable (e.g. suspended).
          anyOf:
            - $ref: "#/components/schemas/InvitationOrganizationBranding"
            - type: "null"

    InvitationOrganizationBranding:
      type: object
      required: [organizationName, logoUrl, systemDisplayName, theme]
      properties:
        organizationName:
          type: string
        logoUrl:
          type: ["string", "null"]
        systemDisplayName:
          type: ["string", "null"]
        theme:
          anyOf:
            - $ref: "#/components/schemas/TenantThemeTokens"
            - type: "null"

    AcceptInvitationInput:
      type: object
      required: [firstName, lastName, password]
      properties:
        firstName:
          type: string
          minLength: 1
        lastName:
          type: string
          minLength: 1
        password:
          type: string
          minLength: 8

    PasswordResetStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [valid, expired, invalid]

    ResetPasswordInput:
      type: object
      required: [password]
      properties:
        password:
          type: string
          minLength: 8

    PrimaryHrAssignment:
      type: object
      required: [id, organizationId, membershipId, assignedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        membershipId:
          type: integer
        assignedAt:
          type: string
          format: date-time
        assignedBy:
          type: ["integer", "null"]
        revokedAt:
          type: ["string", "null"]
          format: date-time
        revokedBy:
          type: ["integer", "null"]

    PrimaryHrAssignmentOrNull:
      type: ["object", "null"]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        membershipId:
          type: integer
        assignedAt:
          type: string
          format: date-time
        assignedBy:
          type: ["integer", "null"]
        revokedAt:
          type: ["string", "null"]
          format: date-time
        revokedBy:
          type: ["integer", "null"]

    SetPrimaryHrInput:
      type: object
      required: [membershipId]
      properties:
        membershipId:
          type: integer

    OrganizationConfig:
      type: object
      required: [organizationId, namespace, schemaVersion, data]
      properties:
        organizationId:
          type: integer
        namespace:
          type: string
        schemaVersion:
          type: integer
        data:
          type: object
          additionalProperties: true
        updatedAt:
          type: ["string", "null"]
          format: date-time
          description: Null when the namespace has never been saved and the response reflects its defaults.

    UpdateOrganizationConfigInput:
      type: object
      required: [data]
      properties:
        data:
          type: object
          additionalProperties: true

    AuditEvent:
      type: object
      required: [id, occurredAt, eventType, targetType]
      properties:
        id:
          type: integer
        occurredAt:
          type: string
          format: date-time
        actorApplicationUserId:
          type: ["integer", "null"]
        actorMembershipId:
          type: ["integer", "null"]
        organizationId:
          type: ["integer", "null"]
        eventType:
          type: string
        targetType:
          type: string
        targetId:
          type: ["string", "null"]
        beforeState:
          type: ["object", "null"]
          additionalProperties: true
        afterState:
          type: ["object", "null"]
          additionalProperties: true
        ipAddress:
          type: ["string", "null"]
        userAgent:
          type: ["string", "null"]
        metadata:
          type: ["object", "null"]
          additionalProperties: true
        category:
          type: string
          enum: [hr, payroll, security, documents, assets_inventory, platform_configuration]
        requestId:
          type: ["string", "null"]
        outcome:
          type: ["string", "null"]
          enum: [success, failure, denied, null]

    AuditEventListResponse:
      type: object
      required: [items, total, page, pageSize, allowedCategories]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AuditEvent"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer
        allowedCategories:
          description: '"all", or the specific categories this caller may read — drives the frontend category filter (WS-3).'
          oneOf:
            - type: string
              enum: [all]
            - type: array
              items:
                type: string
                enum: [hr, payroll, security, documents, assets_inventory, platform_configuration]

    Report:
      type: object
      required: [key, label, description, category]
      properties:
        key:
          type: string
        label:
          type: string
        description:
          type: string
        category:
          type: string

    ReportColumn:
      type: object
      required: [key, label]
      properties:
        key:
          type: string
        label:
          type: string

    ReportRunResult:
      type: object
      required: [key, label, description, generatedAt, columns, rows]
      properties:
        key:
          type: string
        label:
          type: string
        description:
          type: string
        generatedAt:
          type: string
          format: date-time
        columns:
          type: array
          items:
            $ref: "#/components/schemas/ReportColumn"
        rows:
          type: array
          items:
            type: object
            additionalProperties: true

    PersonnelImportRowResult:
      type: object
      description: >-
        One CSV row's validation outcome (Phase 3H, W119). "invalid" rows
        block the whole file at commit time (all-or-nothing); "warning" rows
        (e.g. a staff number that reuses a previously-released one) do not.
      required: [rowNumber, status, errors, warnings]
      properties:
        rowNumber:
          type: integer
          description: 1-based, header row excluded.
        status:
          type: string
          enum: [valid, warning, invalid]
        errors:
          type: array
          items:
            type: string
        warnings:
          type: array
          items:
            type: string

    PersonnelImportValidationSummary:
      type: object
      required: [totalRows, validCount, warningCount, invalidCount, rows]
      properties:
        totalRows:
          type: integer
        validCount:
          type: integer
        warningCount:
          type: integer
        invalidCount:
          type: integer
        rows:
          type: array
          items:
            $ref: "#/components/schemas/PersonnelImportRowResult"

    PersonnelImportCommitResultRow:
      type: object
      required: [rowNumber, employeeId, employeeNumber, pifNumber]
      properties:
        rowNumber:
          type: integer
        employeeId:
          type: integer
        employeeNumber:
          type: ["string", "null"]
        pifNumber:
          type: ["string", "null"]

    PersonnelImportCommitResult:
      type: object
      required: [count, created]
      properties:
        count:
          type: integer
        created:
          type: array
          items:
            $ref: "#/components/schemas/PersonnelImportCommitResultRow"

    PayrollPayeBand:
      type: object
      required: [id, statutoryRuleVersionId, bandOrder, taxpayerCategory, thresholdAmount, ratePercent]
      properties:
        id:
          type: integer
        statutoryRuleVersionId:
          type: integer
        bandOrder:
          type: integer
        taxpayerCategory:
          type: string
          enum: [resident, non_resident]
        thresholdAmount:
          type: ["string", "null"]
          description: Null only for the final, open-ended band.
        ratePercent:
          type: string

    PayrollPensionRate:
      type: object
      required: [id, statutoryRuleVersionId, employeeRatePercent, employerRatePercent, tier1AllocationPercent, tier2AllocationPercent]
      properties:
        id:
          type: integer
        statutoryRuleVersionId:
          type: integer
        employeeRatePercent:
          type: string
        employerRatePercent:
          type: string
        tier1AllocationPercent:
          type: string
        tier2AllocationPercent:
          type: string

    PayrollPensionEarningsCeiling:
      type: object
      required: [id, statutoryRuleVersionId, minimumInsurableEarnings, maximumInsurableEarnings]
      properties:
        id:
          type: integer
        statutoryRuleVersionId:
          type: integer
        minimumInsurableEarnings:
          type: ["string", "null"]
        maximumInsurableEarnings:
          type: ["string", "null"]

    PayrollStatutoryRuleVersion:
      type: object
      description: >-
        Platform-global — not organization-scoped. "active"/"superseded" are
        never stored; a version is in force purely because effectiveFrom has
        arrived and no later approved version of the same ruleType has
        superseded it (effectiveTo set).
      required: [id, ruleType, status, effectiveFrom, effectiveTo, createdByMembershipId, approvedByMembershipId, approvedAt, sourceUrl, sourceDescription, sourceRetrievedAt, confirmedBy, confirmedAt, reasonNote, createdAt, updatedAt]
      properties:
        id:
          type: integer
        ruleType:
          type: string
          enum: [paye_bands, pension_rates, pension_earnings_ceiling]
        status:
          type: string
          enum: [draft, validated, approved]
        effectiveFrom:
          type: string
          format: date-time
        effectiveTo:
          type: ["string", "null"]
          format: date-time
        createdByMembershipId:
          type: integer
        approvedByMembershipId:
          type: ["integer", "null"]
        approvedAt:
          type: ["string", "null"]
          format: date-time
        sourceUrl:
          type: ["string", "null"]
        sourceDescription:
          type: ["string", "null"]
        sourceRetrievedAt:
          type: ["string", "null"]
          format: date-time
        confirmedBy:
          type: ["string", "null"]
        confirmedAt:
          type: ["string", "null"]
          format: date-time
        reasonNote:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    PayrollStatutoryRuleVersionDetail:
      type: object
      required: [version, payeBands, pensionRates, pensionEarningsCeiling]
      properties:
        version:
          $ref: "#/components/schemas/PayrollStatutoryRuleVersion"
        payeBands:
          type: array
          items:
            $ref: "#/components/schemas/PayrollPayeBand"
        pensionRates:
          oneOf:
            - $ref: "#/components/schemas/PayrollPensionRate"
            - type: "null"
        pensionEarningsCeiling:
          oneOf:
            - $ref: "#/components/schemas/PayrollPensionEarningsCeiling"
            - type: "null"

    CreatePayrollPayeBandInput:
      type: object
      required: [bandOrder, taxpayerCategory, thresholdAmount, ratePercent]
      properties:
        bandOrder:
          type: integer
          minimum: 1
        taxpayerCategory:
          type: string
          enum: [resident, non_resident]
        thresholdAmount:
          type: ["string", "null"]
        ratePercent:
          type: string

    CreatePayrollPensionRatesInput:
      type: object
      required: [employeeRatePercent, employerRatePercent, tier1AllocationPercent, tier2AllocationPercent]
      properties:
        employeeRatePercent:
          type: string
        employerRatePercent:
          type: string
        tier1AllocationPercent:
          type: string
        tier2AllocationPercent:
          type: string

    CreatePayrollPensionEarningsCeilingInput:
      type: object
      properties:
        minimumInsurableEarnings:
          type: ["string", "null"]
        maximumInsurableEarnings:
          type: ["string", "null"]

    CreatePayrollStatutoryRuleVersionBody:
      type: object
      description: >-
        Exactly one of payeBands/pensionRates/pensionEarningsCeiling must be
        supplied, matching ruleType — enforced server-side, not merely by
        this shape. No numeric Ghana figures should be supplied here except
        by an explicitly authorized, verified data-entry action; this
        endpoint itself performs no statutory verification.
      required: [ruleType, effectiveFrom]
      properties:
        ruleType:
          type: string
          enum: [paye_bands, pension_rates, pension_earnings_ceiling]
        effectiveFrom:
          type: string
          format: date-time
        sourceUrl:
          type: string
        sourceDescription:
          type: string
        sourceRetrievedAt:
          type: string
          format: date-time
        reasonNote:
          type: string
        payeBands:
          type: array
          items:
            $ref: "#/components/schemas/CreatePayrollPayeBandInput"
        pensionRates:
          $ref: "#/components/schemas/CreatePayrollPensionRatesInput"
        pensionEarningsCeiling:
          $ref: "#/components/schemas/CreatePayrollPensionEarningsCeilingInput"

    EmployeeCompensationComponent:
      type: object
      description: >-
        employeeId always references employees.id — never a staff/PIF
        number, so a reused staff number never carries a former holder's
        compensation to its new holder.
      required: [id, organizationId, employeeId, category, componentTypeCode, amount, currency, recurring, taxableTreatment, pensionable, sourceReferenceType, sourceReferenceId, validFrom, validTo, createdByMembershipId, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        category:
          type: string
          enum: [earning, deduction]
        componentTypeCode:
          type: string
        amount:
          type: string
        currency:
          type: string
        recurring:
          type: boolean
        taxableTreatment:
          type: string
          enum: [ordinary, benefit_in_kind, bonus, overtime]
        pensionable:
          type: boolean
        sourceReferenceType:
          type: ["string", "null"]
        sourceReferenceId:
          type: ["integer", "null"]
        validFrom:
          type: string
          format: date-time
        validTo:
          type: ["string", "null"]
          format: date-time
        createdByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateEmployeeCompensationComponentBody:
      type: object
      required: [category, componentTypeCode, amount, currency, validFrom]
      properties:
        category:
          type: string
          enum: [earning, deduction]
        componentTypeCode:
          type: string
        amount:
          type: string
        currency:
          type: string
        recurring:
          type: boolean
        taxableTreatment:
          type: string
          enum: [ordinary, benefit_in_kind, bonus, overtime]
        pensionable:
          type: boolean
        sourceReferenceType:
          type: string
        sourceReferenceId:
          type: integer
        validFrom:
          type: string
          format: date-time

    EmployeeBankingDetail:
      type: object
      required: [id, organizationId, employeeId, bankCode, accountNumber, accountName, branch, validFrom, validTo, createdByMembershipId, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        bankCode:
          type: string
        accountNumber:
          type: string
        accountName:
          type: string
        branch:
          type: ["string", "null"]
        validFrom:
          type: string
          format: date-time
        validTo:
          type: ["string", "null"]
          format: date-time
        createdByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateEmployeeBankingDetailBody:
      type: object
      required: [bankCode, accountNumber, accountName, validFrom]
      properties:
        bankCode:
          type: string
        accountNumber:
          type: string
        accountName:
          type: string
        branch:
          type: string
        validFrom:
          type: string
          format: date-time

    EmployeeStatutoryIdentifier:
      type: object
      required: [id, organizationId, employeeId, ssnitNumber, tin, validFrom, validTo, createdByMembershipId, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        ssnitNumber:
          type: ["string", "null"]
        tin:
          type: ["string", "null"]
        validFrom:
          type: string
          format: date-time
        validTo:
          type: ["string", "null"]
          format: date-time
        createdByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateEmployeeStatutoryIdentifierBody:
      type: object
      required: [validFrom]
      properties:
        ssnitNumber:
          type: string
        tin:
          type: string
        validFrom:
          type: string
          format: date-time

    PayrollPeriod:
      type: object
      required: [id, organizationId, frequency, periodKey, startDate, endDate, payDate, createdByMembershipId, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        frequency:
          type: string
          enum: [monthly, bi_weekly, weekly]
        periodKey:
          type: string
        startDate:
          type: string
          format: date-time
        endDate:
          type: string
          format: date-time
        payDate:
          type: string
          format: date-time
        createdByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreatePayrollPeriodBody:
      type: object
      required: [frequency, startDate, endDate, payDate]
      properties:
        frequency:
          type: string
          enum: [monthly, bi_weekly, weekly]
        startDate:
          type: string
          format: date-time
        endDate:
          type: string
          format: date-time
        payDate:
          type: string
          format: date-time

    PayrollRun:
      type: object
      required: [id, organizationId, payrollPeriodId, status, preparedByMembershipId, approvedByMembershipId, lockedAt, calculatedAt, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        payrollPeriodId:
          type: integer
        status:
          type: string
          enum: [draft, calculated, approved, locked]
        preparedByMembershipId:
          type: ["integer", "null"]
        approvedByMembershipId:
          type: ["integer", "null"]
        lockedAt:
          type: ["string", "null"]
          format: date-time
        calculatedAt:
          type: ["string", "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreatePayrollRunBody:
      type: object
      required: [payrollPeriodId]
      properties:
        payrollPeriodId:
          type: integer

    PayrollRunLine:
      type: object
      required: [id, organizationId, payrollRunId, employeeId, staffNumberSnapshot, payeBandsVersionId, pensionRatesVersionId, pensionEarningsCeilingVersionId, grossEarnings, pensionableEarnings, employeePensionDeduction, employerPensionContribution, tier1Amount, tier2Amount, taxableIncome, payeAmount, otherDeductions, netPay, currency, calculatedAt, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        payrollRunId:
          type: integer
        employeeId:
          type: integer
        staffNumberSnapshot:
          type: ["string", "null"]
        payeBandsVersionId:
          type: ["integer", "null"]
        pensionRatesVersionId:
          type: ["integer", "null"]
        pensionEarningsCeilingVersionId:
          type: ["integer", "null"]
        grossEarnings:
          type: string
        pensionableEarnings:
          type: string
        employeePensionDeduction:
          type: string
        employerPensionContribution:
          type: string
        tier1Amount:
          type: string
        tier2Amount:
          type: string
        taxableIncome:
          type: string
        payeAmount:
          type: string
        otherDeductions:
          type: string
        netPay:
          type: string
        currency:
          type: string
        calculatedAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    PayrollRunLineComponent:
      type: object
      required: [id, payrollRunLineId, category, componentTypeCode, amount, taxableTreatment, pensionable, source]
      properties:
        id:
          type: integer
        payrollRunLineId:
          type: integer
        category:
          type: string
          enum: [earning, deduction]
        componentTypeCode:
          type: string
        amount:
          type: string
        taxableTreatment:
          type: string
          enum: [ordinary, benefit_in_kind, bonus, overtime]
        pensionable:
          type: boolean
        source:
          type: string
          enum: [recurring, one_off]

    PayrollRunLineWithTrace:
      type: object
      required: [line, components]
      properties:
        line:
          $ref: "#/components/schemas/PayrollRunLine"
        components:
          type: array
          items:
            $ref: "#/components/schemas/PayrollRunLineComponent"

    PayrollRunCalculationSummary:
      type: object
      required: [run, employeeCount]
      properties:
        run:
          $ref: "#/components/schemas/PayrollRun"
        employeeCount:
          type: integer

    PayrollRunValidationErrorBody:
      type: object
      required: [error, employeeErrors]
      properties:
        error:
          type: string
        employeeErrors:
          type: array
          items:
            type: object
            required: [employeeId, error]
            properties:
              employeeId:
                type: integer
              error:
                type: string

    PayrollInputReference:
      type: object
      required: [id, organizationId, payrollPeriodId, employeeId, sourceType, sourceId, category, componentTypeCode, amount, currency, taxableTreatment, description, createdByMembershipId, approvedByMembershipId, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        payrollPeriodId:
          type: integer
        employeeId:
          type: integer
        sourceType:
          type: string
        sourceId:
          type: ["integer", "null"]
        category:
          type: string
          enum: [earning, deduction]
        componentTypeCode:
          type: string
        amount:
          type: string
        currency:
          type: string
        taxableTreatment:
          type: string
          enum: [ordinary, benefit_in_kind, bonus, overtime]
        description:
          type: ["string", "null"]
        createdByMembershipId:
          type: ["integer", "null"]
        approvedByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    CreatePayrollInputReferenceBody:
      type: object
      required: [employeeId, category, componentTypeCode, amount, currency]
      properties:
        employeeId:
          type: integer
        category:
          type: string
          enum: [earning, deduction]
        componentTypeCode:
          type: string
        amount:
          type: string
        currency:
          type: string
        taxableTreatment:
          type: string
          enum: [ordinary, benefit_in_kind, bonus, overtime]
        description:
          type: string

    PayrollCorrection:
      type: object
      required: [id, organizationId, originalRunId, originalRunLineId, employeeId, status, reason, staffNumberSnapshot, payeBandsVersionId, pensionRatesVersionId, pensionEarningsCeilingVersionId, grossEarnings, pensionableEarnings, employeePensionDeduction, employerPensionContribution, tier1Amount, tier2Amount, taxableIncome, payeAmount, otherDeductions, netPay, netPayDelta, currency, createdByMembershipId, approvedByMembershipId, approvedAt, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        originalRunId:
          type: integer
        originalRunLineId:
          type: integer
        employeeId:
          type: integer
        status:
          type: string
          enum: [draft, approved]
        reason:
          type: string
        staffNumberSnapshot:
          type: ["string", "null"]
        payeBandsVersionId:
          type: ["integer", "null"]
        pensionRatesVersionId:
          type: ["integer", "null"]
        pensionEarningsCeilingVersionId:
          type: ["integer", "null"]
        grossEarnings:
          type: string
        pensionableEarnings:
          type: string
        employeePensionDeduction:
          type: string
        employerPensionContribution:
          type: string
        tier1Amount:
          type: string
        tier2Amount:
          type: string
        taxableIncome:
          type: string
        payeAmount:
          type: string
        otherDeductions:
          type: string
        netPay:
          type: string
        netPayDelta:
          type: string
        currency:
          type: string
        createdByMembershipId:
          type: ["integer", "null"]
        approvedByMembershipId:
          type: ["integer", "null"]
        approvedAt:
          type: ["string", "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time

    CreatePayrollCorrectionBody:
      type: object
      required: [originalRunLineId, reason]
      properties:
        originalRunLineId:
          type: integer
        reason:
          type: string

    PayrollCorrectionComponent:
      type: object
      required: [id, payrollCorrectionId, category, componentTypeCode, amount, taxableTreatment, pensionable, source]
      properties:
        id:
          type: integer
        payrollCorrectionId:
          type: integer
        category:
          type: string
          enum: [earning, deduction]
        componentTypeCode:
          type: string
        amount:
          type: string
        taxableTreatment:
          type: string
          enum: [ordinary, benefit_in_kind, bonus, overtime]
        pensionable:
          type: boolean
        source:
          type: string
          enum: [recurring, one_off]

    PayrollCorrectionWithTrace:
      type: object
      required: [correction, components]
      properties:
        correction:
          $ref: "#/components/schemas/PayrollCorrection"
        components:
          type: array
          items:
            $ref: "#/components/schemas/PayrollCorrectionComponent"

    PayslipFigures:
      type: object
      required: [grossEarnings, pensionableEarnings, employeePensionDeduction, employerPensionContribution, tier1Amount, tier2Amount, taxableIncome, payeAmount, otherDeductions, netPay]
      properties:
        grossEarnings:
          type: string
        pensionableEarnings:
          type: string
        employeePensionDeduction:
          type: string
        employerPensionContribution:
          type: string
        tier1Amount:
          type: string
        tier2Amount:
          type: string
        taxableIncome:
          type: string
        payeAmount:
          type: string
        otherDeductions:
          type: string
        netPay:
          type: string

    PayslipComponent:
      type: object
      required: [category, componentTypeCode, amount, taxableTreatment, pensionable, source]
      properties:
        category:
          type: string
          enum: [earning, deduction]
        componentTypeCode:
          type: string
        amount:
          type: string
        taxableTreatment:
          type: string
          enum: [ordinary, benefit_in_kind, bonus, overtime]
        pensionable:
          type: boolean
        source:
          type: string
          enum: [recurring, one_off]

    PayslipOriginal:
      allOf:
        - $ref: "#/components/schemas/PayslipFigures"
        - type: object
          required: [components]
          properties:
            components:
              type: array
              items:
                $ref: "#/components/schemas/PayslipComponent"

    PayslipCorrection:
      allOf:
        - $ref: "#/components/schemas/PayslipFigures"
        - type: object
          required: [id, status, reason, netPayDelta, approvedAt, components]
          properties:
            id:
              type: integer
            status:
              type: string
              enum: [draft, approved]
            reason:
              type: string
            netPayDelta:
              type: string
            approvedAt:
              type: ["string", "null"]
              format: date-time
            components:
              type: array
              items:
                $ref: "#/components/schemas/PayslipComponent"

    PayslipEffective:
      allOf:
        - $ref: "#/components/schemas/PayslipFigures"
        - type: object
          required: [source, correctionId]
          properties:
            source:
              type: string
              enum: [original, correction]
            correctionId:
              type: ["integer", "null"]

    PayslipPeriod:
      type: object
      required: [id, frequency, periodKey, startDate, endDate, payDate]
      properties:
        id:
          type: integer
        frequency:
          type: string
          enum: [monthly, bi_weekly, weekly]
        periodKey:
          type: string
        startDate:
          type: string
          format: date-time
        endDate:
          type: string
          format: date-time
        payDate:
          type: string
          format: date-time

    Payslip:
      type: object
      required: [organizationId, payrollRunId, payrollRunLineId, employeeId, employeeName, staffNumberSnapshot, payrollPeriod, currency, original, corrections, effective]
      properties:
        organizationId:
          type: integer
        payrollRunId:
          type: integer
        payrollRunLineId:
          type: integer
        employeeId:
          type: integer
        employeeName:
          type: string
        staffNumberSnapshot:
          type: ["string", "null"]
        payrollPeriod:
          $ref: "#/components/schemas/PayslipPeriod"
        currency:
          type: string
        original:
          $ref: "#/components/schemas/PayslipOriginal"
        corrections:
          type: array
          items:
            $ref: "#/components/schemas/PayslipCorrection"
        effective:
          $ref: "#/components/schemas/PayslipEffective"

    OwnPayslipSummary:
      type: object
      required: [payrollRunId, payrollRunLineId, payrollPeriod, netPay, currency, hasApprovedCorrection]
      properties:
        payrollRunId:
          type: integer
        payrollRunLineId:
          type: integer
        payrollPeriod:
          type: object
          required: [id, periodKey, payDate]
          properties:
            id:
              type: integer
            periodKey:
              type: string
            payDate:
              type: string
              format: date-time
        netPay:
          type: string
        currency:
          type: string
        hasApprovedCorrection:
          type: boolean

    PayrollReportColumn:
      type: object
      required: [key, label]
      properties:
        key:
          type: string
        label:
          type: string

    PayrollReportResult:
      type: object
      required: [key, label, generatedAt, columns, rows, totals]
      properties:
        key:
          type: string
        label:
          type: string
        generatedAt:
          type: string
          format: date-time
        columns:
          type: array
          items:
            $ref: "#/components/schemas/PayrollReportColumn"
        rows:
          type: array
          items:
            type: object
            additionalProperties: true
        totals:
          type: object
          additionalProperties:
            type: string

    PayrollPaymentBatch:
      type: object
      required: [id, organizationId, payrollRunId, paymentMethod, reference, status, currency, totalAmount, employeeCount, createdByMembershipId, createdAt, exportedAt, exportedByMembershipId, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        payrollRunId:
          type: integer
        paymentMethod:
          type: string
          enum: [bank_transfer]
        reference:
          type: string
        status:
          type: string
          enum: [draft, exported]
        currency:
          type: string
        totalAmount:
          type: string
        employeeCount:
          type: integer
        createdByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        exportedAt:
          type: ["string", "null"]
          format: date-time
        exportedByMembershipId:
          type: ["integer", "null"]
        updatedAt:
          type: string
          format: date-time

    PayrollPaymentBatchLine:
      type: object
      required: [id, paymentBatchId, organizationId, payrollRunLineId, sourceCorrectionId, employeeId, staffNumberSnapshot, amount, currency, bankCode, accountNumber, accountName, branch, paymentReference, createdAt]
      properties:
        id:
          type: integer
        paymentBatchId:
          type: integer
        organizationId:
          type: integer
        payrollRunLineId:
          type: integer
        sourceCorrectionId:
          type: ["integer", "null"]
        employeeId:
          type: integer
        employeeName:
          type: string
        staffNumberSnapshot:
          type: ["string", "null"]
        amount:
          type: string
        currency:
          type: string
        bankCode:
          type: string
        accountNumber:
          type: string
          description: Masked (last 4 digits only) in every JSON response — the full value is only ever present in the CSV export.
        accountName:
          type: string
        branch:
          type: ["string", "null"]
        paymentReference:
          type: string
        createdAt:
          type: string
          format: date-time

    PayrollPaymentBatchExcludedLine:
      type: object
      required: [employeeId, netPay, reason]
      properties:
        employeeId:
          type: integer
        netPay:
          type: string
        reason:
          type: string
          enum: [zero_net_pay, negative_net_pay]

    PayrollPaymentBatchCreateResult:
      type: object
      required: [batch, lines, excludedLines]
      properties:
        batch:
          $ref: "#/components/schemas/PayrollPaymentBatch"
        lines:
          type: array
          items:
            $ref: "#/components/schemas/PayrollPaymentBatchLine"
        excludedLines:
          type: array
          items:
            $ref: "#/components/schemas/PayrollPaymentBatchExcludedLine"

    PayrollPaymentBatchDetail:
      type: object
      required: [batch, lines]
      properties:
        batch:
          $ref: "#/components/schemas/PayrollPaymentBatch"
        lines:
          type: array
          items:
            $ref: "#/components/schemas/PayrollPaymentBatchLine"

    RecruitmentStageBreakdownItem:
      type: object
      required: [stageId, stageName, category, count]
      properties:
        stageId:
          type: ["integer", "null"]
        stageName:
          type: string
        category:
          type: string
          enum: [applied, screening, interview, assessment, offer, hired, rejected, withdrawn]
        count:
          type: integer

    RecruitmentWorkloadItem:
      type: object
      required: [employeeId, employeeName, openRequisitionsCount]
      properties:
        employeeId:
          type: integer
        employeeName:
          type: string
        openRequisitionsCount:
          type: integer

    RecruitmentDashboard:
      type: object
      required: [openRequisitionsCount, openVacanciesCount, totalApplicantsCount, candidatesByStage, recruiterWorkload, hiringManagerWorkload]
      properties:
        openRequisitionsCount:
          type: integer
        openVacanciesCount:
          type: integer
        totalApplicantsCount:
          type: integer
        candidatesByStage:
          type: array
          items:
            $ref: "#/components/schemas/RecruitmentStageBreakdownItem"
        recruiterWorkload:
          type: array
          items:
            $ref: "#/components/schemas/RecruitmentWorkloadItem"
        hiringManagerWorkload:
          type: array
          items:
            $ref: "#/components/schemas/RecruitmentWorkloadItem"

    PerformanceRatingScale:
      type: object
      required: [id, organizationId, name, status, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        description:
          type: ["string", "null"]
        status:
          type: string
          enum: [active, archived]
        createdAt:
          type: string
          format: date-time

    PerformanceRatingScaleLevel:
      type: object
      required: [id, ratingScaleId, value, label, sortOrder]
      properties:
        id:
          type: integer
        ratingScaleId:
          type: integer
        value:
          type: number
        label:
          type: string
        description:
          type: ["string", "null"]
        sortOrder:
          type: integer

    PerformanceRatingScaleWithLevels:
      type: object
      required: [scale, levels, levelsLocked]
      properties:
        scale:
          $ref: "#/components/schemas/PerformanceRatingScale"
        levels:
          type: array
          items:
            $ref: "#/components/schemas/PerformanceRatingScaleLevel"
        levelsLocked:
          type: boolean
          description: True once any Performance review has used this scale — the level set can no longer be edited (archive and create a new scale instead).

    CreatePerformanceRatingScaleInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string

    UpdatePerformanceRatingScaleInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
        status:
          type: string
          enum: [active, archived]

    PerformanceRatingScaleLevelInput:
      type: object
      required: [value, label, sortOrder]
      properties:
        value:
          type: number
        label:
          type: string
          minLength: 1
        description:
          type: string
        sortOrder:
          type: integer
          minimum: 0

    ReplacePerformanceRatingScaleLevelsInput:
      type: object
      required: [levels]
      properties:
        levels:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/PerformanceRatingScaleLevelInput"

    PerformanceReviewTemplate:
      type: object
      required: [id, organizationId, name, ratingScaleId, goalsWeight, competenciesWeight, applicabilityScope, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        description:
          type: ["string", "null"]
        ratingScaleId:
          type: integer
        goalsWeight:
          type: integer
        competenciesWeight:
          type: integer
        applicabilityScope:
          type: string
          enum: [all_active, department, position, manual]
        applicabilityDepartmentIds:
          type: ["array", "null"]
          items:
            type: integer
        applicabilityPositionIds:
          type: ["array", "null"]
          items:
            type: integer
        status:
          type: string
          enum: [draft, active, archived]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    PerformanceTemplateCompetency:
      type: object
      required: [id, templateId, label, weight, sortOrder]
      properties:
        id:
          type: integer
        templateId:
          type: integer
        label:
          type: string
        description:
          type: ["string", "null"]
        weight:
          type: integer
        sortOrder:
          type: integer

    PerformanceReviewTemplateWithCompetencies:
      type: object
      required: [template, competencies]
      properties:
        template:
          $ref: "#/components/schemas/PerformanceReviewTemplate"
        competencies:
          type: array
          items:
            $ref: "#/components/schemas/PerformanceTemplateCompetency"

    CreatePerformanceReviewTemplateInput:
      type: object
      required: [name, ratingScaleId, goalsWeight, competenciesWeight, applicabilityScope]
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
        ratingScaleId:
          type: integer
        goalsWeight:
          type: integer
          minimum: 0
          maximum: 100
        competenciesWeight:
          type: integer
          minimum: 0
          maximum: 100
        applicabilityScope:
          type: string
          enum: [all_active, department, position, manual]
        applicabilityDepartmentIds:
          type: array
          items:
            type: integer
        applicabilityPositionIds:
          type: array
          items:
            type: integer
        status:
          type: string
          enum: [draft, active, archived]

    UpdatePerformanceReviewTemplateInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
        ratingScaleId:
          type: integer
        goalsWeight:
          type: integer
          minimum: 0
          maximum: 100
        competenciesWeight:
          type: integer
          minimum: 0
          maximum: 100
        applicabilityScope:
          type: string
          enum: [all_active, department, position, manual]
        applicabilityDepartmentIds:
          type: array
          items:
            type: integer
        applicabilityPositionIds:
          type: array
          items:
            type: integer
        status:
          type: string
          enum: [draft, active, archived]

    PerformanceTemplateCompetencyInput:
      type: object
      required: [label, weight, sortOrder]
      properties:
        label:
          type: string
          minLength: 1
        description:
          type: string
        weight:
          type: integer
          minimum: 0
          maximum: 100
        sortOrder:
          type: integer
          minimum: 0

    ReplacePerformanceTemplateCompetenciesInput:
      type: object
      required: [competencies]
      properties:
        competencies:
          type: array
          items:
            $ref: "#/components/schemas/PerformanceTemplateCompetencyInput"

    PerformanceCycle:
      type: object
      required:
        [id, organizationId, name, cycleType, startDate, endDate, templateId, ratingScaleId, applicabilityScope, status, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        cycleType:
          type: string
          enum: [annual, semiannual, quarterly, monthly, probation, ad_hoc]
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
        selfAssessmentWindowStart:
          type: ["string", "null"]
          format: date
        selfAssessmentWindowEnd:
          type: ["string", "null"]
          format: date
        managerReviewWindowStart:
          type: ["string", "null"]
          format: date
        managerReviewWindowEnd:
          type: ["string", "null"]
          format: date
        hrFinalizationWindowStart:
          type: ["string", "null"]
          format: date
        hrFinalizationWindowEnd:
          type: ["string", "null"]
          format: date
        templateId:
          type: integer
        ratingScaleId:
          type: integer
        applicabilityScope:
          type: string
          enum: [all_active, department, position, manual]
        applicabilityDepartmentIds:
          type: ["array", "null"]
          items:
            type: integer
        applicabilityPositionIds:
          type: ["array", "null"]
          items:
            type: integer
        status:
          type: string
          enum: [draft, open, closed, archived]
        createdAt:
          type: string
          format: date-time

    CreatePerformanceCycleInput:
      type: object
      required: [name, cycleType, startDate, endDate, templateId, ratingScaleId, applicabilityScope]
      properties:
        name:
          type: string
          minLength: 1
        cycleType:
          type: string
          enum: [annual, semiannual, quarterly, monthly, probation, ad_hoc]
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
        selfAssessmentWindowStart:
          type: string
          format: date
        selfAssessmentWindowEnd:
          type: string
          format: date
        managerReviewWindowStart:
          type: string
          format: date
        managerReviewWindowEnd:
          type: string
          format: date
        hrFinalizationWindowStart:
          type: string
          format: date
        hrFinalizationWindowEnd:
          type: string
          format: date
        templateId:
          type: integer
        ratingScaleId:
          type: integer
        applicabilityScope:
          type: string
          enum: [all_active, department, position, manual]
        applicabilityDepartmentIds:
          type: array
          items:
            type: integer
        applicabilityPositionIds:
          type: array
          items:
            type: integer

    UpdatePerformanceCycleInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        cycleType:
          type: string
          enum: [annual, semiannual, quarterly, monthly, probation, ad_hoc]
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
        selfAssessmentWindowStart:
          type: string
          format: date
        selfAssessmentWindowEnd:
          type: string
          format: date
        managerReviewWindowStart:
          type: string
          format: date
        managerReviewWindowEnd:
          type: string
          format: date
        hrFinalizationWindowStart:
          type: string
          format: date
        hrFinalizationWindowEnd:
          type: string
          format: date
        templateId:
          type: integer
        ratingScaleId:
          type: integer
        applicabilityScope:
          type: string
          enum: [all_active, department, position, manual]
        applicabilityDepartmentIds:
          type: array
          items:
            type: integer
        applicabilityPositionIds:
          type: array
          items:
            type: integer
        status:
          type: string
          description: Only draft->archived, open->closed, or closed->archived are valid — 'open' can never be set directly.
          enum: [closed, archived]

    GeneratePerformanceReviewsInput:
      type: object
      properties:
        employeeIds:
          type: array
          description: Required and non-empty only when the cycle's applicabilityScope is 'manual'; ignored otherwise (eligibility is auto-resolved).
          items:
            type: integer

    PerformanceReview:
      type: object
      required:
        [
          id, organizationId, cycleId, templateId, ratingScaleId, employeeId,
          goalsWeight, competenciesWeight, scoringPrecisionSnapshot,
          acknowledgementRequiredSnapshot, status, revisionNumber, createdAt, updatedAt,
        ]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        cycleId:
          type: integer
        templateId:
          type: integer
        ratingScaleId:
          type: integer
        employeeId:
          type: integer
        reviewerEmployeeId:
          type: ["integer", "null"]
        departmentIdSnapshot:
          type: ["integer", "null"]
        positionIdSnapshot:
          type: ["integer", "null"]
        goalsWeight:
          type: integer
        competenciesWeight:
          type: integer
        scoringPrecisionSnapshot:
          type: integer
        acknowledgementRequiredSnapshot:
          type: boolean
        status:
          type: string
          enum: [draft, self_assessment, manager_review, hr_review, finalized, acknowledged]
        selfAssessmentSubmittedAt:
          type: ["string", "null"]
          format: date-time
        managerReviewSubmittedAt:
          type: ["string", "null"]
          format: date-time
        hrFinalizedAt:
          type: ["string", "null"]
          format: date-time
        acknowledgedAt:
          type: ["string", "null"]
          format: date-time
        employeeFinalComment:
          type: ["string", "null"]
        computedOverallScore:
          type: ["string", "null"]
        hrOverrideScore:
          type: ["string", "null"]
        hrOverrideReason:
          type: ["string", "null"]
        revisionNumber:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    GeneratePerformanceReviewsResult:
      type: object
      required: [cycle, reviewsCreated, reviews]
      properties:
        cycle:
          $ref: "#/components/schemas/PerformanceCycle"
        reviewsCreated:
          type: integer
        reviews:
          type: array
          items:
            $ref: "#/components/schemas/PerformanceReview"

    PerformanceReviewListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/PerformanceReview"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    PerformanceDashboardStatusItem:
      type: object
      required: [status, count]
      properties:
        status:
          type: string
          enum: [draft, self_assessment, manager_review, hr_review, finalized, acknowledged]
        count:
          type: integer

    PerformanceDashboard:
      type: object
      required:
        [
          cycleId, activeCycleCount, employeesAssignedCount, statusBreakdown,
          selfAssessmentPendingCount, selfAssessmentSubmittedCount,
          managerReviewPendingCount, managerReviewSubmittedCount,
          proposedGoalsAwaitingDecisionCount, finalizedCount, acknowledgedCount,
        ]
      properties:
        cycleId:
          type: ["integer", "null"]
          description: The cycle this dashboard is scoped to, if ?cycleId= was supplied; null when aggregating across all cycles in scope.
        activeCycleCount:
          type: integer
          description: Organization-wide count of cycles with status = open, regardless of the caller's own review scope.
        employeesAssignedCount:
          type: integer
          description: Distinct employees among the caller's authorized, optionally cycle-filtered review set.
        statusBreakdown:
          type: array
          description: All 6 lifecycle statuses, zero-filled, fixed order. A reopened review counts only under its current status.
          items:
            $ref: "#/components/schemas/PerformanceDashboardStatusItem"
        selfAssessmentPendingCount:
          type: integer
        selfAssessmentSubmittedCount:
          type: integer
        managerReviewPendingCount:
          type: integer
        managerReviewSubmittedCount:
          type: integer
        proposedGoalsAwaitingDecisionCount:
          type: integer
        finalizedCount:
          type: integer
        acknowledgedCount:
          type: integer

    PerformanceReviewCompetency:
      type: object
      required: [id, organizationId, reviewId, label, weight, sortOrder]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        reviewId:
          type: integer
        label:
          type: string
        description:
          type: ["string", "null"]
        weight:
          type: integer
        sortOrder:
          type: integer
        employeeRatingValue:
          type: ["string", "null"]
        employeeComment:
          type: ["string", "null"]
        managerRatingValue:
          type: ["string", "null"]
        managerComment:
          type: ["string", "null"]
        notApplicable:
          type: boolean
        notApplicableReason:
          type: ["string", "null"]

    PerformanceGoal:
      type: object
      required: [id, organizationId, reviewId, title, measurementType, weight, status, originType, approvalStatus, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        reviewId:
          type: integer
        title:
          type: string
        description:
          type: ["string", "null"]
        measurementType:
          type: string
          enum: [numeric, percentage, currency, boolean, rating, qualitative]
        target:
          type: ["string", "null"]
        actualResult:
          type: ["string", "null"]
        unit:
          type: ["string", "null"]
        weight:
          type: integer
        dueDate:
          type: ["string", "null"]
          format: date
        status:
          type: string
          enum: [not_started, in_progress, completed, missed]
        employeeComment:
          type: ["string", "null"]
        managerComment:
          type: ["string", "null"]
        computedScore:
          type: ["string", "null"]
        originType:
          type: string
          enum: [manager, employee_proposed]
        approvalStatus:
          type: string
          enum: [accepted, proposed, rejected]
        notApplicable:
          type: boolean
        notApplicableReason:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreatePerformanceReviewGoalInput:
      type: object
      required: [title, measurementType]
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: string
        measurementType:
          type: string
          enum: [numeric, percentage, currency, boolean, rating, qualitative]
        target:
          type: number
        unit:
          type: string
        weight:
          type: integer
          description: Must be 0 for a qualitative goal.
        dueDate:
          type: string
          format: date

    UpdatePerformanceReviewGoalInput:
      type: object
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: string
        target:
          type: ["number", "null"]
        unit:
          type: ["string", "null"]
        weight:
          type: integer
        dueDate:
          type: ["string", "null"]
          format: date
        employeeComment:
          type: ["string", "null"]
          description: >-
            W77 addition — the only field the goal's own employee may set
            on an already-`accepted` official goal, self_assessment only
            (§14's "enter comments on goals"). Structural fields above
            remain reviewer-only on an accepted goal.
        actualResult:
          type: ["number", "null"]
          description: >-
            W78 addition, reviewer-of-record only, manager_review only.
            Validated against the goal's own measurementType — rejected
            for qualitative, must be 0/1 for boolean, must match a
            configured rating-scale level for rating.
        managerComment:
          type: ["string", "null"]
          description: W78 addition, reviewer-of-record only, manager_review only.
        notApplicable:
          type: boolean
          description: W78 addition, reviewer-of-record only, manager_review only. Requires notApplicableReason.
        notApplicableReason:
          type: ["string", "null"]

    AcceptPerformanceReviewGoalInput:
      type: object
      description: All fields optional — accept-as-is, or edit-then-accept in the same action (§12).
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: string
        target:
          type: ["number", "null"]
        unit:
          type: ["string", "null"]
        weight:
          type: integer
        dueDate:
          type: ["string", "null"]
          format: date

    RejectPerformanceReviewGoalInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
          description: Stored as the goal's managerComment.

    RateCompetencyInput:
      type: object
      description: >-
        Dual-purpose (W77 employee self-rating / W78 manager rating) —
        which fields apply is resolved server-side from the caller's
        relationship to the review, never a client-supplied flag. The
        review's own employee must supply employeeRatingValue
        (self_assessment only); the reviewer of record may supply any of
        managerRatingValue/managerComment/notApplicable/
        notApplicableReason (manager_review only).
      properties:
        employeeRatingValue:
          type: number
          description: Required for the employee path. Must match one of this review's own configured rating-scale level values.
        employeeComment:
          type: string
        managerRatingValue:
          type: number
          description: W78 addition, reviewer-of-record only. Must match one of this review's own configured rating-scale level values.
        managerComment:
          type: string
        notApplicable:
          type: boolean
          description: W78 addition, reviewer-of-record only. Requires notApplicableReason.
        notApplicableReason:
          type: string

    SelfAssessmentNotReadyError:
      type: object
      required: [error, problems]
      properties:
        error:
          type: string
        problems:
          type: array
          items:
            type: string
          description: Every readiness problem found, listed at once — not just the first.

    FinalizePerformanceReviewInput:
      type: object
      description: Both fields optional, but must be supplied together (or neither) — finalize with or without an override in the same call.
      properties:
        hrOverrideScore:
          type: number
          description: 0-100. computedOverallScore (the manager's own score) is preserved unedited alongside this.
        hrOverrideReason:
          type: string
          description: Mandatory whenever hrOverrideScore is supplied.

    ReopenPerformanceReviewInput:
      type: object
      required: [targetStage, reason]
      properties:
        targetStage:
          type: string
          enum: [self_assessment, manager_review, hr_review]
          description: Must be strictly earlier than the review's current status.
        reason:
          type: string
          minLength: 1

    AcknowledgePerformanceReviewInput:
      type: object
      description: Both fields optional — acknowledgement never requires a comment.
      properties:
        employeeFinalComment:
          type: string
          description: >-
            Optional final response, captured verbatim in employeeFinalComment.
            "I have seen this review," not agreement — never changes any score
            or review field, never constitutes a formal appeal.

    PerformanceReviewWithCompetencies:
      type: object
      required: [review, competencies, goals]
      properties:
        review:
          $ref: "#/components/schemas/PerformanceReview"
        competencies:
          type: array
          items:
            $ref: "#/components/schemas/PerformanceReviewCompetency"
        goals:
          type: array
          items:
            $ref: "#/components/schemas/PerformanceGoal"

    PerformanceReviewEvidence:
      type: object
      required: [id, organizationId, reviewId, goalId, employeeDocumentId, addedByMembershipId, addedAt, fileName, mimeType, fileSize, uploadedBy]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        reviewId:
          type: integer
        goalId:
          type: ["integer", "null"]
        employeeDocumentId:
          type: integer
          description: Points into the existing employee_documents table (§8.9) — no separate Performance storage layer.
        addedByMembershipId:
          type: ["integer", "null"]
        addedAt:
          type: string
          format: date-time
        fileName:
          type: string
          description: Original client-supplied filename, for display only — never used to build a storage path.
        mimeType:
          type: string
        fileSize:
          type: integer
        uploadedBy:
          type: ["integer", "null"]

    LearningCourse:
      type: object
      required:
        [
          id, organizationId, categoryCode, title, deliveryMode, mandatoryDefault,
          requiresApproval, hasAssessment, issuesCertificate, status, createdAt, updatedAt,
        ]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        categoryCode:
          type: string
          description: Free-text code from the training_category Master Data domain, not validated against the domain's item list.
        title:
          type: string
        description:
          type: ["string", "null"]
        deliveryMode:
          type: string
          enum: [self_paced, instructor_led]
        mandatoryDefault:
          type: boolean
          description: Informational default only — snapshotted onto each enrollment, overridable per-assignment (a later workstream's job).
        requiresApproval:
          type: boolean
        hasAssessment:
          type: boolean
        issuesCertificate:
          type: boolean
        certificateValidityMonths:
          type: ["integer", "null"]
          description: Null = certificate never expires. Only meaningful when issuesCertificate is true.
        status:
          type: string
          enum: [draft, active, archived]
        createdBy:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateLearningCourseInput:
      type: object
      required: [categoryCode, title, deliveryMode]
      properties:
        categoryCode:
          type: string
          minLength: 1
        title:
          type: string
          minLength: 1
        description:
          type: string
        deliveryMode:
          type: string
          enum: [self_paced, instructor_led]
        mandatoryDefault:
          type: boolean
        requiresApproval:
          type: boolean
        hasAssessment:
          type: boolean
        issuesCertificate:
          type: boolean
        certificateValidityMonths:
          type: integer
          minimum: 1
        status:
          type: string
          enum: [draft, active, archived]

    UpdateLearningCourseInput:
      type: object
      properties:
        categoryCode:
          type: string
          minLength: 1
        title:
          type: string
          minLength: 1
        description:
          type: string
        deliveryMode:
          type: string
          enum: [self_paced, instructor_led]
        mandatoryDefault:
          type: boolean
        requiresApproval:
          type: boolean
        hasAssessment:
          type: boolean
        issuesCertificate:
          type: boolean
        certificateValidityMonths:
          type: ["integer", "null"]
          minimum: 1
        status:
          type: string
          enum: [draft, active, archived]

    LearningCourseSession:
      type: object
      required: [id, organizationId, courseId, scheduledAt, durationMinutes, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        courseId:
          type: integer
        scheduledAt:
          type: string
          format: date-time
        durationMinutes:
          type: integer
        location:
          type: ["string", "null"]
        meetingLink:
          type: ["string", "null"]
        instructorEmployeeId:
          type: ["integer", "null"]
        capacity:
          type: ["integer", "null"]
          description: Null = uncapped. Configuration only — enforced against actual enrollments by a later workstream, never here.
        status:
          type: string
          enum: [scheduled, completed, cancelled]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateLearningCourseSessionInput:
      type: object
      required: [scheduledAt, durationMinutes]
      properties:
        scheduledAt:
          type: string
          format: date-time
        durationMinutes:
          type: integer
          minimum: 1
        location:
          type: string
        meetingLink:
          type: string
        instructorEmployeeId:
          type: integer
        capacity:
          type: integer
          minimum: 1

    UpdateLearningCourseSessionInput:
      type: object
      description: >-
        Both ordinary field edits and an optional one-way status
        transition (completed/cancelled) may be supplied in the same call
        — only while the session is still 'scheduled'.
      properties:
        scheduledAt:
          type: string
          format: date-time
        durationMinutes:
          type: integer
          minimum: 1
        location:
          type: string
        meetingLink:
          type: string
        instructorEmployeeId:
          type: ["integer", "null"]
        capacity:
          type: ["integer", "null"]
          minimum: 1
        status:
          type: string
          enum: [completed, cancelled]
          description: One-way only — 'scheduled' is never a valid target value here.

    LearningEnrollment:
      type: object
      required:
        [
          id,
          organizationId,
          courseId,
          sessionId,
          employeeId,
          courseTitleSnapshot,
          categorySnapshot,
          deliveryModeSnapshot,
          hasAssessmentSnapshot,
          issuesCertificateSnapshot,
          certificateValidityMonthsSnapshot,
          departmentIdSnapshot,
          positionIdSnapshot,
          managerEmployeeIdSnapshot,
          mandatoryAtAssignment,
          originType,
          assignedByMembershipId,
          dueDate,
          approvalStatus,
          approvalDecidedByMembershipId,
          approvalDecidedAt,
          status,
          attended,
          attendanceMarkedByMembershipId,
          attendanceMarkedAt,
          passed,
          score,
          completedAt,
          cancelReason,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        courseId:
          type: integer
        sessionId:
          type: ["integer", "null"]
        employeeId:
          type: integer
        courseTitleSnapshot:
          type: string
        categorySnapshot:
          type: string
        deliveryModeSnapshot:
          type: string
          enum: [self_paced, instructor_led]
        hasAssessmentSnapshot:
          type: boolean
        issuesCertificateSnapshot:
          type: boolean
        certificateValidityMonthsSnapshot:
          type: ["integer", "null"]
        departmentIdSnapshot:
          type: ["integer", "null"]
        positionIdSnapshot:
          type: ["integer", "null"]
        managerEmployeeIdSnapshot:
          type: ["integer", "null"]
        mandatoryAtAssignment:
          type: boolean
        originType:
          type: string
          enum: [hr_assigned, manager_assigned, employee_requested]
        assignedByMembershipId:
          type: ["integer", "null"]
        dueDate:
          type: ["string", "null"]
          format: date-time
        approvalStatus:
          type: string
          enum: [auto_approved, pending, approved, rejected]
        approvalDecidedByMembershipId:
          type: ["integer", "null"]
        approvalDecidedAt:
          type: ["string", "null"]
          format: date-time
        status:
          type: string
          enum: [assigned, in_progress, completed, failed, cancelled]
        attended:
          type: ["boolean", "null"]
        attendanceMarkedByMembershipId:
          type: ["integer", "null"]
        attendanceMarkedAt:
          type: ["string", "null"]
          format: date-time
        passed:
          type: ["boolean", "null"]
        score:
          type: ["string", "null"]
        completedAt:
          type: ["string", "null"]
          format: date-time
        cancelReason:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    RequestLearningEnrollmentInput:
      type: object
      description: employeeId is never accepted here — always server-derived from the caller's own linked employee record.
      properties:
        sessionId:
          type: integer
          description: Required for an instructor-led course, forbidden for a self-paced one.

    AssignLearningEnrollmentsInput:
      type: object
      required: [scope]
      description: >-
        A caller holding only learning.review.write (manager of record) is
        restricted server-side to scope = 'manual' with every employeeId
        validated as their own current direct report — audience-targeting
        scopes are learning.manage only.
      properties:
        sessionId:
          type: integer
          description: Required for an instructor-led course, forbidden for a self-paced one.
        scope:
          type: string
          enum: [all_active, department, position, manual]
        departmentId:
          type: integer
          description: Required and used only when scope = department.
        positionId:
          type: integer
          description: Required and used only when scope = position.
        employeeIds:
          type: array
          items:
            type: integer
          description: Required and used only when scope = manual.
        mandatory:
          type: boolean
          description: Overrides the course's own mandatoryDefault for these enrollments. Defaults to the course's mandatoryDefault when omitted.
        dueDate:
          type: string
          format: date-time

    AssignLearningEnrollmentsResult:
      type: object
      required: [assignedCount, skippedCount, enrollments, skippedEmployeeIds]
      properties:
        assignedCount:
          type: integer
        skippedCount:
          type: integer
          description: Eligible employees skipped because they already held a non-terminal enrollment for this course/session — not treated as a batch failure.
        enrollments:
          type: array
          items:
            $ref: "#/components/schemas/LearningEnrollment"
        skippedEmployeeIds:
          type: array
          items:
            type: integer

    CancelLearningEnrollmentInput:
      type: object
      properties:
        cancelReason:
          type: string
          description: Required when the caller is not the enrollment's own employee, or when the enrollment is mandatory. Optional for an employee self-cancelling their own non-mandatory, not-yet-started enrollment.

    LearningEnrollmentListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/LearningEnrollment"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    AdvanceLearningEnrollmentProgressInput:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [in_progress, completed]
          description: The one-way target state — assigned→in_progress or in_progress→completed only. No other value is ever accepted here.

    MarkLearningEnrollmentAttendanceInput:
      type: object
      required: [attended]
      properties:
        attended:
          type: boolean

    CompleteLearningEnrollmentInput:
      type: object
      properties:
        passed:
          type: boolean
          description: Required when the enrollment's own hasAssessmentSnapshot is true; must not be supplied otherwise. Determines completed (true) vs. failed (false) server-side.
        score:
          type: number
          description: Optional free-form numeric result, only meaningful alongside passed. Never a substitute for passed — there is no derived pass threshold.

    LearningCertificateStatus:
      type: string
      enum: [active, revoked]
      description: '"Expired" is never a stored value here (§10.4 rule 9) — always computed live from expiresAt < now().'

    LearningCertificate:
      type: object
      required:
        [
          id,
          organizationId,
          enrollmentId,
          employeeId,
          courseTitleSnapshot,
          certificateNumber,
          issuedAt,
          expiresAt,
          status,
          revokedByMembershipId,
          revokedAt,
          revokeReason,
          employeeDocumentId,
          createdAt,
        ]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        enrollmentId:
          type: integer
        employeeId:
          type: integer
        courseTitleSnapshot:
          type: string
          description: Copied from the enrollment's own courseTitleSnapshot at issuance — a chain of snapshots, never re-read from the live course.
        certificateNumber:
          type: ["string", "null"]
        issuedAt:
          type: string
          format: date-time
        expiresAt:
          type: ["string", "null"]
          format: date-time
          description: Computed exactly once at issuance from the enrollment's own certificateValidityMonthsSnapshot. Null means never expires. Never recomputed.
        status:
          $ref: "#/components/schemas/LearningCertificateStatus"
        revokedByMembershipId:
          type: ["integer", "null"]
        revokedAt:
          type: ["string", "null"]
          format: date-time
        revokeReason:
          type: ["string", "null"]
        employeeDocumentId:
          type: ["integer", "null"]
          description: Optional attached file. V1 never generates a certificate PDF — this field is only ever populated by a future HR-uploaded-file workstream, not W90.
        createdAt:
          type: string
          format: date-time

    LearningCertificateListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/LearningCertificate"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    RevokeLearningCertificateInput:
      type: object
      required: [revokeReason]
      properties:
        revokeReason:
          type: string
          minLength: 1

    LearningDashboardStatusItem:
      type: object
      required: [status, count]
      properties:
        status:
          type: string
          enum: [assigned, in_progress, completed, failed, cancelled]
        count:
          type: integer

    LearningDashboard:
      type: object
      required:
        [
          activeCourseCount, enrollmentsAssignedCount, statusBreakdown,
          pendingApprovalCount, overdueCount, certificatesExpiringSoonCount,
        ]
      properties:
        activeCourseCount:
          type: integer
          description: Organization-wide count of courses with status = active, regardless of the caller's own enrollment scope.
        enrollmentsAssignedCount:
          type: integer
          description: Total enrollments in the caller's authorized scope (a raw count, not distinct employees).
        statusBreakdown:
          type: array
          description: All 5 enrollment statuses, zero-filled, fixed order.
          items:
            $ref: "#/components/schemas/LearningDashboardStatusItem"
        pendingApprovalCount:
          type: integer
          description: approvalStatus = pending, within scope.
        overdueCount:
          type: integer
          description: dueDate passed and status not in (completed, failed, cancelled).
        certificatesExpiringSoonCount:
          type: integer
          description: Active certificates with a fixed expiresAt within the next 30 days, within scope.

    LearningEnrollmentEvidence:
      type: object
      required: [id, organizationId, enrollmentId, employeeDocumentId, addedByMembershipId, addedAt, fileName, mimeType, fileSize, uploadedBy]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        enrollmentId:
          type: integer
        employeeDocumentId:
          type: integer
          description: Points into the existing employee_documents table (§8.4) — no separate Learning storage layer.
        addedByMembershipId:
          type: ["integer", "null"]
        addedAt:
          type: string
          format: date-time
        fileName:
          type: string
          description: Original client-supplied filename, for display only — never used to build a storage path.
        mimeType:
          type: string
        fileSize:
          type: integer
        uploadedBy:
          type: ["integer", "null"]

    AssetCondition:
      type: string
      enum: [new, good, fair, poor, damaged]
      description: Deliberately independent of asset status (§7) — a damaged asset can still be assigned or in maintenance.

    AssetStatus:
      type: string
      enum: [available, assigned, maintenance, lost, retired]
      description: The sole-authoritative lifecycle field. retired is permanently terminal.

    Asset:
      type: object
      required: [id, organizationId, assetTag, categoryCode, name, condition, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        assetTag:
          type: string
          description: Server-generated, sequential, per-organization ("AST-00001", ...) — never client-supplied, never editable.
        categoryCode:
          type: string
          description: Free-text code from the asset_category Master Data domain, not validated against the domain's item list.
        name:
          type: string
        description:
          type: ["string", "null"]
        manufacturer:
          type: ["string", "null"]
        model:
          type: ["string", "null"]
        serialNumber:
          type: ["string", "null"]
          description: Unique per organization only when present. An empty string is stored as null.
        branchId:
          type: ["integer", "null"]
          description: Current physical location — a live reference, not a historical snapshot.
        purchaseDate:
          type: ["string", "null"]
          format: date
        purchaseCost:
          type: ["string", "null"]
          description: Optional, reference-only. No depreciation/valuation/accounting logic anywhere. No pairing requirement with purchaseCurrency.
        purchaseCurrency:
          type: ["string", "null"]
        warrantyExpiryDate:
          type: ["string", "null"]
          format: date
        condition:
          $ref: "#/components/schemas/AssetCondition"
        status:
          $ref: "#/components/schemas/AssetStatus"
        notes:
          type: ["string", "null"]
        createdBy:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateAssetInput:
      type: object
      required: [categoryCode, name]
      properties:
        categoryCode:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        description:
          type: string
        manufacturer:
          type: string
        model:
          type: string
        serialNumber:
          type: string
        branchId:
          type: integer
        purchaseDate:
          type: string
          format: date
        purchaseCost:
          type: number
          minimum: 0
        purchaseCurrency:
          type: string
        warrantyExpiryDate:
          type: string
          format: date
        condition:
          $ref: "#/components/schemas/AssetCondition"
        notes:
          type: string

    UpdateAssetInput:
      type: object
      description: Base register fields only — never status, never condition, never assetTag.
      properties:
        categoryCode:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        description:
          type: string
        manufacturer:
          type: string
        model:
          type: string
        serialNumber:
          type: ["string", "null"]
        branchId:
          type: ["integer", "null"]
        purchaseDate:
          type: ["string", "null"]
          format: date
        purchaseCost:
          type: ["number", "null"]
          minimum: 0
        purchaseCurrency:
          type: ["string", "null"]
        warrantyExpiryDate:
          type: ["string", "null"]
          format: date
        notes:
          type: string

    AssetListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Asset"
        total:
          type: integer
        page:
          type: integer
        pageSize:
          type: integer

    RetireAssetInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1

    MarkAssetLostInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1

    RecoverAssetInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1

    UpdateAssetConditionInput:
      type: object
      required: [condition, reason]
      properties:
        condition:
          $ref: "#/components/schemas/AssetCondition"
        reason:
          type: string
          minLength: 1

    AssetAssignmentEndReason:
      type: string
      enum: [returned, lost, transferred, retired]

    AssetAssignment:
      type: object
      required:
        [
          id,
          organizationId,
          assetId,
          employeeId,
          assetTagSnapshot,
          assetNameSnapshot,
          categorySnapshot,
          issuedAt,
          issueCondition,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        assetId:
          type: integer
        employeeId:
          type: integer
          description: Live reference — current identity, used for manager-scope resolution elsewhere. Not a historical snapshot.
        assetTagSnapshot:
          type: string
        assetNameSnapshot:
          type: string
        categorySnapshot:
          type: string
        departmentIdSnapshot:
          type: ["integer", "null"]
          description: The employee's department at issue time — never re-derived from a later transfer.
        positionIdSnapshot:
          type: ["integer", "null"]
        issuedAt:
          type: string
          format: date-time
        issuedByMembershipId:
          type: ["integer", "null"]
        expectedReturnDate:
          type: ["string", "null"]
          format: date
        issueCondition:
          $ref: "#/components/schemas/AssetCondition"
        issueNotes:
          type: ["string", "null"]
        acknowledgedAt:
          type: ["string", "null"]
          format: date-time
          description: Set once, only while custodyEndedAt is null. Never cleared by a later return.
        acknowledgementNote:
          type: ["string", "null"]
        custodyEndedAt:
          type: ["string", "null"]
          format: date-time
          description: NULL means this is the currently-active assignment.
        endReason:
          anyOf:
            - $ref: "#/components/schemas/AssetAssignmentEndReason"
            - type: "null"
        receivedByMembershipId:
          type: ["integer", "null"]
        returnCondition:
          anyOf:
            - $ref: "#/components/schemas/AssetCondition"
            - type: "null"
        returnNotes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    AssignAssetInput:
      type: object
      required: [employeeId]
      properties:
        employeeId:
          type: integer
          description: Must belong to the caller's own organization.
        issueCondition:
          $ref: "#/components/schemas/AssetCondition"
        expectedReturnDate:
          type: string
          format: date
        issueNotes:
          type: string

    ReturnAssetInput:
      type: object
      properties:
        returnCondition:
          $ref: "#/components/schemas/AssetCondition"
        returnNotes:
          type: string

    AcknowledgeAssetAssignmentInput:
      type: object
      properties:
        acknowledgementNote:
          type: string


    AssetIncidentType:
      type: string
      enum: [damage, loss]

    AssetIncidentStatus:
      type: string
      enum: [open, reviewed, dismissed]
      description: Minimal lifecycle. open -> reviewed | dismissed are HR/Asset-Officer-only transitions, both terminal (no reopen).

    AssetIncident:
      type: object
      required: [id, organizationId, assetId, assignmentId, reportedByEmployeeId, incidentType, description, reportedAt, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        assetId:
          type: integer
        assignmentId:
          type: integer
          description: The specific custody period this report concerns.
        reportedByEmployeeId:
          type: integer
          description: Server-derived from caller identity at creation — never client-supplied.
        incidentType:
          $ref: "#/components/schemas/AssetIncidentType"
        description:
          type: string
        reportedAt:
          type: string
          format: date-time
        status:
          $ref: "#/components/schemas/AssetIncidentStatus"
        reviewedByMembershipId:
          type: ["integer", "null"]
        reviewedAt:
          type: ["string", "null"]
          format: date-time
        resolutionNotes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ReportAssetIssueInput:
      type: object
      required: [incidentType, description]
      properties:
        incidentType:
          $ref: "#/components/schemas/AssetIncidentType"
        description:
          type: string
          minLength: 1


    ReviewAssetIncidentInput:
      type: object
      properties:
        resolutionNotes:
          type: string

    DismissAssetIncidentInput:
      type: object
      properties:
        resolutionNotes:
          type: string

    AssetMaintenanceStatus:
      type: string
      enum: [scheduled, in_progress, completed, cancelled]
      description: scheduled -> in_progress -> completed | cancelled. Both completed and cancelled are terminal — no reopen (§7).

    AssetMaintenance:
      type: object
      required: [id, organizationId, assetId, maintenanceType, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        assetId:
          type: integer
        maintenanceType:
          type: string
        description:
          type: ["string", "null"]
        providerText:
          type: ["string", "null"]
          description: Free text — no vendor/provider table (§11).
        status:
          $ref: "#/components/schemas/AssetMaintenanceStatus"
        startedAt:
          type: ["string", "null"]
          format: date-time
        completedAt:
          type: ["string", "null"]
          format: date-time
        cost:
          type: ["string", "null"]
          description: Optional, reference-only. No depreciation/accounting logic.
        notes:
          type: ["string", "null"]
        createdByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateAssetMaintenanceInput:
      type: object
      required: [maintenanceType]
      properties:
        maintenanceType:
          type: string
          minLength: 1
        description:
          type: string
        providerText:
          type: string
        cost:
          type: number
          minimum: 0
        notes:
          type: string

    AssetMaintenanceAction:
      type: string
      enum: [start, complete, cancel]
      description: >-
        The server maps this to the correct validated conditional UPDATE —
        the request body never accepts a raw target status.

    UpdateAssetMaintenanceInput:
      type: object
      required: [action]
      properties:
        action:
          $ref: "#/components/schemas/AssetMaintenanceAction"
        cost:
          type: number
          minimum: 0
        notes:
          type: string

    AssetEvidence:
      type: object
      required: [id, organizationId, assetId, employeeDocumentId, addedByMembershipId, addedAt, fileName, mimeType, fileSize, uploadedBy]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        assetId:
          type: integer
        employeeDocumentId:
          type: integer
          description: Points into the existing employee_documents table — no separate Assets storage layer (§13).
        addedByMembershipId:
          type: ["integer", "null"]
        addedAt:
          type: string
          format: date-time
        fileName:
          type: string
          description: Original client-supplied filename, for display only — never used to build a storage path.
        mimeType:
          type: string
        fileSize:
          type: integer
        uploadedBy:
          type: ["integer", "null"]

    AssetDashboardStatusItem:
      type: object
      required: [status, count]
      properties:
        status:
          $ref: "#/components/schemas/AssetStatus"
        count:
          type: integer

    AssetDashboard:
      type: object
      required: [totalAssetCount, statusBreakdown, employeesWithAssignedAssetsCount, overdueReturnCount, openIncidentCount]
      properties:
        totalAssetCount:
          type: integer
          description: In scope for the caller (§18) — organization-wide total, or the count of assets currently under an active assignment to the caller/a current direct report.
        statusBreakdown:
          type: array
          description: All 5 asset statuses, zero-filled, fixed order.
          items:
            $ref: "#/components/schemas/AssetDashboardStatusItem"
        employeesWithAssignedAssetsCount:
          type: integer
          description: Distinct employees currently holding an in-scope asset.
        overdueReturnCount:
          type: integer
          description: In-scope active assignments past their own expectedReturnDate — only for assignments that set one.
        openIncidentCount:
          type: integer
          description: In-scope asset_incidents with status = open.

    EmploymentPeriodSummary:
      type: object
      description: >-
        One employment_periods row (Phase 2A, W22) — append-only, never
        edited or deleted. previousState/newState snapshot the raw ids that
        changed at the time of the event; they are never resolved to
        current department/branch/position names, since that would silently
        replace historical meaning with present-day meaning.
      required: [id, eventType, effectiveDate, newState, createdAt]
      properties:
        id:
          type: integer
        eventType:
          type: string
          description: Free text — "transfer" | "promotion" | "confirmation" today, per each writing workstream's own event type.
        effectiveDate:
          type: string
          format: date-time
        previousState:
          nullable: true
          description: Raw snapshot of the fields that changed, before the event. Null for the first employment_periods row on a given employee.
        newState:
          description: Raw snapshot of the fields that changed, after the event.
        createdAt:
          type: string
          format: date-time

    MyEmploymentHistoryResponse:
      type: object
      description: >-
        Employee Self-Service Career Profile foundation (Phase 3F, W105).
        `linked: false` (with `items: []`) mirrors GET /me/employee's own
        intentional not-linked state, not an error.
      required: [linked, items]
      properties:
        linked:
          type: boolean
        items:
          type: array
          items:
            $ref: "#/components/schemas/EmploymentPeriodSummary"

    SelfServiceSkill:
      type: object
      description: Career Profile (Phase 3F, W106) — the caller's own skill, restricted from the HR EmployeeSkill shape (organizationId/employeeId/createdBy excluded as redundant/internal).
      required: [id, skillCode, createdAt]
      properties:
        id:
          type: integer
        skillCode:
          type: string
          description: Code from the "skill" Master Data domain.
        proficiencyLevel:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    MySkillsResponse:
      type: object
      required: [linked, items]
      properties:
        linked:
          type: boolean
        items:
          type: array
          items:
            $ref: "#/components/schemas/SelfServiceSkill"

    SelfServiceQualification:
      type: object
      description: Career Profile (Phase 3F, W106) — the caller's own qualification, restricted from the HR EmployeeQualification shape.
      required: [id, qualificationTypeCode, createdAt]
      properties:
        id:
          type: integer
        qualificationTypeCode:
          type: string
          description: Code from the "qualification_type" Master Data domain.
        institution:
          type: ["string", "null"]
        fieldOfStudy:
          type: ["string", "null"]
        startDate:
          type: ["string", "null"]
          format: date-time
        endDate:
          type: ["string", "null"]
          format: date-time
        grade:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    MyQualificationsResponse:
      type: object
      required: [linked, items]
      properties:
        linked:
          type: boolean
        items:
          type: array
          items:
            $ref: "#/components/schemas/SelfServiceQualification"

    SelfServiceCertification:
      type: object
      description: >-
        Career Profile (Phase 3F, W106) — the caller's own HR-maintained/
        external certification (employee_certifications only). Never
        includes system-issued Learning certificates (learning_certificates)
        — those remain My Learning's own exclusive surface. Expired records
        are always included; expiry status is computed client-side from
        expiryDate, mirroring My Learning's own established convention, never
        returned as a field here.
      required: [id, certificationTypeCode, createdAt]
      properties:
        id:
          type: integer
        certificationTypeCode:
          type: string
          description: Code from the "certification_type" Master Data domain.
        issuingOrganization:
          type: ["string", "null"]
        issueDate:
          type: ["string", "null"]
          format: date-time
        expiryDate:
          type: ["string", "null"]
          format: date-time
        credentialId:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    MyCertificationsResponse:
      type: object
      required: [linked, items]
      properties:
        linked:
          type: boolean
        items:
          type: array
          items:
            $ref: "#/components/schemas/SelfServiceCertification"

    ManagerPortalTeamMember:
      type: object
      description: >-
        Manager Portal Team Overview (Phase 3G, W109) — a deliberately narrow
        DTO for one of the caller's own live direct reports. Frozen field set
        (frozen plan Decision 7/§27): never the full Employee shape, never
        address/personal email/phone/emergency contacts/identity documents/
        compensation/disciplinary/medical/Career Profile data.
      required: [id, firstName, lastName, employmentStatus, hasProfilePicture]
      properties:
        id:
          type: integer
        employeeNumber:
          type: ["string", "null"]
        firstName:
          type: string
        lastName:
          type: string
        positionId:
          type: ["integer", "null"]
        positionName:
          type: ["string", "null"]
        departmentId:
          type: ["integer", "null"]
        departmentName:
          type: ["string", "null"]
        branchId:
          type: ["integer", "null"]
        branchName:
          type: ["string", "null"]
        employmentStatus:
          type: string
        hasProfilePicture:
          type: boolean

    ManagerPortalTeamOverview:
      type: object
      description: >-
        Manager Portal Team Overview (Phase 3G, W109). `linked: false` (with
        `directReports: []`) mirrors GET /me/employee's own intentional
        not-linked state, not an error. A linked caller with zero current
        direct reports gets `linked: true, directReports: []` — a valid
        empty state (frozen plan Decision 15), never denied.
      required: [linked, directReports]
      properties:
        linked:
          type: boolean
        directReports:
          type: array
          items:
            $ref: "#/components/schemas/ManagerPortalTeamMember"

    ManagerPortalDashboard:
      type: object
      description: >-
        Manager Portal Dashboard (Phase 3G, W110). Six frozen tiles, no
        invented score/percentage/KPI. Each *Count field for a module-backed
        tile is null when that module is disabled, the caller lacks its own
        permission, or (Attendance only) the organization's timezone isn't
        configured — never conflated with a genuine zero.
      required: [linked, directReportsCount, attendanceAbsentOrLateTodayCount, pendingLeaveActionsCount, pendingPerformanceActionsCount, pendingLearningActionsCount, teamAssetsInCustodyCount]
      properties:
        linked:
          type: boolean
        directReportsCount:
          type: integer
        attendanceAbsentOrLateTodayCount:
          type: ["integer", "null"]
        pendingLeaveActionsCount:
          type: ["integer", "null"]
        pendingPerformanceActionsCount:
          type: ["integer", "null"]
        pendingLearningActionsCount:
          type: ["integer", "null"]
        teamAssetsInCustodyCount:
          type: ["integer", "null"]

    ManagerPortalPendingActionItem:
      type: object
      description: >-
        One pending item from Leave, Performance, or Learning (Phase 3G,
        W110) — a deliberately narrow, safe field set only. Never a leave
        reason, never confidential Performance/Learning fields, never a full
        Employee shape.
      required: [sourceModule, id, employeeId, employeeFirstName, employeeLastName, status, title, createdAt]
      properties:
        sourceModule:
          type: string
          enum: [leave, performance, learning]
        id:
          type: integer
          description: The underlying leave_requests/performance_reviews/learning_enrollments row's own id.
        employeeId:
          type: integer
        employeeFirstName:
          type: string
        employeeLastName:
          type: string
        status:
          type: string
          description: leave_requests.status ("pending") | performance_reviews.status ("manager_review") | learning_enrollments.approvalStatus ("pending").
        title:
          type: string
        createdAt:
          type: string
          format: date-time

    ManagerPortalRecruitmentItem:
      type: object
      description: >-
        WS-15 P2 (§31.28) — one Recruitment responsibility the caller currently
        participates in. Deliberately carries NO candidate name, no application
        detail, no other panel member's scoring or comments, and no offered
        compensation: a row is a reference plus a link into the Recruitment
        surface that owns and re-gates the work.
      required: [kind, id, title, status, occurredAt, deepLink]
      properties:
        kind:
          type: string
          description: >-
            `interview_scorecard` is outstanding own work; `interview_panel` and
            `job_requisition` are participation awareness.
          enum: [interview_scorecard, interview_panel, job_requisition]
        id:
          type: integer
          description: The underlying interviews/job_requisitions row's own id.
        title:
          type: string
        status:
          type: string
          description: The source's own status, passed through.
        occurredAt:
          type: string
          format: date-time
          description: The interview's scheduledAt or the requisition's createdAt. Never a fabricated deadline.
        deepLink:
          type: string

    ManagerPortalPendingActions:
      type: object
      description: >-
        Manager Portal Pending Actions (Phase 3G, W110). `linked: false`
        mirrors Team Overview's/Dashboard's own not-linked semantics — Leave
        items still resolve independently for an org-wide HR/admin caller
        even when unlinked. Sorted by createdAt descending.
      required: [linked, items]
      properties:
        linked:
          type: boolean
        items:
          type: array
          items:
            $ref: "#/components/schemas/ManagerPortalPendingActionItem"
        recruitmentParticipation:
          type: array
          description: >-
            WS-15 P2 (§31.28) — Recruitment responsibilities the caller
            participates in. A SIBLING of `items` rather than part of it,
            because the shipped item shape requires an employee subject and
            Recruitment participation has none: an interview panel seat concerns
            a candidate, not a direct report. Optional, so every existing client
            keeps working unchanged.
          items:
            $ref: "#/components/schemas/ManagerPortalRecruitmentItem"

    DepartmentHead:
      type: object
      description: >-
        Office Inventory, Workstream 1
        (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §5). A general,
        non-Inventory-specific organizational-authority assignment —
        effective-dated; `validTo: null` means this row is the department's
        current Head.
      required: [id, organizationId, departmentId, headMembershipId, validFrom, validTo, assignedByMembershipId, revokedByMembershipId, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        departmentId:
          type: integer
        headMembershipId:
          type: integer
        validFrom:
          type: string
          format: date-time
        validTo:
          type: ["string", "null"]
          format: date-time
        assignedByMembershipId:
          type: ["integer", "null"]
        revokedByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    AssignDepartmentHeadBody:
      type: object
      required: [headMembershipId]
      properties:
        headMembershipId:
          type: integer

    OfficeInventoryItem:
      type: object
      description: Office Inventory, Workstream 1 (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §7.1). The catalog — one row per distinct item type, never a physical unit or a quantity.
      required: [id, organizationId, itemCode, name, description, categoryCode, unitOfMeasure, classification, reorderLevel, unitCost, currency, status, createdByMembershipId, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        itemCode:
          type: string
        name:
          type: string
        description:
          type: ["string", "null"]
        categoryCode:
          type: string
        unitOfMeasure:
          type: string
        classification:
          type: string
          enum: [consumable, returnable]
        reorderLevel:
          type: ["string", "null"]
        unitCost:
          type: ["string", "null"]
        currency:
          type: ["string", "null"]
        status:
          type: string
          enum: [active, inactive]
        createdByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateOfficeInventoryItemBody:
      type: object
      required: [name, categoryCode, unitOfMeasure, classification]
      properties:
        name:
          type: string
        description:
          type: string
        categoryCode:
          type: string
        unitOfMeasure:
          type: string
        classification:
          type: string
          enum: [consumable, returnable]
        reorderLevel:
          type: string
        unitCost:
          type: string
        currency:
          type: string

    UpdateOfficeInventoryItemBody:
      type: object
      description: Never accepts itemCode (permanent) or classification (fixed at creation).
      properties:
        name:
          type: string
        description:
          type: ["string", "null"]
        categoryCode:
          type: string
        unitOfMeasure:
          type: string
        reorderLevel:
          type: ["string", "null"]
        unitCost:
          type: ["string", "null"]
        currency:
          type: ["string", "null"]
        status:
          type: string
          enum: [active, inactive]

    OfficeInventoryStore:
      type: object
      description: Office Inventory, Workstream 1 (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §7.2). Flat — no hierarchy in V1.
      required: [id, organizationId, name, code, branchId, responsibleMembershipId, status, createdByMembershipId, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        code:
          type: string
        branchId:
          type: ["integer", "null"]
        responsibleMembershipId:
          type: ["integer", "null"]
        status:
          type: string
          enum: [active, inactive]
        createdByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateOfficeInventoryStoreBody:
      type: object
      required: [name, code]
      properties:
        name:
          type: string
        code:
          type: string
        branchId:
          type: integer
        responsibleMembershipId:
          type: integer

    UpdateOfficeInventoryStoreBody:
      type: object
      description: Never accepts `code` — the store's stable, permanent identity.
      properties:
        name:
          type: string
        branchId:
          type: ["integer", "null"]
        responsibleMembershipId:
          type: ["integer", "null"]
        status:
          type: string
          enum: [active, inactive]

    OfficeInventoryStockMovement:
      type: object
      description: >-
        Office Inventory, Workstream 2
        (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §7.3). One row of the
        authoritative, append-only stock ledger. Workstream 2 only ever
        produces `movementType: received` rows; every other enum value
        exists for later workstreams.
      required: [id, organizationId, itemId, movementType, quantity, storeId, holderType, holderId, referenceNumber, sourceReferenceType, sourceReferenceId, source, deliveryReference, unitCost, condition, reason, expectedReturnDate, confirmedByMembershipId, confirmedAt, idempotencyKey, actorMembershipId, occurredAt, notes, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        itemId:
          type: integer
        movementType:
          type: string
          enum: [received, issued, returned, transferred_out, transferred_in, adjustment_in, adjustment_out, written_off, missing, recovered, asset_handoff]
        quantity:
          type: string
        storeId:
          type: ["integer", "null"]
        holderType:
          type: ["string", "null"]
          enum: [employee, department, null]
        holderId:
          type: ["integer", "null"]
        referenceNumber:
          type: ["string", "null"]
        sourceReferenceType:
          type: ["string", "null"]
          enum: [request_line, incident, stocktake_line, asset, null]
        sourceReferenceId:
          type: ["integer", "null"]
        source:
          type: ["string", "null"]
        deliveryReference:
          type: ["string", "null"]
        unitCost:
          type: ["string", "null"]
        condition:
          type: ["string", "null"]
          enum: [new, good, fair, poor, damaged, null]
        reason:
          type: ["string", "null"]
        expectedReturnDate:
          type: ["string", "null"]
          format: date
        confirmedByMembershipId:
          type: ["integer", "null"]
        confirmedAt:
          type: ["string", "null"]
          format: date-time
        idempotencyKey:
          type: ["string", "null"]
        actorMembershipId:
          type: ["integer", "null"]
        occurredAt:
          type: string
          format: date-time
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    OfficeInventoryReceipt:
      type: object
      description: >-
        Not a separate DB entity — the set of `received` ledger rows sharing
        one `referenceNumber`, assembled on read.
      required: [referenceNumber, organizationId, storeId, occurredAt, lines, replay]
      properties:
        referenceNumber:
          type: string
        organizationId:
          type: integer
        storeId:
          type: integer
        occurredAt:
          type: string
          format: date-time
        lines:
          type: array
          items:
            $ref: "#/components/schemas/OfficeInventoryStockMovement"
        replay:
          type: boolean
          description: True when this response is the pre-existing result of an earlier identical idempotencyKey submission, not a newly-created receipt.

    OfficeInventoryReceiptSummary:
      type: object
      required: [referenceNumber, storeId, occurredAt, lineCount]
      properties:
        referenceNumber:
          type: string
        storeId:
          type: integer
        occurredAt:
          type: string
          format: date-time
        lineCount:
          type: integer

    ReceiveLineInput:
      type: object
      required: [itemId, quantity]
      properties:
        itemId:
          type: integer
        quantity:
          type: string
          description: Positive numeric(12,2)-shaped string.
        unitCost:
          type: string

    CreateOfficeInventoryReceiptBody:
      type: object
      required: [storeId, lines]
      properties:
        storeId:
          type: integer
        lines:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/ReceiveLineInput"
        source:
          type: string
        deliveryReference:
          type: string
        notes:
          type: string
        idempotencyKey:
          type: string
          description: Optional client-generated key protecting against duplicate submission (e.g. a double click or network retry). Same key + same operation returns the original receipt rather than creating a second one.

    OfficeInventoryStoreBalance:
      type: object
      required: [storeId, balance]
      properties:
        storeId:
          type: integer
        balance:
          type: string

    OfficeInventoryItemBalance:
      type: object
      description: Either a single store's balance (when `storeId` was supplied) or the organization-wide total plus a per-store breakdown (when it was not).
      required: [itemId, total, byStore]
      properties:
        itemId:
          type: integer
        storeId:
          type: ["integer", "null"]
        total:
          type: string
        byStore:
          type: array
          items:
            $ref: "#/components/schemas/OfficeInventoryStoreBalance"

    OfficeInventoryRequestableItem:
      type: object
      description: >-
        WWM Employee Access Remediation (2026-09-07): the requester-facing
        subset of an item — display identity only, active items only.
      required: [id, itemCode, name, unitOfMeasure, classification]
      properties:
        id:
          type: integer
        itemCode:
          type: string
        name:
          type: string
        unitOfMeasure:
          type: string
        classification:
          type: string
          enum: [consumable, returnable]

    OfficeInventoryRequestLine:
      type: object
      description: >-
        Office Inventory, Workstream 3
        (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §7.4). Approval never
        touches the stock ledger — only Workstream 4's own Issue/Fulfilment
        action ever does.
      required: [id, organizationId, requestId, itemId, quantityRequested, approvalStatus, approvedQuantity, approvedByMembershipId, actedAsDelegate, delegatorHeadMembershipId, delegationId, approvedAt, rejectionReason, quantityIssuedSoFar]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        requestId:
          type: integer
        itemId:
          type: integer
        quantityRequested:
          type: string
        approvalStatus:
          type: string
          enum: [pending, approved, rejected]
        approvedQuantity:
          type: ["string", "null"]
        approvedByMembershipId:
          type: ["integer", "null"]
        actedAsDelegate:
          type: boolean
        delegatorHeadMembershipId:
          type: ["integer", "null"]
        delegationId:
          type: ["integer", "null"]
        approvedAt:
          type: ["string", "null"]
          format: date-time
        rejectionReason:
          type: ["string", "null"]
        quantityIssuedSoFar:
          type: string

    OfficeInventoryRequest:
      type: object
      required: [id, organizationId, requestReference, requestedByMembershipId, requestType, forEmployeeId, forDepartmentId, reason, submittedAt, status, cancelledAt, cancelledByMembershipId]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        requestReference:
          type: string
        requestedByMembershipId:
          type: integer
        requestType:
          type: string
          enum: [employee, department]
        forEmployeeId:
          type: ["integer", "null"]
        forDepartmentId:
          type: integer
        reason:
          type: ["string", "null"]
        submittedAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [pending, partially_approved, approved, rejected, fulfilled, partially_fulfilled, cancelled]
        cancelledAt:
          type: ["string", "null"]
          format: date-time
        cancelledByMembershipId:
          type: ["integer", "null"]

    OfficeInventoryRequestWithLines:
      type: object
      required: [request, lines]
      properties:
        request:
          $ref: "#/components/schemas/OfficeInventoryRequest"
        lines:
          type: array
          items:
            $ref: "#/components/schemas/OfficeInventoryRequestLine"

    OfficeInventoryRequestLineInput:
      type: object
      required: [itemId, quantityRequested]
      properties:
        itemId:
          type: integer
        quantityRequested:
          type: string
          description: Positive numeric(12,2)-shaped string.

    CreateOfficeInventoryRequestBody:
      type: object
      description: >-
        For `requestType: employee`, `forEmployeeId` is always the caller's
        own employee identity, resolved server-side — never client-supplied
        (`forDepartmentId` must not be sent; it is resolved from the
        employee's own current department). For `requestType: department`,
        `forDepartmentId` is required and the caller must themselves belong
        to that department.
      required: [requestType, lines]
      properties:
        requestType:
          type: string
          enum: [employee, department]
        forDepartmentId:
          type: integer
          description: Required only for requestType "department".
        reason:
          type: string
        lines:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/OfficeInventoryRequestLineInput"

    ApproveOfficeInventoryRequestLineBody:
      type: object
      required: [approvedQuantity]
      properties:
        approvedQuantity:
          type: string
          description: Must be > 0 and <= the line's quantityRequested.

    RejectOfficeInventoryRequestLineBody:
      type: object
      required: [rejectionReason]
      properties:
        rejectionReason:
          type: string

    OfficeInventoryRepeatRequestWarning:
      type: object
      description: >-
        Query-time only, never a stored flag (§15). Deliberately does not
        include issued/custody fields — those require Workstream 4's data,
        which does not exist yet; this shape is designed to be extended
        additively once it does, without a breaking change.
      required: [itemId, windowDays, recentEmployeeRequests, recentDepartmentRequests]
      properties:
        itemId:
          type: integer
        windowDays:
          type: integer
        recentEmployeeRequests:
          type: array
          items:
            $ref: "#/components/schemas/OfficeInventoryRequest"
        recentDepartmentRequests:
          type: array
          items:
            $ref: "#/components/schemas/OfficeInventoryRequest"

    OfficeInventoryAccountabilityContext:
      type: object
      description: >-
        Office Inventory, Workstream 8
        (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §15, §34). Completes
        the repeat-request warning with real current custody and recent
        issue/return activity for both the employee and the department,
        now that Workstreams 4-7 exist — additive to
        OfficeInventoryRepeatRequestWarning's own shape, not a replacement.
        `employeeCurrentCustody`/`recentEmployeeIssuedQuantity`/
        `recentEmployeeReturnedQuantity` are null only when the request has
        no `forEmployeeId` (a pure department request). For a consumable
        item, current custody is a cumulative issued total, not an
        outstanding return obligation — `overdue` is always false for a
        consumable, since a consumable's `issued` row never carries an
        `expectedReturnDate`.
      required: [employeeCurrentCustody, departmentCurrentCustody, recentEmployeeIssuedQuantity, recentDepartmentIssuedQuantity, recentEmployeeReturnedQuantity, recentDepartmentReturnedQuantity]
      properties:
        employeeCurrentCustody:
          allOf:
            - $ref: "#/components/schemas/OfficeInventoryCustodyEntry"
          nullable: true
        departmentCurrentCustody:
          $ref: "#/components/schemas/OfficeInventoryCustodyEntry"
        recentEmployeeIssuedQuantity:
          type: string
          nullable: true
        recentDepartmentIssuedQuantity:
          type: string
        recentEmployeeReturnedQuantity:
          type: string
          nullable: true
        recentDepartmentReturnedQuantity:
          type: string

    OfficeInventoryRequestLineApprovalContext:
      type: object
      required: [line, storeAvailability, repeatRequestWarning, accountability]
      properties:
        line:
          $ref: "#/components/schemas/OfficeInventoryRequestLine"
        storeAvailability:
          $ref: "#/components/schemas/OfficeInventoryItemBalance"
        repeatRequestWarning:
          $ref: "#/components/schemas/OfficeInventoryRepeatRequestWarning"
        accountability:
          $ref: "#/components/schemas/OfficeInventoryAccountabilityContext"

    OfficeInventoryApprovalAuthority:
      type: object
      description: >-
        Office Inventory, Workstream 8 (§34) — "when the actor is a
        delegate, the delegation authority being exercised, its validity
        window, and the delegating Head's identity." The same
        `resolveApprovalAuthority` result Workstream 3 already re-checks
        fresh on every approval action, carried onto the approval-context
        response the viewer themselves is currently authorized under.
      required: [capacity, headMembershipId, delegation]
      properties:
        capacity:
          type: string
          enum: [department_head, delegate]
        headMembershipId:
          type: integer
        delegation:
          allOf:
            - $ref: "#/components/schemas/OfficeInventoryApprovalDelegation"
          nullable: true

    OfficeInventoryRequestApprovalContext:
      type: object
      required: [request, lines, viewerAuthority]
      properties:
        request:
          $ref: "#/components/schemas/OfficeInventoryRequest"
        lines:
          type: array
          items:
            $ref: "#/components/schemas/OfficeInventoryRequestLineApprovalContext"
        viewerAuthority:
          $ref: "#/components/schemas/OfficeInventoryApprovalAuthority"

    OfficeInventoryApprovalDelegation:
      type: object
      description: >-
        Office Inventory, Workstream 3
        (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §6, §5.3). A row
        surviving the delegating Head's own replacement remains open and
        fully historically queryable, but becomes functionally inert for
        NEW approvals the instant that membership is no longer the
        department's actual current Head — re-checked fresh on every
        approval, never assumed from this row's own existence.
      required: [id, organizationId, departmentId, delegatingHeadMembershipId, delegateMembershipId, validFrom, validTo, createdByMembershipId, revokedByMembershipId, createdAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        departmentId:
          type: integer
        delegatingHeadMembershipId:
          type: integer
        delegateMembershipId:
          type: integer
        validFrom:
          type: string
          format: date-time
        validTo:
          type: ["string", "null"]
          format: date-time
        createdByMembershipId:
          type: ["integer", "null"]
        revokedByMembershipId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    CreateOfficeInventoryDelegationBody:
      type: object
      required: [delegateMembershipId]
      properties:
        delegateMembershipId:
          type: integer

    IssueRequestLineBody:
      type: object
      description: >-
        Office Inventory, Workstream 4
        (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §16). Issues against
        this line's own remaining approved-but-unissued quantity
        (approvedQuantity - quantityIssuedSoFar). Supports partial
        fulfilment — multiple issue calls against the same line are normal.
      required: [storeId, quantity]
      properties:
        storeId:
          type: integer
        quantity:
          type: string
          description: Positive numeric(12,2)-shaped string. Must not exceed the line's remaining approved-but-unissued quantity.
        expectedReturnDate:
          type: string
          format: date
          description: Only meaningful for a returnable item; rejected for a consumable item.
        idempotencyKey:
          type: string

    IssueResult:
      type: object
      required: [storeMovement, holderMovement, line, request, replay]
      properties:
        storeMovement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        holderMovement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        line:
          $ref: "#/components/schemas/OfficeInventoryRequestLine"
        request:
          $ref: "#/components/schemas/OfficeInventoryRequest"
        replay:
          type: boolean

    DirectIssueBody:
      type: object
      description: >-
        docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §17 — bypasses a prior
        request but not inventory controls. `sourceReferenceType` is always
        null on the resulting ledger rows, the permanent historical signal
        distinguishing this from request-based fulfilment.
      required: [storeId, itemId, quantity, holderType, holderId, reason]
      properties:
        storeId:
          type: integer
        itemId:
          type: integer
        quantity:
          type: string
        holderType:
          type: string
          enum: [employee, department]
        holderId:
          type: integer
        reason:
          type: string
        expectedReturnDate:
          type: string
          format: date
        idempotencyKey:
          type: string

    DirectIssueResult:
      type: object
      required: [storeMovement, holderMovement, replay]
      properties:
        storeMovement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        holderMovement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        replay:
          type: boolean

    OfficeInventoryAwaitingFulfilmentEntry:
      type: object
      required: [request, lines]
      properties:
        request:
          $ref: "#/components/schemas/OfficeInventoryRequest"
        lines:
          type: array
          items:
            $ref: "#/components/schemas/OfficeInventoryRequestLine"

    OfficeInventoryCustodyEntry:
      type: object
      description: >-
        Extended in Workstream 5 with live-derived overdue info (§20) — the
        fields are additive to Workstream 4's own original shape, never
        breaking it. `overdue`/`expectedReturnDate` are computed from the
        most recent holder-increasing (`issued`) row for this item, which a
        handover's destination row also is — a handover therefore becomes
        the new "most recent" row and supersedes whatever due date the
        previous holder was tracking.
      required: [itemId, balance, overdue, expectedReturnDate, itemName, itemCode, classification]
      properties:
        itemId:
          type: integer
        balance:
          type: string
        overdue:
          type: boolean
        expectedReturnDate:
          type: string
          format: date
          nullable: true
        itemName:
          type: ["string", "null"]
          description: >-
            The held item's display name (WWM Employee Access Remediation,
            2026-09-07) — resolved server-side for items already in this
            holder's custody so ESS can name them without the catalogue
            grant (office_inventory.item.manage). Null only if the item row
            is gone.
        itemCode:
          type: ["string", "null"]
        classification:
          type: ["string", "null"]
          enum: [consumable, returnable, null]

    OfficeInventoryMyHistoryEntry:
      description: >-
        One row of the caller's own custody history: the ledger movement
        plus the item's display identity (same rationale as
        OfficeInventoryCustodyEntry.itemName — only items the caller was
        themselves issued are ever named).
      allOf:
        - $ref: "#/components/schemas/OfficeInventoryStockMovement"
        - type: object
          required: [itemName, itemCode]
          properties:
            itemName:
              type: ["string", "null"]
            itemCode:
              type: ["string", "null"]

    CreateOfficeInventoryReturnBody:
      type: object
      description: >-
        Office Inventory, Workstream 5
        (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §21). A `returned`
        ledger row pair — holder-side decrease, store-side increase —
        validated against the holder's own live-derived outstanding
        custody, never a client-supplied "current quantity". Consumable
        items are rejected (Owner Decision 10 — issuing a consumable is
        itself its consumption, never reversible).
      required: [itemId, holderType, holderId, destinationStoreId, quantity]
      properties:
        itemId:
          type: integer
        holderType:
          type: string
          enum: [employee, department]
        holderId:
          type: integer
        destinationStoreId:
          type: integer
        quantity:
          type: string
          description: Positive numeric(12,2)-shaped string. Must not exceed the holder's current outstanding custody for this item.
        condition:
          type: string
          enum: [new, good, fair, poor, damaged]
        notes:
          type: string
        idempotencyKey:
          type: string

    OfficeInventoryReturnResult:
      type: object
      required: [storeMovement, holderMovement, replay]
      properties:
        storeMovement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        holderMovement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        replay:
          type: boolean

    CreateOfficeInventoryHandoverBody:
      type: object
      description: >-
        docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §22. A paired
        holder-to-holder movement — no store is ever touched. Supports all
        four directions (employee/department x employee/department). A
        department-to-department handover additionally requires the acting
        membership to resolve as the RECEIVING department's current Head
        or a currently-valid delegate (Owner Decision 17) — the same
        authority-resolution primitive Workstream 3 already proved for
        ordinary department-request approval.
      required: [itemId, fromHolderType, fromHolderId, toHolderType, toHolderId, quantity]
      properties:
        itemId:
          type: integer
        fromHolderType:
          type: string
          enum: [employee, department]
        fromHolderId:
          type: integer
        toHolderType:
          type: string
          enum: [employee, department]
        toHolderId:
          type: integer
        quantity:
          type: string
        reason:
          type: string
        condition:
          type: string
          enum: [new, good, fair, poor, damaged]
        expectedReturnDate:
          type: string
          format: date
          description: Only meaningful for a returnable item; rejected for a consumable item. Unset clears any prior due date rather than carrying it forward.
        idempotencyKey:
          type: string

    OfficeInventoryHandoverResult:
      type: object
      required: [sourceMovement, destinationMovement, replay]
      properties:
        sourceMovement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        destinationMovement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        replay:
          type: boolean

    CreateOfficeInventoryTransferBody:
      type: object
      description: >-
        docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §23. A single atomic
        paired `transferred_out`/`transferred_in` store-to-store movement —
        both rows are inserted in the same locked transaction, so stock is
        never simultaneously available or unavailable in both stores.
        Organization-wide total is unchanged by construction.
      required: [itemId, fromStoreId, toStoreId, quantity]
      properties:
        itemId:
          type: integer
        fromStoreId:
          type: integer
        toStoreId:
          type: integer
        quantity:
          type: string
        notes:
          type: string
        idempotencyKey:
          type: string

    OfficeInventoryTransferResult:
      type: object
      required: [outMovement, inMovement, replay]
      properties:
        outMovement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        inMovement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        replay:
          type: boolean

    OfficeInventoryIncident:
      type: object
      description: >-
        Office Inventory, Workstream 6
        (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §7.7, §25). A pure,
        qualitative record — reporting and reviewing never touch the stock
        ledger. No quantity field exists here on purpose: every
        quantity-affecting resolution (mark missing / recover / write off)
        carries its own quantity, validated fresh against the live ledger,
        referencing this row only for traceability.
      required: [id, organizationId, itemId, holderType, holderId, incidentType, description, reportedByMembershipId, reportedAt, status, reviewedByMembershipId, reviewedAt, resolutionNotes, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        itemId:
          type: integer
        holderType:
          type: string
          enum: [employee, department]
          nullable: true
        holderId:
          type: integer
          nullable: true
        incidentType:
          type: string
          enum: [damage, missing]
        description:
          type: string
        reportedByMembershipId:
          type: integer
        reportedAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [open, reviewed, dismissed]
        reviewedByMembershipId:
          type: integer
          nullable: true
        reviewedAt:
          type: string
          format: date-time
          nullable: true
        resolutionNotes:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ReportOfficeInventoryIncidentBody:
      type: object
      description: >-
        §25/§33 — self-service ONLY: `holderType`/`holderId` must be the
        reporting employee's own current custody or their own current
        department's custody, enforced server-side. Zero ledger effect.
      required: [itemId, holderType, holderId, incidentType, description]
      properties:
        itemId:
          type: integer
        holderType:
          type: string
          enum: [employee, department]
        holderId:
          type: integer
        incidentType:
          type: string
          enum: [damage, missing]
        description:
          type: string

    ReviewOfficeInventoryIncidentBody:
      type: object
      description: A pure status transition (§9) — never appends a ledger row, regardless of outcome.
      required: [outcome]
      properties:
        outcome:
          type: string
          enum: [reviewed, dismissed]
        resolutionNotes:
          type: string

    OfficeInventoryMovementActionResult:
      type: object
      description: Shared response shape for every single-row Workstream 6 disposition action (mark-missing, recover, write-off, adjustment).
      required: [movement, replay]
      properties:
        movement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        replay:
          type: boolean

    MarkOfficeInventoryIncidentMissingBody:
      type: object
      description: The separate, deliberate action that actually removes quantity from a holder's live custody (§25) — reporting alone never does.
      required: [quantity]
      properties:
        quantity:
          type: string
        idempotencyKey:
          type: string

    RecoverOfficeInventoryIncidentBody:
      type: object
      description: Validated against the incident's own derived outstanding-missing tally, never the holder's balance — a found item re-enters a STORE.
      required: [quantity, destinationStoreId]
      properties:
        quantity:
          type: string
        destinationStoreId:
          type: integer
        idempotencyKey:
          type: string

    WriteOffOfficeInventoryIncidentBody:
      type: object
      description: Closes out an incident's own already-missing quantity — an unscoped ledger row, since the quantity already left holder custody.
      required: [quantity, reason]
      properties:
        quantity:
          type: string
        reason:
          type: string
        idempotencyKey:
          type: string

    CreateOfficeInventoryWriteOffBody:
      type: object
      description: >-
        §11-§13 — a DIRECT, single-actor authoritative disposition of
        quantity still nominally accountable somewhere (a store OR a
        holder), independent of any incident. `incidentId` is optional,
        purely for traceability.
      required: [itemId, sourceType, quantity, reason]
      properties:
        itemId:
          type: integer
        sourceType:
          type: string
          enum: [store, employee, department]
        storeId:
          type: integer
          description: Required when sourceType is "store".
        holderId:
          type: integer
          description: Required when sourceType is "employee" or "department".
        quantity:
          type: string
        reason:
          type: string
        incidentId:
          type: integer
        idempotencyKey:
          type: string

    CreateOfficeInventoryAdjustmentBody:
      type: object
      description: §15-§17 — STORE ONLY by design; there is no holder field anywhere in this shape, structurally preventing the custody-discrepancy boundary from ever being crossed.
      required: [storeId, itemId, direction, quantity, reason]
      properties:
        storeId:
          type: integer
        itemId:
          type: integer
        direction:
          type: string
          enum: [in, out]
        quantity:
          type: string
        reason:
          type: string
        idempotencyKey:
          type: string

    OfficeInventoryStocktake:
      type: object
      description: >-
        Office Inventory, Workstream 7
        (docs/OFFICE_INVENTORY_IMPLEMENTATION_PLAN.md §7.8, §28). Store-scoped.
        `draft` -> `counting` (the moment `expectedQuantitySnapshot` is
        captured on every line, permanently frozen from then on) -> `finalized`
        (immutable; finalization itself never mutates stock).
      required: [id, organizationId, storeId, stocktakeReference, status, startedAt, startedByMembershipId, finalizedAt, finalizedByMembershipId, notes, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        storeId:
          type: integer
        stocktakeReference:
          type: string
        status:
          type: string
          enum: [draft, counting, finalized]
        startedAt:
          type: string
          format: date-time
          nullable: true
        startedByMembershipId:
          type: integer
          nullable: true
        finalizedAt:
          type: string
          format: date-time
          nullable: true
        finalizedByMembershipId:
          type: integer
          nullable: true
        notes:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    OfficeInventoryStocktakeLineView:
      type: object
      description: >-
        `variance`/`resolutionType`/`resolutionMovementId`/`resolvedAt` are
        the last-PERSISTED values (as of the most recent count/resolve/
        finalize action). `movementsSinceSnapshot`/`reconciledExpectedQuantity`/
        `currentVariance` are LIVE-recomputed on every read and may differ
        from the persisted `variance` if a movement has occurred since —
        never presented as silently already-corrected.
      required: [id, organizationId, stocktakeId, itemId, expectedQuantitySnapshot, countedQuantity, countedByMembershipId, countedAt, variance, resolutionType, resolutionMovementId, resolvedAt, movementsSinceSnapshot, reconciledExpectedQuantity, currentVariance]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        stocktakeId:
          type: integer
        itemId:
          type: integer
        expectedQuantitySnapshot:
          type: string
        countedQuantity:
          type: string
          nullable: true
        countedByMembershipId:
          type: integer
          nullable: true
        countedAt:
          type: string
          format: date-time
          nullable: true
        variance:
          type: string
          nullable: true
        resolutionType:
          type: string
          enum: [recount, adjustment, missing]
          nullable: true
        resolutionMovementId:
          type: integer
          nullable: true
        resolvedAt:
          type: string
          format: date-time
          nullable: true
        movementsSinceSnapshot:
          type: string
        reconciledExpectedQuantity:
          type: string
        currentVariance:
          type: string
          nullable: true

    CreateOfficeInventoryStocktakeBody:
      type: object
      required: [storeId]
      properties:
        storeId:
          type: integer
        notes:
          type: string

    OfficeInventoryStocktakeDetail:
      type: object
      required: [stocktake, lines]
      properties:
        stocktake:
          $ref: "#/components/schemas/OfficeInventoryStocktake"
        lines:
          type: array
          items:
            $ref: "#/components/schemas/OfficeInventoryStocktakeLineView"

    RecordOfficeInventoryStocktakeCountBody:
      type: object
      description: Supports recount — resubmitting for an already-counted line. Never mutates the stock ledger.
      required: [countedQuantity]
      properties:
        countedQuantity:
          type: string
          description: Non-negative numeric(12,2)-shaped string.

    ResolveOfficeInventoryStocktakeLineBody:
      type: object
      description: >-
        A thin pass-through to Workstream 6's own existing adjustment/missing
        domain services (§15-§16) — this action never appends a ledger row
        itself. `adjustment` accepts any non-zero variance sign; `missing`
        only a shortage (negative variance).
      required: [resolutionType, reason]
      properties:
        resolutionType:
          type: string
          enum: [adjustment, missing]
        reason:
          type: string

    OfficeInventoryDashboard:
      type: object
      description: >-
        Office Inventory, Workstream 9 (§43). Every tile is a live, batched
        query — no persisted aggregate. `outstandingReturnables`/
        `overdueReturnables` count returnable-item holder balances only
        (never a consumable's cumulative issued total). `pendingIssues`
        counts approved-but-not-fully-issued request LINES, excluding
        rejected lines, cancelled requests, and fully fulfilled lines.
      required: [stockItems, lowStockItems, outOfStockItems, itemsWithEmployees, itemsWithDepartments, outstandingReturnables, overdueReturnables, pendingApprovals, pendingIssues, pendingReceiptConfirmations, openMissingDamagedIncidents, unresolvedStocktakeVariances, vacantHeadBlockedDepartments]
      properties:
        stockItems:
          type: integer
          description: Distinct items with a positive organization-wide store total.
        lowStockItems:
          type: integer
          description: Positive store total at or below the item's own reorderLevel.
        outOfStockItems:
          type: integer
          description: Zero or negative organization-wide store total. Never confused with employee/department holder balances.
        itemsWithEmployees:
          type: integer
          description: Distinct items currently held (positive balance) by at least one employee.
        itemsWithDepartments:
          type: integer
          description: Distinct items currently held (positive balance) by at least one department.
        outstandingReturnables:
          type: integer
          description: Outstanding (employee + department) returnable-item holder balances, live-derived.
        overdueReturnables:
          type: integer
          description: Of the above, past their own most-recent-issue expectedReturnDate.
        pendingApprovals:
          type: integer
          description: Requests currently pending or partially approved.
        pendingIssues:
          type: integer
          description: Approved request lines with quantityIssuedSoFar < approvedQuantity.
        pendingReceiptConfirmations:
          type: integer
          description: Issued holder movements with confirmedAt still null — non-gating; stock was already issued.
        openMissingDamagedIncidents:
          type: integer
          description: Incidents with status = open.
        unresolvedStocktakeVariances:
          type: integer
          description: Non-zero, unresolved variance lines on a not-yet-finalized stocktake.
        vacantHeadBlockedDepartments:
          type: integer
          description: Departments with no currently-open Department Head assignment (§5.3).

    CreateOfficeInventoryAssetHandoffBody:
      type: object
      description: >-
        Office Inventory, Workstream 10 (§4 Owner Decision 1). Converts
        exactly ONE unit of a returnable item currently held in a store into
        one individually-identifiable Asset, calling Assets' own existing
        creation API directly. Only a store-held unit is eligible — an item
        currently issued to an employee/department must be returned first
        (Workstream 5); `asset_handoff` is a store-decreasing movement type
        only. assetCategoryCode/assetName mirror CreateAssetInput's own
        categoryCode/name; every other field passes straight through to
        Assets unchanged.
      required: [itemId, storeId, assetCategoryCode]
      properties:
        itemId:
          type: integer
        storeId:
          type: integer
        assetCategoryCode:
          type: string
          minLength: 1
        assetName:
          type: string
          minLength: 1
          description: Defaults to the Office Inventory item's own current name if omitted.
        description:
          type: string
        manufacturer:
          type: string
        model:
          type: string
        serialNumber:
          type: string
        purchaseDate:
          type: string
          format: date
        purchaseCost:
          type: number
          minimum: 0
        purchaseCurrency:
          type: string
        warrantyExpiryDate:
          type: string
          format: date
        condition:
          $ref: "#/components/schemas/AssetCondition"
        notes:
          type: string
        idempotencyKey:
          type: string
          description: Optional client-generated key protecting against duplicate submission. Same key returns the original handoff (same Asset, same movement) rather than creating a second one.

    OfficeInventoryAssetHandoffResult:
      type: object
      required: [asset, movement, replay]
      properties:
        asset:
          $ref: "#/components/schemas/Asset"
        movement:
          $ref: "#/components/schemas/OfficeInventoryStockMovement"
        replay:
          type: boolean

    DocumentCategory:
      type: object
      description: >-
        A document category available to the organization — a
        "document_category" Master Data item merged with its resolved
        behavior. A category with no settings row resolves to safe defaults
        (not confidential, no expiry, no verification required).
      properties:
        categoryCode:
          type: string
        label:
          type: string
        verificationRequired:
          type: boolean
        expirySupported:
          type: boolean
        expiryRequired:
          type: boolean
        sensitivity:
          type: string
          enum: [standard, confidential]
        retentionBasis:
          type: string
          nullable: true
        retentionPeriodMonths:
          type: integer
          nullable: true
      required:
        - categoryCode
        - label
        - verificationRequired
        - expirySupported
        - expiryRequired
        - sensitivity

    DocumentCategorySettingsInput:
      type: object
      description: Every field is optional; omitted fields keep their current value.
      properties:
        verificationRequired:
          type: boolean
        expirySupported:
          type: boolean
        expiryRequired:
          type: boolean
          description: Implies expirySupported — setting this true normalizes that to true as well.
        sensitivity:
          type: string
          enum: [standard, confidential]
        retentionBasis:
          type: string
          nullable: true
        retentionPeriodMonths:
          type: integer
          nullable: true

    DocumentCategorySettings:
      type: object
      properties:
        id:
          type: integer
        organizationId:
          type: integer
          nullable: true
        categoryCode:
          type: string
        verificationRequired:
          type: boolean
        expirySupported:
          type: boolean
        expiryRequired:
          type: boolean
        sensitivity:
          type: string
          enum: [standard, confidential]
        retentionBasis:
          type: string
          nullable: true
        retentionPeriodMonths:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - categoryCode
        - verificationRequired
        - expirySupported
        - expiryRequired
        - sensitivity

    OrganizationDocumentVersion:
      type: object
      description: >-
        One immutable version of an organization document. Superseded
        versions retain their row and their stored object permanently.
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        documentId:
          type: integer
        versionNumber:
          type: integer
        storageKey:
          type: string
        fileName:
          type: string
        mimeType:
          type: string
        fileSize:
          type: integer
        status:
          type: string
          enum: [current, superseded]
        effectiveDate:
          type: string
          format: date
          nullable: true
        expiryDate:
          type: string
          format: date
          nullable: true
        changeNote:
          type: string
          nullable: true
        supersededAt:
          type: string
          format: date-time
          nullable: true
        supersededBy:
          type: integer
          nullable: true
        uploadedBy:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - organizationId
        - documentId
        - versionNumber
        - storageKey
        - fileName
        - mimeType
        - fileSize
        - status
        - createdAt

    OrganizationDocumentCurrentVersion:
      type: object
      description: The current version summary embedded in a document list row.
      properties:
        id:
          type: integer
        versionNumber:
          type: integer
        fileName:
          type: string
        mimeType:
          type: string
        fileSize:
          type: integer
        effectiveDate:
          type: string
          format: date
          nullable: true
        expiryDate:
          type: string
          format: date
          nullable: true
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - versionNumber
        - fileName
        - mimeType
        - fileSize
        - createdAt

    OrganizationDocument:
      type: object
      description: >-
        A document owned by the organization itself (handbook, policy, form,
        procedure). Not a Personnel File — physical custody is a separate
        domain with no storage columns.
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        categoryCode:
          type: string
        title:
          type: string
        description:
          type: string
          nullable: true
        status:
          type: string
          enum: [active, archived]
        currentVersionId:
          type: integer
          nullable: true
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        currentVersion:
          allOf:
            - $ref: "#/components/schemas/OrganizationDocumentCurrentVersion"
          nullable: true
      required:
        - id
        - organizationId
        - categoryCode
        - title
        - status
        - createdAt
        - updatedAt

    DocumentMergeField:
      type: object
      description: >-
        One allow-listed merge field. A template token naming anything
        outside this set is rejected when the version is saved.
      properties:
        key:
          type: string
        label:
          type: string
      required:
        - key
        - label

    DocumentTemplateVersion:
      type: object
      description: >-
        One immutable version of a template's content. Draft versions are
        editable in place; active and superseded ones never are.
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        templateId:
          type: integer
        versionNumber:
          type: integer
        content:
          type: string
        format:
          type: string
          enum: [plain_text]
        status:
          type: string
          enum: [draft, active, superseded]
        approvedBy:
          type: integer
          nullable: true
        approvedAt:
          type: string
          format: date-time
          nullable: true
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - organizationId
        - templateId
        - versionNumber
        - content
        - format
        - status
        - createdAt

    DocumentTemplate:
      type: object
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        categoryCode:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        currentVersionId:
          type: integer
          nullable: true
        status:
          type: string
          enum: [active, inactive]
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        currentVersion:
          allOf:
            - $ref: "#/components/schemas/DocumentTemplateVersion"
          nullable: true
      required:
        - id
        - organizationId
        - categoryCode
        - name
        - status
        - createdAt
        - updatedAt

    DocumentTemplatePreview:
      type: object
      description: Preview text rendered with synthetic sample data, never a real record.
      properties:
        text:
          type: string
      required:
        - text

    GeneratedDocument:
      type: object
      description: >-
        One finalized generated artifact. Records exactly which template
        version, merged with which source entity, produced the stored PDF.
        Later template edits never alter this row or its file.
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        templateId:
          type: integer
          nullable: true
        templateVersionId:
          type: integer
          nullable: true
        categoryCode:
          type: string
        sourceType:
          type: string
          nullable: true
        sourceId:
          type: integer
          nullable: true
        storageKey:
          type: string
        fileName:
          type: string
        mimeType:
          type: string
        fileSize:
          type: integer
        generatedBy:
          type: integer
          nullable: true
        generatedAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - organizationId
        - categoryCode
        - storageKey
        - fileName
        - mimeType
        - fileSize
        - generatedAt
        - createdAt

    DocumentRequirement:
      type: object
      description: >-
        One required/provided/verified/expiry fact for one owner and
        category. "provided" and "verified" are deliberately distinct states.
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        ownerType:
          type: string
          enum: [employee, candidate, organization]
        ownerId:
          type: integer
        categoryCode:
          type: string
        required:
          type: boolean
        status:
          type: string
          enum: [pending, provided, verified, rejected]
        fulfilledDocumentTable:
          type: string
          nullable: true
        fulfilledDocumentId:
          type: integer
          nullable: true
        verifiedBy:
          type: integer
          nullable: true
        verifiedAt:
          type: string
          format: date-time
          nullable: true
        expiryDate:
          type: string
          format: date
          nullable: true
        rejectionReason:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - organizationId
        - ownerType
        - ownerId
        - categoryCode
        - required
        - status
        - createdAt
        - updatedAt

    DocumentExpiryState:
      type: object
      description: >-
        Disjoint windows: expired is strictly before asOf; expiringSoon is
        asOf through asOf+horizonDays inclusive; missing is required but
        still pending.
      properties:
        expired:
          type: array
          items:
            $ref: "#/components/schemas/DocumentRequirement"
        expiringSoon:
          type: array
          items:
            $ref: "#/components/schemas/DocumentRequirement"
        missing:
          type: array
          items:
            $ref: "#/components/schemas/DocumentRequirement"
      required:
        - expired
        - expiringSoon
        - missing

    DocumentRetentionRecord:
      type: object
      description: >-
        Retention, archive, legal hold, and disposal state for one document
        instance, addressed by a polymorphic (documentTable, documentId)
        pair. Archived never means deleted; disposal is never automatic.
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        documentTable:
          type: string
        documentId:
          type: integer
        retentionBasis:
          type: string
          nullable: true
        retainUntil:
          type: string
          format: date
          nullable: true
        legalHold:
          type: boolean
        legalHoldReason:
          type: string
          nullable: true
        legalHoldSetBy:
          type: integer
          nullable: true
        legalHoldSetAt:
          type: string
          format: date-time
          nullable: true
        archiveStatus:
          type: string
          enum: [active, archived]
        archivedAt:
          type: string
          format: date-time
          nullable: true
        archivedBy:
          type: integer
          nullable: true
        disposalStatus:
          type: string
          enum: [none, eligible, disposed]
        disposalReason:
          type: string
          nullable: true
        disposalAuthorizedBy:
          type: integer
          nullable: true
        disposalAuthorizedAt:
          type: string
          format: date-time
          nullable: true
        disposedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - organizationId
        - documentTable
        - documentId
        - legalHold
        - archiveStatus
        - disposalStatus
        - createdAt
        - updatedAt

    DisposedDocumentResult:
      allOf:
        - $ref: "#/components/schemas/DocumentRetentionRecord"
        - type: object
          properties:
            storageDeleted:
              type: boolean
              description: >-
                False when the record was authoritatively marked disposed but
                the storage object could not be removed — a recoverable
                orphan, never a false claim that the file is still retained.
          required:
            - storageDeleted

    ScheduledJob:
      type: object
      description: >-
        A durable, database-authoritative unit of scheduled work (WS-6).
        `jobType` is never client-executable content — every write and read
        path validates it against a server-side handler registry.
      properties:
        id:
          type: integer
        organizationId:
          type: integer
          nullable: true
          description: Null only for a genuinely platform-scoped job.
        jobType:
          type: string
        sourceReferenceType:
          type: string
          nullable: true
        sourceReferenceId:
          type: integer
          nullable: true
        idempotencyKey:
          type: string
        payload:
          nullable: true
          description: Narrow, validated parameters only — never a full domain-entity snapshot.
        status:
          type: string
          enum: [scheduled, running, completed, failed, cancelled]
        priority:
          type: integer
        scheduledFor:
          type: string
          format: date-time
        attemptCount:
          type: integer
        maxAttempts:
          type: integer
        lastAttemptAt:
          type: string
          format: date-time
          nullable: true
        lastErrorClass:
          type: string
          enum: [transient, permanent]
          nullable: true
        lastErrorMessage:
          type: string
          nullable: true
          description: Safe diagnostics only — never secrets, tokens, or confidential payload content.
        lockedAt:
          type: string
          format: date-time
          nullable: true
        lockedBy:
          type: string
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        failedAt:
          type: string
          format: date-time
          nullable: true
        cancelledAt:
          type: string
          format: date-time
          nullable: true
        createdBy:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - jobType
        - idempotencyKey
        - status
        - priority
        - scheduledFor
        - attemptCount
        - maxAttempts
        - createdAt
        - updatedAt

    # --- WS-7: Bulk Import / Multi-Entity Migration ---------------------
    MigrationCanonicalField:
      type: object
      properties:
        key:
          type: string
        label:
          type: string
        required:
          type: boolean
        type:
          type: string
          enum: [string, number, date, enum, boolean]
        enumValues:
          type: array
          nullable: true
          items:
            type: string
      required: [key, label, required, type]

    MigrationEntityType:
      type: object
      properties:
        entityType:
          type: string
        label:
          type: string
        dependsOn:
          type: array
          items:
            type: string
        fields:
          type: array
          items:
            $ref: "#/components/schemas/MigrationCanonicalField"
      required: [entityType, label, dependsOn, fields]

    MigrationEntityTypeList:
      type: object
      properties:
        entityTypes:
          type: array
          items:
            $ref: "#/components/schemas/MigrationEntityType"
      required: [entityTypes]

    MigrationBatch:
      type: object
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        status:
          type: string
          enum:
            - draft
            - mapped
            - validated
            - approved
            - running
            - completed
            - completed_with_errors
            - failed
            - cancelled
        approvedBy:
          type: integer
          nullable: true
        approvedAt:
          type: string
          format: date-time
          nullable: true
        executionStartedAt:
          type: string
          format: date-time
          nullable: true
        executionCompletedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, organizationId, name, status, createdAt, updatedAt]

    MigrationSource:
      type: object
      properties:
        id:
          type: integer
        batchId:
          type: integer
        entityType:
          type: string
        fileName:
          type: string
        mimeType:
          type: string
        fileSize:
          type: integer
        sha256Digest:
          type: string
        sheetName:
          type: string
          nullable: true
        rowCount:
          type: integer
          nullable: true
        status:
          type: string
          enum: [uploaded, mapped, validated]
        createdAt:
          type: string
          format: date-time
      required: [id, batchId, entityType, fileName, mimeType, fileSize, sha256Digest, status, createdAt]

    MigrationSourceUploadResult:
      type: object
      properties:
        source:
          $ref: "#/components/schemas/MigrationSource"
        headers:
          type: array
          items:
            type: string
        rowCount:
          type: integer
        sampleRows:
          type: array
          items:
            type: array
            items:
              type: string
        autoMapping:
          type: object
          additionalProperties:
            type: string
        unmappedHeaders:
          type: array
          items:
            type: string
        missingRequiredFields:
          type: array
          items:
            type: string
      required: [source, headers, rowCount, sampleRows, autoMapping, unmappedHeaders, missingRequiredFields]

    MigrationMappingResult:
      type: object
      properties:
        source:
          $ref: "#/components/schemas/MigrationSource"
        stagedRowCount:
          type: integer
        rowsWithErrors:
          type: integer
      required: [source, stagedRowCount, rowsWithErrors]

    MigrationEntityValidationSummary:
      type: object
      properties:
        entityType:
          type: string
        label:
          type: string
        total:
          type: integer
        valid:
          type: integer
        warnings:
          type: integer
        errors:
          type: integer
      required: [entityType, label, total, valid, warnings, errors]

    MigrationValidationResult:
      type: object
      properties:
        batchId:
          type: integer
        status:
          type: string
        entities:
          type: array
          items:
            $ref: "#/components/schemas/MigrationEntityValidationSummary"
        totalRows:
          type: integer
        totalErrors:
          type: integer
      required: [batchId, status, entities, totalRows, totalErrors]

    MigrationRowIssue:
      type: object
      properties:
        entityType:
          type: string
        rowNumber:
          type: integer
        validationStatus:
          type: string
          enum: [pending, valid, warning, error]
        messages:
          nullable: true
      required: [entityType, rowNumber, validationStatus]

    MigrationExecutionProgress:
      type: object
      properties:
        mode:
          type: string
          enum: [synchronous]
        batchId:
          type: integer
        status:
          type: string
        created:
          type: integer
        matched:
          type: integer
        skipped:
          type: integer
        failed:
          type: integer
        remaining:
          type: integer
      required: [batchId, status, created, matched, skipped, failed, remaining]

    MigrationExecutionQueued:
      type: object
      properties:
        mode:
          type: string
          enum: [background]
        batchId:
          type: integer
        totalRows:
          type: integer
        message:
          type: string
      required: [mode, batchId, totalRows, message]

    MigrationEntityReconciliation:
      type: object
      properties:
        entityType:
          type: string
        label:
          type: string
        sourceRows:
          type: integer
        created:
          type: integer
        matched:
          type: integer
        skipped:
          type: integer
        failed:
          type: integer
        pending:
          type: integer
      required: [entityType, label, sourceRows, created, matched, skipped, failed, pending]

    MigrationReconciliationTotals:
      type: object
      properties:
        sourceRows:
          type: integer
        created:
          type: integer
        matched:
          type: integer
        skipped:
          type: integer
        failed:
          type: integer
        pending:
          type: integer
      required: [sourceRows, created, matched, skipped, failed, pending]

    MigrationExecutionPolicy:
      type: object
      description: >-
        How this migration will execute, decided automatically from its entity
        set and size. Surfaced before approval so an administrator is never
        left to assume an import is atomic when it is not.
      properties:
        policy:
          type: string
          enum: [atomic, batched_resumable]
        totalRows:
          type: integer
        reasons:
          type: array
          items:
            type: string
        nonTransactionalEntityTypes:
          type: array
          items:
            type: string
      required: [policy, totalRows, reasons, nonTransactionalEntityTypes]

    MigrationApprovalResult:
      allOf:
        - $ref: "#/components/schemas/MigrationBatch"
        - type: object
          properties:
            executionPolicy:
              $ref: "#/components/schemas/MigrationExecutionPolicy"
          required: [executionPolicy]

    MigrationReconciliation:
      type: object
      properties:
        entities:
          type: array
          items:
            $ref: "#/components/schemas/MigrationEntityReconciliation"
        totals:
          $ref: "#/components/schemas/MigrationReconciliationTotals"
      required: [entities, totals]

    # --- WS-7 closure: Payroll Opening Balances -------------------------
    PayrollOpeningBalanceAmounts:
      type: object
      description: >-
        Brought-forward totals, denominated exactly as payroll_run_lines
        records the same measures per period, so the two can be summed.
      properties:
        grossEarnings:
          type: string
        taxableIncome:
          type: string
        payeAmount:
          type: string
        pensionableEarnings:
          type: string
        employeePensionDeduction:
          type: string
        employerPensionContribution:
          type: string
      required:
        - grossEarnings
        - taxableIncome
        - payeAmount
        - pensionableEarnings
        - employeePensionDeduction
        - employerPensionContribution

    PayrollOpeningBalance:
      allOf:
        - $ref: "#/components/schemas/PayrollOpeningBalanceAmounts"
        - type: object
          properties:
            id:
              type: integer
            organizationId:
              type: integer
            employeeId:
              type: integer
            taxYear:
              type: integer
            cutoverDate:
              type: string
              format: date-time
            currency:
              type: string
            sourceReferenceType:
              type: string
              nullable: true
            sourceReferenceId:
              type: integer
              nullable: true
            lockedAt:
              type: string
              format: date-time
              nullable: true
            createdAt:
              type: string
              format: date-time
            updatedAt:
              type: string
              format: date-time
          required: [id, organizationId, employeeId, taxYear, cutoverDate, currency, createdAt, updatedAt]

    PayrollYearToDate:
      allOf:
        - $ref: "#/components/schemas/PayrollOpeningBalanceAmounts"
        - type: object
          properties:
            taxYear:
              type: integer
            broughtForward:
              nullable: true
              allOf:
                - $ref: "#/components/schemas/PayrollOpeningBalanceAmounts"
            inSystem:
              $ref: "#/components/schemas/PayrollOpeningBalanceAmounts"
          required: [taxYear, inSystem]

    # --- WS-8: Custom Fields & Form Builder -----------------------------
    CustomFieldMeta:
      type: object
      properties:
        fieldTypes:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
              label:
                type: string
              usesOptions:
                type: boolean
              usesReference:
                type: boolean
            required: [type, label, usesOptions, usesReference]
        scopes:
          type: array
          items:
            type: object
            properties:
              scope:
                type: string
              label:
                type: string
              bindable:
                type: boolean
            required: [scope, label, bindable]
        visibilityOperators:
          type: array
          items:
            type: string
      required: [fieldTypes, scopes, visibilityOperators]

    CustomFieldDefinition:
      type: object
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        scope:
          type: string
        fieldKey:
          type: string
        status:
          type: string
          enum: [active, archived]
        archivedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, organizationId, scope, fieldKey, status, createdAt, updatedAt]

    CustomFieldDefinitionVersion:
      type: object
      properties:
        id:
          type: integer
        definitionId:
          type: integer
        versionNumber:
          type: integer
        label:
          type: string
        helpText:
          type: string
          nullable: true
        fieldType:
          type: string
        required:
          type: boolean
        sensitivity:
          type: string
          enum: [normal, sensitive]
        displayOrder:
          type: integer
        validation:
          nullable: true
        options:
          nullable: true
        visibility:
          nullable: true
        isCurrent:
          type: boolean
        createdAt:
          type: string
          format: date-time
      required: [id, definitionId, versionNumber, label, fieldType, required, sensitivity, displayOrder, isCurrent, createdAt]

    CustomFieldWithVersion:
      type: object
      properties:
        definition:
          $ref: "#/components/schemas/CustomFieldDefinition"
        version:
          $ref: "#/components/schemas/CustomFieldDefinitionVersion"
      required: [definition, version]

    CustomFieldDetail:
      allOf:
        - $ref: "#/components/schemas/CustomFieldWithVersion"
        - type: object
          properties:
            versions:
              type: array
              items:
                $ref: "#/components/schemas/CustomFieldDefinitionVersion"
            usedChoiceValues:
              type: array
              items:
                type: string
          required: [versions, usedChoiceValues]

    CustomFieldInput:
      type: object
      properties:
        scope:
          type: string
        fieldKey:
          type: string
        label:
          type: string
        helpText:
          type: string
          nullable: true
        fieldType:
          type: string
        required:
          type: boolean
        sensitivity:
          type: string
          enum: [normal, sensitive]
        displayOrder:
          type: integer
        validation:
          nullable: true
        options:
          nullable: true
        visibility:
          nullable: true
      required: [scope, fieldKey, label, fieldType]

    CustomFieldVersionInput:
      type: object
      properties:
        label:
          type: string
        helpText:
          type: string
          nullable: true
        fieldType:
          type: string
        required:
          type: boolean
        sensitivity:
          type: string
          enum: [normal, sensitive]
        displayOrder:
          type: integer
        validation:
          nullable: true
        options:
          nullable: true
        visibility:
          nullable: true
      required: [label, fieldType]

    ResolvedCustomFieldValue:
      type: object
      properties:
        definitionId:
          type: integer
        fieldKey:
          type: string
        label:
          type: string
        helpText:
          type: string
          nullable: true
        fieldType:
          type: string
        required:
          type: boolean
        sensitivity:
          type: string
          enum: [normal, sensitive]
        displayOrder:
          type: integer
        options:
          nullable: true
        visibility:
          nullable: true
        value:
          nullable: true
        masked:
          type: boolean
        visible:
          type: boolean
        missingRequired:
          type: boolean
        capturedVersionId:
          type: integer
          nullable: true
        capturedVersionNumber:
          type: integer
          nullable: true
      required:
        - definitionId
        - fieldKey
        - label
        - fieldType
        - required
        - sensitivity
        - displayOrder
        - masked
        - visible
        - missingRequired

    CustomFieldValueList:
      type: object
      properties:
        scope:
          type: string
        entityId:
          type: integer
        values:
          type: array
          items:
            $ref: "#/components/schemas/ResolvedCustomFieldValue"
        revealed:
          type: boolean
        written:
          type: integer
        cleared:
          type: integer
      required: [values]

    CustomForm:
      type: object
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        formKey:
          type: string
        formType:
          type: string
          enum: [internal_hr, employee_ess, onboarding, candidate_application]
        scope:
          type: string
        status:
          type: string
          enum: [active, archived]
        archivedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, organizationId, formKey, formType, scope, status, createdAt, updatedAt]

    CustomFormVersion:
      type: object
      properties:
        id:
          type: integer
        formId:
          type: integer
        versionNumber:
          type: integer
        title:
          type: string
        description:
          type: string
          nullable: true
        layout:
          type: object
          additionalProperties: true
        status:
          type: string
          enum: [draft, published, archived]
        publishedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, formId, versionNumber, title, layout, status, createdAt]

    CustomFormWithVersion:
      type: object
      properties:
        form:
          $ref: "#/components/schemas/CustomForm"
        version:
          $ref: "#/components/schemas/CustomFormVersion"
      required: [form, version]

    CustomFormDetail:
      type: object
      properties:
        form:
          $ref: "#/components/schemas/CustomForm"
        versions:
          type: array
          items:
            $ref: "#/components/schemas/CustomFormVersion"
        publishedVersion:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/CustomFormVersion"
      required: [form, versions]

    CustomFormRender:
      type: object
      properties:
        form:
          $ref: "#/components/schemas/CustomForm"
        version:
          $ref: "#/components/schemas/CustomFormVersion"
        fields:
          type: array
          items:
            $ref: "#/components/schemas/CustomFieldWithVersion"
      required: [form, version, fields]

    CustomFormInput:
      type: object
      properties:
        formKey:
          type: string
        formType:
          type: string
          enum: [internal_hr, employee_ess, onboarding, candidate_application]
        scope:
          type: string
        title:
          type: string
        description:
          type: string
          nullable: true
        layout:
          type: object
          additionalProperties: true
      required: [formKey, formType, scope, title, layout]

    CustomFormVersionInput:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
          nullable: true
        layout:
          type: object
          additionalProperties: true
      required: [title, layout]

    CustomFormSubmission:
      type: object
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        formId:
          type: integer
        formVersionId:
          type: integer
        scope:
          type: string
        entityId:
          type: integer
          nullable: true
        answers:
          type: array
          items:
            type: object
            additionalProperties: true
        submittedByMembershipId:
          type: integer
          nullable: true
        submittedAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
      required: [id, organizationId, formId, formVersionId, scope, answers, submittedAt, createdAt]

    # --- WS-9: Recruitment Completion -----------------------------------
    RecruitmentSource:
      type: object
      properties:
        code:
          type: string
        label:
          type: string
      required: [code, label]

    RecruitmentApprovalStage:
      type: object
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        purpose:
          type: string
          enum: [requisition, hire]
        stageOrder:
          type: integer
        name:
          type: string
        resolverType:
          type: string
          enum: [department_head, permission_holder, specific_membership]
        resolverConfig:
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, organizationId, purpose, stageOrder, name, resolverType, createdAt]

    RecruitmentApprovalStageInput:
      type: object
      properties:
        purpose:
          type: string
          enum: [requisition, hire]
        stageOrder:
          type: integer
        name:
          type: string
        resolverType:
          type: string
          enum: [department_head, permission_holder, specific_membership]
        resolverConfig:
          nullable: true
      required: [purpose, stageOrder, name, resolverType]

    HireAuthorization:
      type: object
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        applicationId:
          type: integer
        status:
          type: string
          enum: [pending, approved, rejected, cancelled]
        totalStages:
          type: integer
        currentStageOrder:
          type: integer
          nullable: true
        requestedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
          nullable: true
      required: [id, organizationId, applicationId, status, totalStages, requestedAt]

    HireAuthorizationDecision:
      type: object
      properties:
        id:
          type: integer
        hireAuthorizationId:
          type: integer
        stageOrder:
          type: integer
        stageName:
          type: string
        resolverType:
          type: string
        authorityBasis:
          type: string
        decision:
          type: string
          enum: [approved, rejected]
        reason:
          type: string
          nullable: true
        decidedByNameSnapshot:
          type: string
          nullable: true
        decidedAt:
          type: string
          format: date-time
      required: [id, hireAuthorizationId, stageOrder, stageName, resolverType, authorityBasis, decision, decidedAt]

    HireAuthorizationDetail:
      type: object
      properties:
        authorization:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/HireAuthorization"
        decisions:
          type: array
          items:
            $ref: "#/components/schemas/HireAuthorizationDecision"
        stages:
          type: array
          items:
            $ref: "#/components/schemas/RecruitmentApprovalStage"
      required: [decisions, stages]

    ManualCandidateInput:
      type: object
      properties:
        vacancyId:
          type: integer
        sourceCode:
          type: string
        firstName:
          type: string
        lastName:
          type: string
        email:
          type: string
        phone:
          type: string
          nullable: true
        capturedAt:
          type: string
          format: date-time
          nullable: true
      required: [vacancyId, sourceCode, firstName, lastName, email]

    ManualCaptureResult:
      type: object
      properties:
        candidate:
          type: object
          additionalProperties: true
        application:
          type: object
          additionalProperties: true
        reusedExistingCandidate:
          type: boolean
      required: [candidate, application, reusedExistingCandidate]

    OfferResponse:
      type: object
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        offerId:
          type: integer
        offerVersionId:
          type: integer
        responseType:
          type: string
          enum: [accepted, declined, withdrawn]
        channel:
          type: string
          enum: [candidate_token, recorded_by_staff]
        reason:
          type: string
          nullable: true
        respondedAt:
          type: string
          format: date-time
      required: [id, organizationId, offerId, offerVersionId, responseType, channel, respondedAt]

    OfferVersionState:
      type: object
      properties:
        version:
          type: object
          additionalProperties: true
        isExpired:
          type: boolean
        isCurrent:
          type: boolean
        canRespond:
          type: boolean
        response:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/OfferResponse"
      required: [version, isExpired, isCurrent, canRespond]

    PublicOfferView:
      type: object
      properties:
        offerVersionId:
          type: integer
        versionNumber:
          type: integer
        proposedStartDate:
          type: string
          nullable: true
        employmentType:
          type: string
          nullable: true
        workplaceType:
          type: string
          nullable: true
        location:
          type: string
          nullable: true
        conditions:
          type: string
          nullable: true
        expiryDate:
          type: string
          nullable: true
        status:
          type: string
        candidateFirstName:
          type: string
      required: [offerVersionId, versionNumber, status]

    EmploymentParticulars:
      type: object
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        offerVersionId:
          type: integer
        employerName:
          type: string
          nullable: true
        workerName:
          type: string
          nullable: true
        dateOfFirstAppointment:
          type: string
          format: date-time
          nullable: true
        jobTitleOrGrade:
          type: string
          nullable: true
        payRate:
          type: string
          nullable: true
        payMethod:
          type: string
          nullable: true
        payInterval:
          type: string
          nullable: true
        hoursOfWork:
          type: string
          nullable: true
        holidayTerms:
          type: string
          nullable: true
        sickPayTerms:
          type: string
          nullable: true
        pensionTerms:
          type: string
          nullable: true
        noticeByEmployer:
          type: string
          nullable: true
        noticeByWorker:
          type: string
          nullable: true
        disciplinaryRules:
          type: string
          nullable: true
        grievanceProcedure:
          type: string
          nullable: true
        overtimeTerms:
          type: string
          nullable: true
        probationTerms:
          type: string
          nullable: true
        issuedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, organizationId, offerVersionId, createdAt]

    EmploymentParticularsInput:
      type: object
      properties:
        employerName:
          type: string
          nullable: true
        workerName:
          type: string
          nullable: true
        dateOfFirstAppointment:
          type: string
          nullable: true
        jobTitleOrGrade:
          type: string
          nullable: true
        payRate:
          type: string
          nullable: true
        payMethod:
          type: string
          nullable: true
        payInterval:
          type: string
          nullable: true
        hoursOfWork:
          type: string
          nullable: true
        holidayTerms:
          type: string
          nullable: true
        sickPayTerms:
          type: string
          nullable: true
        pensionTerms:
          type: string
          nullable: true
        noticeByEmployer:
          type: string
          nullable: true
        noticeByWorker:
          type: string
          nullable: true
        disciplinaryRules:
          type: string
          nullable: true
        grievanceProcedure:
          type: string
          nullable: true
        overtimeTerms:
          type: string
          nullable: true
        probationTerms:
          type: string
          nullable: true

    EmploymentParticularsDetail:
      type: object
      properties:
        particulars:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/EmploymentParticulars"
        suggested:
          nullable: true
          type: object
          additionalProperties: true

    # --- WS-12 — Employee Relations & Offboarding Clearance (§28) ---

    DisciplinaryCaseStatus:
      type: string
      enum: [open, closed]

    CaseConfidentiality:
      type: string
      enum: [normal, confidential, restricted]

    DisciplinaryCaseEventType:
      type: string
      enum:
        - case_opened
        - allegation_recorded
        - notice_issued
        - response_received
        - investigation_recorded
        - hearing_held
        - finding_recorded
        - outcome_recorded
        - stage_changed
        - appeal_lodged
        - appeal_decided
        - evidence_attached
        - case_closed
        - case_reopened

    DisciplinaryCase:
      type: object
      required: [id, organizationId, employeeId, categoryCode, subject, status, confidentiality, openedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        categoryCode:
          type: string
        severityCode:
          type: string
          nullable: true
        stageCode:
          type: string
          nullable: true
        subject:
          type: string
        description:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/DisciplinaryCaseStatus"
        confidentiality:
          $ref: "#/components/schemas/CaseConfidentiality"
        outcomeCode:
          type: string
          nullable: true
        outcomeRecordedAt:
          type: string
          format: date-time
          nullable: true
        warningExpiresAt:
          type: string
          format: date-time
          nullable: true
        responsibleMembershipId:
          type: integer
          nullable: true
        openedAt:
          type: string
          format: date-time
        closedAt:
          type: string
          format: date-time
          nullable: true

    DisciplinaryCaseList:
      type: array
      items:
        $ref: "#/components/schemas/DisciplinaryCase"

    DisciplinaryCaseEvent:
      type: object
      required: [id, organizationId, caseId, eventType, occurredAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        caseId:
          type: integer
        eventType:
          $ref: "#/components/schemas/DisciplinaryCaseEventType"
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true
        details:
          nullable: true
          type: object
          additionalProperties: true
        recordedBy:
          type: integer
          nullable: true

    DisciplinaryCaseDetail:
      allOf:
        - $ref: "#/components/schemas/DisciplinaryCase"
        - type: object
          properties:
            events:
              type: array
              items:
                $ref: "#/components/schemas/DisciplinaryCaseEvent"

    OpenDisciplinaryCaseInput:
      type: object
      required: [employeeId, categoryCode, subject, openedAt]
      properties:
        employeeId:
          type: integer
        categoryCode:
          type: string
          minLength: 1
          maxLength: 100
        severityCode:
          type: string
          maxLength: 100
          nullable: true
        stageCode:
          type: string
          maxLength: 100
          nullable: true
        subject:
          type: string
          minLength: 1
          maxLength: 300
        description:
          type: string
          nullable: true
        confidentiality:
          $ref: "#/components/schemas/CaseConfidentiality"
        responsibleMembershipId:
          type: integer
          nullable: true
        openedAt:
          type: string
          format: date-time

    RecordDisciplinaryEventInput:
      type: object
      required: [eventType, occurredAt]
      properties:
        eventType:
          $ref: "#/components/schemas/DisciplinaryCaseEventType"
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true

    ChangeDisciplinaryStageInput:
      type: object
      required: [stageCode, occurredAt]
      properties:
        stageCode:
          type: string
          minLength: 1
          maxLength: 100
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true

    RecordDisciplinaryOutcomeInput:
      type: object
      required: [outcomeCode, occurredAt]
      properties:
        outcomeCode:
          type: string
          minLength: 1
          maxLength: 100
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true
        warningExpiresAt:
          type: string
          format: date-time
          nullable: true

    CloseDisciplinaryCaseInput:
      type: object
      required: [occurredAt]
      properties:
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true

    ReopenDisciplinaryCaseInput:
      type: object
      required: [occurredAt, reason]
      properties:
        occurredAt:
          type: string
          format: date-time
        reason:
          type: string
          minLength: 1
          maxLength: 1000

    AttachCaseEvidenceInput:
      type: object
      required: [documentId, occurredAt]
      properties:
        documentId:
          type: integer
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true

    GrievanceCaseStatus:
      type: string
      enum: [submitted, acknowledged, under_review, resolved, closed, withdrawn]

    GrievanceRespondentType:
      type: string
      enum: [employee, department, unspecified]

    GrievanceCaseEventType:
      type: string
      enum:
        - submitted
        - acknowledged
        - assigned
        - reassigned
        - review_recorded
        - meeting_held
        - information_requested
        - information_provided
        - finding_recorded
        - resolution_recorded
        - escalated
        - appeal_lodged
        - appeal_decided
        - evidence_attached
        - withdrawn
        - closed
        - reopened

    GrievanceCase:
      type: object
      required:
        [id, organizationId, complainantEmployeeId, categoryCode, respondentType, subject, description, status, confidentiality, submittedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        complainantEmployeeId:
          type: integer
        categoryCode:
          type: string
        respondentType:
          $ref: "#/components/schemas/GrievanceRespondentType"
        respondentEmployeeId:
          type: integer
          nullable: true
        respondentDepartmentId:
          type: integer
          nullable: true
        subject:
          type: string
        description:
          type: string
        status:
          $ref: "#/components/schemas/GrievanceCaseStatus"
        confidentiality:
          $ref: "#/components/schemas/CaseConfidentiality"
        assignedMembershipId:
          type: integer
          nullable: true
        submittedAt:
          type: string
          format: date-time
        acknowledgedAt:
          type: string
          format: date-time
          nullable: true
        resolutionSummary:
          type: string
          nullable: true
        resolvedAt:
          type: string
          format: date-time
          nullable: true
        closedAt:
          type: string
          format: date-time
          nullable: true

    GrievanceCaseList:
      type: array
      items:
        $ref: "#/components/schemas/GrievanceCase"

    GrievanceCaseEvent:
      type: object
      required: [id, organizationId, caseId, eventType, occurredAt, visibleToComplainant]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        caseId:
          type: integer
        eventType:
          $ref: "#/components/schemas/GrievanceCaseEventType"
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true
        details:
          nullable: true
          type: object
          additionalProperties: true
        visibleToComplainant:
          type: boolean
        recordedBy:
          type: integer
          nullable: true

    GrievanceCaseDetail:
      allOf:
        - $ref: "#/components/schemas/GrievanceCase"
        - type: object
          properties:
            events:
              type: array
              items:
                $ref: "#/components/schemas/GrievanceCaseEvent"

    GrievanceEssUpdate:
      type: object
      required: [id, eventType, occurredAt]
      properties:
        id:
          type: integer
        eventType:
          $ref: "#/components/schemas/GrievanceCaseEventType"
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true

    GrievanceEssView:
      type: object
      description: >-
        The §28.5 employee-facing allow-list. Built field by field, never spread
        from the full record: confidentiality, assignment, respondent,
        investigator notes, internal deliberations, draft findings and audit
        information are all absent by construction.
      required: [id, categoryCode, subject, description, status, submittedAt, updates]
      properties:
        id:
          type: integer
        categoryCode:
          type: string
        subject:
          type: string
        description:
          type: string
        status:
          $ref: "#/components/schemas/GrievanceCaseStatus"
        submittedAt:
          type: string
          format: date-time
        acknowledgedAt:
          type: string
          format: date-time
          nullable: true
        resolutionSummary:
          type: string
          nullable: true
        resolvedAt:
          type: string
          format: date-time
          nullable: true
        closedAt:
          type: string
          format: date-time
          nullable: true
        updates:
          type: array
          items:
            $ref: "#/components/schemas/GrievanceEssUpdate"

    GrievanceEssViewList:
      type: array
      items:
        $ref: "#/components/schemas/GrievanceEssView"

    SubmitGrievanceInput:
      type: object
      required: [complainantEmployeeId, categoryCode, subject, description, submittedAt]
      properties:
        complainantEmployeeId:
          type: integer
        categoryCode:
          type: string
          minLength: 1
          maxLength: 100
        respondentType:
          $ref: "#/components/schemas/GrievanceRespondentType"
        respondentEmployeeId:
          type: integer
          nullable: true
        respondentDepartmentId:
          type: integer
          nullable: true
        subject:
          type: string
          minLength: 1
          maxLength: 300
        description:
          type: string
          minLength: 1
        submittedAt:
          type: string
          format: date-time

    SubmitMyGrievanceInput:
      type: object
      description: >-
        No complainant field. The complainant is resolved from the caller's own
        employee link, so an employee cannot file in a colleague's name.
      required: [categoryCode, subject, description, submittedAt]
      properties:
        categoryCode:
          type: string
          minLength: 1
          maxLength: 100
        respondentType:
          $ref: "#/components/schemas/GrievanceRespondentType"
        respondentEmployeeId:
          type: integer
          nullable: true
        respondentDepartmentId:
          type: integer
          nullable: true
        subject:
          type: string
          minLength: 1
          maxLength: 300
        description:
          type: string
          minLength: 1
        submittedAt:
          type: string
          format: date-time

    AcknowledgeGrievanceInput:
      type: object
      required: [occurredAt]
      properties:
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true

    AssignGrievanceInput:
      type: object
      required: [assignedMembershipId, occurredAt]
      properties:
        assignedMembershipId:
          type: integer
        occurredAt:
          type: string
          format: date-time

    RecordGrievanceEventInput:
      type: object
      required: [eventType, occurredAt]
      properties:
        eventType:
          $ref: "#/components/schemas/GrievanceCaseEventType"
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true
        visibleToComplainant:
          type: boolean
          description: Defaults to false. Employee visibility is always a deliberate act.

    ResolveGrievanceInput:
      type: object
      required: [resolutionSummary, occurredAt]
      properties:
        resolutionSummary:
          type: string
          minLength: 1
        occurredAt:
          type: string
          format: date-time

    CloseGrievanceInput:
      type: object
      required: [occurredAt]
      properties:
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true

    WithdrawGrievanceInput:
      type: object
      required: [occurredAt]
      properties:
        occurredAt:
          type: string
          format: date-time
        reason:
          type: string
          nullable: true

    ClearanceTemplateStatus:
      type: string
      enum: [draft, active, archived]

    ClearanceItemType:
      type: string
      enum:
        - general
        - asset_return
        - inventory_return
        - personnel_file
        - access_revocation
        - final_settlement
        - document_handover

    ClearanceItemStatus:
      type: string
      enum: [pending, completed, returned, waived]

    ClearanceTemplate:
      type: object
      required: [id, organizationId, name, status, isDefault]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        name:
          type: string
        description:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/ClearanceTemplateStatus"
        isDefault:
          type: boolean

    ClearanceTemplateList:
      type: array
      items:
        $ref: "#/components/schemas/ClearanceTemplate"

    ClearanceTemplateItem:
      type: object
      required: [id, organizationId, templateId, sequence, label, itemType, required]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        templateId:
          type: integer
        sequence:
          type: integer
        label:
          type: string
        description:
          type: string
          nullable: true
        itemType:
          $ref: "#/components/schemas/ClearanceItemType"
        required:
          type: boolean
        responsibleDepartmentId:
          type: integer
          nullable: true

    ClearanceTemplateDetail:
      allOf:
        - $ref: "#/components/schemas/ClearanceTemplate"
        - type: object
          properties:
            items:
              type: array
              items:
                $ref: "#/components/schemas/ClearanceTemplateItem"

    CreateClearanceTemplateInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: string
          nullable: true

    UpdateClearanceTemplateInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/ClearanceTemplateStatus"
        isDefault:
          type: boolean

    AddClearanceTemplateItemInput:
      type: object
      required: [label]
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 300
        description:
          type: string
          nullable: true
        itemType:
          $ref: "#/components/schemas/ClearanceItemType"
        required:
          type: boolean
        responsibleDepartmentId:
          type: integer
          nullable: true
        sequence:
          type: integer

    ClearanceItem:
      type: object
      required: [id, organizationId, exitProcessId, sequence, label, itemType, required, status]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        exitProcessId:
          type: integer
        sourceTemplateItemId:
          type: integer
          nullable: true
        sequence:
          type: integer
        label:
          type: string
        description:
          type: string
          nullable: true
        itemType:
          $ref: "#/components/schemas/ClearanceItemType"
        required:
          type: boolean
        responsibleDepartmentId:
          type: integer
          nullable: true
        responsibleMembershipId:
          type: integer
          nullable: true
        status:
          $ref: "#/components/schemas/ClearanceItemStatus"
        comment:
          type: string
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        returnedReason:
          type: string
          nullable: true
        waivedReason:
          type: string
          nullable: true
        waivedAt:
          type: string
          format: date-time
          nullable: true

    ClearanceItemList:
      type: array
      items:
        $ref: "#/components/schemas/ClearanceItem"

    AddClearanceItemInput:
      type: object
      required: [label]
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 300
        description:
          type: string
          nullable: true
        itemType:
          $ref: "#/components/schemas/ClearanceItemType"
        required:
          type: boolean
        responsibleDepartmentId:
          type: integer
          nullable: true

    CompleteClearanceItemInput:
      type: object
      properties:
        comment:
          type: string
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true

    ReturnClearanceItemInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 1000

    WaiveClearanceItemInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 1000
          description: Mandatory. Clearance may be waived, but never silently (§28.8).

    ExitProcessStatus:
      type: string
      enum: [legacy, initiated, clearance_in_progress, ready_for_separation, completed, cancelled]

    ExitSeparationBasis:
      type: string
      description: >-
        Only bases backed by an authoritative record. Accepted resignation,
        retirement and approved termination are absent because no such record
        exists in this platform; adding them requires an authoritative record
        first, and therefore its own Owner Decision (§28.6).
      enum: [already_separated, contract_end]

    # `EmployeeExitProcess` is NOT redefined here. WS-12 extends the shipped W29
    # schema in place (§28.8: extend, never replace), so there is exactly one
    # definition of an offboarding record in this specification.

    OffboardingList:
      type: array
      items:
        $ref: "#/components/schemas/EmployeeExitProcess"

    ClearanceProgress:
      type: object
      required: [total, required, completed, waived, pending, returned, requiredOutstanding, clearanceComplete]
      properties:
        total:
          type: integer
        required:
          type: integer
        completed:
          type: integer
        waived:
          type: integer
        pending:
          type: integer
        returned:
          type: integer
        requiredOutstanding:
          type: integer
        clearanceComplete:
          type: boolean

    OutstandingCustody:
      type: object
      description: READ from Assets and Office Inventory. Nothing here alters either module.
      required: [assets, inventory]
      properties:
        assets:
          type: array
          items:
            type: object
            required: [assignmentId, assetId]
            properties:
              assignmentId:
                type: integer
              assetId:
                type: integer
              assetTag:
                type: string
                nullable: true
              assetName:
                type: string
                nullable: true
        inventory:
          type: array
          items:
            type: object
            required: [movementId, itemId]
            properties:
              movementId:
                type: integer
              itemId:
                type: integer
              itemName:
                type: string
                nullable: true
        personnelFileId:
          type: integer
          nullable: true

    ExitInterviewStatus:
      type: string
      enum: [scheduled, completed, cancelled]

    ExitInterview:
      type: object
      required: [id, organizationId, exitProcessId, status]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        exitProcessId:
          type: integer
        status:
          $ref: "#/components/schemas/ExitInterviewStatus"
        interviewDate:
          type: string
          format: date-time
          nullable: true
        interviewerMembershipId:
          type: integer
          nullable: true
        reasonForLeavingCode:
          type: string
          nullable: true
        confidentialNotes:
          type: string
          nullable: true

    OffboardingDetail:
      allOf:
        - $ref: "#/components/schemas/EmployeeExitProcess"
        - type: object
          properties:
            items:
              $ref: "#/components/schemas/ClearanceItemList"
            progress:
              $ref: "#/components/schemas/ClearanceProgress"
            outstandingCustody:
              $ref: "#/components/schemas/OutstandingCustody"
            exitInterview:
              nullable: true
              allOf:
                - $ref: "#/components/schemas/ExitInterview"

    OffboardingEligibility:
      type: object
      required: [eligible]
      properties:
        eligible:
          type: boolean
        basis:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/ExitSeparationBasis"
        expectedSeparationDate:
          type: string
          format: date-time
          nullable: true
        evidence:
          type: string
          nullable: true
          description: What the basis was read from, so a refusal can be explained.

    InitiateOffboardingInput:
      type: object
      properties:
        clearanceTemplateId:
          type: integer
          nullable: true
          description: Defaults to the organization's active default template, if one exists.

    InitiateOffboardingResult:
      type: object
      required: [exitProcess, itemsCreated]
      properties:
        exitProcess:
          $ref: "#/components/schemas/EmployeeExitProcess"
        itemsCreated:
          type: integer
        basis:
          type: object
          properties:
            basis:
              $ref: "#/components/schemas/ExitSeparationBasis"
            expectedSeparationDate:
              type: string
              format: date-time
            evidence:
              type: string

    FinalClearanceResult:
      type: object
      required: [exitProcess, progress]
      properties:
        exitProcess:
          $ref: "#/components/schemas/EmployeeExitProcess"
        progress:
          $ref: "#/components/schemas/ClearanceProgress"

    CancelOffboardingInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 1000

    ScheduleExitInterviewInput:
      type: object
      properties:
        interviewDate:
          type: string
          format: date-time
          nullable: true
        interviewerMembershipId:
          type: integer
          nullable: true

    CompleteExitInterviewInput:
      type: object
      properties:
        interviewDate:
          type: string
          format: date-time
          nullable: true
        reasonForLeavingCode:
          type: string
          nullable: true
        confidentialNotes:
          type: string
          nullable: true

    CancelExitInterviewInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 1000

    CaseAgeingRow:
      type: object
      required: [id, employeeId, status, openedAt, ageDays]
      properties:
        id:
          type: integer
        employeeId:
          type: integer
        status:
          type: string
        openedAt:
          type: string
          format: date-time
        ageDays:
          type: integer

    DisciplinaryReport:
      type: object
      required: [openCases]
      properties:
        openCases:
          type: array
          items:
            $ref: "#/components/schemas/CaseAgeingRow"

    GrievanceAgeingRow:
      type: object
      required: [id, complainantEmployeeId, status, submittedAt, ageDays]
      properties:
        id:
          type: integer
        complainantEmployeeId:
          type: integer
        status:
          type: string
        submittedAt:
          type: string
          format: date-time
        acknowledgedAt:
          type: string
          format: date-time
          nullable: true
        ageDays:
          type: integer

    GrievanceReport:
      type: object
      required: [openGrievances]
      properties:
        openGrievances:
          type: array
          items:
            $ref: "#/components/schemas/GrievanceAgeingRow"

    OffboardingReport:
      type: object
      required: [inProgress, completed, outstandingAssets]
      properties:
        inProgress:
          type: array
          items:
            type: object
            required: [exitProcessId, employeeId, status, requiredOutstanding, outstandingAssets]
            properties:
              exitProcessId:
                type: integer
              employeeId:
                type: integer
              status:
                type: string
              expectedSeparationDate:
                type: string
                format: date-time
                nullable: true
              separationDate:
                type: string
                format: date-time
                nullable: true
              requiredOutstanding:
                type: integer
              outstandingAssets:
                type: integer
        completed:
          type: array
          items:
            type: object
            required: [exitProcessId, employeeId]
            properties:
              exitProcessId:
                type: integer
              employeeId:
                type: integer
              finalClearedAt:
                type: string
                format: date-time
                nullable: true
              separationDate:
                type: string
                format: date-time
                nullable: true
        outstandingAssets:
          type: array
          items:
            type: object
            required: [employeeId, assignmentId, assetId]
            properties:
              employeeId:
                type: integer
              assignmentId:
                type: integer
              assetId:
                type: integer
              assetTag:
                type: string
                nullable: true

    ClearanceQueue:
      type: object
      required: [outstanding]
      properties:
        outstanding:
          type: array
          items:
            type: object
            required: [clearanceItemId, exitProcessId, employeeId, label, itemType, required, status]
            properties:
              clearanceItemId:
                type: integer
              exitProcessId:
                type: integer
              employeeId:
                type: integer
              label:
                type: string
              itemType:
                type: string
              required:
                type: boolean
              status:
                type: string
              responsibleDepartmentId:
                type: integer
                nullable: true

    # --- WS-13 — Employee Data Change Approval & HR Service Requests (§29) ---

    DataChangeOrigin:
      type: string
      enum: [employee_self_service, hr_originated]

    DataChangeStatus:
      type: string
      description: >-
        `approved` and `applied` are distinct on purpose: an approved request
        whose application failed must not read as though the record was written.
        `stale` is not terminal — it awaits audited re-confirmation.
      enum: [pending, returned, approved, applied, rejected, withdrawn, stale, application_failed]

    RequestApprovalPurpose:
      type: string
      enum: [data_change, service_request]

    RequestAuthorityResolver:
      type: string
      enum: [department_head, permission_holder, specific_membership]

    DataChangeFieldSummary:
      type: object
      required: [fieldKey, label, kind, sensitive]
      properties:
        fieldKey:
          type: string
        label:
          type: string
        kind:
          type: string
          enum: [string, date, enum, json]
        sensitive:
          type: boolean

    DataChangeFieldPolicy:
      type: object
      required: [fieldKey, approvalRequired]
      properties:
        fieldKey:
          type: string
        label:
          type: string
        kind:
          type: string
          enum: [string, date, enum, json]
        essEligible:
          type: boolean
        hrEligible:
          type: boolean
        sensitive:
          type: boolean
        approvalRequired:
          type: boolean

    DataChangeFieldPolicyList:
      type: array
      items:
        $ref: "#/components/schemas/DataChangeFieldPolicy"

    SetDataChangeFieldPolicyInput:
      type: object
      required: [approvalRequired]
      properties:
        approvalRequired:
          type: boolean

    RequestApprovalStage:
      type: object
      required: [id, organizationId, purpose, stageOrder, name, resolverType]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        purpose:
          $ref: "#/components/schemas/RequestApprovalPurpose"
        stageOrder:
          type: integer
        name:
          type: string
        resolverType:
          $ref: "#/components/schemas/RequestAuthorityResolver"
        resolverConfig:
          nullable: true
          type: object
          additionalProperties: true

    CreateRequestApprovalStageInput:
      type: object
      required: [purpose, stageOrder, name, resolverType]
      properties:
        purpose:
          $ref: "#/components/schemas/RequestApprovalPurpose"
        stageOrder:
          type: integer
          minimum: 1
        name:
          type: string
          minLength: 1
          maxLength: 200
        resolverType:
          $ref: "#/components/schemas/RequestAuthorityResolver"
        resolverConfig:
          type: object
          additionalProperties: true
          description: "permission_holder -> { permissionKey }; specific_membership -> { membershipId }; department_head -> {}"

    DataChangeRequest:
      type: object
      required: [id, organizationId, employeeId, origin, status, requestedAt, stageCountAtRequest]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        employeeId:
          type: integer
        origin:
          $ref: "#/components/schemas/DataChangeOrigin"
        status:
          $ref: "#/components/schemas/DataChangeStatus"
        reason:
          type: string
          nullable: true
        stageCountAtRequest:
          type: integer
        currentStageOrder:
          type: integer
          nullable: true
        requestedAt:
          type: string
          format: date-time
        effectiveDate:
          type: string
          format: date-time
          nullable: true
        decidedAt:
          type: string
          format: date-time
          nullable: true
        appliedAt:
          type: string
          format: date-time
          nullable: true
        applicationFailureReason:
          type: string
          nullable: true

    DataChangeApprovalField:
      type: object
      description: Sensitive values arrive masked (§29.7).
      required: [fieldKey, label, sensitive]
      properties:
        fieldKey:
          type: string
        label:
          type: string
        sensitive:
          type: boolean
        previousValue:
          nullable: true
        requestedValue:
          nullable: true
        staleDetectedAt:
          type: string
          format: date-time
          nullable: true

    DataChangeEvent:
      type: object
      required: [id, requestId, eventType, occurredAt]
      properties:
        id:
          type: integer
        requestId:
          type: integer
        eventType:
          type: string
        stageOrder:
          type: integer
          nullable: true
        stageName:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        occurredAt:
          type: string
          format: date-time

    DataChangeApprovalView:
      type: object
      description: >-
        The approver's view. Built field by field, never spread from the employee
        record, so a column added to `employees` cannot start leaking here.
      required: [id, employeeId, origin, status, requestedAt, stageCountAtRequest, fields]
      properties:
        id:
          type: integer
        employeeId:
          type: integer
        origin:
          $ref: "#/components/schemas/DataChangeOrigin"
        status:
          $ref: "#/components/schemas/DataChangeStatus"
        reason:
          type: string
          nullable: true
        requestedAt:
          type: string
          format: date-time
        effectiveDate:
          type: string
          format: date-time
          nullable: true
        currentStageOrder:
          type: integer
          nullable: true
        stageCountAtRequest:
          type: integer
        fields:
          type: array
          items:
            $ref: "#/components/schemas/DataChangeApprovalField"
        events:
          type: array
          items:
            $ref: "#/components/schemas/DataChangeEvent"

    DataChangeEssField:
      type: object
      required: [fieldKey, label]
      properties:
        fieldKey:
          type: string
        label:
          type: string
        requestedValue:
          nullable: true
        staleDetectedAt:
          type: string
          format: date-time
          nullable: true

    DataChangeEssView:
      type: object
      description: >-
        The employee's own view. An allow-list built by construction: no approver
        identity, no stage configuration, no internal notes.
      required: [id, status, requestedAt, fields]
      properties:
        id:
          type: integer
        status:
          $ref: "#/components/schemas/DataChangeStatus"
        reason:
          type: string
          nullable: true
        requestedAt:
          type: string
          format: date-time
        effectiveDate:
          type: string
          format: date-time
          nullable: true
        decidedAt:
          type: string
          format: date-time
          nullable: true
        appliedAt:
          type: string
          format: date-time
          nullable: true
        fields:
          type: array
          items:
            $ref: "#/components/schemas/DataChangeEssField"

    RequestedFieldChangeInput:
      type: object
      required: [fieldKey]
      properties:
        fieldKey:
          type: string
          minLength: 1
          maxLength: 100
          description: A key from the eligible-field registry. Never a raw column name.
        requestedValue:
          nullable: true

    CreateDataChangeRequestInput:
      type: object
      required: [fields]
      properties:
        fields:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/RequestedFieldChangeInput"
        reason:
          type: string
          nullable: true
        effectiveDate:
          type: string
          format: date-time
          nullable: true

    SubmitMyDataChangeRequestInput:
      type: object
      description: No employee identifier — the subject is the caller's own record.
      required: [fields]
      properties:
        fields:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/RequestedFieldChangeInput"
        reason:
          type: string
          nullable: true
        effectiveDate:
          type: string
          format: date-time
          nullable: true

    CreateDataChangeRequestResult:
      type: object
      required: [request, approvalRequired]
      properties:
        request:
          $ref: "#/components/schemas/DataChangeRequest"
        approvalRequired:
          type: boolean

    DecideDataChangeRequestInput:
      type: object
      properties:
        notes:
          type: string
          nullable: true

    RejectDataChangeRequestInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 1000

    ReturnDataChangeRequestInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 1000

    ServiceRequestFulfilmentKind:
      type: string
      enum: [acknowledgement, document]

    ServiceRequestStatus:
      type: string
      enum: [submitted, acknowledged, in_progress, awaiting_employee, fulfilled, closed, cancelled, withdrawn]

    ServiceRequestApprovalStatus:
      type: string
      description: Separate from status — an approved request is not thereby fulfilled.
      enum: [not_required, pending, approved, rejected]

    ServiceRequestType:
      type: object
      required: [id, organizationId, code, name, active, employeeVisible, approvalRequired, fulfilmentKind]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        code:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        active:
          type: boolean
        employeeVisible:
          type: boolean
        approvalRequired:
          type: boolean
        fulfilmentKind:
          $ref: "#/components/schemas/ServiceRequestFulfilmentKind"
        formId:
          type: integer
          nullable: true
        responsibleDepartmentId:
          type: integer
          nullable: true
        targetDays:
          type: integer
          nullable: true

    CreateServiceRequestTypeInput:
      type: object
      required: [code, name]
      properties:
        code:
          type: string
          minLength: 1
          maxLength: 100
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: string
          nullable: true
        employeeVisible:
          type: boolean
        approvalRequired:
          type: boolean
        fulfilmentKind:
          $ref: "#/components/schemas/ServiceRequestFulfilmentKind"
        formId:
          type: integer
          nullable: true
        responsibleDepartmentId:
          type: integer
          nullable: true
        targetDays:
          type: integer
          nullable: true

    UpdateServiceRequestTypeInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: string
          nullable: true
        active:
          type: boolean
        employeeVisible:
          type: boolean
        approvalRequired:
          type: boolean
        fulfilmentKind:
          $ref: "#/components/schemas/ServiceRequestFulfilmentKind"
        formId:
          type: integer
          nullable: true
        responsibleDepartmentId:
          type: integer
          nullable: true
        targetDays:
          type: integer
          nullable: true

    ServiceRequest:
      type: object
      required: [id, organizationId, typeId, employeeId, subject, status, approvalStatus, submittedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        typeId:
          type: integer
        employeeId:
          type: integer
        subject:
          type: string
        details:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/ServiceRequestStatus"
        approvalStatus:
          $ref: "#/components/schemas/ServiceRequestApprovalStatus"
        stageCountAtRequest:
          type: integer
        currentStageOrder:
          type: integer
          nullable: true
        assignedMembershipId:
          type: integer
          nullable: true
        formSubmissionId:
          type: integer
          nullable: true
        generatedDocumentId:
          type: integer
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true
        submittedAt:
          type: string
          format: date-time
        acknowledgedAt:
          type: string
          format: date-time
          nullable: true
        fulfilledAt:
          type: string
          format: date-time
          nullable: true
        closedAt:
          type: string
          format: date-time
          nullable: true
        resolutionSummary:
          type: string
          nullable: true

    ServiceRequestEvent:
      type: object
      required: [id, requestId, eventType, occurredAt, visibleToEmployee]
      properties:
        id:
          type: integer
        requestId:
          type: integer
        eventType:
          type: string
        stageOrder:
          type: integer
          nullable: true
        stageName:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        visibleToEmployee:
          type: boolean
        occurredAt:
          type: string
          format: date-time

    ServiceRequestDetail:
      allOf:
        - $ref: "#/components/schemas/ServiceRequest"
        - type: object
          properties:
            events:
              type: array
              items:
                $ref: "#/components/schemas/ServiceRequestEvent"

    ServiceRequestEssUpdate:
      type: object
      required: [id, eventType, occurredAt]
      properties:
        id:
          type: integer
        eventType:
          type: string
        occurredAt:
          type: string
          format: date-time
        notes:
          type: string
          nullable: true

    ServiceRequestEssView:
      type: object
      description: >-
        Employee-facing allow-list: no assignee, no stage configuration, no
        internal notes, no event details.
      required: [id, typeId, subject, status, approvalStatus, submittedAt, updates]
      properties:
        id:
          type: integer
        typeId:
          type: integer
        subject:
          type: string
        details:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/ServiceRequestStatus"
        approvalStatus:
          $ref: "#/components/schemas/ServiceRequestApprovalStatus"
        submittedAt:
          type: string
          format: date-time
        acknowledgedAt:
          type: string
          format: date-time
          nullable: true
        fulfilledAt:
          type: string
          format: date-time
          nullable: true
        resolutionSummary:
          type: string
          nullable: true
        generatedDocumentId:
          type: integer
          nullable: true
        updates:
          type: array
          items:
            $ref: "#/components/schemas/ServiceRequestEssUpdate"

    CreateServiceRequestInput:
      type: object
      required: [typeId, subject]
      properties:
        typeId:
          type: integer
        subject:
          type: string
          minLength: 1
          maxLength: 300
        details:
          type: string
          nullable: true
        formSubmissionId:
          type: integer
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true

    SubmitMyServiceRequestInput:
      type: object
      required: [typeId, subject]
      properties:
        typeId:
          type: integer
        subject:
          type: string
          minLength: 1
          maxLength: 300
        details:
          type: string
          nullable: true
        formSubmissionId:
          type: integer
          nullable: true
        evidenceDocumentId:
          type: integer
          nullable: true

    AcknowledgeServiceRequestInput:
      type: object
      properties:
        notes:
          type: string
          nullable: true

    AssignServiceRequestInput:
      type: object
      required: [assignedMembershipId]
      properties:
        assignedMembershipId:
          type: integer

    ApproveServiceRequestInput:
      type: object
      properties:
        notes:
          type: string
          nullable: true

    RejectServiceRequestInput:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 1000

    RequestServiceRequestInformationInput:
      type: object
      required: [message]
      properties:
        message:
          type: string
          minLength: 1
          maxLength: 2000

    RespondToServiceRequestInput:
      type: object
      required: [message]
      properties:
        message:
          type: string
          minLength: 1
          maxLength: 2000

    FulfilServiceRequestInput:
      type: object
      required: [resolutionSummary]
      properties:
        resolutionSummary:
          type: string
          minLength: 1
        generatedDocumentId:
          type: integer
          nullable: true
          description: The WS-5 document that satisfies the request. WS-13 generates none itself.

    CloseServiceRequestInput:
      type: object
      properties:
        notes:
          type: string
          nullable: true

    PendingDataChangeRow:
      type: object
      required: [id, employeeId, status, requestedAt, ageDays]
      properties:
        id:
          type: integer
        employeeId:
          type: integer
        status:
          type: string
        requestedAt:
          type: string
          format: date-time
        ageDays:
          type: integer

    OpenServiceRequestRow:
      type: object
      required: [id, typeId, employeeId, status, approvalStatus, submittedAt, ageDays, overdue]
      properties:
        id:
          type: integer
        typeId:
          type: integer
        employeeId:
          type: integer
        status:
          type: string
        approvalStatus:
          type: string
        submittedAt:
          type: string
          format: date-time
        ageDays:
          type: integer
        overdue:
          type: boolean
          description: Derived against the type's configured target. Never stored, and never an escalation engine.

    RequestReports:
      type: object
      description: Each half is null when the caller may not read that half.
      properties:
        pendingDataChanges:
          nullable: true
          type: array
          items:
            $ref: "#/components/schemas/PendingDataChangeRow"
        openServiceRequests:
          nullable: true
          type: array
          items:
            $ref: "#/components/schemas/OpenServiceRequestRow"

    # ------------------------------------------------------------------
    # WS-26 — Tenant Form, Workflow & Signature Engine (WS-26A foundation)
    # ------------------------------------------------------------------
    FormTemplateType:
      type: string
      enum: [leave_application, personal_information, staff_evaluation, probationary_assessment, generic]

    FormTemplateStatus:
      type: string
      enum: [active, archived]

    FormTemplateVersionStatus:
      type: string
      enum: [draft, published, archived]

    FormStageParticipant:
      type: string
      enum: [employee, supervisor, department_head, hr, final_approver, assessor]

    FormStageResolver:
      type: string
      enum: [subject_employee, reporting_manager, department_head, permission_holder, specific_membership]

    FormSubmissionStatus:
      type: string
      enum: [draft, submitted, pending_approval, returned, rejected, resubmitted, approved, finalized, archived]

    FormDocumentKind:
      type: string
      enum: [blank, draft, submitted, returned, rejected, approved, final]

    FormStageAction:
      type: string
      enum: [complete, approve, return, reject]

    FormDefinition:
      type: object
      description: >-
        Server-validated document model (sections of field / choice_group / matrix / rated_table / table / note /
        signature / computed items). See artifacts/api-server/src/lib/formEngine/definition.ts for the contract.
      additionalProperties: true

    FormAnswers:
      type: object
      description: Answers keyed by item key; value shape depends on the item kind.
      additionalProperties: true

    FormWorkflowStageInput:
      type: object
      required: [stageOrder, name, participant, resolver, editableSectionKeys, allowedActions]
      properties:
        stageOrder:
          type: integer
        name:
          type: string
        participant:
          $ref: "#/components/schemas/FormStageParticipant"
        resolver:
          $ref: "#/components/schemas/FormStageResolver"
        resolverConfig:
          type: object
          additionalProperties: true
          nullable: true
        editableSectionKeys:
          type: array
          items:
            type: string
        allowedActions:
          type: array
          items:
            $ref: "#/components/schemas/FormStageAction"
        signatureSlotKey:
          type: string
          nullable: true

    FormWorkflowStage:
      allOf:
        - $ref: "#/components/schemas/FormWorkflowStageInput"
        - type: object
          required: [id]
          properties:
            id:
              type: integer

    FormTemplate:
      type: object
      required: [id, organizationId, templateKey, formType, title, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        organizationId:
          type: integer
        templateKey:
          type: string
        formType:
          $ref: "#/components/schemas/FormTemplateType"
        moduleKey:
          type: string
          nullable: true
        title:
          type: string
        description:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/FormTemplateStatus"
        currentPublishedVersionId:
          type: integer
          nullable: true
        archivedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    FormTemplateVersionSummary:
      type: object
      required: [id, templateId, versionNumber, status, definitionSha256, createdAt]
      properties:
        id:
          type: integer
        templateId:
          type: integer
        versionNumber:
          type: integer
        status:
          $ref: "#/components/schemas/FormTemplateVersionStatus"
        publishedAt:
          type: string
          format: date-time
          nullable: true
        firstUsedAt:
          type: string
          format: date-time
          nullable: true
        definitionSha256:
          type: string
        changeNote:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time

    FormTemplateSummary:
      type: object
      required: [id, templateKey, formType, title, status, createdAt, updatedAt, versions]
      properties:
        allowsOnBehalfSubmission:
          type: boolean
          description: True when this template's PUBLISHED version permits an authorized HR user to complete it on an employee's behalf. Absent policy is false.
        id:
          type: integer
        templateKey:
          type: string
        formType:
          $ref: "#/components/schemas/FormTemplateType"
        moduleKey:
          type: string
          nullable: true
        title:
          type: string
        description:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/FormTemplateStatus"
        currentPublishedVersionId:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        versions:
          type: array
          items:
            $ref: "#/components/schemas/FormTemplateVersionSummary"

    FormTemplateVersion:
      type: object
      required: [id, templateId, versionNumber, status, definition, definitionSha256, createdAt, stages]
      properties:
        id:
          type: integer
        templateId:
          type: integer
        versionNumber:
          type: integer
        status:
          $ref: "#/components/schemas/FormTemplateVersionStatus"
        effectiveFrom:
          type: string
          nullable: true
        effectiveTo:
          type: string
          nullable: true
        definition:
          $ref: "#/components/schemas/FormDefinition"
        definitionSha256:
          type: string
        signaturePolicy:
          type: object
          additionalProperties: true
          nullable: true
        renderConfig:
          type: object
          additionalProperties: true
          nullable: true
        changeNote:
          type: string
          nullable: true
        firstUsedAt:
          type: string
          format: date-time
          nullable: true
        publishedAt:
          type: string
          format: date-time
          nullable: true
        archivedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        stages:
          type: array
          items:
            $ref: "#/components/schemas/FormWorkflowStage"

    CreateFormTemplateBody:
      type: object
      required: [templateKey, formType, title, definition]
      properties:
        templateKey:
          type: string
          pattern: "^[a-z][a-z0-9_]*$"
          maxLength: 64
        formType:
          $ref: "#/components/schemas/FormTemplateType"
        moduleKey:
          type: string
          nullable: true
        title:
          type: string
          maxLength: 300
        description:
          type: string
          nullable: true
        definition:
          $ref: "#/components/schemas/FormDefinition"
        signaturePolicy:
          type: object
          additionalProperties: true
          nullable: true
        renderConfig:
          type: object
          additionalProperties: true
          nullable: true
        stages:
          type: array
          items:
            $ref: "#/components/schemas/FormWorkflowStageInput"
        changeNote:
          type: string
          nullable: true

    CreateFormTemplateVersionBody:
      type: object
      required: [definition]
      properties:
        definition:
          $ref: "#/components/schemas/FormDefinition"
        signaturePolicy:
          type: object
          additionalProperties: true
          nullable: true
        renderConfig:
          type: object
          additionalProperties: true
          nullable: true
        stages:
          type: array
          items:
            $ref: "#/components/schemas/FormWorkflowStageInput"
        changeNote:
          type: string
          nullable: true

    UpdateFormTemplateVersionBody:
      type: object
      properties:
        definition:
          $ref: "#/components/schemas/FormDefinition"
        signaturePolicy:
          type: object
          additionalProperties: true
          nullable: true
        renderConfig:
          type: object
          additionalProperties: true
          nullable: true
        stages:
          type: array
          items:
            $ref: "#/components/schemas/FormWorkflowStageInput"
        changeNote:
          type: string
          nullable: true

    FormSubmissionSummary:
      type: object
      required: [id, organizationId, templateId, templateKey, templateTitle, formType, templateVersionId, versionNumber, subjectEmployeeId, subjectName, status, createdByMembershipId, assisted, createdAt, updatedAt]
      properties:
        assisted:
          type: boolean
          description: True when an authorized HR user completed this form on the subject employee's behalf.
        assistanceReason:
          allOf:
            - $ref: "#/components/schemas/FormAssistanceReason"
          nullable: true
          description: Category only. The operator's free-text notes are deliberately not part of any summary.
        currentStageName:
          type: string
          nullable: true
          description: Name of the workflow stage the submission is waiting at (e.g. "Employee Confirmation & Signature"); null unless pending approval.
        id:
          type: integer
        organizationId:
          type: integer
        templateId:
          type: integer
        templateKey:
          type: string
        templateTitle:
          type: string
        formType:
          $ref: "#/components/schemas/FormTemplateType"
        templateVersionId:
          type: integer
        versionNumber:
          type: integer
        subjectEmployeeId:
          type: integer
        subjectName:
          type: string
        status:
          $ref: "#/components/schemas/FormSubmissionStatus"
        currentStageOrder:
          type: integer
          nullable: true
        stageCountSnapshot:
          type: integer
          nullable: true
        createdByMembershipId:
          type: integer
        submittedAt:
          type: string
          format: date-time
          nullable: true
        approvedAt:
          type: string
          format: date-time
          nullable: true
        finalizedAt:
          type: string
          format: date-time
          nullable: true
        finalDocumentId:
          type: integer
          nullable: true
        finalSha256:
          type: string
          nullable: true
        archivedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    FormSubmissionRevisionSummary:
      type: object
      required: [id, revisionNumber, kind, savedByMembershipId, savedAt]
      properties:
        id:
          type: integer
        revisionNumber:
          type: integer
        kind:
          type: string
          enum: [draft, submitted, resubmitted, stage_update]
        stageOrder:
          type: integer
          nullable: true
        savedByMembershipId:
          type: integer
        savedAt:
          type: string
          format: date-time

    FormSubmissionCurrentRevision:
      type: object
      required: [id, revisionNumber, kind, answers, autofillSnapshot, computed, savedAt]
      properties:
        id:
          type: integer
        revisionNumber:
          type: integer
        kind:
          type: string
        answers:
          $ref: "#/components/schemas/FormAnswers"
        autofillSnapshot:
          type: object
          additionalProperties: true
        computed:
          type: object
          additionalProperties: true
        stageOrder:
          type: integer
          nullable: true
        savedAt:
          type: string
          format: date-time

    FormSubmissionEvent:
      type: object
      required: [id, eventType, occurredAt]
      properties:
        id:
          type: integer
        eventType:
          type: string
        stageOrder:
          type: integer
          nullable: true
        stageName:
          type: string
          nullable: true
        revisionId:
          type: integer
          nullable: true
        notes:
          type: string
          nullable: true
        details:
          type: object
          additionalProperties: true
          nullable: true
        actorUserId:
          type: integer
          nullable: true
        actorMembershipId:
          type: integer
          nullable: true
        actorName:
          type: string
          nullable: true
        occurredAt:
          type: string
          format: date-time

    FormSubmissionViewer:
      type: object
      required: [canEdit, editableSectionKeys, canSubmit, availableActions, canFinalize, canArchive, isSubject]
      properties:
        canEdit:
          type: boolean
        editableSectionKeys:
          type: array
          items:
            type: string
        canSubmit:
          type: boolean
        availableActions:
          type: array
          items:
            $ref: "#/components/schemas/FormStageAction"
        canFinalize:
          type: boolean
        canArchive:
          type: boolean
        isSubject:
          type: boolean

    FormSubmissionDetail:
      type: object
      required: [submission, subjectHasAccount, template, version, stages, currentRevision, revisions, events, viewer]
      properties:
        submission:
          $ref: "#/components/schemas/FormSubmissionSummary"
        subjectHasAccount:
          type: boolean
          description: >-
            False when the subject employee has no linked login account. A stage that
            resolves to the subject employee therefore has no actor until an account is
            linked, and the form waits. Deliberately a boolean: no account identifier is exposed.
        template:
          type: object
          required: [id, templateKey, title, formType]
          properties:
            id:
              type: integer
            templateKey:
              type: string
            title:
              type: string
            formType:
              $ref: "#/components/schemas/FormTemplateType"
        version:
          type: object
          required: [id, versionNumber, definition, definitionSha256]
          properties:
            id:
              type: integer
            versionNumber:
              type: integer
            definition:
              $ref: "#/components/schemas/FormDefinition"
            signaturePolicy:
              type: object
              additionalProperties: true
              nullable: true
            definitionSha256:
              type: string
        stages:
          type: array
          items:
            $ref: "#/components/schemas/FormWorkflowStage"
        currentRevision:
          oneOf:
            - $ref: "#/components/schemas/FormSubmissionCurrentRevision"
            - type: "null"
        revisions:
          type: array
          items:
            $ref: "#/components/schemas/FormSubmissionRevisionSummary"
        events:
          type: array
          items:
            $ref: "#/components/schemas/FormSubmissionEvent"
        viewer:
          $ref: "#/components/schemas/FormSubmissionViewer"

    CreateFormSubmissionBody:
      type: object
      required: [templateId]
      properties:
        templateId:
          type: integer
        subjectEmployeeId:
          type: integer
          nullable: true
        # Required only when subjectEmployeeId is another employee. The caller must
        # hold form_submission.create_on_behalf AND the template version's policy
        # must set allowOnBehalfSubmission; absent policy fails closed.
        assistanceReason:
          $ref: "#/components/schemas/FormAssistanceReason"
        assistanceNotes:
          type: string
          nullable: true
          maxLength: 2000
          description: Supporting detail. Required when assistanceReason is "other". May contain sensitive detail and is never returned in list projections.

    FormAssistanceReason:
      type: string
      description: Why a form was completed by someone other than its subject employee.
      enum:
        - system_access_unavailable
        - medical_or_incapacity
        - accessibility_assistance
        - administrative_assistance
        - other

    SaveFormSubmissionDraftBody:
      type: object
      required: [answers]
      properties:
        answers:
          $ref: "#/components/schemas/FormAnswers"

    SubmitFormSubmissionBody:
      type: object
      properties:
        answers:
          $ref: "#/components/schemas/FormAnswers"

    FormSubmissionStageActionBody:
      type: object
      required: [action]
      properties:
        action:
          $ref: "#/components/schemas/FormStageAction"
        answers:
          $ref: "#/components/schemas/FormAnswers"
        notes:
          type: string
          nullable: true

    FormAnswersError:
      type: object
      required: [error]
      properties:
        error:
          type: string
        issues:
          type: array
          items:
            type: object
            required: [key, message]
            properties:
              key:
                type: string
              message:
                type: string
